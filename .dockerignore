# Keep the API build context small and secret-free (root Dockerfile copies
# only package manifests + apps/api, but the whole context is uploaded).
**/node_modules
**/.next
**/dist
**/.env
**/.env.local
**/.env.*.local
.git
.github
docs
kanban
sketches
scripts
*.log
