# KoraLink Factory Board

> Living kanban maintained by the 5-hour Factory Loop (Hermes cron, profile `koralink`).
> Each lane: **P0** = broken/blocking/money or security · **P1** = missing functionality users feel · **P2** = polish/tech debt.
> Items link their cycle docs in `docs/plans/` and run reports in `kanban/RUNS/`.

**Pending (owner directive 2026-09-05 15:45 UTC):** run #35 priority = **P1-41 transactional
email layer** (plan pre-staged at `docs/plans/run35-email/` — read it before building);
reviewer model = `glm-5.3-flash` (owner directive, supersedes run-#15 glm-5.3);
**P1-40 join-approval mode REJECTED by owner** — instant-join is the product, do not build.

**Last updated:** 2026-09-05T11:40Z (run #34 — kanban/RUNS/2026-09-05T11-40Z.md): **last-admin ghost guard** — last-admin protection count now excludes PDPL ghosts (`deleted_at IS NULL`), closing Reviewer A's console-lockout finding (demote/ban of the last LIVING admin passed while only ghost admins remained). Gates: jest 339/339, vitest 362/362, tsc api 0, turbo 3/3. New rows P1-38/39/40 (venue photos, no-show reputation visibility, join-approval mode) + P2-49 (unguarded check-then-act mutations). PREVIOUS (repair session 10:45Z): fires #33 (01:15Z) + #34 (10:15Z) DIED AT BOOT — cron pin left on opencode-go + ALL four fallback rungs on Go; Go weekly 429 killed both before any directive could run, while z.ai sat at 1% weekly. FIXED: cron pin + delegation → zai glm-5.3-flash (live child probe 200), fallback ladder diversified (deepseek → zai → Go×2), watchdog now alerts on wrong pin / single-provider ladder. Run #32's crashed-at-commit code slices REBUILT: PDPL ghost guards `f40acd9` (surviving spec 4/4), WEB-8 reg?.waiting + billboard reduced-motion + InstallLanding `45cdb91`. Gates: jest 338/338, vitest 362/362, tsc 0/0, eslint 0, turbo 3/3 exit 0. ADMIN HOLD LIFTED (owner landed 1043293); board reconciled to run #32's verified state; new P2-47/P2-48.

**Previous (run #31, 2026-09-04T02:15Z):** RESUMED after the 01:16Z cron fire crashed ~5min in (owner pid dead, lock taken over). Crashed segment had landed P1-35 + P1-36 + purge-subscription sweep (d96684b/05f086e/8346aeb) — verified this run (Reviewer A: d96684b "sound"; 40 targeted jest green). Resume hardening: `13bf4d4` (purge UPDATE+subscription DELETE now ONE tx — crash previously stranded subscriptions on an anonymized ghost forever; restore-route gate now query-string-proof) + `ec3862e` (spent restore-token REPLAY GUARD — after restore succeeds the purpose:'restore' JWT was still a full session token until exp; now 403 everywhere on an active account, Reviewer A IMPORTANT #1). P1-37 BLOCKED: ADMIN HOLD (Abdullah's uncommitted apps/admin + admin-API work). P2-45 verified DONE (Reviewer B all ✓). jest 334/334 targeted+full 40 suites 330/330 pre-hardening, vitest 328/330 (2 = sibling PromoBillboard WIP), turbo build 3/3 + api re-built uncached, API restarted + health 200. **zai delegation REPAIRED** — reviewers ran clean on zai/glm-5.3-flash (7-run 401 streak broken); A 112s / B 464s)

> ✅ **ADMIN HOLD LIFTED (run #32 + repair 2026-09-05)** — owner's work landed as 1043293
> (P1-37 deleted-users visibility in the users ops table; verified vs board by Reviewer B,
> run #32). Admin-area items are buildable again (any NEW dirty-tree hold still applies per §3.5).
>
> ✅ ADMIN HOLD LIFTED (run #22, 15:16Z) — historical: at that time the admin tree
> was clean through d0862a7.


---

## 🔴 P0 — Critical (broken / blocking)

| # | Area | Item | Evidence | Status | Cycle |
|---|------|------|----------|--------|-------|
| P0-1 | API | **REST chat reads leaked every match conversation** — `messages` relation in GET /matches/:id + GET /:id/messages had no membership check while WS already enforced it. FIXED by run #1, VERIFIED live by parent session (2026-08-26T18:1xZ). | matches.controller.ts:108-112,149-153; matches.service.ts:322,781 | **DONE ✅** (run #1: 19041aa, 005dec0, 4b87a9f; live: member 200/2 msgs, outsider 403, outsider detail messages stripped to 0, roster intact; jest 30/30) | docs/plans/private-match-access-control/ |
| P0-2 | API/Wallet | **Fake self-credit top-up**: `POST /wallet/topup` credits the caller's wallet with no payment provider behind it (grep `moyasar\|stripe\|tap\|hyperpay` in apps/api/src → 0 hits); PWA wallet page calls it directly up to SAR 10,000. **Interim MITIGATION LANDED (Abdullah: "keep dummy for now")**: endpoint now 403s when `NODE_ENV=production` (mirrors dev-login gating); dummy path intact in dev/test (live-verified: 201 + credit). **⚠️ run #2 finding**: the live box runs `NODE_ENV=development` (systemd loads `apps/api/.env`), so this prod-gate is NOT exercised on the current Tailscale deployment — dev-login AND dummy top-up are both live for any tailnet peer. Acceptable for a private dev box; MUST set `NODE_ENV=production` (+ real secrets, remove dev-login) before public launch. **STRIX P0-7 (run #25)**: CVSS 9.1 admin bypass — dev-login and wallet topup share the same `NODE_ENV` string-gate and tokens are signed without `exp`. The fix is to retire the string gate for **explicit opt-in feature flags** (DEV_LOGIN_ENABLED / WALLET_TOPUP_ENABLED) that default to off, and **always sign JWTs with `expiresIn`**. | wallet.controller.ts:53-68; wallet/page.tsx:87-103; topup-wallet.dto.ts:23; systemd koralink-api.service EnvironmentFile; auth.controller.ts:120-124; auth.service.ts:234 | WIP — dummy kept in dev; **real payment provider still needed** (Moyasar/HyperPay/Tap); Strix P0-7 booted | docs/plans/wallet-topup-dummy-gate/ |
| P0-3 | API/Security | **Placeholder auth secrets in live deployment** — `JWT_SECRET=change-me` + `COOKIE_SECRET=change-me` loaded into the running systemd process → any tailnet peer could forge an Admin/user JWT or signed cookie (HS256 key publicly known). **FIXED by run #2 (42d2d86)**: new `assertBootstrapSecrets()` guard in `main.ts` hard-fails boot on placeholder/missing secrets (any env) and <32-char secrets (prod); live `.env` regenerated with `openssl rand -hex 32`; `.env.example` now ships empty secrets + generation guidance. Verified: guard throws on `change-me` (compiled-dist proof), API boots clean with real secrets, health 200. | main.ts:23-30; common/security/bootstrap-secrets.ts; /proc/<pid>/environ | **DONE ✅** (run #2: 42d2d86; jest 52/52, build 3/3, live restart + health 200) | docs/plans/env-secret-hardening/ |
| P0-4 | API/Money | **Scheduler auto-cancel lost refunds (non-atomic)** — `checkMinPlayers` Pass 2 committed the guarded `Cancelled` UPDATE first, then ran wallet credit + ledger insert + slot release as separate auto-committed statements inside a catch-that-logs; a mid-sequence failure left a cancelled match with a permanently booked slot and a host who was never refunded (silent money loss, no retry once status left Open/Full). **FIXED run #13 (ecdf29c)**: guard + refund + ledger + slot release now in ONE `db.transaction` mirroring manual cancelMatch (same `refund-<id>` key, refunded from `pitch_cost_sar`); lost guard races skip side effects; ops log carries refundedSAR/slotReleased. 5 jest specs incl. refund-failure rollback. | matches.service.ts:408-465 (was 410-448); matches.auto-cancel-atomicity.spec.ts | **DONE ✅** (run #13: ecdf29c; jest 130/130 at build, API restarted + healthy) | docs/plans/run13-auto-cancel-refund-tx/ |
| P0-5 | API/Push | **No per-category push preferences** — only a global `push_muted` toggle + quiet hours (P1-20). Users who want kickoff reminders but not chat pushes have all-or-nothing. Required for any real-world push strategy: `notification_categories` table (kickoff/removed/cancelled/rescheduled/POTM/chat/promos) + per-user `user_notification_prefs(category → muted)` + UI in profile. Product-decision: 8 categories vs 4 (collapse chat/promos). (Reviewer B, run #25.) **UPDATED by Abdullah 2026-09-02: push/notifications ELEVATED to P0-class — the category model will be defined by the new push-reliability + email-strategy runs (PWA install-triggered push enablement, email-worthiness matrix, OTP efficiency incl. P2-19 per-IP cap). The old 8-vs-4 default is superseded. Run #27 split: per-IP cap built (P2-19 DONE); per-category prefs + install-triggered push DEFERRED to #28.** **RUN #28: vertical slice DONE ✅** — schema (`user_notification_prefs` + `NotificationCategory` enum, migration 0030 applied live), API (PATCH/GET `/users/me/push-preferences` with partial PATCH on `categoryMutes`), PWA UI (4 toggle rows with localized hints + install-gate hint), iOS install gate in `usePushNotifications`. Live E2E: PATCH→200, GET→200, DB rows persisted correctly. 6 new jest cases. Future P0-5 sub-cycles (deferred, not blocking): 8-category taxonomy, per-category quiet hours, per-subscription push, observability for mute-rate metrics. | notifications.service.ts:135-166 (mutes); push_subscriptions has no category cols; new `user_notification_prefs` table; new `CATEGORY_BY_KEY` map at notifications.service.ts:18-30 | **DONE ✅ (run #28 — vertical slice; 4-category taxonomy covering all current triggers)** | docs/plans/run28-push-categories-install/ |
| P0-9 | API/Email | **No email infrastructure — zero providers in repo** (grep nodemailer/sendgrid/resend/mailgun/smtp → 0 hits across apps/). The P0-5 email-worthiness matrix (run #27) is unanswerable without a transport. Required for: (1) KSA launch: account lifecycle emails (welcome, ban/suspend, dispute resolution, payment receipts); (2) push fallback when push delivery fails or is opted-out; (3) OTP email backup; (4) scheduled reminders that don't depend on the app being open. Architecture decision needed: managed provider (Resend / SendGrid / Mailgun — $0-50/mo at MVP scale, deliverability + DKIM/SPF/DMARC built in) vs. self-hosted SMTP (cheaper at scale, more ops, deliverability harder). Then the matrix: per-event decide (in-app activity / push / email / none). Owner's call required before any code. | apps/ (grep mailgun/sendgrid/resend/nodemailer/smtp → 0) | TODO (decision required) | — |
| P0-6 | API/PDPL | **No account-delete or data-export** — `users.controller.ts` has no `DELETE /users/me` and no `GET /users/me/export`. KSA PDPL requires both (right to erasure + data portability). Plus soft-delete vs hard-delete product decision + 30-day grace period (so accidental deletes can be reversed) + JSON export shape (matches/wallet/transactions/disputes/reports/activities/push_subscriptions). Legal blocker for KSA launch. (Reviewer B, run #25.) **DECIDED by Abdullah 2026-09-02: BUILD IT — soft delete (deactivate+hide instantly, purge after 30 days) + JSON export of the 7 data groups. Owner: "keep that, it's important."** | users.controller.ts:??; schema has `deleted_at` column? | **DONE ✅** (run #29 — delete+export+restore + auth gate + migration 0031; **run #30 — hard-purge cron + restore-flow auth fix**: 2 commits `a66906d` (PWA fetcher restore-token Bearer fallback + login-page RestoreAccountBanner + 4 vitest cases; the previous `RestoreAccountBanner` only rendered on the profile page, unreachable after soft-delete's `logout()` + `clearAuthToken()` — Reviewer A caught this dead-UI defect on the run #29 vertical slice) + `43289ec` (API `UsersScheduler` @Cron('0 */5 * * *') + `UsersService.purgeExpiredAccounts()` UPDATE-not-DELETE anonymizer with `purged-<last-12-of-uuid>` placeholder that fits the existing `varchar(20)` phone column without a schema change + 4 jest cases; LIVE E2E PASS — inserted 31-day-old test user + manually invoked the UPDATE; 5-day-deleted + active users NOT touched; 8 jest + 4 vitest new; jest 316/316; vitest 324/326). Sub-cycles deferred: PDPL email receipt (needs P0-9 email infra), admin "deleted users" back-office view (#31). | docs/plans/run29-pdpl-delete-export/ + docs/plans/run30-pdpl-hard-purge/ |
| P0-7 | API/Security | **Dev-login admin bypass (Strix, CVSS ~9.1) — FIXED run #26 (commit bfd453b)**: replaced the `NODE_ENV === 'production'` string gate with explicit opt-in flags `DEV_LOGIN_ENABLED` (default OFF) + `WALLET_TOPUP_ENABLED` (default OFF). The devLogin service method now ALWAYS passes `{expiresIn: JWT_EXPIRY || '7d'}` to signAsync — defense in depth so the issued JWT is never non-expiring (live-confirmed: with flag=true the token now carries iat+exp claims; with flag=false 403). DevLoginBar in PWA honors build-time `NEXT_PUBLIC_DISABLE_DEV_LOGIN_BAR=true` (component short-circuits to null; webpack tree-shakes the seeded-phone list from the prod bundle). P1-17b partial: deleted `apps/api/.env.bak-expose-20260901101040` (the prior-exposure backup file) and extended .gitignore with `.env.*` + `.env.bak-*` (with `!.env.example` exception). Live-confirmed: dev-login as admin on the live Tailscale deployment returns 403 instead of minting a 7-day Admin JWT. 19 new jest specs (12 auth.dev-login + 5 wallet.topup-flag + 2 migrated wallet.controller.spec). | auth.controller.ts:120-124; wallet.controller.ts:66-84; auth.service.ts:234; DevLoginBar.tsx; .gitignore; .env / .env.example | **DONE ✅** (run #26: bfd453b; jest 232/232 at build, vitest 272/272, build 3/3, API restart + live 403 + 200 with flag flip) | docs/plans/run26-security-sprint/ |
| P0-8 | API/SSRF | **Push-endpoint SSRF (Strix, CVSS ~7.7) — FIXED run #26 (commit 9678790)**: new `apps/api/src/common/security/push-endpoint.validator.ts` provides `assertSafePushEndpoint(endpoint, allowlist)` — HTTPS-only, port 443, no userinfo, no fragment, length ≤ 2048, host in allowlist, IP literals (v4/v6) must be publicly routable (rejects 127/8, 10/8, 172.16/12, 192.168/16, 169.254/16 incl. cloud metadata, CGN 100.64/10, IPv6 ULA fc/fd, link-local fe80, multicast, ::1). `DEFAULT_PUSH_HOST_ALLOWLIST` ships FCM, Mozilla, Apple, Windows; `ADMIN_PUSH_HOST_ALLOWLIST` env (comma-separated) for Huawei/Samsung/relays. Validation is applied at BOTH subscribe (entry-point, DB stays clean) AND send (defense-in-depth, stale rows pre-dating the rule are rejected before web-push fetches; uniform Sentry capture tagged `web-push-validate`). 48 jest specs cover the validator; 8 push-preferences specs migrated to FCM-formatted endpoints + the mock ConfigService now returns `''` for unrecognised keys. Live-confirmed on restarted API: `https://attacker.example/push` → 400 "host not in allowlist"; `http://fcm.googleapis.com/abc` → 400 "must use https"; `https://fcm.googleapis.com/fcm/send/x` → 201 "subscribed:true". | apps/api/src/common/security/push-endpoint.validator.ts; notifications.service.ts subscribe + sendNotification; notifications.controller.ts | **DONE ✅** (run #26: 9678790; jest 280/280, build 3/3, live E2E PASS) | docs/plans/run26-security-sprint/ |

## 🟠 P1 — High-value missing functionality

| # | Area | Item | Evidence | Status | Cycle |
|---|------|------|----------|--------|-------|
| P1-1 | API | **No scheduler/cron anywhere — FIXED + LIVE-VERIFIED**: `@nestjs/schedule` added — (1) `*/5m` auto-complete past matches (closes the restart gap permanently), (2) `*/5m` POTM finalize (announces winners when the 24h window closes; no-vote/tie stamped so no re-scan), (3) `*/15m` "match starting soon" reminders to confirmed players (once per match via `reminders_sent_at`, migration 0017). **Live-proof**: cron tick announced 2 POTM winners (18:40Z) + stamped no-vote matches; run #2 re-verified: 3 @Cron jobs in code, `reminders_sent_at` col live, `pom_winner_id` set on 4 matches, live log line "finalized 2 match(es)". | matches.scheduler.ts; matches.service.ts:131,145-300; schema.ts | **DONE ✅** (run #2 re-verified 2026-08-26T22:2xZ) | docs/plans/scheduler/ |
| P1-2 | API | **Discovery filters incomplete — gender filter FIXED + LIVE-VERIFIED (run #9, 306c74c); time-of-day filter FIXED + LIVE-VERIFIED (run #12)**: (1) gender: FilterBar tokens → `normalizeGenderRule()` → DB enum (both forms accepted, bogus 400; 10 jest specs). (2) time-of-day: new `?time=morning|afternoon|evening|night` param (get-matches.dto.ts `TIME_WINDOWS` presets) → additive Riyadh-TZ SQL clause in `findNearby` (night wraps midnight via OR-form; bounds 04–12 / 12–17 / 17–23 / 23–04); FilterBar time chips + hook passthrough + en/ar i18n (`filters.time.*`); 4 new jest specs + 4 new FilterBar vitest cases. Live proof (run #12, restarted service): `?time=evening` 200 → exactly the 18:36-Riyadh match, `?time=morning` 200 → 0 rows, `?time=bogus` 400, no-filter 200; both filtered results strict subsets of unfiltered. **Remaining (descoped)**: `radius_km` hard cutoff deliberately descoped. **skill_level filter DROPPED by Abdullah 2026-09-02 — feature not wanted, do not build.** | get-matches.dto.ts (TIME_WINDOWS + time param); matches.service.ts findNearby timeClause; FilterBar.tsx; useMatches.ts | **DONE ✅** (gender+limit+time run #12; skill_level DROPPED by owner 2026-09-02 — item closed) | docs/plans/run12-discovery-time-filter/ |
| P1-3 | API/Chat | **Chat idempotency + pagination indexes — idempotency DONE + VERIFIED**: `client_message_id` dedup backed by unique index `match_messages_client_msg_uidx` (partial, WHERE NOT NULL) + keyset index `match_messages_match_created_idx` (migration 0014); REST + WS both `onConflictDoNothing` + winner re-read (commits 6351726, 65bac93). **Remaining**: history hard-capped `limit: 50` asc no cursor pagination; `match_messages.content` text-only (no attachment table). | matches.service.ts:781-799,834-863; schema.ts:390 | WIP (idempotency DONE; pagination + media TODO) | docs/plans/ (run #1 report) |
| P1-4 | DB | **Missing hot-FK indexes — DONE + APPLIED**: `matches.host_id`/`pitch_id`, `activities.actor_id`, `disputes.reporter_id`, `reports.reporter_id`, `venues.owner_id`, `transactions.reference_id` — all 7 indexes via migration `0015_bored_wraith` (drizzle-kit generated, applied to live DB). **run #2 re-verified live: 7/7 in pg_indexes.** | schema.ts:336-339; drizzle/0015 | **DONE ✅** (run #2 re-verified 2026-08-26T22:2xZ) | docs/plans/hot-fk-indexes/ |
| P1-5 | PWA | **SW push deep-link hardcodes `locale='en'` — FIXED + VERIFIED**: SW reads `data.locale` (fallback en); API stores per-subscription locale (`push_subscriptions.locale`, migration 0016) captured at subscribe time; `sendPushToUsers` injects the subscriber's locale. **run #2 re-verified: `locale` col live (varchar)**. ⚠️ caveat: `sendPomDecidedNotification` (notifications.service.ts:192-196) builds its own payload and OMITS locale → POTM pushes always deep-link `en`. Tracked as P2-8. | apps/player-pwa/worker/index.js:19; notifications.service.ts:125-166,192-196 | **DONE ✅** (run #2 re-verified; POTM-locale caveat → P2-8) | docs/plans/ (P1-5 slice) |
| P1-6 | Admin | **Admin partner-portal scope inconsistency — FIXED + VERIFIED**: `updatePitch` routes through `assertPitchAccess` (Admin bypass, 403 non-owner non-admin, 404 only if missing); `getDashboard`/`getEarnings` accept `actorRole` and scope to ALL venues for Admin via `scopedVenueIds`/`scopedPitchIds`. Controller forwards `user.role`. | partner.service.ts:57-68,149,317,374,463; partner.controller.ts:31,103,120 | **DONE ✅** (run #3: d93e1ea; run #4 re-verified: jest 71/71, build 3/3) | docs/plans/partner-portal-admin-scope/ |
| P1-7 | API | **markNoShow 500 for non-roster target — FIXED + VERIFIED**: `wasFlagged = player.no_show` was read BEFORE the `if (!player)` guard → `TypeError` 500 instead of `NotFoundException` 404 when a host marks a user not in the roster. Guard reordered. | matches.service.ts:1414-1419 | **DONE ✅** (run #3: ccfeeb7; run #4 re-verified: guard precedes deref) | docs/plans/partner-portal-admin-scope/ (run #3 slice 2) |
| P1-8 | API/Wallet | **`joinMatch` never debits the joiner's `price_per_player`** — joiners join paid matches free; only the host is charged pitch cost (koralink mode). **Blocked on P0-2** (no real payment provider — wallet is dummy self-credit). Correct once real payments land. | matches.service.ts:603-716,879-894 | BLOCKED (P0-2) | — |
| P1-9 | PWA/Realtime | **POTM realtime socket dialed the wrong namespace — FIXED (run #5, 58b3ba3)**: `PostMatchSection` used a raw `io()` with the pathful `NEXT_PUBLIC_API_URL` base → namespace `/api/v1/lobby` → gateway rejects the handshake ("Invalid namespace") → the `pom-decided` winner toast silently never fired for users viewing the match-detail page. All 5 other realtime call sites already used `createLobbySocket()`. Now routes through the shared helper (origin `/lobby`) + regression test. Live probe: `/lobby` → CONNECTED, `/api/v1/lobby` → Invalid namespace. | PostMatchSection.tsx:45 | **DONE ✅** (run #6 verified: reviewer CONFIRMED + regression test present; build 3/3, vitest 218/218). Note: commit msg said "6 call sites" — actual is 5 (overcount, substance correct). | docs/plans/run5-pom-realtime-namespace/ |
| P1-10 | API/Security | **WS handshake skipped ban/suspend + role-staleness — FIXED (run #6, 4df0d4d)**: `handleConnection` verified only the JWT signature, so a banned/suspended user kept full chat/DM/lobby access over the socket until token expiry (≤7d) while REST 401'd immediately (`jwt-cookie.strategy.ts:71-76`). Also the `ops` room join used the stale token `role`, so a demoted admin retained live console pings. Now re-reads the user row (id/role/banned_at/suspended_until) on every handshake, mirrors the strategy, disconnects banned/future-suspended/missing accounts with a Pino warn, and joins `ops` by DB role. +7 jest cases. | app.gateway.ts:108-154 (was 108-123); jwt-cookie.strategy.ts:48-83 | **DONE ✅** (run #7 verified: handleConnection re-reads the user row and disconnects banned/future-suspended/missing accounts; joins `ops` by DB role; spec 7 cases; build 3/3, jest 83/83, vitest 218/218) | docs/plans/run6-ws-moderation-enforcement/ |
| P1-11 | API/Chat | **DM send idempotency is SELECT-then-INSERT (TOCTOU), no unique index** — `personal_messages` had only `conv_idx`/`conv_created_idx` (schema.ts:651-652); no partial unique on `(sender_id, conversation_id, client_message_id)`. `sendMessage` did findFirst→insert (`conversations.service.ts:210-241`), so concurrent retried DMs could duplicate. | conversations.service.ts:210-241; schema.ts:632-652 | **DONE ✅** (run #7 built dfc671f; run #8 verified: `onConflictDoNothing`+winner re-read+side-effect skip confirmed in code, live `personal_messages_client_msg_uidx` in pg_indexes, reviewer CONFIRMED; build 3/3, jest 87/87, vitest 218/218) | docs/plans/run7-dm-idempotency/ |
| P1-12 | Admin | **Admin/partner console is English-only — PARTNER PORTAL SLICE DONE run #14 (c4b92b0)**: next-intl wired into apps/admin (client provider; en/ar catalogs 198 keys, parity-verified); localStorage `admin_locale` + sidebar language toggle (EN/العربية); `<html lang/dir>` flips via pre-hydration boot script (no LTR flash) + Tajawal font; **partner portal fully localized** (dashboard, venues, pitches incl. SlotManager + EditPitchSheet, earnings, settings) + shared components (Sidebar, StatusBadge, Pagination); times/dates/IDs keep dir=ltr, money label localized (SAR/ر.س). **REMAINING (next slice)**: HQ console pages (dashboard/users/matches/venues/disputes/reports/transactions/settlements/audit/settings), login page, error files. | apps/admin/src/i18n/*; apps/admin/src/messages/*; components/LanguageToggle.tsx | IN-REVIEW (run #14: c4b92b0 — admin tsc 0, build 3/3, chunks carry both catalogs; run #15 RE-VERIFIED: all gates re-run green + Reviewer B BUILT-AS-CLAIMED (198/198 parity re-counted); run #22 extended to HQ dashboard + load-failure lines (5052fe1, 2c46403); **run #23 VERIFIED: 'Failed to load' 0 hits in apps/admin/src/app, hq namespace 124/124 en/ar leaf keys**; visual RTL check still pending — no browser on box, Abdullah to eyeball /partner + /dashboard with admin_locale=ar) | docs/plans/run14-admin-i18n/ |
| P1-13 | API | **No match reschedule/edit-after-create — BUILT run #20 (644ab89 API + f772b78/fixed ledger + 4150fb8 PWA + a5b9251 review fixes)**: `PATCH /matches/:id/schedule` (host-only, koralink-mode, Open/Full) moves the match to a FREE slot on the SAME pitch in ONE tx: match-row FOR UPDATE, both slots locked ORDER BY id (no deadlocks), old slot released + exact `pitch_cost_sar` refund (guarded oldCost>0), new slot booked + new cost charged (rate×duration, net wallet delta with balance floor), `scheduled_at/duration_mins/price_per_player` re-derived server-authoritatively (scheduler + pricing formulas untouched). Populated `findOne` OUTSIDE tx + `reschedule` summary block; roster notified (new ActivityVerb `match_rescheduled`, migration 0027, + push); WS broadcast. PWA: `RescheduleSheet` (5 UX states, current slot excluded) + host button on match detail; `Match` carries bookingMode/pitchId/bookingSlotId; i18n 683/683. **CROSS-DAY extended (2026-08-31 session, 48170d8 API + ff49a35 PWA):** the sheet now spans the NEXT 7 DAYS (Play-feed `DatePicker` strip, controlled, defaults to TODAY; day change clears the slot pick; `reschedule.pickDay` + day-aware `noSlots` en/ar) and `rescheduleMatch` REJECTS PAST TARGET SLOTS before any money moves (2 new specs; reschedule fixtures wall-clock-proof via `riyadhDate()`). **Live E2E PASS**: guards 403/400/404/409 + the move 200 (slot swap, delta 0, roster 3 kept) **run #21 RE-VERIFIED (Reviewer B): jest 13/13 reschedule incl. 2 past-slot specs, 74/74 matches suite, i18n 684/684 pickDay/noSlots en+ar — all PASS** + A→B→A round-trip + create→reschedule→cancel drill returns wallet to the sar (ledger refund-charge-refund). | matches.service.ts rescheduleMatch (:1737+, past-guard :1835) + cancelMatch FOR UPDATE; dto/update-match-schedule.dto.ts; matches.controller.ts:220; drizzle/0027; RescheduleSheet.tsx (day strip); useMatchActions.ts useRescheduleMatch | **DONE ✅** (run #20: 5 commits, jest 176/176, vitest 246/246, build 3/3, live E2E PASS; cross-day session: jest 178/178, vitest 248/248, api tsc 0, build 3/3, live E2E 14/14 — cross-day move today 22:00→tomorrow 16:00 AST, past-slot 400, wallet 45.00 restored) | docs/plans/run20-reschedule/ + docs/plans/run21-reschedule-cross-day/ |
| P1-14 | API/Wallet | **Settlement payout never executes** — settlements track `pending/paid` but no actual bank payout; IBAN collected at verification but unused. Venue owners never receive earnings. | admin/settlements.service.ts:99-150 | BLOCKED (P0-2 — needs real payment/payout provider) | — |
| P1-15 | API | **createDispute TOCTOU duplicate race — FIXED (run #8, 8fa95f9) + VERIFIED (run #9)**: `createDispute` deduped with a non-atomic select-then-insert (`matches.service.ts:1559-1607`) and no unique index on `(match_id, reporter_id, type)`, so a concurrent double-tap opened two identical disputes. Now `disputes_open_uidx` (partial, WHERE status IN ('opened','under_review')) + `onConflictDoNothing` + winner re-read attaches the appeal as evidence. Migration 0020. **Run #9 verification**: code re-read (onConflictDoNothing :1608, winner re-read + attachAppeal :1611-1640), index definition in schema.ts:774-776, `disputes_open_uidx` live in pg_indexes (alongside reports/settlements uidix), build 3/3 + jest 102/102 + vitest 218/218. | matches.service.ts:1594-1640; schema.ts:772-777 | **DONE ✅** (run #8 built; run #9 verified: code + live index + full gates) | docs/plans/run8-dispute-idempotency/ |
| P1-16 | PWA/API | **No padel support — product is football-only** despite "padel/football" framing: `pitchSizeEnum = ['5v5','7v7','8v8','11v11']` (schema.ts:47-52), `surfaceTypeEnum = ['Grass','Artificial']` (:54), no `sport_type` column, zero "padel"/"2v2"/"doubles" strings in en.json/ar.json. Whole-sport gap if padel is in scope (2v2 size, glass/padel surface, doubles format). **DECIDED by Abdullah 2026-09-02: FOOTBALL ONLY — padel will NEVER be in scope. Item closed as no-work-needed (code is already football-only); do not re-propose padel features.** | schema.ts:47-54; en.json/ar.json | **DONE ✅** (closed by owner decision 2026-09-02 — football only, no padel ever) | — |
| P2-36 | PWA/UI | **Shared DatePicker strip upgrade — DONE (2026-08-31 session, Abdullah request)**: `components/matches/DatePicker.tsx` is now THE modular calendar-day strip — 30-day window (default `days=30`, prop-tunable), horizontal scroll (RTL-aware), TODAY card keeps the "TODAY" label AND gains a dot (green unselected / white on the active dark chip; aria-hidden; every chip reserves the dot row so heights stay equal); a11y: `aria-pressed` per chip, `aria-current="date"` on today, full localized accessible names. All three consumers inherit it with zero prop changes: Play feed (filter + all-games default), club detail, RescheduleSheet (cross-day picker from 48170d8/ff49a35). +10 dedicated specs (window length, dot states, controlled precedence, aria contract); RescheduleSheet spec asserts 30 chips + today dot inside the sheet. Gates: vitest 258/258, tsc 0, eslint 0, build 3/3; deployed chunk serves the new code (today-dot + aria-current verified over HTTP). Visual on-device check pending (no browser on box). | DatePicker.tsx; DatePicker.test.tsx (NEW); RescheduleSheet.test.tsx; consumers: play/page.tsx, clubs/[id]/page.tsx, RescheduleSheet.tsx | **DONE ✅** (2026-08-31; run #21 VERIFIED: vitest 17/17 DatePicker+RescheduleSheet specs, deployed chunk carries aria-current + TODAY dot, i18n 684/684) | docs/plans/run21-reschedule-cross-day/ |
| P1-17 | PWA/API | **No waitlist/overflow for full matches** — 0 hits for "waitlist" across apps/; a Full match gives players no queue path (churn point for popular padel/football slots). | apps/ (grep waitlist → 0) | TODO | — |
| P1-17a | **API/Infra** | **Strix supply-chain CVE cluster — PARTIALLY FIXED run #26 (commit 835e27d, 7 of 8 CVEs closed)**. **DONE**: (a) `axios ^1.16.0` (apps/api) closes CVE-2026-44494/42264/44495 (prototype-pollution/MITM/info-disclosure at unifonic.service.ts:27). (b) `next 15.5.21` exact pin (apps/player-pwa + apps/admin) closes CVE-2026-44575 (middleware bypass — PWA src/middleware.ts is the route auth boundary) AND CVE-2026-64649 (Server-Action SSRF). (c) `ws ^8.21.0` + `engine.io ^6.6.7` via root npm overrides (incl. nested engine.io-client path) closes CVE-2026-48779 (ws DoS) + CVE-2026-59725 (engine.io DoS) transitively pulled by @nestjs/platform-socket.io. **BONUS** (latent-error sweep exposed by the npm install cache invalidation): 6 pre-existing tsc errors fixed (disputes.service / reports.service / settlements.service / conversations.service / matches.service + 2 specs). **DEFERRED**: **(b-part) `drizzle-orm ^0.45.2`** — 0.45.2 introduces 6 type tightenings (eq() right-operand narrowing, returning() array projection typing, `.returning()` schema inference) that require a half-day audit. Strix's own usage analysis confirmed no reachable sink in 0.44.7 (no `sql.identifier()` / `.as()` with user input). **(d) Sentry trio pinned to 10.70.0** via root overrides — 10.73.x's `server-utils/orchestrion/bundler/webpack.mjs` fails to load in vitest's ESM context. Re-run Strix dep scanning in run #27 to confirm resolution on the 7 closed CVEs + the 1 deferred (drizzle). | apps/api/package.json axios/drizzle-orm; apps/player-pwa + apps/admin package.json next 15.5.21; root package.json overrides; 4 service + 2 spec files (tsc sweep) | **PARTIAL (7/8 done, drizzle deferred)** — run #26: 835e27d; jest 280/280, vitest 272/272, tsc 0, build 3/3 | docs/plans/run26-security-sprint/ |
| P1-17b | **Infra/Secrets** | **Strix secrets + session hygiene (run #25) — PARTIAL (run #26 closed portions a+c)**: **(a) `apps/api/.env.bak-expose-20260901101040` DELETED run #26 (bfd453b)**; `.gitignore` extended with `.env.*` + `.env.bak-*` (with `!.env.example` exception) so the next backup is auto-ignored. **STILL TODO (run #27+)**: rotate the secrets the .bak file held (rotation unverified; the file is gone but any actor who exfiltrated it before the rm still has a window). **(b) live Sentry org auth token in `apps/player-pwa/.env.local`** (used by prod source-map upload). Rotate + move to a build-time secret not in the repo. **(c) `REDIS_PASSWORD` empty across defaults and live env; compose publishes 6379 on all interfaces.** Fix: bind `127.0.0.1:6379:6379` + `requirepass`; review Postgres published port the same way. **(d) Bearer tokens in `localStorage` (admin + player) + Zustand persist duplicate, 7d lifetime, **no logout/revocation server-side**; player fetcher rewrites the API base to plaintext HTTP for non-localhost. Fix: move admin auth to HttpOnly cookies; drop `Authorization: *** on browser surfaces; add server-side session invalidation; remove tokens from Zustand persist. | apps/player-pwa/.env.local; docker-compose.yml redis/postgres; apps/player-pwa/.../fetcher.ts; apps/admin/src/lib/rbac.ts | **PARTIAL (a+c done, b+d TODO)** — run #26: 835e27d gitignore + bfd453b .bak removed | — |
| P1-17c | **API/WS** | **WebSocket origin allowlist enforced only in production (Strix, run #25) — FIXED run #27**: `app.gateway.ts:80-97` drops the `NODE_ENV === 'production'` gate; unlisted origins are now ALWAYS disconnected (was: warn-and-allow in dev). The 4 new jest cases cover: unlisted origin rejected in dev, unlisted origin rejected in prod, PLAYER_URL accepted, ADMIN_URL accepted. Live probe (real socket.io upgrade with `Origin: https://attacker.example`) → journal shows `WS connection from unlisted origin "https://attacker.example" rejected`; first attempt disconnected (socket.io client's auto-fallback to a transport that drops Origin is the reason a naive client script may report "connected" — the attack-attempt is rejected as designed). | app.gateway.ts:80-97; app.gateway.spec.ts (4 new cases) | **DONE ✅** (run #27; jest 11/11 gateway, dist grep confirms, live journal probe PASS) | docs/plans/run27-push-reliability/ |
| P1-17d | **API/PWA** | **ICS calendar export — RFC 5545 TEXT injection (Strix LOW, CVSS ~3.5) — FIXED run #27**: new `apps/api/src/common/security/ics-text.ts` provides `escapeIcsText()` per RFC 5545 §3.3.11 (backslash `\\`, semicolon `\;`, comma `\,`, CRLF/CR/LF → `\n`). Applied to `match.title`/`location`/description in `matches.controller.ts:33,91-105`. Bonus fix: original DESCRIPTION had `\\u2022` (literal `\u2022`) instead of a real bullet — now `•`. 7 new jest cases on the helper. Live probe (`/api/v1/matches/<id>/calendar`): commas escaped, real `0d 0a` line terminators, real bullet character. (Strix run #25) | apps/api/src/common/security/ics-text.ts (new); apps/api/src/modules/matches/matches.controller.ts:33,91-105; ics-text.spec.ts (new) | **DONE ✅** (run #27; jest 7/7 ics-text, build 3/3, live ICS download PASS) | docs/plans/run27-push-reliability/ |
| P1-18 | API/Partner | **deleteSlot booking TOCTOU — FIXED (run #9 reviewer, b9ef749)**: DELETE had no `is_booked = false` predicate, so a match booking the slot between the is_booked SELECT and the DELETE got its slot deleted out from under the booking (`booked_match_id` left dangling). Now conditional DELETE + `.returning()` affected-row check → zero rows = 409 Conflict (slot survives; release via match cancel only). 3 jest specs; live-probed create→delete→re-delete (201→200→404). | partner.service.ts:607-637 | **DONE ✅** (run #9: b9ef749; jest 105/105, build 3/3, vitest 218/218, live probe) | docs/plans/run9-discovery-gender-filter/ (report) |
| P1-19 | PWA/API | **Play feed silently capped at 50 matches — no pagination — FIXED run #13 (6b0ffab)**: `GET /matches` now returns `{ matches, total, hasMore }` (total via `COUNT(*) OVER()` window — pre-LIMIT, no second COUNT query; hasMore = offset+rows<total) + new `?offset=` DTO param (Min 0). `useMatches` converted to `useInfiniteQuery` (limit 50; offset cursor = cumulative rows; legacy bare-array responses never page) with aggregated return `{ matches, total, hasMore, fetchNextPage, isLoading, error, refetch }` — both consumers (play feed, club detail) updated to the aggregated shape. Play page renders a Load More button (hidden on last page); `play.loadMore` en/ar. **Bundled dead-wire fix**: play/page stored `filters.time` (run #12 chips) but never passed it to the hook — time filter now actually filters the feed from the UI. 3 new jest + 4 new vitest cases; live probe: envelope+total+offset paging+negative-offset 400 on restarted service. | useMatches.ts (useInfiniteQuery); get-matches.dto.ts (offset); matches.service.ts findNearby envelope; play/page.tsx (Load More + time wire) | **DONE ✅** (run #13: 6b0ffab; jest 133/133, vitest 233/233 at build, live probe PASS) | docs/plans/run13-auto-cancel-refund-tx/ |
| P1-20 | PWA/API | **No push-notification preferences / quiet hours — FIXED run #13 (2b520e3)**: 4 new `users` columns (`push_muted`, `quiet_hours_enabled`, `quiet_start_hour` 23, `quiet_end_hour` 7; migration 0022, applied after code commit). `sendPushToUsers` joins the user row: muted users skipped; enabled quiet window (Riyadh-local, wraps midnight, end-exclusive) suppresses deliveries. `PATCH /users/me/push-preferences` (partial update, returns full set); `/users/me` exposes prefs. PWA profile: mute toggle + quiet-hours switch + from/until selects (24h, dir=ltr in RTL); `useUpdatePushPreferences` write-through; en/ar i18n. 6 new jest cases (midnight-wrap math, skip paths, control). Live probe: PATCH → full set, `/users/me` reflects, hour 99 → 400. | notifications.service.ts (isInQuietHours + sendPushToUsers join); users.service.ts updatePushPreferences; users.controller.ts:81-92; schema.ts:193-198; drizzle/0022; profile/page.tsx; useUser.ts | **DONE ✅** (run #13: 2b520e3; jest 144/144, vitest 234/234, build 3/3, live probe PASS, migration live in information_schema; run #21: aa7a2d2 spec wall-clock fix re-verified — jest push-preferences 9/9 at any hour) | docs/plans/run13-auto-cancel-refund-tx/ |
| P1-21 | API | **Clearing a no-show mark rejected admin under_review disputes** — the clear branch closed disputes with `status IN ('opened','under_review')` → `rejected`, silently overwriting an admin's in-flight review (Reviewer A IMPORTANT, verified in code run #13). Now closes `status = 'opened'` only. Regression spec asserts the exact SQL predicate via PgDialect.sqlToQuery (no `in (`, equality on disputes.status). | matches.service.ts:1777-1800 (was inArray :1775); matches.mark-noshow.spec.ts (new case) | **DONE ✅** (run #13: ecdf29c) | docs/plans/run13-auto-cancel-refund-tx/ |
| P1-24 | API/Moderation | **Host cannot remove a player from a roster — FIXED run #14 (b4d098b)**: `DELETE /matches/:id/players/:playerId` (host only, pre-match only — once InProgress/Completed the no-show/dispute flow owns the roster). Ordered guards in tx (self-remove 400 → match 404 → non-host 403 → status 400 → roster 404); Full → Open flip keeps the freed spot joinable; returns populated `findOne` outside tx (contract §2); removed player notified via new `player_removed` ActivityVerb (schema + migration 0023 `ADD VALUE IF NOT EXISTS`, reconciled out-of-band live verbs) + push. PWA: `useRemovePlayer`, two-tap Remove in PlayerProfileSheet (host, open matches, non-host targets), verb wired in feed/bell/toast maps, en/ar i18n (655/655 parity). 7 jest specs. **Live E2E PASS**: create(self-mode)→join→joiner-remove-host 403→host-remove-joiner 200 (roster 2→1, populated response, status Open)→feed shows player_removed→cleanup cancel 200. (Reviewer B finding, run #14.) | matches.controller.ts:255-266; matches.service.ts removePlayer; matches.remove-player.spec.ts; drizzle/0023; PlayerProfileSheet.tsx | **DONE ✅** (run #14: b4d098b, live E2E PASS; run #15 RE-VERIFIED: jest 151/151, vitest 234/234, tsc 0, build 3/3, feed.playerRemoved en/ar labels confirmed) | — |
| P1-25 | API/Venues | **No venue operating-hours / blockout support — BUILT run #17 (7c5a6e1 code + 1141b40 migration 0024)**: `venues` += `open_hour`/`close_hour` (Riyadh-local 24h ints, close exclusive, 24=midnight) + `closed_day_0..6` (0=Sun); `PATCH /partner/venues/:id` accepts hours + closed days (cross-field close>open enforced, 400 otherwise); `generateSlots` skips closed weekdays and clamps every window to `[open,close]` (excess reported in `skipped`); `createSlot` 400s closed-day/out-of-hours with explicit message; player `GET /venues` + `GET /venues/:id` + partner `GET /partner/venues` return the hours fields; PWA clubs page shows Open/Closed badge (Riyadh-TZ, `lib/venue-hours.ts` + 4 vitest cases); admin venues edit form gains hours selects + 7 closed-day checkboxes (en/ar 234/234 parity). **Live E2E PASS**: closed-Friday generate → 0 created/3 skipped; window past close → clamped; createSlot Friday → 400 "closed on that day"; before open → 400 with hours; inside hours → 201. (Reviewer B, run #14.) | schema.ts venues columns; partner.service.ts generateSlots/createSlot; venues.service.ts; venue-hours.ts; clubs/page.tsx; admin venues/page.tsx | **DONE ✅** (run #17: 7c5a6e1, 1141b40; build 3/3, jest 18/18, vitest 238/238, tsc 0, live E2E PASS, migration applied) | docs/plans/run17-venue-hours/ |
| P1-26 | API/Partner | **Partner console has no match/roster visibility — BUILT run #16 (4e7ad44 API + 6171bd4 admin)**: `GET /partner/matches` (scope `today` Riyadh-local / `upcoming`, `?status=` enum filter, limit/offset paging, `COUNT(*) OVER()` total, spots incl. host, no_show counts) + `GET /partner/matches/:id` (populated roster: name/phone/team/is_host/no_show, host first; ownership via assertPitchAccess, Admin bypass). Admin app: `/partner/matches` list + roster detail, `partner.matches` nav section, en/ar 223/223 parity. (Reviewer B, run #14; built run #16; verified run #17.) | partner.controller.ts:130-152; partner.service.ts getPartnerMatches/getPartnerMatch; apps/admin/src/app/(dashboard)/partner/matches/ | **DONE ✅** (run #17 RE-VERIFIED: Reviewer B BUILT-AS-CLAIMED on scope DTO/Riyadh day boundary/roster/audit path + jest re-run 18/18 + live probe `?scope=upcoming` → 2 Cancelled rows exactly as handoff predicted) | docs/plans/run16-partner-match-visibility/ |
| P1-27 | PWA/Wallet | **No wallet withdrawal / cash-out** — withdraw button only shows a `wallet.comingSoon` toast; API has no withdraw/refund endpoints (wallet.controller.ts: balance/history/topup/pay only). Hosts/partners who earn cannot cash out. (Reviewer B, run #16.) | wallet/page.tsx:178; wallet.controller.ts (route grep) | TODO | — |
| P1-28 | API/Venues | **Venue-level discovery missing — BUILT run #21 (2caffce API + 0a2b05d PWA)**: `GET /venues` gains `?search=` — additive name/city ILIKE (never short-circuits geo/city/partner/approved predicates), DTO `MaxLength(80)` → 400; PWA clubs search now runs SERVER-side (300ms debounce → queryKey refetch) instead of client-filtering the first ≤50 rows. City/geo filters already existed server-side — the gap was name search + UI wiring. First-ever venues jest spec (4 cases, PgDialect.sqlToQuery) + 3 hook specs. **Live probe PASS** (restarted API): `?search=KSU`→KSU Stadium, `?search=olaya`→Olaya Sports Park (city, case-insensitive), `?search=ksu&lat/lng`→distance_m 5425m (additive), 81-char→400, nonexistent→0, no-search→4. (Reviewer B, run #16; boarded run #16.) | venues.controller.ts; get-venues.dto.ts; venues.service.ts findNearby; useVenues.ts; (main)/clubs/page.tsx | **DONE ✅** (run #21: 2caffce + 0a2b05d; jest 183/183, vitest 261/261, build 3/3, live probe PASS) | docs/plans/run21-venue-search/ |
| P1-29 | API/Money | **Dispute resolution moves no money** — admin can resolve a dispute but no wallet credit/refund side-effect exists for the wronged player/host (admin/disputes.service.ts:86 defers it); for paid bookings, resolution without refund = support dead-end. Money path must follow P0-2 (real provider) or run through the dev wallet carefully. (Reviewer B, run #17.) | admin/disputes.service.ts:86 | TODO | — |
| P1-31 | PWA/API/Moderation | **Chat messages not reportable — BUILT run #18 (89acff8 A1 fix, e50c727 API, 0b7b8cb admin filter fix, 695c896 PWA)**: `REPORT_SUBJECT_TYPES` += `message` (varchar(50) column — NO migration); self-report guard extended (400 "You cannot report your own message." via sender lookup); `assertSubjectExists` 404s missing messages; `listMine` joins personal_messages + aliased sender users → label `Message from <sender>` (message content stays admin-only); admin `resolveSubject('message')` returns sender + 60-char content snippet + `status:'sent'`; admin list SQL joins pm/su with `COALESCE(..., 'Message from ' || sender)`; `ListReportsDto.subjectType` now imports the shared `REPORT_SUBJECT_TYPES` (was hardcoded 3-value list — caught LIVE in E2E); PWA: per-bubble overflow ⋯ on received DMs → generic ReportSheet (new optional `title` prop, `subjectType="message"`); `reports.subjectType.message` + `report.reportMessage/subjectMessage/messageSheetTitle` en/ar (677/677 parity); **observability restored** in `useReport()` (run-#17 rewrite had dropped `trackEvent('report_submitted')` + `onError captureError` — failures were invisible to Sentry). 4 new jest + 3 new vitest cases. **Live E2E PASS**: 002 sends msg → 001 reports 201 → duplicate 400 → 002 reports OWN 400 → mine-list label → admin queue filter → detail `Message from Khalid Al-Otaibi · "<snippet>"` → resolve → mine-list resolved+text. (Reviewer B, run #18.) | reports/dto/create-report.dto.ts; reports.service.ts (create guard/exists/listMine); admin/reports.service.ts (list SQL/resolveSubject); admin/dto/list-reports.dto.ts; messages/[id]/page.tsx; ReportSheet.tsx; useReports.ts | **DONE ✅** (run #18: 4 commits; run #19 RE-VERIFIED live: report 201 / dup 400 / own-msg 400 / mine label / admin ?subjectType=message / resolve — full E2E PASS on re-run; build 3/3, jest 164/164, vitest 241/241, tsc 0) | docs/plans/run18-review-fixes/ |
| P1-32 | PWA | **Club detail missing hours + pitches — TRUE-GAP STORY**: reviewer-B run #18 claimed "no detail page"; run #19 found the page EXISTS (`clubs/[id]/page.tsx`, built run #9 era 94b16fb, linked from the list card) — the misdiagnosis came from the route sitting OUTSIDE `(main)`. The REAL residual gap: page showed only a pitch count, never operating hours or the pitch list. BUILT run #19: open/closed badge (`isVenueOpenNow`, reuses list-page pattern) + hours row (dir=ltr, hidden when venue defines no hours — post-cycle review minor 515908e) + pitch cards (name, size · surface, SAR/hr via Intl, dir=ltr); i18n `clubs.hours`/`clubs.perHour` en/ar (679/679 parity). | apps/player-pwa/src/app/[locale]/clubs/[id]/page.tsx; useVenues.ts VenueDetailApi; venues.service.ts findOne | **DONE ✅** (run #19: 6c34730 + 515908e; build 3/3, vitest 241/241, i18n 6/6, type-check 0, route 200 live, post-cycle review clean) | docs/plans/run19-review-fixes/ |
| P1-33 | DB/Infra | **Orphan migration `0014_admin_notification_verbs.sql` — chain COMPLETION run #23 (4807bcd)**: run #19's fix (0026) folded back only 4 of the orphan's 7 ActivityVerb values — `account_suspended`/`account_banned`/`no_show_marked` existed in NO journaled migration, so a FRESH `drizzle migrate` errored at 0018 (`ADD VALUE 'account_unbanned' BEFORE 'no_show_marked'` — missing neighbor). Fix: journaled `0029_dear_lizard.sql` (3× `ADD VALUE IF NOT EXISTS`), applied live with 3 `already exists, skipping` NOTICEs (idempotency proof); live enum = 20 values. Orphan file left untouched as historical record. | apps/api/drizzle/0029_dear_lizard.sql; 0018_absent_shotgun.sql:1; 0014_mean_franklin_storm.sql header | **DONE ✅** (run #19: 17d8e78 + **run #23: 4807bcd chain completion — jest 190/190, build 3/3, migrate applied, health 200**; run #24 verify: 0029 in journal + `IF NOT EXISTS` idempotency) | docs/plans/run23-fresh-migrate-verbs-en-offline/ |
| P1-34 | API/Partner | **Partner "upcoming" list included Cancelled matches — FIXED run #19 (06976ab)**: `statusFilter` defaulted `sql\`true\``; now upcoming defaults `status <> 'Cancelled'` (additive AND — never short-circuits time/scope predicates), explicit `?status=` (incl. Cancelled) wins, today recap unchanged (ops surface rationale preserved). jest asserts via `PgDialect.sqlToQuery` (run-#13 P1-21 pattern). Live probe: upcoming default 1 row 0 Cancelled (was 3 incl. 2 dead), `?status=Cancelled` → 2 rows, today unrestricted. | partner.service.ts:801-810,842; partner.match-visibility.spec.ts | **DONE ✅** (run #19: 06976ab; build 3/3, jest 165/165, vitest 241/241, tsc 0, live probe PASS) | docs/plans/run19-review-fixes/ |
| P1-35 | API/PDPL | **`getPublicProfile` leaks soft-deleted users — TODO #31 (booted run #30)**: Reviewer B I2 finding (`users.service.ts:411-427` has no `isNull(users.deleted_at)` filter — a soft-deleted user's name/handle/avatar stays fetchable at `GET /users/:id` for the full 30-day grace, contradicting migration 0031's stated exclusion). 1-line filter to add (matches `searchUsers` pattern at :491) + jest spec. | users.service.ts:411-427 | **DONE ✅** (run #31: d96684b — and(eq(id), isNull(deleted_at)); Reviewer A verdict "sound", SQL-tripwire spec pins the filter; deleted profiles now 404) | docs/plans/pdpl-hardening-31/ |
| P1-36 | API/Auth | **`purpose:'restore'` JWT acts as a full session token on every guarded route — TODO #31 (booted run #30)**: Reviewer B I1 finding (`jwt-cookie.strategy.ts:90-98` only rejects `deleted_at && purpose!=='restore'` — a 31-day restore_token is effectively a 31-day Admin/User session on PATCH profile, POST matches, wallet reads, etc.). Scope `purpose === 'restore'` to `POST /users/me/restore` only — small strategy change + 2 jest specs. | jwt-cookie.strategy.ts:90-98; users.service.ts:566-574 | **DONE ✅** (run #31: 05f086e route-strict scope + signed-iat window + WS deleted-user rejection; resume hardening `13bf4d4` query-string-proof gate + `ec3862e` spent-token replay guard — a restore JWT is NEVER a session token, even after restore succeeds (Reviewer A IMPORTANT #1); strategy spec matrix 15 cases green) | docs/plans/pdpl-hardening-31/ |
| P1-37 | Admin | **No "deleted users" back-office view — TODO #31 (booted run #30)**: approved in the run #29 plan but descoped from #30 to keep the slice tight. New `/admin/users?status=deleted` filter (Admin only — `AdminAuthGuard` already covers) that lists `deleted_at IS NOT NULL` users with their purge_at + restore status; support can cancel a scheduled purge or assist a user past their grace window. | new: apps/api/src/modules/admin/users-deleted.controller.ts; admin/users?status=deleted; en/ar i18n | **DONE ✅** (owner 1043293, run #31 — purge visibility in the users ops table; Reviewer B run #32 verified it matches the board. Run #32's API ghost-guard hardening was rebuilt in the 2026-09-05 repair session: f40acd9, surviving spec 4/4. Residual carved to **P2-47**) | docs/plans/pdpl-hardening-31/ |
| P1-38 | PWA/Admin/API | **No venue photos anywhere** — `venues` table has no images column (schema.ts:287-320: name/city/address/amenities/rating only); clubs list + detail cards are text-only; admin venue form has no image field. Venue photo is the #1 conversion driver for a first-time player choosing between clubs — text-only listings read untrustworthy (Reviewer B, run #34). Size M: image URL-array column + upload/URL MVP + PWA carousel + admin form + i18n. | schema.ts:287-320; grep photo/image in venues/partner DTOs → 0 | TODO (needs owner call: upload pipeline vs URL-field MVP) | — |
| P1-39 | API/PWA | **No-show reputation tracked but invisible & consequence-free outside admin** — `no_show_count` increments (matches.service.ts:2094) and rides the PWA user type, but `getPublicProfile` doesn't expose it and nothing screens on it. Reputation VISIBILITY (roster sheet + public profile show games_played/no-show; optional soft screening) needs NO payment provider — distinct from the money penalty parked on P0-2 (Reviewer B, run #34). Size S-M. | users.service.ts:411-427 (getPublicProfile); matches.service.ts:2094 | **DONE ✅ (visibility half — run #34: 148acb2)** — getPublicProfile returns `no_show_count` (users col + completed-roster FILTER aggregate), PWA PlayerProfileSheet third stat cell (grid 2→3, `profile.noShowCount` en/ar), live E2E 200 with field present. Residual: screening rules/soft-block = owner decision (money penalty parked on P0-2). Mirror t_03c0762e. | docs/plans/run34-last-admin-ghost-guard/ |
| P1-40 | API/PWA | **No join-request/approval mode for matches** — `joinMatch` is instant-first-come (guards: status/dup/roster only, matches.service.ts:931-951); the only host control is after-the-fact removal (P1-24). Approval-gated match mode (host vets joiners) is a core expectation in the Saudi amateur scene; combined with P1-39 there is zero pre-join screening (Reviewer B, run #34). Size M: match mode + request table + host queue UI + push + i18n. | matches.service.ts:931-951 | REJECTED (owner 2026-09-05 — instant-join is the product) | — |
| P1-41 | API/PWA | **No transactional email channel — notifications are push-only** — no mail dependency in apps/api, no mailer module, `users` has NO email column (phone-first, schema.ts); every roster/money/moderation event reaches users only via web-push. Needed: optional user email (collected + verified), mailer module (transport + suppression + bilingual RTL templates), and email mirrors of the transactional events (welcome, reminders, reschedule/cancel+refund, POTM, wallet, disputes/reports, moderation, no-show, removal, venue ownership, underfilled nudge, deletion + purge warning). Full plan pre-staged: docs/plans/run35-email/. | schema.ts (users: no email col; ActivityVerb :100); notifications.service (push-only); no mailer module | TODO — RUN #35 PRIORITY (owner directive) | docs/plans/run35-email/ |

## 🟡 P2 — Polish & tech debt

| # | Area | Item | Evidence | Status | Cycle |
|---|------|------|----------|--------|-------|
| P2-1 | Admin | No generic "refund player" tool — refunds only via dispute no-show reversal + host cancelMatch; payouts = manual status flip with synthetic `PO-` reference, no provider. | admin/settlements.service.ts:65-92,99-150 | TODO | — |
| P2-2 | Admin | **Admin dispute replies — DONE run #24 (ec7cc56 + 87386cf)**: `POST /admin/disputes/:id/messages` (@IsString/@IsNotEmpty/@MaxLength(2000); populated `findOne` return outside tx, contract §2; content-free audit `dispute.message`; allowed in ANY status — closing the loop after a decision is ops-legal) + admin detail-page reply composer (en/ar 5 keys). **Run #24 Reviewer-A CRITICAL fixed in the same slice**: resolve() is now ONE tx — guarded status UPDATE (WHERE status IN (opened,under_review)) FIRST, race loser throws BEFORE the no-show reversal (was: side-effect tx + status UPDATE as two commits → mid-sequence failure un-marked the player while the dispute stayed open → re-resolve double-decremented no_show_count); reopen() got the same status-predicated guard (87386cf). LIVE E2E PASS: reply 201, whitespace 400, unknown 404, re-resolve 400, re-reopen 400, audit rows present. jest 208/208, build 3/3. | apps/api/src/modules/admin/disputes.{service,controller}.ts; dto/post-dispute-message.dto.ts; disputes.service.spec.ts (8/8); apps/admin/.../disputes/[id]/page.tsx; en/ar messages | **DONE ✅** (run #24: ec7cc56, 87386cf, 34694a1, 5c57649; live E2E on restarted API) | docs/plans/run24-admin-dispute-replies/ |
| P2-3 | Partner | Payout flow not partner-initiated; partner earnings view read-only. | partner.service.ts:374-405 | TODO | — |
| P2-4 | API | Money aggregated via `::float` casts in settlement/user totals — rounding risk in sums. **run #5 reviewer added**: `settlements.service.ts:115` `COALESCE(SUM(pitch_cost_sar),0)::float` feeds `amount.toFixed(2)` (float precision risk on large payout sums). | matches.service.ts:198; admin/users.service.ts:103; admin/settlements.service.ts:36,107,115 | TODO | — |
| P2-5 | API | Bare mutation responses (contract §2): `castVote` → `{matchId,votedFor,message}` (matches.service.ts:1752), `createDispute` → bare row (:1585/:1606), `createVenue` → partial `{id,name,city}` (partner.service.ts:91), `deletePitch`/`deleteSlot` → `{deleted:true}` (:369/:630), `createSlot` → bare row (:604), wallet topup/pay → `{ledgerEntry, wallet_balance}`. Matches join/leave/start/complete/cancel are compliant ✅. | matches.service.ts:1752,1585,1606; partner.service.ts:91,369,604,630; wallet.controller.ts:61-67 | TODO | — |
| P2-6 | API | WS `leave-conversation` unguarded — **FIXED run #13 (b3c7445)**: handler now throws `WsException('Unauthenticated')` without `client.userId`, matching its siblings and closing the pre-auth event window. A participant check is deliberately NOT added: leave only shrinks the caller's own event surface, and blocking a since-removed participant's leave would trap them in the room (reasoning recorded in code). | app.gateway.ts:381-394 | **DONE ✅** (run #13: b3c7445; jest 138/138, build 3/3) | — |
| P2-7 | PWA | Offline is read-only cache + banner; no offline mutation queue / background sync (grep `outbox\|BackgroundSync` → 0). | worker/index.js; play/page.tsx:188 | TODO | — |
| P2-8 | API | **Push text localization — DONE (sibling commit 10f94ce, verified run #24)**: `push-text.ts` shared en/ar catalog for all 8 localizable push sites (reminder, nudge, renudge, auto-cancel, reschedule, removal, POTM, report outcome) with per-locale kickoff formatting on Asia/Riyadh; `sendPushToUsers` semantic-key branch renders title/body per `push_subscriptions.locale` (P1-5); legacy title/body branch unchanged (DM/chat pushes locale-neutral); fan-out Promise.allSettled parallel; worker renders ar dir=rtl. jest push-text + push-preferences 17/17. **Run #24 follow-ups (Reviewer-A)**: 34694a1 — kickoffISO fallback no longer fabricates 'now' ('--:--' marker), renudge title fallback 'Match', Sentry capture on non-404/410 push-send failures; 5c57649 — worker deep-links for match-cancelled/player-removed/match-rescheduled/report-resolved (was: 4 types fell through to '/'). | push-text.ts + spec; notifications.service.ts sendPushToUsers; matches.service.ts call sites; worker/index.js | **DONE ✅** (sibling 10f94ce verified run #24; follow-ups 34694a1 + 5c57649) | — |
| P2-9 | API | **Money/state races — FIXED (run #4, 3377567)**: settlement `generatePending` NOT-EXISTS+insert not in tx, no `unique(venue_id, period_start)` → concurrent double-insert; `settlement.pay()` TOCTOU (no `WHERE status='pending'`) → concurrent double-pay; `reports.create` select-then-insert dedup, no unique index → duplicate reports. **Fixed**: unique indexes (`settlements_venue_period_uidx`, partial `reports_open_subject_uidx`), tx-wrap + `onConflictDoNothing` for generatePending, conditional UPDATE for pay. Migration 0018. | admin/settlements.service.ts:65-150; reports.service.ts:19-56; schema.ts:812-892 | **DONE ✅** (run #4: 3377567; jest 71/71; live: generate→201, report dup→400, both indexes in pg_indexes) | docs/plans/run4-data-integrity-races/ |
| P2-10 | API/Wallet | **recordTransaction idempotent replay — FIXED (run #11, 6ad7524)**: a retried request (webhook redelivery) with a used `idempotency_key` now returns `201 { replayed: true, ledgerEntry: <ORIGINAL row>, wallet_balance: <current> }` (Stripe-style) instead of 409; the concurrent unique-violation race path replays too (re-select outside the aborted tx) instead of 500. `isUniqueViolation()` structural guard (23505 + constraint). 5 new jest cases. | wallet.service.ts:77-96,108-146 | **DONE ✅** (run #11: 6ad7524; jest 112/112, vitest 227/227, tsc 0, build 3/3; run #12 RE-VERIFIED live: fresh key 201 credits once, same-key replay 201 `replayed:true` same ledger id, balance 726→727 exactly once) | docs/plans/run11-wallet-replay/ |
| P2-11 | API/Auth | **verify-otp returns JWT in response body when NODE_ENV ≠ production** — **FIXED run #22 (ed7a63f)**: body is `{ isNewUser }` only in every environment, matching the Swagger contract; PWA reads the cookie (its interface never had `token`); scripts use dev-login. jest 190/190, build 3/3, live login+me 200 after restart. | auth.controller.ts verifyOtp | **DONE ✅** (run #22: ed7a63f) | — |
| P2-12 | API/Admin | **Admin scope `sql\`true\`` inconsistent** — `scopedVenueIds`/`scopedPitchIds`/`getDashboard` use it but `getVenues`/`getPitches` inline the ternary. | partner.service.ts:45,55,155,279 | TODO | — |
| P2-13 | PWA | **Clubs screen decorative UI — PARTIALLY FIXED run #17 (088942c)**: "Available Now" pill now filters venues by real operating hours (`isVenueOpenNow`, Riyadh-local) and the dead MapPin header button was removed (no onClick — decorative); location is applied automatically from device permission. **Remaining**: "Nearby" and "Top Rated" pills still no-ops (Nearby needs radius scoring; Top Rated needs ratings). | clubs/page.tsx:44-49 | PARTIAL (Available Now DONE run #17; Nearby/TopRated TODO) | — |
| P2-14 | API/Moderation | **Unban wrong verb — FIXED (run #4, 3377567)**: `verb: updates.banned_at ? 'account_banned' : 'account_suspended'` fired `account_suspended` on unban → a reinstated user told they were suspended. Now emits `account_unbanned` (enum + type + PWA feed/bell/toast + ar/en i18n). | users.service.ts:188-194; activities.service.ts:18-31; ActivityCard.tsx; NotificationSheet.tsx; NotificationProvider.tsx | **DONE ✅** (run #4: 3377567; build 3/3, jest 71/71, vitest 217/217) | docs/plans/run4-data-integrity-races/ |
| P2-15 | PWA/Wallet | **Wallet history queryKey omits page/perPage — FIXED run #13 (b3c7445)**: `useWalletHistory` now accepts `{page, perPage}` (defaults 1/20), includes them in the queryKey (page change refetches) and always sends them to `GET /wallet/history` (API DTO already accepted them). 2 test cases updated/added (params always sent, page-2 key varies). | useWallet.ts:29-52; test/hooks/useWallet.test.tsx | **DONE ✅** (run #13: b3c7445; vitest 8/8 suite, 234/234 full) | — |
| P2-16 | Observability | **`console.error` instead of Sentry `captureError` — FIXED run #13 (b3c7445)**: `usePushNotifications` subscribe/unsubscribe and `ErrorBoundary.componentDidCatch` now `captureError()` with scope tags (console kept for local dev visibility), restoring AGENTS.md §4 compliance. | usePushNotifications.ts:75,95; layout/ErrorBoundary.tsx:33-38 | **DONE ✅** (run #13: b3c7445; vitest 234/234, lint clean) | — |
| P2-17 | PWA/Auth | **Stale-session self-heal — FIXED (run #10, 03d9baa)**: (1) `fetcher` global 401 handler — clears `koralink_token`, logs the store out, redirects to `/login`; auth endpoints (`/auth/*`) excluded so wrong-OTP errors stay inline. (2) `AuthBootstrap` per-session probe — persisted users get exactly ONE `/users/me` verification per browser session (sessionStorage flag); failure clears token + store instead of trapping the shell in error states. 9 new vitest cases (fetcher-401-selfheal ×4, AuthBootstrap ×5). | fetcher.ts:106-126; AuthBootstrap.tsx:15-23,66-95 | **DONE ✅** (run #10: 03d9baa; tsc 0 errors, vitest 227/227, build 3/3, PWA restarted + 200) | docs/plans/authbootstrap-stale-user/ |
| P2-18 | API | **No recurring/standing player matches** — only venue-level weekly slot generation (partner.service.ts:511, SlotManager.tsx:145); players can't set up a repeating booking. | partner.service.ts:511; SlotManager.tsx:145 | TODO | — |
| P2-19 | API/Auth | **`send-otp` rides the global 60 req/min/IP throttler only — FIXED run #22 + run #27**: (run #22) route-level `@Throttle({ default: { ttl: 60_000, limit: 3 } })`; (run #27) **per-IP DAILY cap (50/day) added** — `OtpStoreService` extends with `keys.ip_day(ip)`, `getIpDailyCount(ip)`, `incrementIpDaily(ip)`, `OTP_DAILY_IP_CAP = 50` (cache-backed, same sliding-TTL semantics as the per-phone counter). `auth.service.ts:sendOtp(phone, ip?)` gates BEFORE the per-phone increment so a bot net rotating phones against one source IP cannot drain the Unifonic budget. `auth.controller.ts` threads `req.ip` via `@Req()`. 3 new jest cases. Live-confirmed: dev-login 403, send-otp 200 (P0-7 still in force). | otp-store.service.ts:6,15,29,73-90; auth.service.ts:58,87-107,121-123; auth.controller.ts:4,11,52-61; auth.service.spec.ts (3 new) | **DONE ✅** (run #22 + run #27; jest 9/9 auth, build 3/3, API restart + health 200) | docs/plans/run22-notifications-refund-admin-i18n/ + docs/plans/run27-push-reliability/ |
| P2-20 | API | **`GET /matches` rejects undocumented query params** — `forbidNonWhitelisted` + `GetMatchesDto` has no `limit` → ANY unknown param 400s discovery for external callers. `limit` fixed in run #9 (306c74c); audit remaining consumers (webhooks/partners) for other param shapes before launch, or add an OpenAPI-documented allowlist. | get-matches.dto.ts; Sentry KORALINK-API-E (3× GET /matches 400) | PARTIAL (limit DONE run #9; param audit TODO) | docs/plans/run9-discovery-gender-filter/ |
| P2-21 | Admin | **apiFetch treated 403 as 401 — FIXED (run #9 reviewer, b9ef749)**: any forbidden response cleared the token + hard-redirected to `/login`, logging the user out of the whole console (role downgraded mid-session, admin hitting a partner-only action). Now 401-only logs out; 403 throws a visible permission error with the API's message. | admin/src/lib/api.ts:36-56 | **DONE ✅** (run #9: b9ef749; build 3/3, admin service restarted + 200) | docs/plans/run9-discovery-gender-filter/ (report) |
| P2-22 | API | **ICS calendar route serves private-match metadata without a membership check — FIXED run #13 (24e59af)**: `GET /matches/:id/calendar` now resolves via `getCalendarMatch(matchId, viewerId)` — a minimal projection (no roster/chat): public matches serve as before; private matches are members-only (host fast-path, roster via `isMatchMember`, others 403). Rationale: a calendar export carries the venue ADDRESS and leaves the user's control — it is an export, not an invite-link page view. 5 new jest specs. Note: the residual invite-link exposure of `/matches/:id` metadata itself remains deliberate (invite links), unchanged. | matches.controller.ts:57-105; matches.service.ts getCalendarMatch; matches.calendar-access.spec.ts | **DONE ✅** (run #13: 24e59af; jest 138/138, build 3/3, live probe: roster member 200 on restarted service) | — |
| P2-23 | PWA/API | **Reporters never learn the outcome — FIXED run #17 (14ab3a2)**: player-facing `GET /reports` mine-list (status, reason, resolution, subject label, Riyadh-formatted dates; `subject_label` falls back to the raw id; resolution text only after closure); admin `resolve()` now fires a best-effort `report_resolved` activity (feed + bell, new enum value via migration 0025) + `sendPushToUsers` closure push honoring P1-20 prefs (English push text = accepted P2-8 gap). PWA: new `(main)/reports/page.tsx` "My Reports" (5 UX states, expandable outcome), profile menu link, verb wired in feed/bell/toast maps, `reports.*` + `feed/notifications.reportResolved` i18n en/ar (673/673 parity); `useReports.ts` also carries `useReport()` submission mutation consumed by the sibling-built `ReportSheet.tsx`. **Live E2E PASS**: player report 201 → mine-list open+labeled → admin resolve → mine-list resolved+text → bell shows `report_resolved` (unread, from KoraLink Admin). (Boarded run #10; built run #17.) | reports.controller.ts:22-28; reports.service.ts listMine; admin/reports.service.ts resolve; (main)/reports/page.tsx; useReports.ts | **DONE ✅** (run #17: 14ab3a2; build 3/3, vitest 238/238, tsc 0, jest 160/160, live E2E PASS, migration 0025 applied) | docs/plans/run17-reporter-closure/ |
| P2-24 | Admin | **`apiFetch` crashes on empty/204 bodies + no timeout — FIXED run #14 (24c7388) + run #15 hardening**: 204/zero-length/non-JSON success bodies now resolve `undefined` instead of throwing SyntaxError on unconditional `res.json()`; 30s AbortController timeout surfaces "Request timed out" instead of perpetual loading. **Run #15 (Reviewer A follow-ups):** caller-supplied `signal` previously DISABLED the 30s timeout entirely — now linked via `AbortSignal.any` (with fallback to old precedence where unsupported); caller-initiated aborts are rethrown as cancellations instead of being mislabeled "timed out". (Reviewer A run #14 + run #15.) | apps/admin/src/lib/api.ts:33-60 | **DONE ✅** (run #14: 24c7388; run #15 re-verified + hardened: admin tsc 0, build 3/3, service live 200) | — |
| P2-25 | PWA/API | **Quiet hours with equal start/end (e.g. 23→23) means muted 24/7 with no warning — UI-warning half BUILT run #16 (cda3833)**: profile shows an inline amber warning (role=status, en/ar) when start===end. **RESOLVED by Abdullah decision 2026-08-29 (option 1 on both): KEEP current behavior on both halves — equal start/end = muted 24/7 (warning shipped), and NO quiet-hours exceptions (all categories incl. kickoff/removed/cancelled stay muted inside the window). Nothing to build.** (Reviewer A run #14 + Reviewer B run #15 + run #16 build.) | notifications.service.ts:135; profile/page.tsx quiet selects | DONE (decision 2026-08-29: keep current behavior both halves — equal=muted 24/7 w/ warning; no exceptions. Nothing to build.) | — |
| P2-26 | PWA | **Profile + host screens missing UX states — FIXED run #19 (357c543)**: profile stats row now has all three states — animate-pulse skeleton (wallet idiom), error + Try Again via `useUserStats().refetch` (reuses common.error/common.retry; no new keys), success unchanged; host page's `<Suspense fallback={null}>` blank flash replaced with a form-shaped `HostFormSkeleton` (aria-busy/aria-live). | apps/player-pwa/src/app/[locale]/(main)/profile/page.tsx; apps/player-pwa/src/app/[locale]/host/page.tsx | **DONE ✅** (run #19: 357c543; build 3/3, vitest 241/241, type-check 0, lint clean, /en/profile + /en/host 200 with skeleton in HTML) | docs/plans/run19-review-fixes/ |
| P2-27 | API | **POTM pushes bypassed delivery preferences — FIXED run #17 (b12d13e)**: `sendPomDecidedNotification` iterated raw match subscriptions (no mute/quiet-hours/locale) — the only push path ignoring P1-20 prefs. Now routes through `sendPushToUsers` (roster user IDs via Drizzle selectDistinct); also fixes the `as unknown as` raw-execute cast (Reviewer A) and per-subscription locale for POTM deep-links (P1-5 gap); dead `winnerId` dropped (0 PWA consumers). 3 new jest cases (muted/quiet/locale+winnerId). | notifications.service.ts getMatchRosterUserIds + sendPomDecidedNotification; notifications.push-preferences.spec.ts | **DONE ✅** (run #17: b12d13e; jest 9/9, build 3/3, restart + health 200, dist grep verified) | docs/plans/run17-potm-push-preferences/ |
| P2-28 | PWA | **SW serves match data up to 60s stale on error paths — FIXED run #18 (86b927c)**: `/api/matches` route was `StaleWhileRevalidate` (stale-first on EVERY load — with React Query's own 60s staleTime the user could never see fresher-than-60s availability); now `NetworkFirst` with `networkTimeoutSeconds: 3` — fresh data preferred, cache only the offline/slow-network fallback. Source fixed in `next.config.mjs` (generated `public/sw.js` is gitignored; regenerated + verified `NetworkFirst` in sw.js post-build). (Reviewer B, run #17.) | next.config.mjs runtimeCaching[0] | **DONE ✅** (run #18: 86b927c; run #19 RE-VERIFIED: sw.js grep NetworkFirst + networkTimeoutSeconds:3 PASS) | docs/plans/run18-review-fixes/ |
| P2-29 | PWA | **Host page sits outside the (main) shell** — `host/page.tsx` is not under the tab-bar layout while wallet/play/clubs/messages are; hosts have no persistent nav entry (creation discovered via the two `/host` CTAs on the play feed, play/page.tsx:93,175). Confirm intent; consider nav placement for the supply side. (Reviewer B, run #17; downgraded from reviewer's P1 — CTAs exist.) **DECIDED by Abdullah 2026-09-02: keep host page OUT of the tab bar; instead make the "+" icon button a MORE FEATURED, INFORMATIVE CTA for hosting a match** (prominent placement/visual weight on the play feed, informative treatment — label/hint of what hosting does — per koralink-ui-standards, EN+AR). | apps/player-pwa/src/app/[locale]/host/page.tsx; play/page.tsx:93,175 | TODO (decision locked 2026-09-02: featured "+" CTA, no tab) | — |
| P2-30 | Admin | **Partner matches UI lacks pager + venue/pitch filter — BUILT run #18 (589cd2d)**: `GET /partner/matches` adds `?venueId=`/`?pitchId=` (additive AND clauses per the venue-feed rule; pitchId wins when both sent; foreign pitch ids ignored — scope filter falls through to venue; never short-circuits status/time predicates; ownership unchanged — scopedPitchIds still bounds everything). Admin `/partner/matches`: venue select (from `GET /partner/venues`) + dependent pitch select (from `GET /partner/pitches`, filtered client-side by venue, cleared on venue change) + server-side pager (page size 20, Prev/Next, `Page X of Y`, resets to page 1 on any filter change); `OpsEntity` union += `pitches` for live refresh; i18n en/ar 239/239 parity (`partner.matches.filterAllVenues/filterAllPitches/prevPage/nextPage/pageOf`). **Live verify PASS** (owner +966**0003, restarted API): unfiltered upcoming 3 rows/2 venues → venue filter 1 row (KSU) + 2 rows (Olaya) with 0 wrong-venue rows; pitch filter 1 row 0 wrong-pitch; pager `limit=1&offset=0/1` distinct rows + `hasMore:true`; fake venueId → 0 rows (no scope widening). (Reviewer B, run #17.) | get-partner-matches.dto.ts; partner.service.ts getPartnerMatches scopeFilter; partner.controller.ts; apps/admin/.../partner/matches/page.tsx; use-live-data.ts | **DONE ✅** (run #18: 589cd2d; run #19 RE-VERIFIED live: venue containment 0 wrong-venue rows, pitch filter 1/0, pager distinct+hasMore, fake venueId 0 rows; residual pitchId-silent-ignore documented as P2-32) | docs/plans/run18-review-fixes/ |
| P2-31 | API/PWA/Admin | **Reviewer-A minors from run #18 (queue, not built)**: (1) admin venues form hours guard — **DONE run #20 (c9e9b0b)**: saveEdit validates open∈[0,23]/close∈[1,24]/close>open BEFORE the request; empty/NaN shows localized `venues.hoursInvalid` (en/ar 240/240) instead of serializing null + raw API error. (2) `reports.service.ts` listMine hard cap 50, no pagination — **DONE run #23 (5d9684f)**: `?limit` (1-50)/`?offset` DTO bounds, `{reports,total,hasMore}` envelope via `COUNT(*) OVER()`, PWA `useInfiniteQuery` + Load More (`reports.loadMore` en/ar); jest 193/193 (+4), vitest 261/261, build 3/3, LIVE E2E PASS (envelope, paging hasMore:true, limit=999/offset=-3/foreign-param all 400). (3) Overnight venue windows (close≤open) unrepresentable — 20→02 venues can't be modeled (documented limitation). (4) Offline banner only on feed/play/wallet — **DONE run #22 (3016721)**: clubs/my-games/messages/reports now carry the same `useOnlineStatus` + `common.offlineBanner` banner. (5) POTM push `data.winnerId` dropped — **VERIFIED run #22: zero `winnerId` consumers in PWA/API/SW — nothing to do.** | partner/venues/page.tsx:70-100 (guard); reports.service.ts listMine + dto/list-my-reports.dto.ts; reports/page.tsx Load More; venues page | PARTIAL ((1)(2)(4)(5) DONE — (2) run #23; (3) documented limitation) | — |
| P2-32 | API/Partner | **Foreign `pitchId` on GET /partner/matches silently ignored — FIXED run #27**: owner decision 2026-09-02 was 404 for foreign-but-valid pitchId. `partner.service.ts:860-869` now throws `NotFoundException('Pitch not in your scope.')` after the `scopedPitchIds` check, BEFORE the existing query. Distinguishes from `assertPitchAccess` 403/404: getPartnerMatches is a read-only listing, so a missing-pitch path is a "not visible to you" answer, not a permission error. 2 new jest cases (foreign throws 404, own accepted). | partner.service.ts:860-869; partner.match-visibility.spec.ts (2 new cases) | **DONE ✅** (run #27; jest partner.match-visibility PASS, build 3/3) | docs/plans/run27-push-reliability/ |
| P2-33 | Admin | **Admin could not survive a FRESH build (latent, exposed run #19)**: since the 2026-08-29 lockfile sync (React 19 types), `useParams()`/`usePathname()` are typed nullable — admin's 5 detail pages (`venues/reports/partner-matches/users/disputes [id]`) and Sidebar.tsx failed `tsc` on any cache-busting build; admin also lacked `not-found.tsx` (PWA has it). Runs #17/#18 never saw it because their admin builds were turbo cache hits. FIXED run #19 (2d715a1): `params?.id ?? ''` ×5, `usePathname() ?? ''`, app-router not-found.tsx added. **Runbook lesson: turbo caches by env hash — `NODE_ENV` leakage (e.g. sourcing apps/api/.env) silently flips builds; unset before gates.** | apps/admin/src/app/(dashboard)/*/\[id\]/page.tsx; Sidebar.tsx:46; not-found.tsx | **DONE ✅** (run #19: 2d715a1; fresh admin build green + service 200) | docs/plans/run19-review-fixes/ |
| P2-34 | PWA | **NotificationBell mounts on the feed page ONLY — FIXED run #22 (0803e10)**: the same self-contained `NotificationBell` now mounts in the header rows of play, clubs, my-games, wallet and messages (feed keeps its own); sheet/badge behavior unchanged (z-[60]/[70], server-synced badge, multi-tab safe). No dedicated `/notifications` route (would duplicate the sheet); Reviewer B's recommendation implemented as the per-tab mounts. vitest 261/261, tsc 0, lint 0, build 3/3. | play/clubs/my-games/wallet/messages page.tsx (P2-34 mounts); components/layout/NotificationBell.tsx | **DONE ✅** (run #22: 0803e10; run #23 VERIFIED-TRUE: exactly 6 mounts — play:80, clubs:82, my-games:59, wallet:149, messages:103, feed:97 — no double-mount) | docs/plans/run22-notifications-refund-admin-i18n/ |
| P2-35 | PWA | **clubs/[id] `formatDateLabel` hardcodes `'en-US'`** for weekday/month — Arabic users see English date labels on club detail cards; sibling `dayLabel` in RescheduleSheet and MatchDetailsForm already locale-switch (`ar-SA`/`en-US`). Also `venue-hours.ts:36` `riyadhHour` should pin `hourCycle: 'h23'` (en-GB + hour12:false can yield "24" at midnight on some ICU builds). (Reviewer A run #20 minors.) | clubs/[id]/page.tsx:55-61; lib/venue-hours.ts:36-40 | **DONE ✅** (run #21: locale-aware + h23 rider) | — |
| P2-37 | API | **rescheduleMatch `price_per_player` divide-by-(max_players−1) unguarded** (matches.service.ts:1920) — same expression class as createMatch; `max_players` DTO-floored ≥2 (create-match.dto.ts @Min(2)) so not reachable today, but data-drift/legacy rows would persist Infinity/NaN (no guard unlike the `newDuration <= 0` guard at :1831). (Reviewer A, run #21.) | matches.service.ts:1920; create-match.dto.ts:50 | **DONE ✅** (run #21: guard + spec) | docs/plans/run21-venue-search/ |
| P2-38 | Admin | **Admin disputes/reports pages lack empty/error states** — **STALE/DONE (run #22)**: the admin-ux-overhaul cycle (slices 5, a4cf5da) already shipped loading, `t('empty')` and localized error states on both pages; run #22 additionally localized the error line (`hq:loadFailed`) across all 11 HQ pages (2c46403). Nothing left to build. | disputes/page.tsx:50-51; reports/page.tsx:69-70 | **DONE ✅** (verified run #22: states exist + localized; 2c46403) | — |
| P2-39 | API/Money | **Admin refund TOCTOU — two concurrent refunds of the same completed debit both pass the outside-tx pre-check; loser 500s on the refund-<id> unique key (or double-credits if the key were ever dropped) — FIXED run #22 (85e8326)**: FOR UPDATE row lock + status re-assert INSIDE the tx, idempotent insert with zero-rows → clean 400 for the loser (rollback no-op). 7 jest cases incl. concurrent-loser + zero-side-effects. (Reviewer A, run #22.) | admin/transactions.service.ts refund; transactions.refund.spec.ts | **DONE ✅** (run #22: 85e8326; run #23 VERIFIED-TRUE: FOR UPDATE at transactions.service.ts:87, in-tx re-assert, loser-400; jest 7/7) | docs/plans/run22-notifications-refund-admin-i18n/ |
| P2-40 | PWA | **Offline navigation fallback is AR-only and misses inner pages — FIXED run #23 (175df30)**: built sw.js had NO `setCatchHandler` — the `fallbacks.document: '/ar/offline'` glue only fired via `handlerDidError` on the registered routes (start-url `/`, API, assets), so an offline navigation to an inner page (`/en/play`) matched NO route → browser error page; and when the fallback DID fire, EN users got Arabic copy (precache carried only `/ar/offline`). Fix: `worker/index.js` warm-caches `/en/offline` + `/ar/offline` at install (`koralink-offline-pages`) and intercepts document navigations under `/en/*` + `/ar/*` — network first, locale-matching offline page on network failure. Verified: new bundle `worker-579f91e6df06602a.js` carries the logic, live sw.js references it, both offline routes 200; vitest 261/261, tsc 0, lint clean, build 3/3. (Reviewer A, run #23.) | apps/player-pwa/worker/index.js; next.config.mjs comment; sw.js precache | **DONE ✅** (run #23: 175df30; on-device visual check pending — no browser on box) | docs/plans/run23-fresh-migrate-verbs-en-offline/ |
| P2-41 | API/Wallet | **Koralink-booking wallet TOCTOU — FIXED run #25 (8d890ce, Reviewer-A IMPORTANT #1)**: `createMatch` (koralink mode) read the host's `wallet_balance` with a plain SELECT inside the booking tx, then ran an UPDATE with NO `wallet_balance >= cost` guard. Two concurrent koralink match creations by the same host against two different slots could both pass the in-tx balance check, both subtract, and drive the balance negative. Same fix class as P0-4 (run #13) + P2-39 (run #22): the integrity guard is now INSIDE the row update (`UPDATE users SET wallet_balance = wallet_balance - $cost WHERE id=$hostId AND wallet_balance >= $cost`), zero rows = `BadRequestException` (re-reads the user for the same-shape error message), ledger insert is reached only on a successful deduct. 4 jest specs (sufficient balance, insufficient balance w/ no ledger, concurrent same-host two-slot race, self-mode unaffected). Pino warn on the zero-rows path carries `hostId/required/slotId`. **BUNDLED with Reviewer-A #3**: `notifications.service.ts:234-238` Sentry capture now fires only on `statusCode >= 500` or `statusCode === undefined` (timeout); 4xx per-subscription noise (429, 400) was flooding Sentry — now a `status_code` tag (filterable) + debug log only. **Gates**: jest 213/213 (+4), vitest 272/272, tsc 0, build 3/3, dist grep confirms. **Live E2E deferred to run #26** (requires partner-side slot generation on a fresh pitch; spec covers all 4 branches). | apps/api/src/modules/matches/matches.service.ts:1247-1280; matches.koralink-wallet.spec.ts (4/4); apps/api/src/modules/notifications/notifications.service.ts:234-238 | **DONE ✅** (run #25: 8d890ce; build 3/3, jest 213/213, vitest 272/272) | docs/plans/run25-wallet-koralink-booking-tx/ |
| P2-42 | PWA/Security | **CSP `script-src` keeps `'unsafe-inline' 'unsafe-eval'`** (next.config.mjs:171) — standing Reviewer-A item 11, re-flagged runs #14, #15, #23, #25. Weakening CSP. Migration path: Next.js per-request nonces (`__webpack_require__` injection via middleware) + `unsafe-inline` removal. `unsafe-eval` is harder — needed for `next/script` `dangerouslySetInnerHTML` patterns and some webpack chunks. A 2-PR effort: (1) nonce middleware + drop `unsafe-inline`; (2) drop `unsafe-eval` after auditing every `<Script>` usage. (Reviewer A standing, run #25.) | next.config.mjs:171 | TODO (standing) | — |
| P1-19 | API/Auth | **No phone-number change flow** — auth identity is `users.phone`; grep `updatePhone\|change-phone` across `apps/api/src/modules/users/` → 0 hits. A user who loses their SIM is permanently locked out (OTP = sole credential). Compounds P0-6 (PDPL account-delete): a soft-delete + recreate cycle is the only escape hatch today. (Reviewer B, run #26.) | apps/api/src/modules/users/ | TODO (booted run #26) | — |
| P1-20 | API/Auth | **No partner self-onboarding** — auth surface is OTP login only (`auth.service.ts` methods: sendOtp/verifyOtp/devLogin only, :58/:103/:206); no registration endpoint assigns `VenueOwner`; roles exist only via seed/admin SQL. A Saudi venue partner cannot sign up — ops dependency on manual DB seeding. Decision needed: invite-token flow (admin issues link, partner completes profile) vs. self-registration with admin approval gate. (Reviewer B, run #26.) | auth.service.ts:206-239; users.controller.ts | TODO (booted run #26) — needs product decision | — |
| P2-43 | API/Push | **No push-subscription lifecycle sweep** — `notifications.controller.ts` exposes subscribe/unsubscribe only (POST pair, grep → 0); no stale-subscription sweep beyond per-send 404/410 prune. Devices that silently drop push (browser uninstall, OS settings change) accumulate dead rows in `push_subscriptions`. A nightly cron `DELETE FROM push_subscriptions WHERE updated_at < now() - interval '90 days' AND endpoint NOT IN (recently-delivered)` would close the gap. (Reviewer B, run #26.) | notifications.controller.ts; schema.ts:push_subscriptions | TODO (booted run #26) | — |
| P2-44 | PWA/Security | **DevLoginBar hostname gate was allowlist-by-prefix** (apps/player-pwa/src/components/auth/DevLoginBar.tsx:23-30) — rendered when `NODE_ENV !== 'production'` OR hostname startsWith `192.168.|172.|10.|100.` / `.ts.net`. Run #26 (P0-7 slice 1) replaced this with a build-time `NEXT_PUBLIC_DISABLE_DEV_LOGIN_BAR` constant. The hostname allowlist was a SECURITY GAP: on a prod build served from a tailnet host, the bar was visible AND its `devLogin()` calls would 403 (after the P0-7 fix), but the seeded phone list leaked. Closed as part of P0-7. | DevLoginBar.tsx:23-30 (pre-run-#26) | **DONE ✅** (run #26: bfd453b — folded into P0-7 slice 1) | docs/plans/run26-security-sprint/ |
| P2-45 | PWA/Perf | **Persisted query cache (offline-first cold start)** — kill+reopen previously refetched every screen from zero (memory-only TanStack Query; SW cache only helps offline/timeout). NOW: last snapshot restores from IndexedDB BEFORE network responses; staleTime/refetchOnMount unchanged; `wallet*`/`auth*`/`notifications*` never hit disk (shared-device privacy, mirrors SW NetworkOnly); logout wipes the snapshot (covers SignOut + fetcher-401 + AuthBootstrap funnels via the single `logout()` convergence point); 7-day maxAge + `v1` buster invalidation. Zero new dependencies (inline ~60-line IDB wrapper `query-cache-db.ts`; upstream `@tanstack/query-persist-client` not installed and npm approval-gated). Zero API/i18n/SW changes. | src/lib/query-persister.ts; src/lib/query-cache-db.ts; src/providers/QueryProvider.tsx; src/store/slices.ts (logout wipe); test/lib/query-persister.test.ts (11 cases) | **DONE ✅** (verified run #31, Reviewer B all ✓: query-persister.test green, IDB persister with ≤1 write/s coalescing + corrupt/quota fail-open confirmed, logout wipe funnel via single logout() convergence at query-persister.ts:182-185; vitest 328/330 — the 2 failures are sibling PromoBillboard WIP, not this slice) | docs/plans/pwa-cache-persistence/ |
| P2-46 | PWA/Perf | **Local DB layer + offline mutation queue (Level 4)** — Abdullah-requested follow-up to P2-45: queryable local store (Dexie) with partial sync + offline writes (join/leave/message queued while offline, replayed on reconnect). Only worth building if offline ACTIONS become a product requirement; P2-45 covers the cold-start speed need. | — (design note in docs/plans/pwa-cache-persistence/) | TODO (parked by owner request 2026-09-03) | — |
| P2-47 | Admin/PDPL | **Admin cannot cancel a scheduled purge or restore-on-behalf** — P1-37 (1043293) delivered VISIBILITY only. Support has no action to cancel a scheduled purge (clear deleted_at within the grace window) or assist a user past grace. NEEDS OWNER DECISION first: cancel-purge vs full restore-on-behalf (scope + audit verbs). (Reviewer B, run #32.) | apps/admin users ops table; apps/api admin/users.service.ts | TODO (needs Abdullah decision) | — |
| P2-48 | Infra/Docker | **Admin image build-breakers (latent — deliberately NOT rebuilt)** — run #32 fixed these in-tree (Dockerfile `RUN mkdir -p public` + declare `socket.io-client` in apps/admin/package.json — a phantom dep that host hoisting hides and in-image npm ci cannot resolve) but the edits were lost when run #32 crashed at its commit step. Coolify reportedly builds fine today, so the 2026-09-05 repair did NOT re-apply them. Re-apply + live `docker build` verify if the next Coolify deploy breaks, or fold into the next docker cycle alongside A3 (root-Dockerfile workspace-manifest strictness). (Reviewer B run #32; repair session 2026-09-05.) | apps/admin/Dockerfile:35; apps/admin/package.json | TODO (Coolify-verified cycle) | — |
| P2-49 | API | **Unguarded check-then-act mutations (class-3, no status predicate in WHERE)** — reports.service.ts:112 resolve / :178 reopen / :206 update + disputes.service.ts:270 update use `where(eq(id))` only (concurrent resolve/reopen race can double-write and leave audit `after` stale; dispute resolve/reopen themselves ARE correctly status-predicated); venues.service.ts:117/126 decide() silently overwrites an already-decided venue. Correct exemplars: settlements.pay, transactions.refund (FOR UPDATE + in-tx re-assert), partner deleteSlot. (Reviewer A, run #34.) | reports.service.ts:112,178,206; disputes.service.ts:270; venues.service.ts:117,126 | **DONE ✅ (run #34: 1dea7b0)** — reports resolve/reopen status-predicated UPDATE + returning zero-rows→409 (run #24 pattern); venues decide guards `venue_verifications.status='pending'` INSIDE the tx (verification UPDATE first — raced decision can never flip approval; partner resubmit upserts back to pending so flow intact). reports.update/disputes.update deliberately unguarded (contract = edit in ANY status). Spec reports-venues.guard.spec.ts 4/4, jest 343/343. Mirror t_41f3f42c. | — |

---

## Backlog (overflow — minor polish, revisit when P-lanes clear)

- **Run #34 notes**: (1) narrow hydration window — `Date.now()` in render at my-games/page.tsx:19 + MatchCard.tsx:35 (POTM voting-open check); client components self-heal next render, mismatch only if server HTML straddles the deadline instant — fold into the next hydration-safety touch, not urgent (Reviewer A run #34). (2) OS research for P1-38 venue photos: UploadThing (managed, best DX), Cloudinary (managed + transforms), self-hosted S3/MinIO (fits the VPS-first infra) — Abdullah picks the approach; adoption only via a Gate cycle. (3) drizzle parameterizes enum literals in rendered SQL (`role = $1`, not `'Admin'`) — SQL-tripwire specs must assert the bind-param shape.

- **OS research (run #19)**: open-source maps for the P1-32 club-detail map — **Leaflet** (42KB, BSD-2, OSM tiles, no API key; `react-leaflet` wrapper) is the lightest fit for a PWA static map embed; **MapLibre GL JS** (BSD-3, maplibre/maplibre-gl-js, ~4.7k★ active) if vector tiles/routing polish is wanted later. Both keyless + license-clean. Adoption through a normal Gate cycle — never mid-run. (Run #16's `@serwist/background-sync` card below still stands for P2-7.)

- **OS research (run #16)**: `@serwist/background-sync` (`BackgroundSyncPlugin` + `registerRoute`, MIT, Workbox-lineage) is the natural path for P2-7 offline mutation queue — same API family the PWA's SW already uses. Caveat: the PWA worker is hand-written `worker/index.js` (not Serwist-generated), so it's either a Serwist migration or a hand-rolled IDB queue + sync-event replay. Adoption goes through a normal Gate cycle.

- Admin `useLiveAdminData` opens a separate Socket.IO connection per hook instance (dashboard mounts 3 → 3 sockets + 3×30s intervals); should share one socket. `use-live-data.ts:61-86`.
- Admin settings mutations not audit-logged and never capture `adminId`. `settings.service.ts:27-40`.
- `users.service.ts:191` unban emits verb `'account_suspended'` (ternary misuse). Minor.
- `drizzle/seed.ts:8,10` doc drift: header says "8 users" (inserts 15) and stale "reviews" mention. Cosmetic.
- WS gateway `JWT_SECRET` fallback `'fallback-dev-secret'` (app.gateway.ts:109) survives the P0-3 bootstrap hardening — dev-only fallback, but should be flagged/removed for prod parity.
- Zustand persists server cache (`balance`, `paymentMethods`, `bookedMatchIds`, useAppStore.ts:90-99) — server state shouldn't live in the persisted store.
- `GetMatchesDto.format`/`gender` typed `string` not union (get-matches.dto.ts:55,64) — weak DTO enum typing (body DTOs use proper unions).
- Notifications subscribe/unsubscribe accept a raw `SubscribeBody` interface, no class-validator DTO (notifications.controller.ts:21-28). Minor parity gap.
- Dead `rating` column persists after the reviews removal (schema.ts:187,222) — dropped in code, not in schema. Minor.
- `POST /wallet/pay` reuses `TopupWalletDto` (wallet.controller.ts:93) — should have its own DTO. Minor.
- Play search bar is decorative dead UI — `<span>"Where to play?"</span>` in `TopAppBar.tsx:31`, no input/handler (run #6 reviewer).
- No per-category notification preferences — only a global push toggle in profile (run #6 reviewer).
- CSP `script-src` includes `'unsafe-eval' 'unsafe-inline'` (`next.config.mjs:171`) — weakens CSP; narrow via nonces later (run #6 reviewer).
- `personal_messages` DM idempotency TOCTOU — tracked as P1-11 (run #6 reviewer). **BUILT run #7 (dfc671f).**
- API/PWA systemd units (`koralink-api.service`, `koralink-pwa.service`) not committed to repo — only `apps/admin/deploy/koralink-admin.service` is. Commit for reproducible deploys (run #7 reviewer).
- `mark-no-show.dto.ts:6` uses `@IsUUID()` while every other ID DTO uses varchar(36)/`@IsString` — rejects non-UUID-format IDs (run #7 reviewer).
- `castVote` upsert resets `created_at` on a vote change (matches.service.ts:1750) — minor (run #7 reviewer).
- `seed.ts` still seeds `rating` (seed.ts:65,76,87,239) after the reviews removal — dead column (run #7 reviewer).
- **Migration files 0002/0008 were edited in-place after apply**; tracking reconciled manually run #8 (`drizzle.__drizzle_migrations` now 22 rows, `db:migrate` re-verified working). Do NOT edit applied migration .sql files — generate a new migration instead (run #8).
- `TopAppBar.tsx` is wholly orphaned dead code (imported nowhere) with no-op bell/`+`/search (run #8 reviewer).
- `reports.service.ts:64` create() returns bare report row; `settlements.service.ts:155` generatePending returns bare array — bare-return contract (P2-5 class), low impact (run #8 reviewer).
- **2026-08-28 09:09Z incident (run #9 post-mortem)**: API restart at 09:09:38 → boot-time `autoCompletePastMatches` hit transient Postgres ECONNRESET → unhandled rejection killed boot → **API down ~52 min** until parent session's `1bccdb0` (10:04Z) added try/catch + `/health` alias. Sentry KORALINK-API-W has the single event. Scheduler ticks (`matches.scheduler.ts:19-47`) were ALREADY individually try/caught — only the boot path wasn't. Fix verified compiled+live (dist grep + `/health` 200). Reconcile systemd units into the repo (backlog item below) so restarts during deploys don't race builds.
- Reviewer delegation note (run #9): BOTH GLM reviewer dispatches hit the 600s child cap (27/49 tool calls completed, no findings emitted — findings-only-at-end pattern dies on timeout). Run #10 should instruct reviewers to emit findings INCREMENTALLY (per-area summary comments mid-run) or split into 2 shorter scoped dispatches.
- Product gaps (run #9 reviewer #2): PWA has NO search at all (TopAppBar search is static decoration; no venue/match search screen exists) · Admin has no broadcast/announcement tooling (ops can't notify user segments) · API has no venue ratings/reviews module (reputation = admin approval only).
- `partner.service.ts:83-97` createVenue returns bare partial `{id,name,city}` while createPitch/updateVenue/updatePitch return populated rows — bare-return contract class (P2-5), low impact (run #9 reviewer #2).
- **Run #18 leftovers**: A3 refuted (generateSlots "skipped double-count" is exact disjoint accounting: (rows−created)+(capacity−rows) = capacity−created — no fix needed; do NOT re-flag). Wallet FetchError Sentry noise is handled 4xx (PublishWarningSheet classifies + top-up CTA) — MINOR, no action. Auth `surface:"ops"` needed for admin dev-login (E2E recipe). `/tmp/run18-e2e.sh` has the full message-report E2E recipe.
- `apps/admin/src/lib/rbac.ts:94-135` — 15 `ConsoleAction` flags defined, never consumed (only `SECTION_BY_ROLE` is read). Dead scaffolding, not an exploit — API remains the boundary (run #9 reviewer #2).
- **Mirror-board hygiene debt (run #10)**: 4 mirror cards were found already fixed in code but never re-verified/marked — t_61e5ff50 (partner Admin-scope: DONE run #3 d93e1ea), t_09ccc370 (POTM socket namespace: DONE run #5 58b3ba3), t_0727919d (DM idempotency TOCTOU: DONE run #7 dfc671f), t_222e530a (WS ban/suspend handshake: DONE run #6 4df0d4d); plus t_65ac117e overstated the private-match exposure (real residual demoted to P2-22). Mirror cards are commented, never completed (t_ce9a513a gate) — BOARD.md remains the authoritative DONE record.
- Run #12 reviewer minors: FilterBar.tsx:147 hardcodes "≤ {p} SAR" (not localized for ar) · profile/page.tsx:130-136 camera-icon button actually navigates to personal-info (misleading affordance) · match/[id]/page.tsx:607 WhatsApp invite href is an LTR construct (verify RTL) · `Date.now()` in render (my-games:17, wallet:42, (main):40, MatchCard:35) is a latent hydration risk — currently safe only because no SSR-prefetched data feeds it · bare `.returning()` remains in matches.service.ts:1101 (evidence-attach) and dispute create (:1624) — P2-5 class, non-core.
- **PWA lint is a CI gate (`--max-warnings 0`)**: run #12 found the tree's `npm run lint` already failing on 3 COMMITTED files (react-hooks/exhaustive-deps in usePushNotifications.ts:79, unused vars in PostMatchSection.test.tsx:7 + usePwaInstall.test.tsx:3) — fixed in run #12's lint commit. `next build` does NOT run eslint; only CI and explicit `npm run lint` do. Run lint after test-file edits.
- **Infra minors (Reviewer A, run #23)**: CSP `script-src 'unsafe-inline' 'unsafe-eval'` + blanket `ws:`/`wss:` connect-src (next.config.mjs:171,144-145) — already-backed nonce migration stands, now with wildcards noted; `manifest.json` hardcodes `lang: "ar"`/`dir: "rtl"` (EN installs get Arabic manifest metadata) — candidate per-locale manifest; GiST geo indexes bootstrap only via docker-entrypoint + `scripts/dev-bootstrap.sh:46` (documented in gist_indexes.sql; fresh NON-docker DBs also need the 0029 verb chain — now complete). Both GLM refutations recorded for the record: "sw.js committed build artifact" — gitignored (`git check-ignore` PASS); "no partner web app" — full portal at `apps/admin/src/app/(dashboard)/partner/` (venues/pitches/matches/earnings/settings).
- **GLM reviewer 600s child cap (run #10, 2nd consecutive)**: reviewer dispatched with incremental-output instructions STILL timed out at 600s (42 API calls, comprehensive in-progress work visible in the live transcript, no final emitted). The cap kills the audit mid-PWA regardless of output pattern. Run #11: split into 2 shorter scoped dispatches (e.g. Admin+API then PWA+DB) or accept self-review as primary and treat GLM as supplementary.
- **Run #21 reviewer notes**: A2 REFUTED — app.gateway.ts:353 `join-conversation` DOES enforce the participant check (`if (!ok) throw WsException('You are not a participant…')`); the "deliberately omitted" comment at :388-390 is the documented P2-6 leave-conversation decision. Reviewer B P0 "no chat moderation" partially refuted — P1-31 already ships per-message reporting (ReportSheet subjectType=message, admin queue); residual = AUTOMATED UGC filtering (profanity) = product decision, NOT boarded. B P0 "wallet ledger unauditable vs bank records" → folded into P0-2 (needs real provider + reconciliation report). B P1 no-show/late-cancel penalty = product rule needing the P0-2 money path → not boarded (recorded; decide with payments). B P1 SMS-bomb throttle re-confirms existing P2-19 (no new item). B P1 reminder fan-out has no failure surface (reminders_sent_at set, no dead-letter/Sentry alert when Unifonic fails) → observability backlog with koralink-observability skill. B P1 unread-badge offline sync → P2-7-adjacent. B P2 offline deep-link fallback (sw.js scope) + per-sheet error isolation → polish backlog.
- **Run #22 reviewer notes**: Reviewer B re-ranked P2-34 as its top P0-adjacent gap (fixed this run, 0803e10). Reviewer B residuals: offline MUTATION queue (P2-7, not boarded new), automated UGC/profanity filtering (product decision, stands), admin broadcast/announcement tooling + venue ratings (pre-existing backlog), float-money `::float` math in settlement sums (P2-4-adjacent, polish). Reviewer A residual MINOR: dashboard/page.tsx `.toLocaleString()` without locale arg won't follow ar number formatting (contrast partner/page.tsx:97 pinning 'en-GB' + dir=ltr) — fold into the next admin i18n touch. Sentry KORALINK-API-Y (`schema "v" does not exist`, 1 event at 10:22:49Z inside run #21's probing window) judged a probe artifact — query text matches no shipped code (admin pitches SQL reads `FROM pitches p` with `v` properly aliased; partner module has no raw pitches SQL); never reproduced. Resolved as noise.
- **P2-19 residual (run #22)**: per-IP DAILY cap on send-otp deferred — needs a counter-store decision (Redis vs DB table) before implementing.

---

## Verified-OK (reviewer + run #1/#2 spot-check — do not re-litigate without new evidence)

- i18n parity perfect: ar.json = en.json, 630/630 leaf keys, 0 diff.
- Wallet page renders all 5 UX states (wallet/page.tsx:76-83,148,235,350); feed has offline edge state.
- Wallet ledger idempotency correct (in-tx check + unique key, wallet.service.ts:49-88); slot booking uses `FOR UPDATE` ✅.
- Matches join/leave/start/complete/cancel return populated `findOne` outside tx ✅.
- `sendMessage` (REST + WS) enforces membership ✅; WS `join-lobby`/`join-conversation` enforce membership/participant ✅.
- Admin RBAC correctly enforced at the API boundary: all 9 `admin/*` controllers carry `AdminAuthGuard`; `partner/*` uses `Roles('VenueOwner','Admin')` + per-service ownership checks; self-ban + last-admin guards present. Client `rbac.ts` is single source of truth; layout guard redirects Players/no-role.
- No console.log/mock data in prod paths ✅.
- dev-login gated by NODE_ENV ✅ (code-correct; note P0-2 caveat that the live box runs NODE_ENV=development).
- Services healthy: api/pwa/admin all active, API /health 200, 0 journal errors in 5h window.

## Areas in scope (every run must assess all four)

- **PWA** (`apps/player-pwa`) — player experience, i18n, offline, realtime UX, 5 UX states
- **Admin console** (`apps/admin`) — HQ dashboard + partner portal, RBAC, moderation queues
- **API** (`apps/api`) — NestJS endpoints, contracts, realtime gateways, auth
- **DB / Infra** — Drizzle schema, migrations, indexes, deployment services, observability

## Status legend

- `TODO` — queued, not started
- `WIP` — a run is actively building it (see latest RUNS report)
- `IN-REVIEW` — built, awaiting next run's verification pass
- `DONE ✅` — verified by a subsequent run (build + tests + spot-check)
- `BLOCKED` — cannot proceed; blocker noted in `kanban/RUNS/` and STATE.json

## Rules

1. A run may only mark an item `DONE` **after** `turbo run build` + `npx vitest run` are green for its slices.
2. The **next** run re-verifies previous `IN-REVIEW` items before trusting them (claims ≠ facts).
3. New findings always cite evidence (file:line, endpoint, failing flow) — no vibes-based items.
