{
  "schema": 1,
  "loop": "koralink-factory-5h",
  "last_run": 35,
  "last_run_at": "2026-09-05T16:20:00Z",
  "last_run_commits": [
    "16d4cc1"
  ],
  "last_run_report": "kanban/RUNS/2026-09-05T16-20Z.md",
  "in_review_items": [
    "Run #35 (verify next run): commit 16d4cc1 — P1-41 transactional email layer (API layer). Verify: (1) jest src/modules/mailer 15 specs green (template parity en/ar, RTL, XSS escaping, suppression matrix, transport gating, verb filter); (2) live DB has users.email/email_verified_at/email_muted + users_email_uidx + users_email_suppression_idx (information_schema/pg_indexes); drizzle.__drizzle_migrations id=34 tag 0033_user_email_columns; (3) GET /api/v1/email/verify (no token) renders bilingual RTL HTML error page 200; (4) activities.record() carries the EMAIL_ACTIVITY_VERBS hook (queueMicrotask, fire-and-forget); (5) matches reminder + users softDelete carry mailer hooks; purge anonymizer nulls email columns. NOTE: mailer is LOG-ONLY until RESEND_API_KEY + MAIL_FROM are provisioned in apps/api/.env (self-disabling by design) — owner must provision a Resend account + verified sender before any real email flows. Residual IN-REVIEW: PWA profile email field + i18n deliberately deferred (Abdullah's hostOnboarding messages WIP owns the staging collision; run #34 lesson)."
  ],
  "open_blockers": [
    "zai delegation-path 401 history — HEALTHY (run #35: probe 200, both reviewers zai/glm-5.3-flash 175s/180s, zero errors; 2nd consecutive clean run). Policy unchanged: try zai first, failover on first child error.",
    "P1-41 RESEND PROVISIONING (run #35): email layer is log-only until Abdullah provisions Resend (API key + verified sender domain) and sets RESEND_API_KEY + MAIL_FROM (+ optional API_PUBLIC_URL) in apps/api/.env, then restarts koralink-api. Feature self-disables until then — no keys are needed for build/test.",
    "P1-41 PWA RESIDUAL (run #35): profile-screen email field + emailMuted toggle + i18n keys — deferred because apps/player-pwa/src/messages/{en,ar}.json carry Abdullah's UNCOMMITTED hostOnboarding keys (staging collision burned run #34). Build it as soon as his WIP lands.",
    "P2-47 (run #32): admin cannot cancel a scheduled purge or restore-on-behalf — P1-37 landed VISIBILITY only (1043293). Needs Abdullah decision: cancel-purge (clear deleted_at) vs full restore-on-behalf.",
    "P1-38 (run #34): venue photos missing everywhere — needs Abdullah decision BEFORE build: upload pipeline (UploadThing/Cloudinary/self-hosted S3-MinIO) vs URL-field MVP. P2-50 (avatar upload) folds into the same decision.",
    "P1-42 (new, run #35): WS chat rate limiting missing (app.gateway.ts message handlers unbounded — flood risk). Size S-M: per-socket token bucket on send-message/DM handlers.",
    "P2-49 follow-up (Reviewer A run #35): matches.service.ts:2262 removePlayer reverts status to 'Open' with NO status predicate in tx (concurrent join flipping Open→Full between read and write is silently reverted). Mirror the auto-cancel guarded-UPDATE pattern (line 423).",
    "Root Dockerfile workspace-manifest strictness (Reviewer A run #32 A3, LOW-MED): root-stage npm ci copies only apps/api manifests against the 3-workspace lockfile — latent npm-strictness risk; needs a Coolify-verified cycle before touching.",
    "RUNBOOK: never regen app standalone lockfiles in-tree on this box — npm 12 walks up and mutates the ROOT workspace lockfile; regen in an isolated /tmp dir.",
    "P0-2 wallet top-up: dummy kept per Abdullah (2026-08-26), prod-gated (f109cf6). Live box runs NODE_ENV=development so the gate is NOT exercised; must set NODE_ENV=production + real secrets before public launch.",
    "P1-8 joinMatch debit + P1-14 settlement payout — BLOCKED on P0-2 (real payment provider).",
    "P1-29 dispute money side-effect — needs P0-2 money-path decision before building.",
    "P1-16 padel — CLOSED by Abdullah 2026-09-02: FOOTBALL ONLY. P1-2 skill_level filter — DROPPED 2026-09-02. P1-40 join-approval — REJECTED 2026-09-05.",
    "P1-12 admin i18n visual RTL check — needs Abdullah's eyes (browserless box).",
    "P2-29 host nav placement — DECIDED 2026-09-02: host OUT of tab bar; play-feed '+' featured CTA.",
    "Headless browser unavailable on VPS — visual checks need Abdullah or a working browser.",
    "RUNBOOK: never source apps/api/.env in the run shell (NODE_ENV leak breaks turbo builds); probe DB via one-shot `node --env-file=.env -e` subprocess; unset NODE_ENV before gates.",
    "RUNBOOK (run #21): HERMES_DELEGATED_CHILD_CONTEXT injected LATE — always prefix kanban calls with `env -u HERMES_DELEGATED_CHILD_CONTEXT`.",
    "OPS (run #21): first admin build can OOM (137); solo `turbo run build --filter=admin` retry passes.",
    "OPS (run #35): drizzle bookkeeping on this VPS = drizzle.__drizzle_migrations (sha256 file hash + journal `when`); migrations 0030+ are hand-written + journal-appended WITHOUT snapshot json — follow that convention.",
    "P0-5 per-category push prefs — BUILT run #28. P0-6 PDPL — BUILT runs #29-31/#34. Sub-cycles deferred: PDPL email receipt NOW UNBLOCKED by P1-41 (needs Resend provisioning first).",
    "P2-42 CSP script-src migration — standing reviewer item, 2-PR effort.",
    "P1-17b partial: secret rotation still TODO (Sentry token, Redis password, admin→HttpOnly cookies, server-side session invalidation).",
    "drizzle-orm 0.45.2 deferred to a focused half-day cycle."
  ],
  "consecutive_skip_count": 0,
  "initialized_at": "2026-08-26T00:00:00Z",
  "notes": "Run #34 (2026-09-05 11:40Z, kanban/RUNS/2026-09-05T11-40Z.md): full cycle on zai (probe 200; reviewers glm-5.3 completed clean — 327s/239s). Repair-session claims VERIFIED (pdpl spec 4/4 run live, WEB-8 guard in source, turbo 3/3) and promoted DONE; Sentry showed ZERO WEB-8 events since the 45cdb91 deploy. Sentry triage: API-X (BOOKING enum) + API-Z (mp.joined_at) are historical, already fixed in code; no new P0/P1. Built 3 slices: 6ef1d77 last-admin ghost guard (Reviewer A MEDIUM console-lockout hole), 148acb2 P1-39 no-show reputation visibility (live E2E), 1dea7b0 P2-49 decision-mutation guards. Gates: jest 343/343, vitest 362/362, tsc 0/0, lint 0, turbo 3/3 x3, API health 200 x3. Board: +P1-38/39/40, +P2-49 (DONE), P1-39/P2-49 marked DONE, mirror cards t_284af34d/t_03c0762e/t_0478d117/t_41f3f42c gated under t_ce9a513a. Strix: not due (day 5). Abdullah's host-onboarding WIP remains uncommitted + untouched (tests+lint green). Owner decisions queued: P1-38 venue-photos approach, P2-47 cancel-purge vs restore, P0-9 email. Next scheduled fire 20:15Z → run #35."
}