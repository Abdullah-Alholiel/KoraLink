{
  "schema": 1,
  "loop": "koralink-factory-5h",
  "last_run": 30,
  "last_run_at": "2026-09-03T15:50:00Z",
  "last_run_commits": [
    "30-PDPL-RESTORE-FLOW",
    "30-PDPL-HARD-PURGE"
  ],
  "last_run_report": "kanban/RUNS/2026-09-03T15-19Z.md",
  "in_review_items": [
    "P2-45 persisted query cache (offline-first cold start) — commit c0980b0. Verify: vitest test/lib/query-persister.test.ts + slices logout-wipe, tsc, build; spot-check cold-open render from IDB.",
    "P2-46 local DB + offline mutation queue — PARKED by Abdullah 2026-09-03 (build only if offline ACTIONS become a requirement); mirror card t_9338488a carries the note.",
    "P0-6 sub-cycles #31 (booted this run, not built): P1-35 getPublicProfile deleted_at filter (1-line), P1-36 restore-purpose JWT route-scope (strategy change + 2 specs), P1-37 admin 'deleted users' back-office view."
  ],
  "open_blockers": [
    "P0-2 wallet top-up: dummy kept per Abdullah (2026-08-26), prod-gated (f109cf6). Live box runs NODE_ENV=development so the gate is NOT exercised; must set NODE_ENV=production + real secrets before public launch.",
    "P1-8 joinMatch debit + P1-14 settlement payout — BLOCKED on P0-2 (real payment provider).",
    "P1-29 dispute money side-effect — needs P0-2 money-path decision before building.",
    "P1-16 padel support — CLOSED by Abdullah 2026-09-02: FOOTBALL ONLY, padel never in scope (code already football-only).",
    "P1-2 skill_level discovery filter — DROPPED by Abdullah 2026-09-02 (feature not wanted, do not build).",
    "P1-12 admin i18n visual RTL check — needs Abdullah's eyes (browserless box). HQ copy fully translated + verified 124/124 (run #23); only the visual pass remains.",
    "P2-29 host page nav placement — DECIDED 2026-09-02: keep host OUT of tab bar; make play-feed '+' a featured, informative hosting CTA (EN+AR).",
    "Headless browser unavailable on VPS — visual checks need Abdullah or a working browser.",
    "RUNBOOK: never source apps/api/.env in the run shell (NODE_ENV leak breaks turbo builds); probe DB via one-shot `node --env-file=.env -e` subprocess; unset NODE_ENV before gates.",
    "RUNBOOK (run #21): HERMES_DELEGATED_CHILD_CONTEXT is injected LATE into cron sessions — always prefix kanban calls with `env -u HERMES_DELEGATED_CHILD_CONTEXT`.",
    "OPS (run #21): first admin build can OOM (137) under memory pressure; solo `turbo run build --filter=admin` retry passes.",
    "P0-5 per-category push preferences — BUILT run #28 (vertical slice DONE: 4-category taxonomy, schema + API + PWA UI + iOS install gate; 6 new jest cases; 302/302 tests; live E2E PASS). Future sub-cycles (deferred, not blocking): 8-category taxonomy, per-category quiet hours, per-subscription push, observability for mute-rate metrics.",
    "P0-6 PDPL account-delete / data-export + restore-flow fix + hard-purge cron — BUILT run #29 + run #30 (4 commits total: 8ef3106/a17755a/a66906d/43289ec). Sub-cycles remaining: admin 'deleted users' back-office view (P1-37 #31), PDPL email receipt (needs P0-9), getPublicProfile deleted_at filter (P1-35 #31), restore-purpose JWT route-scope (P1-36 #31).",
    "P0-9 email infrastructure — CARVED run #27. Zero providers in repo (no nodemailer/sendgrid/resend/mailgun/smtp). Required for KSA launch. Architecture decision needed: managed vs self-hosted SMTP. Buildable only AFTER owner call.",
    "P2-42 CSP `script-src` migration — standing reviewer item 11, 2-PR effort (nonce middleware → drop unsafe-inline; then audit Script usage → drop unsafe-eval).",
    "P1-17b partial (run #26): .env.bak-expose-* deleted + .gitignore extended. STILL TODO: rotate the secrets the .bak file held (window unverified); Sentry token rotation; Redis password; admin→HttpOnly cookies; server-side session invalidation; drop Bearer on browser surfaces.",
    "drizzle-orm 0.45.2 deferred to a focused half-day cycle — 6 type tightenings, no reachable sink per Strix's own analysis.",
    "zai delegation 7th consecutive 401 (runs #24-#30). Direct z.ai probes 200 OK (2.8s) but credential pool routes delegation children with HTTP 401 'token expired or incorrect'. auth.json pool `last_status: ok` is misleading — needs probe-based re-validation. Working path for #31: keep opencode-go with glm-5.3-flash (Go weekly 56-72%, healthy for 2 reviewers)."
  ],
  "consecutive_skip_count": 0,
  "initialized_at": "2026-08-26T00:00:00Z",
  "notes": "Run #30 (2026-09-03 15:19-15:50Z) — P0-6 PDPL restore-flow fix + hard-purge cron DONE ✅ (2 commits). **Slice 1 a66906d**: Reviewer A caught the dead-UI defect on the run #29 PDPL vertical slice — after DELETE /users/me, profile/page.tsx calls logout()+clearAuthToken()+window.location=/login, but the RestoreAccountBanner only renders on the (main)/profile page (which requires auth). The persisted koralink_pdpl_restore_token was never used by fetcher (which only attaches koralink_token Bearer). So the 30-day grace restore UX was fiction for the happy-path user. Fix: fetcher.getBearerForRequest() falls back to the PDPL restore-token for /users/me/restore; the 401 self-heal excludes /users/me/restore so an expired-window error renders inline (no global redirect); /login page renders the banner when koralink_pdpl_purge_at is in the future; 4 new vitest cases; i18n login.restoreAccount.{title,body,restore} en+ar (746/746 parity). **Slice 2 43289ec**: hard-purge cron — UsersService.purgeExpiredAccounts() (UPDATE-in-place, no DELETE; transactions FK is RESTRICT per migration 0031; phone→purged-<last-12-of-uuid> which fits varchar(20) without a migration; nulls all PII cols; refreshes deleted_at=NOW() so the next tick excludes the row) + UsersScheduler @Cron('0 */5 * * *') with Sentry capture on error + UsersModule registers the provider + users.purge-expired.spec.ts (4 jest cases, isNotNull tripwire + INTERVAL '30 days' tripwire). LIVE E2E PASS — inserted 31-day-old test user, ran the exact UPDATE; phone → 'purged-00-test00001' (15 chars), full_name → 'Deleted User', handle/avatar → null, deleted_at refreshed. 5-day-deleted user NOT touched (deleteted_at < NOW()-30d WHERE excludes them). Active user (deleted_at NULL) NOT touched (isNotNull excludes). All 3 test rows cleaned up. **Gates**: vitest 324/326 (PromoBillboard failures are sibling WIP, not this commit; was 272 at #26 baseline, +52 across #27-#30), jest 316/316 (+7 from baseline 309: 4 new purge-expired + 3 sibling matches.default-booking-mode.spec), tsc 0, build 3/3, dist grep confirms new code shipped, API restart + health 200, 0 journal errors. **ZAI 7th consecutive 401** — direct probes 200 OK; pool routes 401; fell back to OpenCode Go on https://opencode.ai/3-flash/v1/chat/completions with glm-5.3-flash (2 reviewers 313+137s, 25+10 calls). P0-6 fully DONE (delete+export+restore+hard-purge+auth gate). Reviewer A+B surfaced 5 follow-ups: C1+C2 fixed; I1 (restore-JWT route-scope) + I2 (public-profile deleted_at) + M1-M5 polish — descoped to #31 as P1-35/P1-36/P1-37 (boarded). Admin 'deleted users' back-office view descoped to #31. Money items PARKED. **Strix DONE for Sept** (run #25). **APPROVED RUN PLAN #31**: P1-35 + P1-36 + P1-37 (one tight PDPL hardening slice) + a quick drizzle 0.45.2 bump if budget allows. drizzle 0.45.2 + P1-17b secret rotation still queued. Money items PARKED until KSA company. strix_scan=2026-09-02 (run #25) covers Sept monthly gate."
}
