{
  "schema": 1,
  "loop": "koralink-factory-5h",
  "last_run": 28,
  "last_run_at": "2026-09-03T01:18:00Z",
  "last_run_commits": [
    "28-PUSH-CAT-API",
    "28-PUSH-CAT-PWA"
  ],
  "last_run_report": "kanban/RUNS/2026-09-03T01-18Z.md",
  "in_review_items": [
    "P0-5 per-category push mutes — built run #28 (API + PWA + iOS install gate), verified by next run"
  ],
  "open_blockers": [
    "P0-2 wallet top-up: dummy kept per Abdullah (2026-08-26), prod-gated (f109cf6). Live box runs NODE_ENV=development so the gate is NOT exercised; must set NODE_ENV=production + real secrets before public launch.",
    "P1-8 joinMatch debit + P1-14 settlement payout — BLOCKED on P0-2 (real payment provider).",
    "P1-29 dispute money side-effect — needs P0-2 money-path decision before building.",
    "P1-16 padel support — CLOSED by Abdullah 2026-09-02: FOOTBALL ONLY, padel never in scope (code already football-only).",
    "P1-2 skill_level discovery filter — DROPPED by Abdullah 2026-09-02 (feature not wanted, do not build).",
    "P1-12 admin i18n visual RTL check — needs Abdullah's eyes (browserless box). HQ copy fully translated + verified 124/124 (run #23); only the visual pass remains.",
    "P2-29 host page nav placement — DECIDED 2026-09-02: keep host OUT of tab bar; make play-feed '+' a featured, informative hosting CTA (EN+AR).",
    "Headless browser unavailable on VPS — visual checks need Abdullah or a working browser.",
    "RUNBOOK: never source apps/api/.env in the run shell (NODE_ENV leak breaks turbo builds); probe DB via one-shot `node --env-file=.env -e` subprocess; unset NODE_ENV before gates.",
    "RUNBOOK (run #21): HERMES_DELEGATED_CHILD_CONTEXT is injected LATE into cron sessions — always prefix kanban calls with `env -u HERMES_DELEGATED_CHILD_CONTEXT`.",
    "OPS (run #21): first admin build can OOM (137) under memory pressure; solo `turbo run build --filter=admin` retry passes.",
    "P0-5 per-category push preferences — BUILT run #28 (vertical slice DONE: 4-category taxonomy, schema + API + PWA UI + iOS install gate; 6 new jest cases; 302/302 tests; live E2E PASS). Future sub-cycles (deferred, not blocking): 8-category taxonomy, per-category quiet hours, per-subscription push, observability for mute-rate metrics.",
    "P0-6 PDPL account-delete / data-export — DECIDED by Abdullah 2026-09-02: soft delete + 30-day grace + JSON export of 7 data groups. Buildable.",
    "P0-9 email infrastructure — CARVED run #27. Zero providers in repo (no nodemailer/sendgrid/resend/mailgun/smtp). Required for KSA launch. Architecture decision needed: managed vs self-hosted SMTP. Buildable only AFTER owner call.",
    "P2-42 CSP `script-src` migration — standing reviewer item 11, 2-PR effort (nonce middleware → drop unsafe-inline; then audit Script usage → drop unsafe-eval).",
    "P1-17b partial (run #26): .env.bak-expose-* deleted + .gitignore extended. STILL TODO: rotate the secrets the .bak file held (window unverified); Sentry token rotation; Redis password; admin→HttpOnly cookies; server-side session invalidation; drop Bearer on browser surfaces.",
    "drizzle-orm 0.45.2 deferred to a focused half-day cycle — 6 type tightenings, no reachable sink per Strix's own analysis.",
    "zai delegation for children STILL 401 at gateway layer — 4th consecutive run (#24/#25/#26/#27). Pool itself is clean; gateway daemon (PID 3537113, booted 2026-09-02T06:52:35Z) is resolving children through a stale state. Direct zai probe returns 200. Run #27 used parent self-review. Working path next: re-probe zai, fall back to opencode-go (if key added) or deepseek-v4-flash (402 confirmed unavailable). Recommend gateway restart investigation OR live with self-review."
  ],
  "consecutive_skip_count": 0,
  "initialized_at": "2026-08-26T00:00:00Z",
  "notes": "Run #27 (2026-09-02 15:33Z) — SECURITY + INFRA SPRINT (4 small slices DONE, all verified): **P1-17c WS origin FIXED**: app.gateway.ts:80-97 drops the NODE_ENV==='production' gate; unlisted origins are now ALWAYS disconnected. 4 new jest cases; live journal probe shows the rejection. **P1-17d ICS escape FIXED**: new apps/api/src/common/security/ics-text.ts (escapeIcsText per RFC 5545 §3.3.11) applied to title/location/description in matches.controller.ts:33,91-105; also fixed a latent bug where the bullet char was literally `\\u2022` instead of `•`. 7 new jest cases; live ICS download shows real `\\,` escapes + CRLF + bullet. **P2-19 per-IP daily OTP cap FIXED**: OtpStoreService extends with `OTP_DAILY_IP_CAP=50` + `keys.ip_day(ip)` + `getIpDailyCount`/`incrementIpDaily`; auth.service.ts:sendOtp(phone, ip?) gates BEFORE per-phone increment; auth.controller.ts threads `req.ip` via @Req(). 3 new jest cases. **P2-32 foreign pitchId 404 FIXED**: partner.service.ts:860-869 throws NotFoundException('Pitch not in your scope.') after scopedPitchIds check, before the existing query. 2 new jest cases. **NEW P0-9 email infra CARVED**: zero providers in repo (no nodemailer/sendgrid/resend/mailgun/smtp) — needs owner call on managed vs self-hosted before any code. P0-5 split: per-category prefs + install-triggered push DEFERRED to #28 (clean cycle with full Gate 3 contract). P0-2/P0-4/P0-6/P0-7/P0-8 still in force. **GATES**: tsc 0, jest 35 suites 296 tests (+16 from #26: 4 gateway + 7 ics-text + 3 auth + 2 partner), vitest 272/272, build 3/3, API restarted + health 200 + dist grep confirms new code. **zai delegation 4th consecutive 401** — parent self-review used; next run MUST re-probe and consider gateway restart. APPROVED RUN PLAN UPDATE: #28 P0-5 per-category push preferences + install-triggered push enablement (full cycle); #29 P0-6 PDPL delete + export; #30 P0-9 email infra (after owner call). drizzle 0.45.2 + P1-17b rotation + P1-17c/#28 + P1-17d/#28 + chat pagination/waitlist/search still queued. Money items (P0-2 real + P1-8/P1-14/P1-27/P1-29/P2-1/P2-3) PARKED until KSA company. strix_scan=2026-09-02 (run #25) — no Strix due this run."
}
