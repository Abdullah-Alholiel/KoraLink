# Factory Run #5 — 2026-08-27T14:42Z

**Mode:** full (review → kanban → build POTM realtime namespace fix → board → report)
**Budget:** ~1.5h (well under 3.5h cap)

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260827T144207Z).
- Delegation self-heal: reset `delegation.model=deepseek-v4-pro` / `provider=deepseek` / `base_url=''`.
- `gh auth status` OK (Abdullah-Alholiel). `git pull --ff-only` → already up to date at `b5e2618`.
- Tree clean on `main`. All 3 services `active`; API `/api/v1/health` **200** (localhost:3001);
  0 journal errors in 5h window.
- Baseline (before edit): build 3/3 · API jest 71/71 · PWA vitest 217/217 · type-check 0.
- **Provider:** session deepseek-v4-pro (live); reviewer subagent completed in 311s (31 API
  calls), no 402/429. `HERMES_DELEGATED_CHILD_CONTEXT=1` was set in env → all kanban CLI calls
  prefixed with `env -u HERMES_DELEGATED_CHILD_CONTEXT`.

## Findings (reviewer `deleg_00fdec0e` + self-verification, evidence-cited)

**CRITICAL:** none. (uuid casts, eq(col,null), membership checks, mutation-return contract all clean.)

**IMPORTANT (built this run):**
- **POTM realtime namespace bug** — `PostMatchSection.tsx:45` was the only remaining call site
  using raw `io(\`${env.NEXT_PUBLIC_API_URL ?? ''}/lobby\`)`. With the pathful base
  (`http://localhost:3001/api/v1` — the `env.mjs:12` default AND the deployed `.env.local`), this
  dials namespace `/api/v1/lobby`; the gateway rejects the handshake ("Invalid namespace") and the
  `pom-decided` winner toast silently never fires. The other 5 realtime call sites
  (`NotificationProvider`, `useMessages`, `useMatches`, `useConversations`) already used
  `createLobbySocket()`. `socket.ts:11-20` documents the exact bug; proven live via
  `scripts/ws-namespace-probe.mjs`.

**IMPORTANT (recorded, not built):**
- Money summed as `::float` before payout — `settlements.service.ts:115`
  `COALESCE(SUM(pitch_cost_sar),0)::float` feeds `amount.toFixed(2)` (float precision risk).
  Folded into existing **P2-4**.

**MINOR (→ existing board items / backlog):** `createDispute` bare return + `castVote {message}`
(already P2-5) · WS `leave-conversation` unguarded (already P2-6) · notifications raw
`SubscribeBody` (no class-validator DTO) · dead `rating` column (`schema.ts:187,222`) ·
`POST /wallet/pay` reuses `TopupWalletDto`.

## Previous-run verification (claims ≠ facts)

All **5** prior-run claims independently CONFIRMED by reviewer (deepseek-v4-pro) + baseline green:
1. `settlement.pay()` conditional UPDATE `WHERE status='pending'` — CONFIRMED (`settlements.service.ts:77-85`).
2. `generatePending` `onConflictDoNothing` inside tx — CONFIRMED (`:134-153`, index `schema.ts:836`).
3. `reports.create` `onConflictDoNothing` — CONFIRMED (`reports/reports.service.ts:48-62`, index `schema.ts:900-902`).
4. unban → `account_unbanned` — CONFIRMED (`admin/users.service.ts:191`, enum `activities.service.ts:31`).
5. migration `0018_absent_shotgun.sql` — CONFIRMED (ALTER TYPE + both unique indexes).

→ **P2-9** and **P2-14** promote to fully DONE; `in_review_items` cleared.

## Board changes

- **NEW P1-9** POTM realtime namespace (built this run, `58b3ba3`) → IN-REVIEW.
- **P2-9** + **P2-14** confirmed DONE ✅ (verified run #5).
- **P2-4** evidence expanded (`settlements.service.ts:115`).
- 3 minor backlog additions (notifications DTO, dead rating col, wallet/pay DTO).

## Built this run — verification evidence (real terminal output)

- `npm run build` (root) → **3/3 tasks successful** (1m47s; 1 rebuilt, 2 cached). postbuild
  synced standalone + restarted `koralink-pwa.service`.
- `npx vitest run` (PWA) → **218/218 tests, 30 files** (+1 new `PostMatchSection.test.tsx`).
- `npm run type-check` (PWA, covers test/) → **0 errors**.
- `npx jest` (API) → **71/71** (baseline, unaffected).
- **Live probe** `scripts/ws-namespace-probe.mjs`: `http://100.93.99.24:3001/lobby` →
  **CONNECTED (namespace accepted)**; `…/api/v1/lobby` → **connect_error: Invalid namespace**.
- Compiled bundle check: zero `api/v1/lobby` occurrences in shipped `.js` (only source-map
  comment text remains).

## Blockers & decisions

- **P0-2** unchanged — real payment provider is an external vendor decision (Abdullah).
- Decision: built the POTM namespace fix (P1, ~30 min, no external dependency) over larger
  candidates (settlement money-as-numeric, chat pagination, discovery filters). Small completed
  beats large half-done.

## Next-run recommendation

1. **Verify P1-9** (POTM namespace) → promote to DONE.
2. **P1-2** discovery filters (`skill_level` filter + `radius_km` hard cutoff) — clean vertical slice.
3. **P2-4** settlement money-as-numeric (drop `::float` for payout sums).
4. **P1-3** chat pagination/media.
