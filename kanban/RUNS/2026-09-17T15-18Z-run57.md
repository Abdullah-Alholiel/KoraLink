# Factory Run #57 — 2026-09-17T15:18Z (reconstructed 2026-09-18 by run #58)

> **RECONSTRUCTION NOTE:** Run #57 was killed mid-Phase-5 (8th kill-pattern event, root cause
> unknown — cron/scheduler/host side). Its code commits LANDED (`381186f`, `2c71671`) but the
> RUNS report was never written and the BOARD.md/STATE.json updates sat UNCOMMITTED in the tree.
> Run #58 adopted them per the dead-run watchdog directive: this file reconstructs the report
> from STATE.json's in_review_items (run #57's own claims) + run #58's independent verification
> (below). Run #58 committed the adopted board/state records alongside its own.

**Mode:** full (API rotation, 57%4=1) — reconstructed
**Budget:** killed before Phase 5 (~2.5h estimated)
**Models:** session glm-5.3-flash · reviewer glm-5.3-flash (probe 200 via ZAI_API_KEY — GLM_API_KEY .env line is STALE/401)

## What run #57 built (its claims, now verified)
- **`381186f` P1-48 — WS per-message moderation gate**: shared `moderationReason()` predicate
  (banned → suspended_until>now → deleted, same order as jwt-cookie.strategy) + `requireActiveUser()`
  users-row re-read wired into ALL 6 state-changing handlers (join-lobby, send-message,
  join-conversation, mark-read, mark-chat-read, send-dm) after the `!client.userId` guard, BEFORE
  rate-limit/membership/writes; handshake check refactored onto the same predicate;
  leave-conversation deliberately ungated (P2-6 rationale). 12 new jest cases → jest 542/542,
  tsc 0, turbo 3/3. LIVE E2E probe on staging API: clean connect → admin ban mid-session → same
  socket's next send rejected via the Nest `exception` channel with the gate message → unban →
  reconnect clean; seed user left banned_at=null.
- **`2c71671` P1-50 — admin ban force-disconnect**: `AppGateway.disconnectUser(userId)`
  (`server.in(userRoom).disconnectSockets(true)`) called by `AdminUsersService.update()` after the
  moderation write commits, gated to ban/suspend ONLY (unban/suspension-lift never disconnects;
  best-effort try/catch). 4 jest cases (`users.moderation-disconnect.spec.ts`). jest 546/546,
  tsc 0, turbo 3/3. LIVE probe: victim connected → mid-session ban → `io server disconnect` <1s →
  unban → reconnect clean.

## Verification (run #58, 2026-09-18) — claims ≠ facts, both confirmed
- **P1-48 VERIFIED TRUE** (Reviewer B, file:line): predicate at `app.gateway.ts:217-224`;
  `requireActiveUser()` at :234 called in all 6 handlers — join-lobby :270, send-message :298
  (rate-limit :312 after), join-conversation :449, mark-read :473, mark-chat-read :499, send-dm
  :527 (rate-limit :538 after); leave-conversation ungated :556 (documented P2-6 exemption).
  11 test blocks in `app.gateway.spec.ts` + 1 in `mark-chat-read.spec.ts` = 12 confirmed.
- **P1-50 VERIFIED TRUE** (Reviewer B + parent): `disconnectUser()` at `app.gateway.ts:584`;
  gate `users.service.ts:237-245` — ban (`banned_at !== null`) or suspend (`suspended_until !==
  null`) only; unban/lift fail the gate; try/catch after the committed write (moderation op cannot
  fail from a gateway error). 4 jest cases confirmed.
- **E2E spot-check TRUE**: PATCH /admin/users/:id ban path → REST sign-in blocked with stable
  codes (`auth.service.ts:211/:219` password path, :314/:322 phone-OTP path, ACCOUNT_DELETED PDPL);
  WS handshake rejects; PWA `lib/error-classify.ts` classifies the codes.
- Parent grep: `requireActiveUser(client.userId)` ×6; no console.* in the touched API paths.

**Both items → DONE ✅ STAGING** on the run #58 board rewrite (PROD at next promote).

## Run #57 residuals (found by run #58's review, boarded)
- Reviewer A IMPORTANT-class: nothing structural enforces `requireActiveUser` on NEW WS handlers
  (convention only) → P2-74.
- Reviewer A MINOR: "unban while still suspended" never force-disconnects (gate requires
  banned_at OR suspended_until non-null AFTER the write — unban sets banned_at:null while
  suspension may persist); per-message gate still blocks, cosmetic → P2-77.
- Reviewer B P1: `POST /wallet/pay` client-supplied amount → **REMOVED by run #58** (commit
  recorded there; the endpoint had zero consumers — full analysis in
  `docs/plans/run58-review-sweep/00-retro.md`). Remaining Reviewer-B payout findings deduped
  against existing P1-27 / P2-3 / P1-14 (all P0-2-gated).
