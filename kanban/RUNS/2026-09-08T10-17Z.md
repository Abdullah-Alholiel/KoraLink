# Factory Run #43 — 2026-09-08T10:17Z

**Mode:** full (cron slot 10:15Z)
**Budget:** on plan (~55 min used of ~3.5h window; 4 slices + review + board)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on `zai`
(probe: **HTTP 200 in 1.8s**, weekly quota 1% — directive said zai-primary; delegation config
already correct: glm-5.3-flash / zai / coding base). Both reviewers completed clean:
A 135.9s (10 API calls), B 168.4s (12 API calls), zero errors, no failover.

## Preflight

- Lock absent → written `2026-09-08T10:17:55Z` (removed at end).
- **Push retry (run directive #1): FAILED AGAIN — 403.** fullstack-dev gh PAT still invalid
  (`gh auth status`: "The token ... is invalid"); default-profile fine-grained PAT authenticates
  but lacks Contents:write (`remote: Permission to Abdullah-Alholiel/KoraLink.git denied ... 403`).
  Local main is now **9 commits ahead of origin** (cff4e71, 9335ede, d2d5226, cefb4a0, d07da0b,
  1359195, 49a271b, a8a64ff, cc24d8a). Blocked on Abdullah: provision a write-scoped PAT.
- `git pull --ff-only` → already up to date. Tree dirty only with foreign `.local/` dirs (untouched,
  unstaged).
- Strix monthly gate: **not due** (Sept 8 = outside the 1st–3rd window; STATE notes carry
  `strix_scan=not-due-2026-09`).
- ADMIN STATE CHECK: clean tree in apps/admin + partner modules; not needed (no admin item planned
  initially; slice 4 later re-checked — still clean — before the P2-54 residual touch).

## Sentry / error-log triage (Phase 1.6: Sentry EU API, read token, 200)

- **koralink-api 24h:** API-17 (fatal, 1 event) + API-16 (1 event) — both lastSeen 2026-09-07
  20:00:0xZ, BEFORE this run's window → the run-#42-classed Render-demo transient did **NOT
  recur**. API-14/15 = Sep 6 drift window (old). API-11 (demo purge) quiet through the **Sept 8
  00:00Z window → 4th consecutive clean purge** (P2-51 hotfix holds).
- **koralink-web 24h:** WEB-6 `viewport-diagnostic:standalone` — 33 events / 5d, **handled=yes**,
  release a9c889e, ViewportHeightSync.tsx:53/84 (intentional per-session telemetry, run-#37
  classified) → MINOR noise-reduction candidate (lower to info or sample), not boarded (already
  on the known list). WEB-9 (1) / WEB-7 (3) = network blips on :8443. Nothing new actionable.
- journalctl (api/pwa/admin, 5h): **zero error entries**.

## Findings (GLM reviewers + self-verification, merged, evidence-cited)

**Run-#42 verification (Reviewer B, 12 claim points, all CONFIRMED):**
9335ede — LoadError.tsx:24 shared + wired (23 files referencing; 26 reference the hook, the 3
without are the hook/badge/sort deps themselves); OfflineBanner (dashboard)/layout.tsx:8,88; raw
error demoted to muted LTR detail; dashboard formatMetricInt (utils.ts:34, page.tsx:67-69);
common namespace 23 leaves EN=AR. d2d5226 — LiveBadge on 12 surfaces; honest stale via
use-live-data.ts:45/80/89 + LiveBadge.tsx:27; EmptyState 8 pages. cefb4a0 — SlotManager.tsx:301-306
focus-reveal, :179/240 28px targets, Sidebar.tsx:83 aria-current. → **run #42 items promoted DONE.**

**Reviewer A findings — triaged with evidence (2 refuted, 1 re-scoped):**
1. "0014_admin_notification_verbs.sql unjournaled/duplicate-tag" → **REFUTED**: documented
   run-#23/P1-33 orphan, folded via 0029 `ADD VALUE IF NOT EXISTS`, allowlisted in
   drizzle-migration-journal.spec.ts `KNOWN_ORPHANS` (:29); tripwire suite green.
2. "Missing FK indexes / zero plain .index()" → **half-refuted then re-scoped**: 15 index blocks
   exist; precise gap = 11 FK columns leading no index. Query-path audit: all 11 only probed from
   the PK side (partner.service.ts:280,612 join ON matches.id; no `WHERE booked_match_id = X`
   anywhere); live EXPLAIN = hash join over 16-row tables (sub-ms). → **P2-56 boarded, DEFERRED**
   (pre-launch batch; also avoids re-triggering P2-51 demo drift with a migration).
3. Bug-class sweep: 0 `::uuid`, 0 `eq(col,null)`, 0 console.*, FOR UPDATE present at all
   money/status transitions (transactions.service.ts:95; matches.service ×8; waitlist ×3). MINORs
   queued: LoadError role='status' vs 'alert' (arguable), z-[80..100] documented-lineage gap (PWA).

**Reviewer B infra gaps — triaged (2 confirmed→built, 2 refuted/corrected, rest queued):**
- Pool unconfigured (database.module.ts:18-20) → **CONFIRMED, BUILT (slice 1)**.
- SW caches opaque status-0 responses (sw.js matches-feed/user-profile routes) → **CONFIRMED,
  BUILT (slice 2)** — actually 4 routes carried `[0,200]`.
- "Backups local-only, restore never rehearsed" → **half-refuted**: nightly dumps + gzip -t run
  (03:01 today); `scripts/db-restore-runbook.md` exists and records a 2026-09-02 drill. True
  residual = drill staleness → **RE-DRILLED (slice 3)**; offsite copy remains the real P1-18
  owner decision (needs Abdullah's storage account).
- Rollback-less forward-only migrations, single PG host, log shipping, helmet drift → queued as
  notes (pre-launch checklist material, not this cycle).
- ADMIN_URL missing from .env.example → **REFUTED** (present at .env.example:32).
- OS research (z.ai web_search_prime, 1 call): PgBouncer/pooling explainers only — nothing to
  adopt; noted: Render demo can switch to Neon's **pooled connection URL** if demo connection
  exhaustion ever shows up (no card; tied to P2-51 owner call).

**Self-found ops fact:** live `drizzle.__drizzle_migrations` now returns **35 rows** (newest id 37
= 0036, then 35, 34) vs "37" in run-#39-era docs — run #39's phantom-0035 dedupe means the count
is **not an invariant**; do not "repair" row counts; journal↔file parity is enforced by the
tripwire spec. The drill-restore reproducing 35 rows exactly confirms dump↔live consistency.

## Previous-run verification

Run #42's 3 in-review items: **verified (above), promoted DONE** in BOARD.md (P2-55 row annotated
"verified run #43, 4/4 claim points"; P2-54 partial → residual closed this run; connection-ux was
already DONE-marked by run #42's own gates + Reviewer-B confirmation).

## Board changes (added/promoted/demoted/completed)

- **DONE ✅:** run-#42 items (connection-ux 9335ede, P2-55 d2d5226, P2-54 partial cefb4a0) verified.
- **P2-54 → fully closed:** residual (list-page filter aria) built this run (cc24d8a).
- **NEW P2-56 (DB/Perf):** 11 FK columns lead no index — DEFERRED, pre-launch batch; mirror card
  `t_cbd554e8` created + gated under `t_ce9a513a` (status todo, parents ✓).
- STATE.json: last_run 43, in_review_items = 4 new items, push blocker updated (9 commits).

## Built this run (slices, commits, verification evidence)

1. **49a271b — feat(api): env-tunable DB pool with fail-fast timeouts.**
   `buildPoolOptions()` (database.module.ts): DATABASE_POOL_MAX / DATABASE_IDLE_TIMEOUT /
   DATABASE_CONNECT_TIMEOUT, defaults 10/30/10, garbage→default, SSL_MODE passthrough pinned.
   4 jest specs (`database.pool-options.spec.ts`) 4/4. `.env.example` documents the vars.
   API **restarted 11:04Z after build** (dist grep: 5 hits) + `/health` 200.
2. **a8a64ff — fix(pwa): SW caches only real 200s.** `cacheableResponse.statuses [0,200]→[200]`
   ×4 in next.config.mjs (matches feed, static assets, clubs/venues, user profile). Regenerated
   sw.js verified in **both** public/ and standalone copies: `statuses:[200]` ×4, `[0,200]` ×0.
3. **Restore drill (no commit of its own; runbook updated):** throwaway restore of
   `koralink-20260908-030122.sql.gz` (3237 SQL lines) into `koralink_restore_drill_20260908`
   → exit 0; tables 29=29, users 44=44, matches 16=16, transactions 24=24, bookkeeping 35=35;
   users CREATE TABLE carries zero `skill_level` (post-0035 confirmed; the dump's single
   `skill_level` string is the 0035 bookkeeping row). Drill DB left in place — DROP deferred to
   an interactive session (cron approval wall blocks DROP DATABASE).
4. **cc24d8a — fix(admin): aria-labels on list-page filter selects (P2-54 residual).**
   10 bare filter `<select>`s across 7 list pages labelled; 4 new `common` keys
   (filterByStatus/Role/Type/Subject) EN+AR; parity **553=553** leaves; admin tsc 0.

**Gates:** `turbo run build` 3/3 ×2 (2m38s full, 1m16s incremental; no OOM) · api tsc 0 ·
admin tsc 0 · vitest **58 files / 412 tests** ✅ · jest **53 suites / 423 tests** ✅ ·
i18n parity jq-verified · services all active post-build (admin restarted by postbuild,
API restarted manually, PWA restarted by postbuild).

## Blockers & decisions

- **PUSH AUTH STILL LOST (9 commits local-only through cc24d8a).** Needs Abdullah: a PAT with
  repo Contents:write (fullstack-dev profile), or fix the default-profile fine-grained token's
  repo permission. Every factory run will keep retrying push first until then.
- Drill-DB cleanup pending: `DROP DATABASE koralink_restore_drill_20260908` (approval wall).
- Offsite backup leg remains the P1-18 owner decision (needs Abdullah's storage account).
- Owner decisions queue unchanged: P0-2 payment provider · P1-38+P2-50 uploads · P2-47
  cancel-purge · P2-51 demo-migrate strategy (4th clean purge window) · P1-41 Resend.

## Next-run recommendation

1. **Retry push FIRST** (may succeed the moment Abdullah fixes the PAT); verify origin/main.
2. Verify run #43's 4 in-review items (expected quick: spot-check dist buildPoolOptions, sw.js
   counts, drill log, aria-labels) + **drop the drill DB** if interactive approval available.
3. Rotation 44 % 4 = 0 → **PWA screens** focus (feed/venue/match-detail/host/chat/profile).
4. Reviewer-A PWA minor (document the z-[80]/[90]/[100] modal/toast lineage) is a natural small
   item if the rotation sweep surfaces nothing bigger.
