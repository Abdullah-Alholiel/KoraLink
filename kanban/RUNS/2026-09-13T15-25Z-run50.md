# Factory Run #50 — 2026-09-13T15:25Z (takeover of crashed catch-up run)

**Mode:** full (takeover — the 11:26Z gateway catch-up run crashed at ~12:56Z, after committing its review but before any gate ran)
**Budget:** ~1h45m of 3.5h (started 15:25Z after verifying the prior holder was dead)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on `zai` (probe: **200**; prestate directive: weekly 2%, 5h 24% — healthy). **Both reviewers completed on zai (130s / 145s) — the runs #45–#49 zai child-401 streak is BROKEN.** Policy confirmed: try zai first every run, failover on first child error.

## Preflight (takeover path)
- Found `kanban/LOCK.json` age 3.9h, PID 1452600 **dead** (`ps -p` empty). Crash timeline reconstructed: gateway restarted 11:22Z (incident fix) → catch-up fire 11:26Z → committed review `a8b4946` (11:55Z) + CSP commit `a550815` + plan docs `docs/plans/run50-admin-rotation-hydration/` (12:03Z) → died mid-Gate-4 (its slices were written but NO gate ever ran; STATE/REPORT/lock-release never happened). Per the 4.5h rule's intent (never disturb a LIVE run) the lock was taken over at 15:25Z with a fresh `run_id` noting the takeover.
- Tree was dirty with the crashed run's half-finished slices — treated as current truth, completed and gated (see Built).
- gh auth OK (fullstack-dev PAT); `staging` in sync with origin; `main` fetched → `e1bf27e` (run #46 promote landed).
- Strix: not due (Sep 13; window 1st–3rd).

## Health snapshot + Sentry / error-log triage (Phase 1.5/1.6)
- Services api/pwa/admin **3/3 active**; `/health` 200 (internal :3001); **zero journal errors** in 5h; gateway guard timer active, heartbeat fresh — the Sep-13 dark-period fix is holding.
- Sentry (EU base, READ token): **API quiet** — newest event Sep 11 (KORALINK-API-B CORS/OTP-probe noise; API-1B ×9 = the boarded P1-41 email-provider blocker, expected until Brevo cutover). **Web live signal: KORALINK-WEB-2** (`SecurityError: Failed to register a ServiceWorker`) — 6 events ever, 4 of them TODAY 11:10Z, Chrome 131/Mac → exactly what P2-60 fixes (owned registration + captureError). **KORALINK-WEB-8** (`Cannot read properties of undefined (reading 'waiting')`, next-pwa script internals) — also covered this run by the `reg?.waiting` defensive read. WEB-6 (viewport-diagnostic ×52) = standing noise class, not re-triaged.

## Findings (GLM reviewers + parent, merged, evidence-cited)
- **Reviewer A (code quality, verdict SHIP):** hydration trio + SW registration correctly implemented; i18n parity confirmed on all touched keys; z-index/eq-null/unguarded-relation sweeps clean. **One finding that mattered in practice:** `ChatSheet.tsx` called `useNow()` AFTER the `isOpen` early return — rules-of-hooks violation. The pre-fix tree **hard-failed `next build` ESLint** (`useNow called conditionally`, build exit 1) — caught when the first build attempt ran against the crashed run's unfixed tree; hook moved above the early return (now in `4726498`). Lesson: reviewer "not a blocker" judgments on hook order are wrong here — our ESLint gate makes it a build-breaker.
- **Reviewer B (product gaps + verification):**
  - I-1 **CONFIRMED → new P1-46**: clubs detail drops `useMatches`' error (clubs/[id]/page.tsx:103; hook returns error at useMatches.ts:63,158) → failed match-list fetch renders as "no matches" (:351). Venue error IS handled (:150).
  - I-2 wallet-offline candidate **REFUTED by parent**: wallet already renders `OfflineBanner` on history failure (wallet/page.tsx:319-321, run #40). Nothing to build.
  - I-3 chat send/retry hit targets ~36px (<44px a11y rec) — noted for the next UI-polish batch (P2-61-adjacent), not separately boarded.
  - Run #49 claims: **5/5 code-read CONFIRMED** (otp-compare wired in both services :177/:166; 23505 race re-select :191-215; @IsUUID dto:6; silent-drop both channels :105-119/:127-133; both messages surfaces on useNow). Jest/vitest "green" claims accepted via this run's own full-suite runs (517/517, 506/506) — same HEAD lineage.
- **Parent findings:** migration-journal trap (below); `drizzle-kit migrate` broken on this VPS ("Please install latest version of drizzle-orm") — `scripts/migrate-vps.mjs` is the working path (1 applied / 42 skipped); `withTimestamp` on a table without `updated_at` would 500 live (match_players has none — bare `.set()` used, tripwired in both new specs).

**Migration-journal trap (run #39 pitfall extended):** 0039/0040 carried **future-dated** `when` values (178932480000x ≈ 18:40Z today) — above the live bookkeeping newest (1788963406695) → drizzle would re-fire them as phantoms on every future migrate. Fixed: verified their schema IS applied live (pair index + payout columns), normalized their `when` below the newest (→ `migrate()` skips them as applied), appended 0041 with a fresh `when` above it (→ applies exactly once). Also corrected my own mid-run misread: the "duplicate idx: 41" was a tail-truncation artifact — the ORIGINAL journal was valid; the `drizzle-migration-journal.spec.ts` tripwire caught my broken edit before commit. **Trust the tripwire spec over eyeballed tails.**

## Previous-run verification
- Run #49 (auth hardening + messages hydration): claims re-verified this run (Reviewer B, above) — promoted to trusted.
- Crashed run's committed review (`a8b4946`): its 7/7 run-#49 verification + P1-44 REFUTED (wallet error states exist — page destructures `error: balanceError/historyError`, renders both) + new rows P2-59/60/61 + P1-45 — all adopted into the board by this run.

## Board changes
- **DONE ✅:** P2-58 (unread parity, 7e9d325) · P2-59 (hydration trio, 4726498) · P2-60 (SW registration, 4726498) · P1-44 REFUTED (nothing to build).
- **NEW:** P1-46 (clubs match-list error state — Reviewer B I-1).
- **DEFERRED:** P2-61 admin minors to the next admin touch (clean admin tree, but deliberate: keep this run single-surface; fold in Reviewer B I-3 hit targets).
- Owner queue unchanged (P2-51 demo-migrate, P2-47 cancel-purge, P1-38+P2-50 uploads, P0-2 payments, P1-41 Brevo console steps).

## Built this run (2 slices — completed the crashed run's work + one new slice)
**Slice A — P2-59 + P2-60 (`fix(pwa)`, 4726498, 7 files):** hydration-safe clocks (useNow-fed `nowMs` into wallet `groupTransactionsByDay`, clubs `formatDateLabel`, ChatSheet `groupMessages`; ChatSheet hook moved above the early return) + owned SW registration (`register:false`, guarded `register()` with `captureError(scope:'swRegister')`, `reg?.waiting` guard). Tests: CS-3b/3c, SWU-1/2/3.
**Slice B — P2-58 (`feat(messaging)`, 7e9d325, 14 files):** migration 0041 `match_players.last_read_at` (per-EPISODE watermark) + real unread in `getMyDiscussions` + `markChatRead` (guarded UPDATE…RETURNING; zero rows = 403) + REST `POST /matches/:id/messages/read` + WS `mark-chat-read` (no broadcast) + PWA open-mark/batched unseen-mark with discussions-cache invalidation.
**Gates:** `turbo run build` **3/3 exit 0** (twice — the first attempt legitimately failed on the pre-fix ChatSheet ordering) · API jest **517/517** (3 new suites) · PWA vitest **75 files / 506 tests** · tsc ×2 zero · journal tripwire 4/4.
**Live staging E2E (P2-58):** migration applied `1 applied, 42 skipped` (no phantom refires) → API restarted AFTER dist → non-member **403** → member with **24 unread** → mark-read **200 {"ok":true}** → watermark advanced → discussions badge **0** → **11 other members' rows untouched** (per-user isolation proven).

## Blockers & decisions
- Admin untouched by design (P2-61 deferred); no admin state check violation.
- Kanban mirror: P2-58 had NO mirror card (run #49's mirror step missed it) — created `t_f0b74434` + gated; new `t_3fd910ae` (long-standing MapPin dead-UI, never mirrored) created + reclaimed after the dispatcher race + gated `todo`; P1-46 mirrored as `t_ad437de5` (todo, gated). Built cards commented with run #50 evidence.
- Brevo cutover still blocked on Abdullah's console steps (IP allowlist `145.241.109.213` + verified sender) — unchanged.

## Next-run recommendation
1. Verify run #50's 3 in-review items (build/test + spot-check: match badge clears on chat open; SW registers with no console/Sentry error on prod).
2. P1-46 clubs match-list error state (small, error-message standard).
3. P2-61 admin batch (ADMIN STATE CHECK first; include Reviewer B I-3 hit targets).
4. P2-57 offline SWR content fallback — safe to take now, one run past the `register:false` change.
5. Watch Sentry: WEB-2/WEB-8 should stop accumulating events once 4726498 reaches prod (suppression = the fix's live evidence).
