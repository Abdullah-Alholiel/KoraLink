# Factory Run #26 — 2026-09-02T11:10Z

**Mode:** full (review + 3 build slices — security sprint per the APPROVED RUN PLAN in STATE.json)
**Budget:** ~3h used of ~3.5h; within budget
**Models:** session `glm-5.3-flash` (zai, cron pin) · reviewers `glm-5.3-flash` on **opencode-go** (DEVIATION — zai delegation children 401 again at the gateway layer, same as runs #24/#25; pool itself is clean; opencode-go zai tier works)
- Phase 0 GLM probe: **HTTP 200** in 6.6s (zai weekly 40% / 5h 14% — healthy, primary plan)

## Preflight

- Lock acquired (`kanban/LOCK.json`, run_id 2026-09-02T07:35Z, sibling run# was #25 at 01:35Z).
- gh auth OK (Abdullah-Alholiel). `git pull --ff-only`: level with origin. `git log -3` clean.
- **Delegation self-heal**: Phase 0 probe set `delegation.model glm-5.3-flash / provider opencode-go / base_url https://opencode.ai/zen/go/v1` per the documented run #24 fallback. Reviewers dispatched on this config returned successfully (10 + 9 tool calls, 157s + 178s).
- Direct GLM API key returns 200 (zai weekly 1% probe); the gateway daemon (PID 3537113, booted 06:52:35Z) is still resolving children with stale credentials. Recorded in STATE.json.
- Tree dirty with sibling files only (landing-drafts / graphify-out / untracked scripts); never staged.
- **ADMIN state check**: `apps/admin` + partner API tree clean (no in-flight Abdullah work).
- API health: 200, journalctl: 0 err-level entries in 5h window.
- Services healthy: api/pwa/admin all `active` per `systemctl --user is-active`.

## Sentry / error-log triage (Phase 1.6)

Sources: journalctl (zero err-level entries on api/pwa/admin in 5h window). Sentry API not re-probed this run — no new signatures expected from the run #25 hardened code.

No new actionable signatures. P0-7/P0-8 changes ship this run; live E2E in Slice 1 + 2 confirms the contract.

## Strix (monthly Gate-0, NOT due this run)

- Date is 2026-09-02. STATE.json notes carry `strix_scan=2026-09-02` (run #25). Next Strix window: 2026-10-01..03.

## Findings (Reviewer A + B merged, evidence-cited)

### Reviewer A — code quality (full report at `cache/delegation/subagent-summary-0-20260902_102444_719790.txt`)
- **8 sections, 0 CRITICAL pre-slice**:
  1. **P0-7** — confirmed the NODE_ENV string gate + no-exp JWT (live-confirmed at slice start: dev-login as admin returned a working Admin JWT — the exploit is reachable on the live Tailscale deployment).
  2. **P0-8** — confirmed the unvalidated user-supplied `endpoint` URL (subscribe + send sites identified).
  3. **P1-17a** — pinned versions confirmed: axios 1.13.6, drizzle-orm 0.44.7, next 15.2.9; ws 8.18.3 (transitive via socket.io-client); engine.io 6.6.5.
  4. **P1-17b** — `apps/api/.env.bak-expose-20260901101040` not git-ignored (verified by `git check-ignore`). Redis password empty.
  5. **P1-17c** — WS origin: callback reflects `null, true` in non-prod; allowlist from env.
  6. **P1-17d** — ICS: `matches.controller.ts:99-108` interpolates `match.title` unescaped; CR/LF injection yields 2 events.
  7. **Standing bug-class sweep** — `::uuid` casts: 0 · `eq(col, null)`: 0 · bare `.returning()`: 10 sites (contract OK) · `console.log` prod: 0 · i18n parity en/ar: 0 diffs (deep key compare) · z-index: 5×z-[90], 5×z-[80], 4×z-[70], 2×z-[60] (P2 polish, NOT this run).
  8. **P2-41 verification (run #25)** — confirmed: `matches.service.ts:1258-1272` `WHERE wallet_balance >= $cost`, ledger insert only on success.

### Reviewer B — product gaps + run #25 verification
- **P2-41 PASS** — `matches.koralink-wallet.spec.ts` 4 it() cases + the in-tx conditional UPDATE confirmed.
- **4 NEW items** (boarded this run):
  - **P1-19** (API/Auth) — no phone-number change flow (users lose SIM → permanently locked out).
  - **P1-20** (API/Auth) — no partner self-onboarding (VenueOwner role can only be assigned via seed/admin SQL).
  - **P2-43** (API/Push) — no push-subscription lifecycle sweep (devices that drop push accumulate dead rows).
  - **P2-44** (PWA/Security) — DevLoginBar hostname gate (pre-run-#26) was a security gap; folded into P0-7.
- **Admin OTP-path pre-check (P0-7 nuance)** — verified `auth.service.ts:206-239` (devLogin signs JWT — does it pass expiresIn?) and the live Admin login flow: with `DEV_LOGIN_ENABLED=false` the admin console's OTP path (via `verify-otp` with `surface:'ops'`) is the only login option and is functional. (Pre-emptive live verify before flipping the flag.)
- **Strix sequencing (vuln-0004 / 0005)** — drizzle-orm SQLi has no reachable sink (per Strix's own analysis); safe to batch AFTER P0-7/P0-8. Next 15.5.21 middleware-bump is the only reachable CVE — bundle with P0-7.

## Board changes (added/promoted/demoted/completed)

- **DONE P0-7** (commit bfd453b) — dev-login admin bypass fix.
- **DONE P0-8** (commit 9678790) — push-endpoint SSRF fix.
- **PARTIAL P1-17a** (commit 835e27d, 7/8 CVEs) — next 15.5.21 + axios 1.16.0 + ws/engine.io overrides + Sentry 10.70.0 pin. **Drizzle 0.45.2 DEFERRED** (6 type tightenings need half-day audit; Strix confirmed no reachable sink in 0.44.7).
- **PARTIAL P1-17b** (commits bfd453b + 835e27d) — `.env.bak-expose-*` deleted, `.gitignore` extended with `.env.*` + `.env.bak-*`. (b + d still TODO: Sentry token rotation, Redis password, admin→HttpOnly cookies, server-side session invalidation.)
- **ADDED P1-19** (API/Auth) — no phone-number change flow.
- **ADDED P1-20** (API/Auth) — no partner self-onboarding.
- **ADDED P2-43** (API/Push) — no push-subscription lifecycle sweep.
- **DONE P2-44** (PWA/Security) — DevLoginBar hostname gate (folded into P0-7).

## Built this run (slices, commits, verification evidence)

### Slice 1 — P0-7 dev-login admin bypass (CRITICAL, commit bfd453b)
- **Code**:
  - `apps/api/src/modules/auth/auth.service.ts:206-249` — `devLogin` now ALWAYS passes `{expiresIn: JWT_EXPIRY || '7d'}` to signAsync (was: no options, never-expiring token).
  - `apps/api/src/modules/auth/auth.controller.ts:109-138` — gate flipped from `NODE_ENV === 'production'` to `DEV_LOGIN_ENABLED === 'true'` (default OFF).
  - `apps/api/src/modules/wallet/wallet.controller.ts:57-86` — gate flipped from `NODE_ENV === 'production'` to `WALLET_TOPUP_ENABLED === 'true'` (default OFF).
  - `apps/player-pwa/src/components/auth/DevLoginBar.tsx` — build-time `NEXT_PUBLIC_DISABLE_DEV_LOGIN_BAR` constant. When set, the component short-circuits to `null` BEFORE any hooks; the seeded-phone list is webpack-tree-shaken from the prod bundle.
  - `apps/api/.env.example` + `apps/api/.env` (live) — both flags set to `false`; `ADMIN_PUSH_HOST_ALLOWLIST=` documented.
  - `.gitignore` — added `.env.*` + `.env.bak-*` (with `!.env.example` exception). The next backup file is auto-ignored.
  - `apps/api/.env.bak-expose-20260901101040` DELETED (was untracked; the filename implies a prior exposure incident with unverified rotation).
- **Tests**:
  - NEW `apps/api/src/modules/auth/auth.dev-login.spec.ts` (12 cases: always-pass-expiresIn, JWT_EXPIRY env override, 404, ban, suspend, suspension elapsed, Admin surface, + 5 gate-string contract tests).
  - NEW `apps/api/src/modules/wallet/wallet.topup-flag.spec.ts` (5 cases: flag-true credits, flag-false 403, flag-undefined 403, "production" string still 403, user sub/dto passthrough).
  - MIGRATED `apps/api/src/modules/wallet/wallet.controller.spec.ts` (4 cases: flag-true credits, flag-false 403, unset 403, "production" still 403).
- **Live E2E** (with `DEV_LOGIN_ENABLED=true` temporarily, then restored):
  - `DEV_LOGIN_ENABLED=true`: dev-login returns 200; decoded JWT carries `iat` + `exp` claims (7d window = 604800s exactly).
  - `DEV_LOGIN_ENABLED=false` (live): dev-login returns 403 with message "dev-login is disabled in this environment".
- **Gates**: jest 232/232 (was 213, +19), vitest 272/272, build 3/3, tsc 0, API restarted + health 200.

### Slice 2 — P0-8 push-endpoint SSRF (CRITICAL, commit 9678790)
- **Code**:
  - NEW `apps/api/src/common/security/push-endpoint.validator.ts` (205 lines):
    - `assertSafePushEndpoint(endpoint, allowlist)` — HTTPS-only, port 443, no userinfo, no fragment, length ≤ 2048, host in allowlist, IP literals (v4/v6) must be publicly routable. Rejects: 127/8, 10/8, 172.16/12, 192.168/16, 169.254/16 (incl. cloud metadata 169.254.169.254), CGN 100.64/10, IPv6 ULA fc/fd, link-local fe80, multicast ff, ::1, ::.
    - `DEFAULT_PUSH_HOST_ALLOWLIST` (7 hosts): FCM (`fcm.googleapis.com`), Mozilla (`updates.push.services.mozilla.com`, `push.services.mozilla.com`), Apple (`web.push.apple.com`, `api.push.apple.com`, `api.sandbox.push.apple.com`), Windows (`wns.notify.windows.com`).
    - `ADMIN_PUSH_HOST_ALLOWLIST` env (comma-separated) — operators add Huawei/Samsung/relay hosts without code changes.
  - `apps/api/src/modules/notifications/notifications.service.ts:32-58` — constructor reads `ADMIN_PUSH_HOST_ALLOWLIST` + merges into `hostAllowlist`.
  - `apps/api/src/modules/notifications/notifications.service.ts:65-87` — `subscribe()` calls `assertSafePushEndpoint` BEFORE the DB write.
  - `apps/api/src/modules/notifications/notifications.service.ts:223-246` — `sendPushToUsers` re-validates each row at send time (defense in depth); uniform Sentry capture tagged `web-push-validate`.
- **Tests**:
  - NEW `apps/api/src/common/security/push-endpoint.validator.spec.ts` (48 cases):
    - Scheme/port/userinfo/fragment/length rejection (8 cases).
    - Allowlist enforcement incl. custom-allowlist (5 cases).
    - Private/loopback/link-local IP rejection — both v4 and v6 (10 cases).
    - `assertSafePushEndpoint` throws BadRequestException with stable messages (2 cases).
    - `isPublicIp` IPv4 + IPv6 ranges (23 cases via `it.each`).
    - `DEFAULT_PUSH_HOST_ALLOWLIST` sanity (2 cases).
  - MIGRATED `apps/api/src/modules/notifications/notifications.push-preferences.spec.ts` (8 cases, 11 total) — replaced `'ep-X'` fixtures with `'https://fcm.googleapis.com/ep-X'`; mock ConfigService now returns `''` for unrecognised keys.
- **Live E2E** (with `DEV_LOGIN_ENABLED=true`):
  - `https://attacker.example/push` → 400 "host not in allowlist" ✓
  - `http://fcm.googleapis.com/abc` → 400 "must use https" ✓
  - `https://fcm.googleapis.com/fcm/send/x` → 201 "subscribed:true" ✓
  - Cloud-metadata URL test (`https://169.254.169.254/...`) blocked by the terminal security scanner (the exact SSRF exploit shape); 48 jest specs cover the rejection path.
- **Gates**: jest 280/280 (was 232, +48), vitest 272/272, build 3/3, tsc 0, API restarted + health 200.

### Slice 3 — P1-17a dep upgrade + tsc latent-error sweep (commit 835e27d, 7/8 CVEs closed)
- **Dep upgrades (root package.json overrides + per-workspace)**:
  - `next 15.2.9 → 15.5.21` (apps/player-pwa + apps/admin) — closes CVE-2026-44575 (middleware bypass, reachable via PWA src/middleware.ts) + CVE-2026-64649 (Server-Action SSRF).
  - `axios ^1.7.4 → ^1.16.0` (apps/api) — closes CVE-2026-44494/42264/44495 (prototype-pollution/MITM/info-disclosure at unifonic.service.ts:27).
  - `ws ^8.21.0` + `engine.io ^6.6.7` via root `overrides` (incl. nested engine.io-client path) — closes CVE-2026-48779 (ws DoS) + CVE-2026-59725 (engine.io DoS) transitively pulled by @nestjs/platform-socket.io.
  - Sentry trio (nextjs/node/core/react/browser/server-utils) pinned to **10.70.0** via root overrides — 10.73.x's `server-utils/orchestrion/bundler/webpack.mjs` fails to load in vitest's ESM context (the file uses `import.meta.url` which resolves to a non-`file://` scheme when loaded via the CJS `require()` shim).
  - **`drizzle-orm` DEFERRED to run #27** — 0.45.2 introduces 6 type tightenings (eq() right-operand narrowing, returning() array projection typing, `.returning()` schema inference) that require a half-day audit. Strix's own usage analysis confirmed no reachable sink in 0.44.7 (no `sql.identifier()` / `.as()` with user input).
- **Tsc latent-error sweep (6 pre-existing bugs unmasked by the npm install cache invalidation)**:
  - `apps/api/src/modules/admin/disputes.service.ts:155` — `match_id` is varchar(nullable); `activities.record` wants `string|undefined`. Coerced with `?? undefined`.
  - `apps/api/src/modules/admin/reports.service.ts:131-150` — `before.reporter` typed as `Object|Object[]` (Drizzle 0.44.x relations quirk). Added `Array.isArray()` narrowing.
  - `apps/api/src/modules/admin/settlements.service.ts:162` — `entityId` wanted `string`; `.returning()` with no projection returns `unknown[]`. Cast to `string` + `before: null → undefined` (audit log interface is `before?: unknown`).
  - `apps/api/src/modules/conversations/conversations.service.ts:251-256` + `apps/api/src/modules/matches/matches.service.ts:1482-1487` — `eq()` right operand (`clientMessageIdValue`) is `string|null`; tsc couldn't narrow through `?.trim() || null` inside the `if (!inserted)` branch. Added explicit `if (!clientMessageIdValue) throw new ConflictException(...)` to force the narrowing.
  - `apps/api/src/modules/matches/matches.reschedule.spec.ts:217-221` — result shape needed a cast to pull `id`/`title` off the `{reschedule:{...}}` spread.
  - `apps/api/src/modules/partner/partner.match-visibility.spec.ts:201,213,223` — `captured.get(matches)` returns `unknown`; needed a cast to `Parameters<typeof dialect.sqlToQuery>[0]`.
- **Gates**: tsc 0, jest 280/280, vitest 272/272, build 3/3 (full clean rebuild 2m48s after node_modules change), API restarted + health 200 + dev-login still 403 (P0-7 still in force).

## Blockers & decisions

- **zai delegation children still 401 at the gateway layer** (run #24 + #25 + #26, third run in a row). Pool itself is clean; the gateway daemon (booted 06:52:35Z) is resolving children through a stale state. Reviewers routed on `opencode-go glm-5.3-flash` (same model tier, more expensive plan, ~3% rolling spend for 2 reviewers). **Next run (#27) MUST re-probe** — if still 401, the fallback chain is `opencode-go` → `deepseek-v4-flash`.
- **drizzle-orm 0.45.2 DEFERRED** to run #27 — the type tightenings need a dedicated cycle; not reachable in 0.44.7 (Strix's own analysis). Drizzle 0.44.7 stays in apps/api/package.json.
- **Run #25 'vitest 272/272' claim was based on a stale turbo dist cache** — the Sentry 10.73.0 + vitest ESM incompatibility was latent. Today's `npm install` invalidated the cache and exposed it. Run #26 pinned Sentry to 10.70.0 via root overrides; vitest 272/272 is now a true green.
- **Live E2E for the cloud-metadata SSRF test was blocked by the terminal security scanner** (correctly — the exact exploit shape). 48 jest specs cover the rejection path.
- **No budget for P1-17c (WS origin) + P1-17d (ICS TEXT injection)** — small (≤30 min each) but ran out of time after the P1-17a dep upgrade + tsc sweep. Both stay TODO for run #27.

## Next-run recommendation

Per the APPROVED RUN PLAN in STATE.json (runs #26-44, money items parked until KSA company):

1. **#27 — Push reliability + P0-5 (per-category prefs)** — install-triggered push enablement, email-worthiness matrix, OTP efficiency (incl. P2-19 per-IP cap), P0-5 category model.
2. **#28 — Email module + P2-42 CSP migration** — nonces + drop unsafe-inline; ical.js TEXT escape (P1-17d); WS origin (P1-17c).
3. **#29 — P0-6 PDPL delete + export** (owner-approved).
4. **#30 — chat pagination, waitlist, search upgrade** (server-side + city + club filters).
5. **#31-34 — money int math, contract hygiene, offline queue, UI polish (P2-29 featured + CTA, P2-32 404), P1-19 (phone change), P1-20 (partner onboarding decision), P2-43 (push sweep), drizzle 0.45.2 upgrade**.
6. **#35-44 — QA/stability sweeps**.
7. **Re-run Strix dep scan in #27** to confirm 7 closed CVEs resolved + verify the deferred drizzle 0.44.7 has no reachable sink in current code.
