# Factory Run #14 — 2026-08-29T15:15Z

**Mode:** full
**Budget:** ~1h05m actual (15:15Z preflight → ~16:20Z lock release) vs 3.5h — stopped at the
3-item quota guardrail ("NEVER exceed 3 items per run").
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (**5.3-as-reviewer trial,
run 2 of 3**; probe: **200 OK** 5-token ping on z.ai coding base)

## Preflight

- No prior lock; lock taken 15:15Z. gh auth OK. `git pull --ff-only` clean at `2891a23`.
- Tree dirty with FOREIGN files only (`docs/architecture/hermes-harness*` — sibling session);
  untouched, never staged.
- GLM probe `glm-5.3-flash` **200** → delegation set to `glm-5.3`/zai/coding base (trial,
  within-z.ai swap touches only delegation.model). `reasoning_effort low` already set.
- **Trap variation:** this cron session DID inherit `HERMES_DELEGATED_CHILD_CONTEXT=1`
  (run #3's trap returned; run #13 did not have it). All kanban CLI calls ran under
  `env -u HERMES_DELEGATED_CHILD_CONTEXT` per runbook §Phase 3.7.2.
- graphify update: no code-graph topology changes. god-nodes sane (CurrentUser 69, fetcher 61,
  useAppStore 58, MatchesService 49).

## Health snapshot (Phase 1.5)

api/pwa/admin all `active`; `/api/v1/health` 200; journalctl err-scan 5h: **zero errors**
across all three services.

## Sentry / error-log triage (Phase 1.6)

Source: Sentry API (EU `de.sentry.io`, SENTRY_READ_TOKEN) — both projects, 24h window.
- koralink-api: all signatures known/historical — CORS `evil.example` probe ×21 (blocked),
  auth-cookie 401 noise ×117, legacy `/health` probes ×19, single known ECONNRESET
  (KORALINK-API-W, covered by run #9 post-mortem), duplicate-report 400s (working as
  designed, P2-9 idempotency), roster/member 404-403s (correct auth behavior).
- koralink-web: SW registration CSP noise ×2 (localhost scriptURL, dev-only), 1 info ping.
- **Nothing new to fix; no new boards from triage.**

## 5.3-as-reviewer trial — data point 2 of 3

- Reviewer A (code quality + Admin rotation) + Reviewer B (verification + product gaps)
  dispatched in parallel on `glm-5.3`, `reasoning_effort low`, ≤30-call discipline.
- **Reviewer A: completed, 103s, 9 tool calls. Reviewer B: completed, 137s, 14 tool calls.**
  Zero timeouts (vs runs #9–#10 flash/600s-cap double timeouts; baseline runs #11–#12 <4 min).
- Findings quality: **0 false positives** — every A finding verified in code by the parent
  before boarding; every B verification verdict independently re-checked (run #13 spot
  checks matched line-for-line, incl. the narrowed dispute predicate at :1864).
- Trial tally: dp1 (run #13) 150s/119s, mostly-true; dp2 (this run) 103s/137s, all-true.
  Decision after run #15.

## Findings (GLM-5.3 reviewers + self, merged, evidence-cited)

**Reviewer A (all verified in code by parent):**
1. IMPORTANT — Admin `apiFetch` calls `res.json()` unconditionally on success
   (apps/admin/src/lib/api.ts:62): any 204/empty-body endpoint → SyntaxError; no timeout
   either (hung fetch = perpetual loading). → **P2-24, built this run.**
2. IMPORTANT — quiet-hours `start === end` ⇒ muted 24/7, no UI warning
   (notifications.service.ts:135 + profile selects). → **P2-25 (boarded).**
3. MINOR — useMatches offset cursor can skip/duplicate rows if filters change mid-pagination
   (inherent infinite-query-by-offset; documented risk, not boarded).
4. MINOR — over-paged envelope request returns `total: undefined` (cosmetic `—` in client).
5. MINOR — messages/page.tsx:23 `new Date()` in render (hydration-risk class, already
   boarded run #12; unchanged).
6. Bug-class sweep all CLEAN: 0 `::uuid`, 0 `eq(col,null)`, 0 console.* in API prod paths,
   0 unguarded mutations, z-index compliant; `Date.now()` render sites = 5 (previously boarded).
7. Recent-cycle verdict: run #13 commits match their claims — no overstated DONE.

**Reviewer B:**
- Run #13 verification: **all 5 commits BUILT-AS-CLAIMED** (ecdf29c tx+predicate, 6b0ffab
  envelope/hook/wire, 2b520e3 quiet math+DTO+UI, 24e59af calendar ACL, b3c7445 key+WS auth).
  Promoted to DONE.
- New product gaps (deduped vs board): **P1-24** host cannot remove a player (highest
  impact — missing half of the no-show/moderation loop) · **P1-25** no venue operating
  hours/blockouts (players can book 3am slots) · **P1-26** partner console has no
  match/roster visibility · P2 (new) slot price not per-slot editable; play feed lacks an
  offline branch (folded into P2-7 evidence trail, not separately boarded).

## Previous-run verification (run #13 claims)

- ecdf29c / 6b0ffab / 2b520e3 / 24e59af / b3c7445 — all verified BUILT-AS-CLAIMED by
  Reviewer B with file:line evidence; parent re-checked the load-bearing ones (tx boundary
  matches.service.ts:419-466; dispute predicate :1864; COUNT(*) OVER() :660; isInQuietHours
  :127-148; getCalendarMatch :866-895). **Promoted to DONE** (already recorded on board).
- Load More render + quiet-window suppression: code paths confirmed; live night-suppression
  observation deferred (next quiet window is tonight; unit-pinned midnight-wrap math stands).

## Board changes

- NEW **P1-24** (host roster removal) → **DONE ✅ this run** (`b4d098b`).
- NEW **P1-25** (venue operating hours), **P1-26** (partner match visibility) — TODO.
- NEW **P2-24** (apiFetch empty-body/timeout) → **DONE ✅ this run** (`24c7388`).
- NEW **P2-25** (quiet-hours equal-hours warning) — TODO.
- **P1-12** (admin i18n) TODO → **IN-REVIEW** — partner-portal slice built this run (`c4b92b0`),
  HQ console strings remain as the follow-up scope.

## Built this run (slices, commits, verification evidence)

All on `main`, pushed `2891a23..24c7388` (3 commits, interleaved with sibling docs commits —
pushes fast-forwarded clean).

1. **`c4b92b0` feat(admin): i18n infrastructure + Arabic partner portal (P1-12 slice)**
   - next-intl in apps/admin: client `AdminI18nProvider` (SSR always `en` — matches static
     `<html lang="en">`, no hydration mismatch), `messages/en|ar.json` (198 keys each,
     parity-verified via node key-diff script), `admin_locale` localStorage + module-level
     `useSyncExternalStore` locale store, sidebar `LanguageToggle` (EN/العربية),
     pre-hydration boot script flips `<html lang/dir>` (no LTR flash) + Tajawal font.
   - Partner portal fully localized: dashboard, venues, pitches (incl. SlotManager day
     chips/grid + EditPitchSheet), earnings, settings; shared components Sidebar,
     StatusBadge (wire-keyed catalog), Pagination (ICU plural). Times/dates/IDs keep
     `dir="ltr"`; money label localized (SAR / ر.س) with Latin digits.
   - Gates: admin tsc 0 · turbo build 3/3 (postbuild restarted admin service). Live: SSR
     renders clean auth-guard shell (0 MISSING_MESSAGE), built chunks carry BOTH catalogs
     (Arabic strings verified verbatim in .next chunks), boot script + Tajawal in HTML.
     ⚠️ Visual RTL check deferred — headless Chrome cannot launch on this box (browser
     harness "chrome-not-running"); next run should eyeball /partner with admin_locale=ar.
2. **`b4d098b` feat(matches): host can remove a player from the roster (P1-24)**
   - API: `DELETE /matches/:id/players/:playerId` — host only, pre-match only; ordered
     guards in tx (self 400 → match 404 → non-host 403 → status 400 → roster 404);
     Full → Open flip; returns populated `findOne` OUTSIDE tx (contract §2); removed player
     notified via new `player_removed` verb (activities union + DIRECTED_VERBS + push).
   - Schema: `ActivityVerb` enum reconciled — schema.ts now carries `host_underfilled_nudge`
     + `match_auto_cancelled` (applied out-of-band previously, journal had no migration) +
     `player_removed`; migration **0023** uses `ADD VALUE IF NOT EXISTS` (idempotent on the
     live DB). Committed BEFORE `db:migrate` (no-half-slices order); applied; **live enum
     verified: 16 values incl. player_removed** (porsager `postgres` driver at monorepo root).
   - PWA: `useRemovePlayer` hook; PlayerProfileSheet two-tap Remove (host, open matches,
     non-host targets, confirm resets on player change); verb wired into feed/bell/toast
     maps; en/ar i18n (655/655 parity).
   - Tests: 7 jest specs (guards, notify fan-out, Full→Open, populated return).
   - **Live E2E PASS** (real services): create(self-mode)→joiner joins→joiner-removes-host
     403 → host-removes-joiner 200 (roster 2→1, joiner absent, status Open, host relation +
     messages array present) → joiner feed shows `player_removed` (actor Ahmed Al-Rashid) →
     re-join + cancel cleanup 200. Self-mode used deliberately (koralink-mode create debits
     the host wallet; cleanup cancel refunds — a self-mode probe keeps the wallet untouched).
   - Gates: api tsc 0 · jest 151/151 · pwa tsc 0 · vitest 234/234 · build 3/3.
3. **`24c7388` fix(admin): apiFetch survives empty/204 bodies + 30s fetch timeout (P2-24)**
   - 204 / zero-length / non-JSON success bodies → `undefined` (no SyntaxError crash);
     AbortController 30s timeout surfaces "Request timed out…" instead of perpetual
     loading; caller `options.signal` still wins.
   - Gates: admin tsc 0 · turbo build 3/3.

Post-slice final sweep: root build **3/3**, jest **151/151**, vitest **234/234**,
tsc 0 ×3, i18n parity 655/655 (PWA) + 198/198 (admin).

## Blockers & decisions

- **Decision:** stopped after 3 items per the quota guardrail (~1h05m used; headroom left
  for next run). P1-25/P1-26 left TODO deliberately (schema-touching slices deserve a full
  budget, not a tail-end rush).
- **Decision:** self-mode match for the E2E probe — koralink-mode create debits the host
  wallet via the dummy top-up flow; cancel refunds exactly `pitch_cost_sar`. A self-mode
  probe exercises the identical roster-removal path with zero wallet writes.
- **Decision:** migration 0023 hand-adjusted to `ADD VALUE IF NOT EXISTS` after generation —
  2 of its 3 values already exist live (out-of-band ALTER from a pre-journal era, discovered
  via `enum_range` inspection). Snapshot left as drizzle generated it (harmless: it models
  the code truth).
- **Decision:** P1-12 marked IN-REVIEW (not DONE) — partner portal is done but HQ strings
  remain; visual RTL check pending (no headless browser on the box).
- Sibling interleaving: board/STATE received sibling edits mid-run (patch warnings); all
  writes re-based on fresh reads; foreign files never staged.
- write_file produced corrupted partial content twice (Sidebar.tsx, utils.ts) — both caught
  on read-back and rewritten cleanly before any commit. Worth watching.

## Next-run recommendation

1. **Verify run #14 claims** — esp. the visual admin RTL check (set `admin_locale=ar` in
   localStorage → /partner + /partner/pitches; Arabic day names, mirrored layout, no LTR
   flash) and the removed player's bell label (feed.playerRemoved).
2. Board top items: **P1-26** (partner match/roster visibility — natural follow-on, same
   partner pages as P1-12, needs a scoped `GET /partner/matches` first) or **P2-13**
   (clubs decorative pills), then **P2-25** (quiet-hours equal-hours warning, small).
   P1-25 (venue hours) is the bigger schema slice — take it with a full budget.
3. Trial: reviewer data point 3/3, then decide glm-5.3 vs flash for the review phase.
