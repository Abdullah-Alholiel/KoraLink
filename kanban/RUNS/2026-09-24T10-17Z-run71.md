# Factory Run #71 — 2026-09-24 (completed by the 10:17Z RECOVERY session)

**Mode:** full (DB & Infra lane, rotation 71%4=3) — **DEAD-RUN RECOVERY #3**: the 01:15Z
incarnation completed Phases 0–4 (both reviewers, gates, board edits) but was KILLED ~01:48Z
before any commit/push/report. Lock pid 3225877 found dead, 9.0h stale; executions.db row for
01:15:17Z = `unknown` ("owner exited before a durable terminal state"). This session (started
10:15Z scheduled slot) adopted the coherent half-work per the run-#51-manual recipe: every
claim re-verified parent-side before adoption; ALL gates re-run fresh on the final tree.
**Budget:** ~55 min actual (recovery path; the heavy phases were already done — and verified —
by the killed session). Next run resumes the normal ~3h budget.

**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` via zai
(probe 200 in 6.0s on ZAI_API_KEY; GLM_API_KEY line remains stale-401; weekly quota 1% per the
prestate directive — healthy, primary plan). Reviewers dispatched by the dead session
(deleg_bdac72d5, completed 01:29Z, 24 calls total) — findings re-read and re-verified from
task-0/task-1 logs by this session; no re-dispatch needed (quota discipline).

## Preflight (recovery variant)
- Lock: dead-session lock removed; new lock written (pid liveness noted; removed at end).
- git sync: staging HEAD 0850497, 0 ahead / 0 behind origin/staging.
- Delegation config found CLEAN on glm-5.3-flash/zai/coding base (dead session's Phase 0 had
  set it; verified, no change needed).
- gh auth: logged in as Abdullah-Alholiel.
- Half-work attribution: all modified/untracked files have mtimes 01:41–01:48Z — entirely
  inside the dead session's kill window; nothing newer touched. Adoption safe.
- Strix: not due (Oct 1–3 window; September scan recorded in STATE notes).

## Sentry / error-log triage (Phase 1.6 — THIS session's independent pass)
- journalctl 10h: zero error entries on all three services. API /health 200 (localhost:3001).
- koralink-api 24h issues: **NEW — KORALINK-API-1C** `PostgresError: Your account or project
  has exceeded the quota. Upgrade your plan to increase limits.` — production environment,
  `users.purgeExpired` scheduled job, Render host `srv-dadabif10e5c73e207bg`, lastSeen
  **2026-09-24T10:00Z (17 minutes before this session's triage)**. This is the PROD Neon
  database failing on quota — NOT staging (VPS local PG probed healthy). → Boarded as
  **P1-52 (owner/infra: Neon billing/usage check)**; mirror card t_a450b5a9.
- KORALINK-API-B "CORS origin not allowed: evil.example" (113 lifetime, 3/24h): re-verified as
  SELF-GENERATED — `scripts/release-verify.sh:48` deliberately fires a fake-origin OPTIONS to
  prove the CORS guard rejects foreign origins (it does). Expected noise; filter-note only.
- koralink-web: nothing new since Sep 14 (all issues stale).

## Findings (dead session's reviewers, re-verified by this session)
- Reviewer A (deleg_bdac72d5/task-0, zai glm-5.3-flash): 4 DB/Infra findings —
  1. IMPORTANT: migrate-vps.mjs applied statements with NO per-file transaction → built as
     P2-98 this run (see below).
  2. IMPORTANT (known P2-88): torn drizzle meta snapshot chain (0026→0029 with `prev` absent;
     0027/0028/0030–0043 missing) → guard spec shipped (below), rebuild queued separately.
  3. MINOR: `0014_admin_notification_verbs` journal entry = documented run-#46 late-adoption
     convention; `when` strictly increasing; REFUTED as defect.
  4. MINOR: duplicate `0014_` prefix on disk — harmless while both stay journaled; noted in
     spec header. Standing sweeps CLEAN (::uuid casts, eq(col,null), FOR UPDATE, console.*,
     i18n parity 985/985).
- Reviewer B (task-1): verified ALL run-#70 in-review claims independently; product-gap leads
  (venue-review submission route — existing backlog; mada missing from P0-2 payment-provider
  scoping — boarded on the P0-2 row; favorites/dark-mode/referrals — backlog only).

## Previous-run verification (run #70, 3/3 claims TRUE — re-verified by THIS session, not trusted)
- P2-95: jest waitlist-push 4/4 green (re-run); greps waitlist_promoted=2 in waitlist.service.ts,
  `type: 'match-chat'`=0 in matches.service.ts, worker kl-push-nav=1. DONE confirmed.
- P2-96: usePushNotifications suite 14/14 (rollback case included). DONE confirmed.
- P2-93/94: structure suites + hook units green in the full 710/710 vitest; worker
  `pushsubscriptionchange` reads absolute API origin from the KV (`body.b` → `${apiBase}/…`).
- P2-97 residue: `npm run lint` exit 0 on the final tree (re-run).

## Board changes
- NEW P1-52 (Infra/DB): PROD Neon quota-exceeded — owner/infra (mirror t_a450b5a9, gated).
- NEW P2-98 (DB/Infra): migrate-vps per-file transaction atomicity — DONE this run
  (mirror t_929e66db, created+gated+built-comment).
- P2-88: row updated to PARTIAL (guard spec shipped; rebuild = dedicated half-cycle off-VPS);
  mirror t_df7b26f0 commented with run #71 evidence.
- P2-95/P2-96 rows: VERIFIED run #71 stamps appended (with evidence).
- P0-2 row: mada scoping note added (Reviewer B: no mada|stcpay|applepay strings anywhere in
  api/pwa — the provider decision MUST explicitly consider mada, the dominant KSA scheme).
- Header: run #71 block corrected (report pointer now the recovery report), RECOVERY paragraph.
- ADMIN HOLD unchanged (projects-dir merge conflict untouched, lane-local).

## Built this run (slices, commits, verification evidence)
- **P2-98 — migrate-vps per-file transaction atomicity** (`scripts/migrate-vps.mjs`):
  statements + journal INSERT share ONE `sql.begin` tx per migration file; P2-80
  duplicate-DDL tolerance SAVEPOINT-scoped (`tx.savepoint`) so a tolerated error leaves the
  tx usable; non-tolerated failure → sentinel throw → full ROLLBACK → exit 5 (gap-detection
  and exit-code contract preserved). Runtime-verified beyond the static spec: postgres-js
  3.4.9 `savepoint` support confirmed in-node, PLUS a live `sql.begin`/savepoint/tolerated-
  error/rollback smoke against the staging DB returned OK (tolerated dup-schema → tx
  continued → commit).
- **P2-88 guard — `drizzle-snapshot-chain.spec.ts`**: pins the exact torn chain in BOTH
  directions (a snapshot rebuild flips red on purpose → update pins in the rebuild commit; a
  NEW hand-written migration without a snapshot flips red → regenerate or consciously extend
  the pin; third case keeps the pin honest vs disk).
- **migrate-vps-atomicity.spec.ts**: static contract pins (sql.begin, savepoint, journal-in-tx,
  old out-of-tx INSERT gone, DUP_CODES tolerance, exit 5).
- Gate evidence (ALL fresh, this session, on the final tree): `npx turbo run build
  --concurrency=1` → 3/3 exit 0 — first cached 110ms, then FORCED clean rebuild 1m28s
  0-cached to eliminate the cached-build doubt; `npx vitest run` → **710/710 (98 files)**;
  api `tsc --noEmit` 0; PWA `npm run lint` 0; both new jest suites 6/6 + journal tripwire
  4/4; `node --check scripts/migrate-vps.mjs` OK; fallback-*.js build churn checked out
  (never committed). Post-build tree clean of factory artifacts.

## Blockers & decisions
- P1-52 Neon quota = owner/infra (billing); factory cannot touch billing — surfaced for
  Abdullah. Highest urgency this week: prod DB availability.
- Deploy-target fork unchanged: systemd units serve /home/ubuntu/projects/koralink; staging
  commits reach live only via Abdullah's directed merge (this run's P2-98 script + specs are
  not part of any service dist, so no live impact either way).
- PR #27 (promote staging→main) remains OPEN — Abdullah's gate.
- KILL-PATTERN #15 logged: 01:15Z session died ~01:48Z (post-review post-build, pre-commit).
  dmesg/kernel-log unreadable from the agent shell (CapEff=0) — no OOM line captured this
  time; gateway journal around the window shows no kill either (execution row = plain
  `unknown`). Not OOM-diagnosable from here; occurred despite sequential builds (~1.5-2 GiB
  peak ≪ 4 GiB cap). Correlate next occurrence.

## Next-run recommendation (run #72)
1. Rotation 72%4 = 0 → **PWA screens lane**. Admin stays HELD (projects-dir conflict).
2. P1-52 (Neon quota) may be resolved by then — if Abdullah fixed billing, verify
   `users.purgeExpired` runs clean (no new KORALINK-API-1C events) and mark DONE with
   evidence; if unresolved it stays the top open row.
3. Board pressure: P2-80 (Infra) and P2-31/32/35 minors are quick candidates in an API/Infra
   rotation; the P2-88 snapshot rebuild still wants its dedicated off-VPS half-cycle.
4. Keep `--concurrency=1`; keep the recovery recipe for any dead lock (verify mtime window
   before adopting).
