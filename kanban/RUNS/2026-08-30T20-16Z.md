# Factory Run #20 — 2026-08-30T20:16Z

**Mode:** full
**Budget:** ~1h20m actual (of ~3.5h) — 3 build items + post-cycle fix closed cleanly; verification/mirror/report consumed the rest
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (permanent; probe: 200) · post-cycle review `glm-5.3` (78s — zero timeouts, 6th consecutive clean review run)

## Preflight
- No lock on arrival (wrote `20260830T201627Z`, pid 2427160). GLM probe 200 → delegation `glm-5.3`/zai/coding-endpoint set. gh auth OK (Abdullah-Alholiel). `git pull --ff-only` clean; HEAD `847b747` (run #19 docs).
- Dirty foreign paths respected, never staged: `.gitignore` (M), `graphify-out/*` (watch daemon), `docs/architecture/hermes-harness*` (untracked sibling work).
- `HERMES_DELEGATED_CHILD_CONTEXT` NOT present this run — kanban calls ran plain (runbook 3.7 rule 2 check).

## Sentry / error-log triage (Phase 1.6)
Sources: Sentry API (EU read token, both projects) + journalctl (3 services, 5h).
- journalctl: **zero error lines** on api/pwa/admin.
- Sentry 24h: newest item is the known handled `FetchError: Insufficient wallet balance` (KORALINK-WEB-3, 08-30 07:53Z, ×1, client-classified); everything else pre-dates 08-29 (auth-401 probes 117×, CORS canary, boot-incident residue). Old CORS canary `evil.example` count now 21 — permanent canary noise, MINOR, no action.
- **Verdict: clean — nothing new, nothing to fix.**

## Previous-run verification (Phase 1 — run #19's five claims)
Reviewer B verified ALL FIVE independently (code + jest + one-shot DB probes + live HTTP):
- **P1-33** → PASS (journal idx 26, 4× ADD VALUE IF NOT EXISTS, enum 17 values 0 dupes, migrations 28).
- **P1-34** → PASS (additive `<> 'Cancelled'` in upcoming branch, jest 7/7, live probes honored).
- **P1-32** → PASS (badge/hours/pitch-cards in code, i18n keys both locales, live club page 200).
- **P2-26** → PASS (profile skeleton + error/retry, host HostFormSkeleton, /en/host 200 with animate-pulse).
- **P2-33** → PASS (not-found.tsx live 404, `?? ''` guards in all detail pages + Sidebar).
→ All five promoted **DONE ✅** on the board.

## Findings (GLM A/B + parent, merged, evidence-cited)
- **Reviewer A (code quality, PWA rotation):** 0 CRITICAL. 2 latent IMPORTANTS: clubs/[id] hours row renders when only ONE bound defined (`||` guard — latent, schema notNull default) → **fixed this run** (rider); `Date.now()`-in-render hydration class (already backlog line 131). MINORS boarded as P2-35: `formatDateLabel` hardcodes `en-US`; `venue-hours.ts` should pin `hourCycle h23`. Standing sweeps clean: `::uuid` 0, `eq(col,null)` 0, console.* 0, i18n 679/679 + 239/239, admin guards present, no dead UI in touched screens.
- **Reviewer B (gaps + verification):** all 5 claims PASS (above). NEW gap flagged "P0: no notifications surface in PWA" — parent verification DOWNGRADED: bell+sheet exist but mount ONLY on the feed page (`(main)/page.tsx:97`), no other tab, no notifications route → boarded as **P2-34** (reachability gap, not missing feature). Also flagged: no match/venue share deep-links (real, overlaps existing wallet/payment P1s in size — recorded in backlog notes, not a new numbered item), thin empty-state coverage on wallet/my-games.
- **Parent self-review:** confirmed `GET /pitches/:id/slots` is already player-facing (reschedule needs no new read route); cancelMatch template extracted for the reschedule tx.
- **OS research (Phase 1.7):** none this run — existing Leaflet/MapLibre backlog card already covers the maps rotation domain; saved the budget for the build.

## Board changes
- Verified DONE: P1-33, P1-34, P1-32, P2-26, P2-33 (run #19's items, re-verified live).
- Built→DONE: **P1-13** (match reschedule), **P2-31(1)** (admin hours guard → item now PARTIAL).
- Added: **P2-34** (notification bell scope), **P2-35** (clubs date i18n + hourCycle nit).
- Mirror: new cards + comment updates applied under `t_ce9a513a` per runbook 3.7 (see mirror section in Phase 3.7 log; idempotency keys `board-P2-34`, `board-P2-35`).

## Built this run (slices, commits, verification)
| Item | Commit(s) | What | Verification |
|---|---|---|---|
| P1-13 API | `644ab89` + `f772b78` | `PATCH /matches/:id/schedule`: one tx — match-row FOR UPDATE, both slots locked ORDER BY id, old released + exact `pitch_cost_sar` refund (oldCost>0 guard), new booked + rate×duration charge, net wallet delta with balance floor, `scheduled_at/duration/price_per_player` re-derived server-side; populated `findOne` OUTSIDE tx + `reschedule` block; migration 0027 `match_rescheduled` verb; roster activity+push, WS broadcast | jest 11 new specs (guards, slot swap, wallet math, ledger keys, insufficient-balance 400, cheaper-slot refund); **live E2E**: 403 non-host / 400 wrong-pitch / 404 bogus slot / 200 move (slot swap + delta 0 + roster 3 kept) / 400 re-PATCH / 400 bad DTO / **A→B→A round-trip 200** |
| P1-13 PWA | `4150fb8` | `RescheduleSheet` (today's free slots via `usePitchSlots`, 5 UX states, current slot excluded, reset on close) + `useRescheduleMatch` (invalidations + PostHog + toast) + host button on match detail (koralink, pre-start) + `Match` type/adapter carry bookingMode/pitchId/bookingSlotId + i18n `reschedule.*` en/ar (683/683) | vitest 5 new cases (loading/free-list/empty/error+retry/confirm gating); vitest 246/246, tsc 0, lint clean, build 3/3 |
| P1-13 review fixes | `a5b9251` | Post-cycle reviewer CRITICAL: cancelMatch read the match UNLOCKED → a reschedule committing between read and slot release left cancel holding a STALE slot id → **refund silently skipped**. Fixed: cancelMatch takes the row FOR UPDATE. Plus reviewer IMPORTANTS: refund leg guarded (no zero-amount CREDIT rows), ledger keys UUID-per-attempt (same-ms collision-free) | jest 176/176, tsc 0, build 3/3, restarted; **live money drill**: create(350 debit)→reschedule(delta 0)→cancel(refund 350) → wallet exactly restored (500.00→500.00), both slots freed, ledger = refund-charge-refund |
| Quiet-hours specs | `aa7a2d2` | 2 specs used `quiet_end: 23` believing it "covers all hours" — windows are end-exclusive so they FAILED every day 23:00–23:59 AST. Now `0→0` (muted-24/7, P2-25 semantics), wall-clock independent | jest 176/176 green at 23:2x AST (previously failing window) |
| P2-31(1) admin | `c9e9b0b` | Venue hours edit validates open∈[0,23]/close∈[1,24]/close>open BEFORE the request; empty/NaN → localized `venues.hoursInvalid` (en/ar) instead of null payload + raw API error | admin tsc 0, i18n 240/240, build 3/3 |

## Blockers & decisions (new this run)
- **DECISION (autonomous):** Reviewer B's "P0 no notifications surface" downgraded to P2-34 — the feature exists (bell, sheet, prefs); what's missing is reachability outside the feed tab. Boarded with both fix options (layout bell / dedicated route).
- **DECISION (autonomous):** reschedule ledger idempotency keys are per-ATTEMPT (UUID), not per-(match,slot) — deterministic keys would 500 on a legal A→B→A move (unique-constraint hit, caught in live E2E); concurrency is serialized by the slot-state check + FOR UPDATE row lock instead, `onConflictDoNothing()` as belt-and-braces.
- **E2E hygiene:** throwaway match + provisioned slot used for the cancel drill; all artifacts deleted, seed match `7c4c6864` and host wallet (45.00) restored byte-identical.
- P0-2 / P1-8 / P1-14 / P1-29 unchanged (real payment provider pending — Abdullah).

## Next-run recommendation
1. Verify run #20's three in-review items (STATE.in_review_items) — gates re-run + live reschedule probe on a fresh slot (recipe `/tmp/run20-e2e.sh`; provision a slot first).
2. Board queue: **P1-28 venue discovery** (search/city filter at venue level — good vertical slice) or **P2-34** (bell in (main) layout or `/notifications` route — cheap, high player value). P2-35 is a 2-line rider.
3. Keep probing GLM first; reviewer pipeline healthy (6 clean runs on glm-5.3).
