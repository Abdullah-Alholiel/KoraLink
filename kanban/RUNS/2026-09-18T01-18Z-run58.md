# Factory Run #58 — 2026-09-18T01:18Z
**Mode:** full (review + adoption sweep + build)
**Budget:** on track (~3h of 3.5h)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe: **200** via ZAI_API_KEY — GLM_API_KEY .env line is STALE/401; zai weekly 1% healthy per provider directive) · delegation config verified `glm-5.3-flash`/`zai`/coding base, reasoning_effort low

## Preflight
- Lock acquired 01:18:48Z (pid 111765 = bridge pid). No prior lock.
- `git pull --ff-only origin staging` — already up to date. gh auth OK (Abdullah-Alholiel, write-scoped PAT).
- NODE_ENV clean in run shell (never sourced app .env).
- Dirty tree (worked alongside, NEVER staged): **Abdullah's admin WIP** (ConfirmDialog/DataTable/Drawer — ADMIN HOLD continues), **sibling auth-login-revamp WIP** (login/verify/complete-profile/profile pages, messages ar/en, 2 test files, worker/index.js, untracked `components/common/`), dirty kanban/BOARD.md+STATE.json (run #57's killed Phase-5 — ADOPTED, see below), untracked `.local/` + `scripts/verify-pr16-overflow-flow.mjs` (foreign, untouched).

## Adoption of run #57 (killed mid-Phase-5, 8th kill event)
- Code commits LANDED: `381186f` (P1-48 WS moderation gate), `2c71671` (P1-50 force-disconnect).
- RUNS report missing → reconstructed at `kanban/RUNS/2026-09-17T15-18Z-run57.md`; BOARD/STATE records sat uncommitted → verified then committed by this run.

## Sentry / error-log triage (Phase 1.6)
- Sources: Sentry API (EU base, SENTRY_READ_TOKEN, both projects) + journalctl all 3 services.
- Services: api/pwa/admin all **active**, `/api/v1/health` 200, **zero** `-p err` journal entries in 5h.
- Sentry 24h: **zero new signatures**. Newest API = KORALINK-API-B (CORS `evil.example` probe noise, lastSeen Sep 11). Newest WEB = KORALINK-WEB-D hydration (Sep 14, known). Nothing to fix, nothing to board.

## Findings (reviewers + parent, merged, evidence-cited)
**Reviewer A (code quality, 272s):**
- CRITICAL: none. Standing bug-class sweeps clean (0 `::uuid`, 0 `eq(col,null)`, 0 console.* in API, FOR UPDATE present).
- IMPORTANT: (1) nothing structural enforces `requireActiveUser` on NEW WS handlers → **P2-74**;
  (2) sibling's new `LanguageToggle.tsx` `ariaLabel` defaults to untranslated English → **P2-75**.
- MINOR: unban-while-still-suspended skips force-disconnect (users.service.ts:238-240; cosmetic — per-message gate still blocks) → **P2-77**. Owner's uncommitted DataTable keyboard activation handles Enter not Space + missing row role (read-only note for Abdullah).
**Reviewer B (product gaps + verification, 289s):**
- P1-48 **VERIFIED TRUE** (predicate `app.gateway.ts:217-224`; `requireActiveUser` in all 6 handlers :270/:298/:449/:473/:499/:527 BEFORE rate-limit/membership; leave-conversation exemption :556 documented; 12 jest cases) and P1-50 **VERIFIED TRUE** (`disconnectUser` :584; gate `users.service.ts:237-245` ban/suspend-only, unban/lift never disconnect; try/catch after committed write; 4 cases) + E2E chain spot-check TRUE (admin ban → REST sign-in blocked with stable codes auth.service.ts:211/:219/:314/:322 → WS handshake reject) → **both DONE ✅ STAGING**.
- NEW P1 candidate: `POST /wallet/pay` debits a **client-supplied amount** as MATCH_FEE. Parent verification: real (wallet.controller.ts:92-107 + wallet.service recordTransaction has no re-pricing; joinMatch's in-tx `chargeMatchFeeTx` at server price is the authoritative path; refund sweeps key off `fee_paid_sar` so client-minted rows were invisible to the money cycle) BUT the surface is dead — zero PWA consumers (`usePayWallet` unused), zero tests.
- Other Reviewer-B P1/P2 leads deduped: wallet payout → existing P1-27; partner settlement initiation → existing P2-3/P1-14 (all P0-2-gated). New P2s: **P2-76** (unsubscribe DELETE-body fragility + critical events lack non-push channel), plus carried P2-72/P2-73 (run #57 died before mirroring them — cards created this run).

## Board changes
- **DONE ✅ STAGING:** P1-48, P1-50 (verified, review notes appended to rows).
- **REMOVED-by-build:** reviewer-B wallet/pay P1 (folded into the P2-5 row as resolved-by-removal; never had its own ID).
- **ADDED:** P2-74 (requireActiveUser structural enforcement), P2-75 (LanguageToggle ariaLabel required), P2-76 (unsubscribe DELETE-body + critical-event channel), P2-77 (unban-while-suspended disconnect, cosmetic).
- Mirror cards: commented verified evidence on `t_6060894c` (P1-48) + `t_9c9c6300` (P1-50); created+gated `t_cdf49f5b` (P2-74), `t_1b06f447` (P2-75), `t_720b2888` (P2-76), `t_e9db0779` (P2-77), `t_aebdf289` (P2-72), `t_e84401fa` (P2-73).
- STATE.json: last_run 58, commits [16dd5f9], in_review = wallet-pay removal; kill-pattern count 6→8; zai-probe note refreshed.

## Built this run
**`16dd5f9` — fix(api): remove dead `/wallet/pay` client-priced MATCH_FEE endpoint** (+ its unused `usePayWallet` PWA hook)
- Why: any authenticated user could mint MATCH_FEE ledger debits at a client-chosen price; the endpoint was unreachable from any UI (dead legacy of the pre-0040 payment design) and its rows were invisible to the refund/forfeit money cycle. P0-2 must not inherit a client-priced fee surface.
- Gates: jest wallet **17/17** (incl. NEW regression pin: `prototype.pay` undefined AND `PATH_METADATA` route list lacks `'pay'`) · API tsc 0 · PWA tsc 0 · **turbo 3/3 exit 0** · **vitest 82f/578t passed** (`Test Files 82 passed`) · dist grep: `wallet/pay` gone from compiled controller, topup intact.
- Gate docs: `docs/plans/run58-review-sweep/` (00-retro.md incl. Gate-3 contract checklist, 00-status.md).
- Strix: not due (Sep 18; next window Oct 1–3). OS research: skipped (held rotation lane; budget went to adoption + removal).

## Blockers & decisions
- **ADMIN HOLD** continues (ConfirmDialog/DataTable/Drawer uncommitted) — rotation's Admin lane skipped; P2-69/P2-68 untouched.
- **Sibling auth-login-revamp in flight** (PWA auth pages + i18n + worker/index.js + tests, uncommitted) — P2-72/P2-73/P2-75 target those files; boarded with coordination notes instead of building.
- Decision: resolved reviewer-B's `/wallet/pay` P1 by REMOVAL (dead surface) rather than server-side re-pricing — re-pricing a route nobody calls adds contract burden; the join path already owns the pricing boundary. Recorded in gate docs.
- Kill-pattern now 8 events; this run completed cleanly (no new kill during the run).

## Next-run recommendation
1. Verify this run's in_review claim (recipe in STATE.json; jest pin + grep + live 404 after deploy).
2. ~~Deploy staging~~ **DONE this run**: `bash scripts/deploy-staging.sh` on `cc76eb4` (post-lock-release) — preflight clean, full turbo build, 0 pending migrations, services restarted API→PWA→Admin, **health matrix 6/6 [ok]**; live probe: `POST /api/v1/wallet/pay` → **404** (removal live), `/wallet/topup` → 401-guarded, 0 journal errors post-deploy. The WS moderation gate (381186f) + force-disconnect (2c71671) + auth-login-revamp commits are now LIVE on staging.
3. Sibling auth-login-revamp will land soon → then P2-75 (required ariaLabel) + P2-72/73 (worker locale/URL) become buildable.
4. P2-74 (WS gate structural enforcement) is the best self-contained API build next run.
5. Owner decisions queue unchanged: P1-49 appeal channel · P1-41 Brevo keys · P2-51 Render migrate hook · P2-47 cancel-purge · P1-38/P2-50 uploads · P0-2 real provider · P1-51 rating shape.
