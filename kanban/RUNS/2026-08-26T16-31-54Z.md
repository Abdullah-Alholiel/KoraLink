# Factory Run #1 — 2026-08-26T16:31:54Z (bootstrap)
**Mode:** full (bootstrap review + 4-Gate dev cycle)
**Budget:** ~55 min active (well under 3.5h cap) · review delegation: 2 subagents ($0.085 total)

## Preflight

- Lock acquired (run_id 20260826T16Z, pid 323230). No prior lock.
- Delegation config reset per runbook (deepseek-v4-pro/deepseek/'') — **note: deepseek
  actually WORKED this run** (both subagents completed: 442s + 177s + 194s post-cycle). The
  "402 no balance" status from 2026-08-26 morning appears stale/recovered. Runbook's
  provider-reality section should be re-validated next run.
- gh auth OK (fullstack-dev profile), `git pull --ff-only` clean, node 22.22.3 / npm 10.9.8.
- Dirty tree: only sibling `graphify-out/*` artifacts (never staged, left in place — no stash).

## Health snapshot (baseline)

- `koralink-api`, `koralink-pwa`, `koralink-admin` services: all **active**.
- API `/api/v1/health`: **OK** (direct :3001; :8000 is a Coolify proxy, :8443 tailscale).
- `journalctl -p err` last 5h: **0 entries**.
- API serves on :3001 (PORT env), PWA :3000, admin :3002; tailscale serve fronts 9450/8443.

## Findings (delegated reviewer × 2 + parent verification, merged)

Every P0/P1 finding below was **independently verified by the parent** (file reads + live
endpoint probes) before boarding.

### P0
1. **REST chat reads leaked every match's conversation** (now FIXED this run): WS enforced
   membership on join-lobby/send-message, but `GET /matches/:id/messages` took no viewer and
   `findOne` embedded the full `messages` relation for any authenticated user
   (matches.controller.ts:108-112,149-153; matches.service.ts:322,781).
2. **Wallet top-up is a self-credit endpoint** — no payment provider behind
   `POST /wallet/topup` (grep moyasar|stripe|tap|hyperpay → 0 hits); PWA calls it directly
   up to SAR 10,000. **BLOCKED on Abdullah's provider decision** (Moyasar/HyperPay/Tap).

### P1
3. No scheduler/cron anywhere (grep @nestjs/schedule → 0): match reminders never fire
   (web-push send path exists), POTM 24h window never finalized by a job, auto-complete only
   at module init.
4. Discovery: no `skill_level` filter; `radius_km` dead param (soft sort only).
5. Chat: LIMIT 50 no pagination/cursor; text-only (no attachments); `client_message_id`
   dedup has NO unique index → concurrent retry duplicates.
6. Missing hot-FK indexes: matches.host_id/pitch_id, activities.actor_id,
   disputes/reports.reporter_id, venues.owner_id, transactions.reference_id,
   match_messages(match_id, created_at).
7. SW push handler hardcodes `locale='en'` (worker/index.js:19) — Arabic users get en links.

### P2
Admin refund/payout tooling (manual flips, synthetic PO- refs), dispute_messages table has
no write endpoint, partner payouts not partner-initiated, `::float` money casts in
settlement sums, bare mutation returns (castVote {message}, createDispute bare row, wallet
topup/pay {ledgerEntry,wallet_balance}), unguarded WS leave-conversation, no offline
mutation queue.

### Verified-OK (no action)
i18n parity 631/631 (was 630/630 pre-cycle), wallet ledger idempotency in-tx + unique key,
matches join/leave/start/complete/cancel mutation contract compliant, dev-login NODE_ENV
gated, no console.log/mock data in prod paths, slot booking FOR UPDATE.

## Previous-run verification

N/A — bootstrap run (no prior claims). Baseline commits recorded for run #2:
`85c3ca0` (kanban scaffold), `19d610c` (sibling HTTPS cutover).

## Board changes

- BOARD.md populated from scratch: 2 × P0, 5 × P1, 7 × P2 + Verified-OK section.
- P0-1 WIP → IN-REVIEW (this run's build, below).
- P0-2 BLOCKED (external decision needed).

## Built this run (P0-1: private-match chat access control)

Cycle docs: `docs/plans/private-match-access-control/` (Gate 0 retro + Gates 1-3 compact
program design with explicit contract checklist + status table).

| Commit | Slice | Verification |
|---|---|---|
| `19041aa` | API: `findOne(id, viewerId?)` strips embedded chat for non-members; `getMessages(id, viewerId?)` 403s non-members; controller passes `@CurrentUser().sub` | jest 8/8 → hardened to 11/11; tsc --noEmit 0 errors; turbo build 3/3; **live on :3001**: member 200+2msgs / outsider 403 / outsider detail 200 with messages:[] roster intact / member detail unchanged |
| `005dec0` | PWA: discussion card members-only + join prompt; i18n `matchDetail.joinToChat` ar+en | tsc clean; build 3/3; vitest **217/217**; PWA serving /ar /en 200 |
| `4b87a9f` | Post-cycle review fix: spec stub now evaluates the real Drizzle where-clause (wrong-column comparison would fail), host-viewer + cross-match cases, fixture clone (service mutates messages array) | jest 11/11 |

Design decision (recorded): private matches are **invite-link/unlisted by design**
(create-match.dto.ts:68 — "accessible via the invite link"; `joinMatch` takes no token), so
metadata stays readable by ID; **chat** is what got the members-only enforcement, matching
the WS layer's existing rule. Full ACL/invite tokens = separate product decision for Abdullah.

API service restarted AFTER dist write (stale-code trap respected); dist grep-verified
(`isMatchMember` ×3) before restart.

## Blockers & decisions

- **P0-2 blocked** on payment-provider choice (needs Abdullah + credentials).
- Delegation provider: deepseek funded again this run — runbook's "provider reality" note
  updated knowledge; next run re-probe before assuming 402.
- Reviewer subagent #1's full report body was truncated by the log pipe (live-log formatter
  truncates `final` summaries; only compact <10k-char outputs survive intact). Worked around
  by dispatching a compact-format second reviewer. **Runbook patch candidate**: instruct
  future reviewers to keep summaries under 10k chars.
- No auto-stash used (runbook v2 says never stash sibling work) — nothing to pop.

## Next-run recommendation

1. Verify run #1's claims (build+vitest+live 403 probe) → promote P0-1 to DONE.
2. Best next build: **P1-3 chat idempotency unique index** (small: one migration + insert
   conflict handling) or **P1-6 hot-FK indexes** (pure migration, zero code risk) — both fit
   a single run easily.
3. **P1-1 scheduler** (@nestjs/schedule: reminders + POTM finalize + periodic auto-complete)
   is the highest user-value item — worth a dedicated run.
4. Ask Abdullah (via report): payment provider for P0-2; whether private-match metadata
   should ALSO be token-gated (currently unlisted-by-ID).
