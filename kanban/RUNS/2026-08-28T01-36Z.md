# Factory Run #7 — 2026-08-28T01:36Z

**Mode:** full (review → kanban → build DM idempotency → board → report)
**Budget:** ~40 min (well under 3.5h cap)

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260828T012136Z).
- Delegation self-heal: reset to `delegation.model=deepseek-v4-pro` / `provider=deepseek` / `base_url=''`.
- `gh auth status` OK (Abdullah-Alholiel). `git pull --ff-only` → already up to date at `38fef86`.
- Tree clean on `main`. All 3 services `active`; API `/api/v1/health` **200**; 0 journal errors in 5h.
- Baseline: build 3/3 · API jest 78/78 · PWA vitest 218/218.

## Findings (reviewer `deleg_bdc821d9`, deepseek-v4-pro, 266s)

**CRITICAL:** none new — the reviewer's "critical" flags (castVote/createDispute bare returns,
verify-otp JWT-in-body, recordTransaction 409, DM TOCTOU) are all already-board items
(P2-5 / P2-10 / P2-11 / P1-11).

**IMPORTANT (already on board, status re-confirmed):** dead `rating` columns still seeded ·
money `::float` in read paths (P2-4) · CSP `unsafe-inline`/`unsafe-eval` · admin English-only
(P1-12) · WS JWT `fallback-dev-secret` · `radius_km` ignored + `skill_level` absent (P1-2) ·
no reschedule (P1-13).

**NEW:** P2-17 — AuthBootstrap stale-persisted-user (see board). Note: the reviewer's
"key mismatch" sub-claim (`removeItem('koralink_token')` vs persist key) is a **FALSE
POSITIVE** — `koralink_token` is the correct dev-login Bearer-token key (`fetcher.ts:19`
`TOKEN_KEY='koralink_token'`), distinct from the Zustand persist key `koralink-app-store`.

**Verified clean:** no `::uuid` casts · no `eq(col,null)` · mutation contract
(join/leave/start/complete/cancel) all `findOne` outside tx · `spots_filled` counts host ·
bottom-sheet z-[60]/z-[70] · no `(main)` page renders its own MobileFrame/BottomNav ·
i18n key parity intact.

## Previous-run verification (claims ≠ facts)

**P1-10 (WS ban/suspend, run #6, 4df0d4d) — CONFIRMED ✅:**
- `app.gateway.ts:112-159` `handleConnection` re-reads the user row (id/role/banned_at/
  suspended_until) and disconnects missing/banned/future-suspended accounts; joins `ops` on
  `user.role` (DB), never the stale token claim.
- `app.gateway.spec.ts`: 7 cases (clean Player, Admin→ops, demoted-admin→no ops, banned,
  future-suspended, expired-suspension, missing-user).
- Baseline green (build 3/3, jest 78→83/83, vitest 218/218).

→ **P1-10 promoted to DONE ✅.**

## Board changes

- **P1-10** → DONE ✅ (verified run #7).
- **P1-11** → IN-REVIEW (built run #7, `dfc671f`).
- **NEW P2-17** AuthBootstrap stale-persisted-user → TODO.
- Backlog: systemd units not committed · mark-no-show `@IsUUID` · castVote resets created_at ·
  seed.ts still seeds rating.

## Built this run — verification evidence (real terminal output)

- `npx tsc --noEmit -p apps/api/tsconfig.json` → **0 errors**.
- `npx jest` (API) → **83/83 tests, 13 suites** (+5 new `conversations.service.spec.ts`).
- `npx vitest run` (PWA) → **218/218 tests, 30 files** (unaffected, green).
- `npm run build` (root) → **3/3 tasks successful** (API rebuilt).
- `npm run db:generate` → `0019_keen_mulholland_black.sql` (partial unique index).
- `npm run db:migrate` → **applied successfully**.
- `systemctl --user restart koralink-api.service` → `active`, health **200**.
- Live index verified: `personal_messages_client_msg_uidx` present in `pg_indexes`.

## Blockers & decisions

- **P0-2** unchanged — real payment provider is an external vendor decision (Abdullah).
  P1-8 and P1-14 remain blocked on it.
- Decision: built DM idempotency (data integrity) over discovery filters (feature) and
  money-numeric (P2-4) — mirrors the match-chat P1-3 pattern; one slice per run.

## Next-run recommendation

1. **Verify P1-11** (DM idempotency) → promote to DONE.
2. **P1-2** discovery filters (`skill_level` filter + `radius_km` hard cutoff).
3. **P2-17** AuthBootstrap stale-persisted-user (small: run `/users/me` on every cold load +
   401→logout in fetcher).
4. **P2-4** settlement money-as-numeric (drop `::float` for payout sums).
