# Factory Run #52 — 2026-09-14T10:20Z (recovered + completed)

**Mode:** full (recovery: a 01:17Z factory session died mid-run — lock pid 909570
DEAD at ~39 min, ZERO commits — this run adopted its in-flight work and finished it)
**Budget:** ~55 min active (10:20–11:15Z) vs 3.5h slot — the heavy analysis was
already in-tree from the dead session; remaining work was verify → gate → commit →
admin slice → banner slice → board/mirror/report.
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe
**200**, 5h 27%, weekly 1% — healthy) · provider `zai` · delegation config reset in
Phase 0 (model/provider/base_url/reasoning_effort=low). Both reviewers ran clean on
zai: A 71s, B 142s — **the runs #45/#24-30/#32 zai-child-401 streak is broken
again** (it has flipped before; policy stays try-zai-first, failover on first child
error).

## Preflight
- Stale `kanban/LOCK.json` (run 52, started 01:17:34Z, pid 909570 **DEAD**) — cleared,
  re-acquired as this run.
- Tree dirty with the dead session's coherent half-done work: PWA hydration/locale
  slices + `docs/plans/run52-hydration-finish-admin-i18n/` (full Gate 0–3 docs) +
  untracked `scripts/verify-pr16-overflow-flow.mjs` (mtime Sep 9 — OLDER than the
  dead run; foreign artifact, left alone, never staged). Per the no-stash rule the
  foreign graphify-out churn was left untouched and unstaged.
- `git pull --ff-only origin staging` clean; branch staging == origin/staging at
  87b5268. gh auth OK (Abdullah-Alholiel). NODE_ENV unset. Gateway active,
  heartbeat fresh.
- Delegation config self-heal: probe 200 → set glm-5.3-flash / zai / coding base /
  reasoning_effort low.

## Sentry / error-log triage (Phase 1.6)
Sources: Sentry API (de.sentry.io, read token) — koralink-api 25 issues 24h,
koralink-web 13; journalctl all three services (0–1 err lines each, 5h — none
actionable); services all active, API /health 200.

- **KORALINK-WEB-D "Hydration Error" — CONFIRMED RECURRING (n=5, lastSeen 09:02Z
  TODAY).** Run #51's "stale cutover tab, closed" verdict was premature. Evidence
  chain from the latest event: URL `https://aa.tail2948f9.ts.net:10000/en/play` —
  **:10000 is CLOSED since the Sep-13 cleanup (nothing listens on it — verified via
  ss)** — yet a device loaded it TODAY, so an old service worker is self-servicing a
  zombie tab; release tag = 6bb22d3c (prod cutover SHA) whose DiscussionCard still
  carries the render-path `new Date()` (verified via `git show`). Root fix = this
  run's `ecfab67`; expect WEB-D to stop accumulating only after the next promote to
  prod. NOT re-opened as a new board item — covered by P2-59 (survivor closed) +
  P2-60 hardening (guarded SW registration, already built run #50, in the promote
  pipeline).
- KORALINK-WEB-6 (n=53, accumulating): intentional per-session standalone diagnostic
  from ViewportHeightSync (one capture per device session by design). MINOR/noise.
- KORALINK-WEB-2/8: no new events since Sep 13/12 — P2-60 fix (register:false +
  reg?.waiting guard, 4726498) awaiting promote. Watch continues.
- koralink-api: zero new issues since Sep 11. Top unresolved signatures are known:
  KORALINK-API-B CORS `evil.example` probe noise (n=110, stale), API-1B email 503
  (P1-41 boarder, provider unprovisioned). Nothing new to triage.
- **Refuted reviewer-B hydration candidates (evidence over vibes):**
  PostMatchSection `useNow` + RestoreAccountBanner `Date.now()` — the clock is only
  read where client-fetched data or a localStorage-gated banner is displayed; server
  and first client render produce IDENTICAL output (loader / null), so no mismatch
  is reachable. Not boarded.

## Findings (GLM reviewers + parent, merged, evidence-cited)
- Reviewer A (uncommitted-slices review, PASS): useNow() null posture verified
  correct against the hook implementation; no render-path clock reads remain in the
  two components (remaining `new Date()` are data parses, hydration-safe);
  riyadhDateFromYMD 09:00Z anchor + formatShortTime local-parse/local-display
  self-consistency confirmed; i18n justNow/minutesAgo/hoursAgo/yesterday present in
  BOTH locales, parity 947/947; ran the 3 affected suites (41/41). API standing-bug
  sweep clean: 0 `::uuid` casts, 0 `eq(col,null)`, 0 console.log in services.
  Minors noted (accepted, self-documented): DISC-3 en-weekday doesn't discriminate
  en-US vs en-GB (DISC-4/5 carry the discrimination); CLDR-coupled literals will
  need attention on ICU upgrades.
- Reviewer B (verification + design lens): **all 3 run-#51 in-review claims
  CONFIRMED** — P2-57 (SW runtimeCaching byte-identical in built public/sw.js;
  NetworkOnly money/auth registered FIRST; programmatic URL-reality check;
  12/12 suite), P2-63 (classified copy in ChatSheet:11,188 + reports + profile; 9
  errors.* keys in both locales; banners at profile:231 + personal-info:92),
  P2-map-pin (deep-link with encoded query, target/rel, aria-label, RTL chevron;
  clubs.openInMaps EN+AR). i18n parity 947/947 (programmatic, symmetric diff
  empty).
- Parent refutations: offline-banner claim re-scoped 9→1 (club detail only);
  PostMatchSection/RestoreAccountBanner hydration candidates refuted (above).
- Ops: multica bridge posted its weekly digest (t_d7f0d6ef) during the run — the
  Sep-13 reclaim loop on t_3fd910ae did not recur into a collision this run.

## Previous-run verification
Run #51's 3 in-review items (P2-57 / P2-63 / P2-map-pin) → **promoted DONE** on
Reviewer-B confirmation (see Findings) + same-HEAD full gates (turbo 3/3, vitest
79f/546t, tsc 0). Board rows updated; mirror cards commented with evidence.

## Board changes (added/promoted/demoted/completed)
- **DONE ✅ promoted:** P2-57 (e305eb9) · P2-63 (1e3778f; row already PARTIAL→closed)
  · P2-map-pin (e6e7a9f; no standalone row existed — the MapPin mirror card
  t_3fd910ae carries the verification comment).
- **DONE ✅ built this run:** P2-59 survivor (ecfab67, row extended) · **P2-64 NEW
  row, built + closed same run** (ecfab67) · P2-61 (86723ea) · OfflineBanner on club
  detail (1066a29 — folded as a scope note under the P2-63/banner lineage; no new
  row: one-line change, fully verified).
- **Mirror:** built-comments on t_04d66a1e (P2-61) + t_7dded2f0 (P2-59);
  verified-comments on t_a34d0303 (P2-57), t_bde44ea9 (P2-63), t_3fd910ae (MapPin);
  P2-64 created as t_e881f155 and gated under t_ce9a513a (status todo, parents
  verified). No mirror card completed/blocked; parent t_ce9a513a untouched.

## Built this run (3 slices — each: full gates + separate commit)
1. **`ecfab67` (feat→fix(pwa), P2-59 survivor + P2-64):** recovered from the dead
   session, re-verified (Reviewer A PASS + 41/41 targeted), then committed.
   DiscussionCard render-path `new Date()` → useNow() null-pre-mount posture;
   en-US weekday/month hardcode → resolved-locale Intl; lib/format gains
   resolveDateLocale / formatShortDate / riyadhDateFromYMD / formatShortTime;
   MatchDetailsForm ×4 bare-`ar-SA` sites routed through them (Hijri fix); 8 new
   DiscussionCard specs + 4 format describe blocks + updated MatchDetailsForm
   assertion.
2. **`86723ea` (fix(admin), P2-61):** venues/[id] approval surface fully localized
   (new `venueDetail` ns: 18 keys ×2 locales, parity 571/571; reused
   hq.approveAction/rejectAction + common.loading), decision failure → localized
   role=alert inline (error-message standard), back-arrow RTL-mirrored,
   partner/settings select label associated (htmlFor/id=partner-settings-venue),
   5 ad-hoc empty states → shared EmptyState (pitches, partner/venues,
   partner/pitches, partner/matches, partner/matches/[id]).
   **ADMIN STATE CHECK (Phase 3.5 step 0) ran clean pre-build** (no dirty admin
   files, service active, last admin commits old).
3. **`1066a29` (feat(pwa)):** OfflineBanner + useOnlineStatus on club detail — the
   last main PWA surface without a staleness signal, on a page whose games list is
   SW-cached (SWR 24h via the venues pattern from P2-57).

**Gates (every slice):** `npx turbo run build` **3/3 exit 0** (×3: on the recovered
tree, after the admin slice, after the banner slice — admin rebuilt uncached, no
OOM; PWA + admin services restarted by postbuild on fresh dist) · PWA vitest **79
files / 546 tests green** (+1 file, +14 tests vs run #51) · `npm run type-check`
(PWA) **0 errors** · admin `tsc --noEmit` **0 errors** · admin i18n parity **571/571
EN=AR** · NO DB surface touched → no migrations, nothing to journal (Phase 4.5
sweep clean — only graphify-out foreign churn + the foreign script remain, both
deliberately unstaged).

## Blockers & decisions
- **Killed-run recovery recipe (proven, added to STATE.json blockers as OPS note):**
  dead pid check → rm lock → adopt coherent in-flight work → verify via reviewers +
  full gates → commit → continue. The dead session lost ~39 min but zero work — its
  slices + plan docs were fully recoverable from the tree.
- Decision: adopted run number **52** (the dead session's own number) rather than
  incrementing to 53 — STATE.json still said last_run 51, so continuity is
  preserved and the next run increments cleanly to 53.
- Decision: P2-64 boarded as a standing-rule item (components must never call bare
  `ar-SA` Intl) even though the defect is fixed — the class has recurred 3×.
- Strix monthly gate: **not due** (Sep 14; window is the 1st–3rd; STATE notes carry
  `strix_scan=not-due-2026-09`). Skipped per runbook.
- Multica bridge activity observed (weekly digest) — no collisions; ops watch
  continues.

## Next-run recommendation
1. **Verify run #52's 3 in-review items** (claims ≠ facts): ecfab67 (spot-check:
   DiscussionCard renders "just now" pre-mount, no Hijri in host-form summary),
   86723ea (venues/[id] renders Arabic with admin_locale=ar), 1066a29 (banner on
   club detail).
2. **Watch Sentry WEB-D** — it should stop accumulating once 4726498+ecfab67
   reach prod via the next promote; if hydration errors reappear from `:9450`
   (staging) URLs, the P2-59 class has a 5th survivor — hunt it then.
3. **Ops:** the dead-run pattern (lock with dead pid + dirty coherent tree) has now
   happened twice (run #51-manual, run #52 original). Consider a watchdog check at
   cron fire: if LOCK pid is dead → alert + auto-adopt per this run's recipe.
4. Owner queue unchanged — P1-41 (Resend/Brevo) remains the single unblock for the
   PDPL email-receipt sub-cycle; P2-51/47/38+50 need Abdullah's calls.
5. Board pressure after this run: TODO lanes carry P1-27 (wallet withdraw),
   P2-18 (recurring matches), P2-7 (offline mutation queue) as the next
   buildable-without-owner candidates.
