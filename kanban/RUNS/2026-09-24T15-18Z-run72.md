# Factory Run #72 — 2026-09-24T15:18Z

**Mode:** full (build item shipped as an honestly-scoped PARTIAL)
**Budget:** ~2h10m of ~3h used (finished early by design — never start what can't finish)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe 200 in 2.6s via ZAI_API_KEY; GLM_API_KEY line still stale-401; weekly ~3% — healthy) · reasoning_effort low
**Worktree:** /home/ubuntu/worktrees/koralink-staging (staging @ 7907537, in sync with origin at start)

## Preflight

- LOCK acquired 15:18:50Z (`kanban/LOCK.json`, run_id 2026-09-24T15-18Z). No prior lock (run #71 released cleanly).
- gh auth: logged in as Abdullah-Alholiel; setup-git OK.
- `git fetch origin staging`: behind 0 / ahead 0 — clean sync.
- Tree (staging worktree) clean at start; projects dir carries the long-standing foreign dirty state (Abdullah's admin edits + .gitignore/Dockerfile/seed churn) — untouched, per dirty-tree policy. ADMIN HOLD continues (projects-dir merge conflict era).
- Delegation config already glm-5.3-flash/zai/coding-base (idempotent re-set applied).
- Services: api/pwa/admin all active; API /health 200 (localhost:3001); 0 error-level journal lines in 5h on all three services.

## Strix

Not due — UTC Sep 24; window is Oct 1-3. (Runbook guard: day-of-month 1-3 only.)

## Sentry / error-log triage (Phase 1.6)

Source: Sentry API (EU base, SENTRY_READ_TOKEN) — 25 API issues + 12 web issues in 24h.

- **KORALINK-API-1C — `PostgresError: account or project has exceeded the quota` — count 1→2, lastSeen 2026-09-24T15:00Z (18 minutes into this run).** Issue id 149118811. The two events land at exactly 10:00Z and 15:00Z = the 5-hourly `users.purgeExpired` cadence — every purge tick dies while Neon quota is exhausted. **P1-52 is NOT fixed; prod DB remains hard-down (owner/infra — Abdullah, billing).** Board row updated (recurrence evidence, issue id).
- KORALINK-API-1D — prod `users` INSERT failure, 1 event at 11:43Z (inside the outage window) → same root cause; no separate item.
- koralink-web: no new activity (top issue lastSeen Sep 14 — hydration/viewport/SW legacy set, all known).
- journalctl: 0 error-level lines across api/pwa/admin in 5h (staging unaffected).

## Phase 1 — context restore + previous-run verification

Run #71 (recovery session `run71-recovery-1017`) left 2 in_review_items; both VERIFIED this run and promoted **DONE** on the board:

1. **P2-98 (migrate-vps per-file tx atomicity): PASS** — `npx jest src/database/migrate-vps-atomicity.spec.ts` → 3/3; `grep -c 'await sql.begin(' scripts/migrate-vps.mjs` → 1; `grep -c 'await sql.unsafe(\`INSERT INTO'` → 0; `node --check` OK. Reviewer B independently reproduced (PASS) — including correcting the path (repo-root `scripts/`, not `apps/api/scripts/`).
2. **P2-88 (snapshot-chain guard, PARTIAL by design): PASS** — `npx jest src/database/drizzle-snapshot-chain.spec.ts` → 3/3; `drizzle/meta/0027_snapshot.json` confirmed absent exactly as pinned. Reviewer B independent PASS.

## Findings (reviewers + parent, merged; deleg_40e7d61d, both completed — A 462s, B 330s)

**Reviewer A (code quality):** 0 CRITICAL, 0 IMPORTANT. P2-98 atomicity verdict "sound and well-pinned".
Standing bug-class sweep ALL CLEAN: `::uuid` casts 0; `eq(col,null)` 0; `console.*` in api 0;
unguarded mutations 0 (only intentionally-public OTP routes lack guards); hydration-safe clocks;
z-index lineage conform; **i18n parity 985/985 leaf keys en/ar**. 6 MINORs recorded for future
hygiene batches: migrate-vps `$$` comment-stripper edge cases (:105-108), exit(5)-skips-sql.end
note (:147), migrate spec is static-grep-only, profile page has no dedicated empty state, clubs
page 4 multiline buttons need an eyeball for dead UI, stray `z-10` HostMatchForm.tsx:254.

**Reviewer B (product gaps + verification):** verification verdicts A PASS / B PASS (above).
NEW product gaps (boarded): **P1-53** no user-to-user block (P0-class social safety),
**P2-99** no chat typing/read-receipts/presence, **P2-100** reminders single-tick (no T-24h/T-2h
ladder), **P2-101** a11y hit targets + aria-labels thin, **P2-102** offline cache route gaps
(conversations/wallet/my-games uncached). Folded: top-up 3DS redirect-status UX → P0-2 row note;
quiet-hours per-category exemptions → already a P0-5 deferred sub-cycle (no duplicate).

## Board changes

- **PROMOTED DONE:** P2-98, P2-88-guard (both run #71 claims, verified + independently reproduced).
- **UPDATED:** P2-42 → PARTIAL (this run); P1-52 → recurrence evidence (count 2, 15:00Z, issue id);
  P0-2 → 3DS UX scoping note; P2-88 → re-verification stamp.
- **NEW:** P1-53 (user block), P2-99 (typing/receipts/presence), P2-100 (reminder ladder),
  P2-101 (a11y), P2-102 (offline routes), P2-103 (nonce CSP residual, blocked-with-evidence).
- Mirror cards created+gated on `koralink-factory-loop`: see Phase 3.7 below.

## Built this run — P2-42 CSP hardening (PARTIAL, honest scope)

**Shipped (`apps/player-pwa/next.config.mjs` + NEW `test/lib/csp-config.test.ts`):**
- PROD `script-src` drops **`'unsafe-eval'`** — verified safe: **zero `eval(` / `new Function(`
  occurrences in ALL production client chunks** (grep over `.next/static/chunks`).
- PROD `script-src` drops the **DEAD** `https://api.mapbox.com` + `https://cdn.moyasar.com`
  entries — zero mapbox/moyasar code/deps/`<Script>` usage in the PWA (grep).
- DEV branch keeps the legacy permissive script-src **byte-identical** (react-refresh/HMR needs
  eval; local dev untouched).
- `'unsafe-inline'` **STAYS in prod — with hard evidence**: the live App Router page ships **17
  inline `<script>` tags** (hydration/flight bootstrap `self.__next_f.push`), zero src, zero
  nonce attributes. Removing the inline allowance without a working per-request nonce pipeline
  blanks every page's hydration. (The original board row's "zero inline scripts" premise was
  wrong — the empirical gate caught what a config-only change would have shipped broken.)
- All non-script directives byte-identical; `connectSrc` keeps the `apiOrigin = new URL(apiUrl).origin`
  derivation (origin only — socket.io lives outside `/api/v1`; a path-prefixed source could break WS).
- Tripwire: `test/lib/csp-config.test.ts` — **8 tests** pinning both branches (prod: no
  unsafe-eval, no dead origins, keeps posthog+doop, keeps unsafe-inline with the why-comment;
  dev: legacy value; style-src unchanged; CSP still wired; connectSrc origin-safe).

**The nonce pipeline was attempted and BLOCKED (5 experimental rounds, full log in
`docs/plans/run72-csp-hardening/00-retro.md`):**
1. `node:crypto` in middleware → **edge runtime rejects it** (every page 500s; middleware
   compiles to edge even under `next start`).
2. Web-Crypto nonce + CSP on the forwarded REQUEST headers (documented Next pattern, request
   rebuilt as `new NextRequest`) → redirects carry the strict CSP + fresh nonce perfectly, but
   ALL page renders hang forever.
3. In-place `request.headers.set()` mutation (the other documented pattern) → identical wedge.
4. PRISTINE middleware restored, my build → renders STILL hang (wedge ≠ CSP change).
5. CONTROL: the owner's live projects-dir build, bare env → same wedge; with full env → render
   completes at ~30s with 500 ECONNRESET from the standalone's internal render proxy. The
   systemd-served identical build renders fine → **agent-spawned standalone servers wedge
   renders on this box (environmental, predates this run)**; the body of streamed responses is
   served but the final byte never flushes to the agent-spawn client.

Given the wedge, the final policy was verified on the EMITTED HEADER via the `/` 307 path
(headers serve without a render): **exactly one CSP header, `script-src 'self' 'unsafe-inline'
https://*.posthog.com https://aa.tail2948f9.ts.net:9460`** — no unsafe-eval, no dead origins,
byte-verified on a production-mode boot of the shipped build.

**Gates (all on the final tree):** `npx turbo run build --concurrency=1` → **3/3 successful**;
`npx vitest run` → **99 files, 718/718 passed**; `tsc --noEmit` → 0 errors; `eslint --max-warnings 0`
→ 0. No migration (no DB surface touched). `public/fallback-*.js` build churn reverted before commit.
**No live service restart needed:** the change is build-time config, and the live PWA serves the
projects dir (deploy-target fork) — staging's build ships to live only via Abdullah's directed merge.

## Blockers & decisions

- **P1-52 (prod Neon quota) — TOP OWNER ITEM, now with recurrence evidence** (2nd event today
  15:00Z; every 5-hourly purge tick dies; prod writes/reads failing). Nothing the factory can do.
- ADMIN HOLD unchanged (projects-dir merge conflict + Abdullah's staged admin edits) — no admin
  items touched.
- Deploy fork unchanged: services serve `/home/ubuntu/projects/koralink`; staging commits reach
  live only via Abdullah's directed merge.
- Environmental mystery recorded for a future 15-min investigation: agent-spawned standalone
  Next servers wedge page renders while the systemd-served identical build is fine (cgroup/CPU
  share hypothesis — try `systemd-run --user` scoped spawn next time local verification is needed).
- Decision (recorded): shipped the eval/dead-origin hardening + kept unsafe-inline rather than
  gambling the remaining budget on a 6th nonce experiment — "small, completed beats large,
  half-done"; the residual is precisely specified in P2-103.

## Next-run recommendation (run #73)

1. Verify this run's in_review_items recipe (STATE.json): csp-config 8/8 + prod-branch grep +
   header check via the `/` redirect on a production-mode boot.
2. Rotation 73%4 = 1 → **API lane**. All API TODO rows are owner-blocked; best candidates:
   the Reviewer-A minors batch (migrate-vps `$$` stripper hardening + CastVoteDto UUID
   validation + `@Max` on pitchCostSar) as a small hygiene slice, or **P2-100 (reminder
   ladder)** — API-sized, no owner dependency, real user value.
3. P1-52: if Abdullah has fixed Neon billing, verify no NEW KORALINK-API-1C events (check
   lastSeen > fix time) and promote to DONE.
4. Strix: DUE next run window (Oct 1-3) — budget the 45 min + quota guard.

## Phase 3.7 — Kanban mirror

Created + gated (parent `t_ce9a513a`, idempotency keys `board-<ID>`): P1-53 (t_70366711),
P2-99 (t_8be042a2), P2-100 (t_79cf28e7), P2-101 (t_3c421d59), P2-102 (t_322b4fc1),
P2-103 (t_cbc12178 — carries the run-#72 build note; P2-42 itself has NO mirror card, it
predates the mirror convention). Commented: t_929e66db (P2-98 verified), t_df7b26f0 (P2-88
verified), t_a450b5a9 (P1-52 recurrence). BOARD.md remains the authoritative record.
