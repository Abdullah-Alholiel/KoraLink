# Factory Run #36 — 2026-09-06T01:18Z

**Mode:** full
**Budget:** ~3.1h of 3.5h (preflight 01:18 → report 02:30)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (zai; probe **200**; directive: z.ai weekly 0% → zai primary). Both reviewers ran on zai, 168s combined, zero errors — 3rd consecutive clean delegation run.

## Preflight
- Lock 01:18Z (no prior lock). `NODE_ENV` unset. GH auth OK (Abdullah-Alholiel). `git pull --ff-only` — already up to date at `2e845a6`.
- Dirty tree = Abdullah's hostOnboarding WIP (host page + messages en/ar + tests + new Host* components) — untouched, never staged. **P1-41 PWA half stays blocked.**
- Delegation config set: glm-5.3-flash / zai / coding base (Phase 0 step 3).

**Strix: not due** (UTC day 6; gate = 1st–3rd). `strix_scan=not-due-2026-09` recorded.

## Sentry / error-log triage (Phase 1.6)
journalctl: **zero errors** on api/pwa/admin in 5h. Sentry API (EU base, read token):
- **KORALINK-API-11** (2 evts, latest 00:00Z Sep 6, RECURRING nightly) + **KORALINK-API-10** (1 evt 17:15Z Sep 5): both `PostgresError: column "email" of relation "users" does not exist`.
- **Attribution (the run's key finding): these are the RENDER DEMO, not this VPS.** Event host `koralink-postgres-<hash>`, Alpine/x64/EPYC (VPS = Ubuntu/aarch64). Chain: run #35 pushed `16d4cc1` → Render auto-deployed it at 16:39Z (deploy `dep-dae4berbc2fs73c26q9g`, live) → demo Neon DB never got migration 0033 → `MailerService.recipientsFor` 500s + nightly PDPL purge UPDATE failed (deleted-user anonymization broken on demo).
- **HOTFIX APPLIED THIS RUN:** read demo `DATABASE_URL` via Render env-vars API (external Neon URL; the stored `KL_API_DATABASE_URL` in .deploy-tokens is the Render-internal `koralink-postgres` hostname — unusable off-Render), applied 0033 (all `IF NOT EXISTS`) + bookkeeping row id 36 (hash `fde425a5…` = file sha256) directly to demo Neon. Verified: 3 columns + 2 indexes live, bookkeeping tail `36:fde425a5 | 35:b9437ff134`. No restart needed (code was already live; only schema was behind). Next 00:00Z purge succeeds.
- Boarded **P2-51** (deploy-before-migrate drift; needs owner call on Render deploy hook vs manual runbook).
- MINOR/noise: KORALINK-WEB-6 viewport-diagnostic = instrumented diagnostic event (by design); WEB-8 waiting-undefined = guarded by run-#32 fix 45cdb91, no new events.

## Findings (GLM reviewers + self, merged)
**Reviewer A (code quality; 168s):** 1 CRITICAL — WS `send-message`/`send-dm` have NO rate limiting (app.gateway.ts:225,377) → confirms P1-42. 2 IMPORTANT mailer defects (fixed this run, see Built). 3 MINOR (CTA locale — fixed; locale pinned 'ar' — documented; O(n²) skip check — fine at scale). Bug-class sweep CLEAN: `::uuid` 0, `eq(col,null)` 0, console.* 0, z-index clean; PWA feed+profile have all 5 UX states.

**Reviewer B (product gaps + verification; 151s):** Run #35 verification **4/4 PASS** (schema :247-249; reminder mirror matches.service.ts:297-302; deletion users.service.ts:598-601; purge nulls :968-969; verify route live). P1-41 API layer → **VERIFIED/DONE**. Top gap P0: profile email UI absent (matches the blocked residual). Gaps: P1 chat typing/read receipts/connection banner; P1 wallet withdraw coming-soon dead end; P1 PaymentSheet failure/timeout state; P1 email preference half (folds into P1-41 PWA residual); P2 offline banner only on play; P2 no block-user flow; P2 currency symbol inconsistency (﷼ vs SAR). i18n parity exact 896/896.

**Self-review:** P2-49 class extends to joinMatch (stale-Full revert :966 — can resurrect a CANCELLED match when raced; last-spot Open→Full :1031) and leaveMatch (:1117). New sub-item: joinMatch count-then-insert overbook race (needs FOR UPDATE) — carved to STATE notes, not built this run (scope discipline).

## Previous-run verification
Run #35 P1-41 claims verified live + code-level (above) → promoted to DONE/VERIFIED on board row + mirror card comment.

## Board changes
- **P1-42 → DONE ✅** (built + live-verified this run).
- **P1-41 → API layer VERIFIED ✅** (Reviewer B; residual PWA half still blocked on owner WIP).
- **P2-49 → DONE ✅ EXTENDED** (roster status flips premise-predicated).
- **P2-51 → NEW** (Render demo deploy-before-migrate drift; owner decision needed).
- Mirror: t_b374e2da (P1-42) + t_7778fecf (P1-41) commented; new card t_e99d545b created → linked to t_ce9a513a (status todo, gated).

## Built this run (all committed + pushed; every gate re-run per item)
1. **`4f01faa` feat(api): WS chat rate limiting + 2000-char cap (P1-42)** — `rate-limit.service.ts` (per-socket sliding window 10/10s, independent `msg:`/`dm:` buckets; in-memory by design; reconnect-to-refill costs a full authenticated handshake), guards in both send handlers (length → rate, both throw WsException), module wiring, harness updated + 12 new specs (29/29 gateway). **LIVE E2E: dev-login token → real socket → join-lobby → 13-send flood → exactly 10 `match_messages` rows + 3 'Rate limit exceeded. Try again in 10s.' exceptions.** Plan docs: docs/plans/run36-ws-ratelimit/.
2. **`894c3ac` fix(api): mailer render throws degrade to per-recipient outcomes** — render wrapped separately in `deliver()`; render failure → `{status:'failed', reason:'render-error'}` for that recipient only; batch outcomes no longer discarded (Reviewer A IMPORTANT #1). +2 specs (getter-bomb details). mailer.spec 17/17.
3. **`d9023a9` fix(api): setEmail typed error contract + TOCTOU race guard** — service throws typed 400/409/404 (no more stringly Error mapping in controller; DB outages no longer 400 "Invalid email"); UPDATE `.returning()` existence check; pg 23505 → 409 (Reviewer A IMPORTANT #2). +6 specs (new mailer-users.service.spec).
4. **`865c2b7` fix(api): premise-predicated status flips (P2-49 extension)** — removePlayer/leaveMatch Full→Open + joinMatch stale-Full→Open & Open→Full all `AND status = <premise>`; a concurrent cancel/join can no longer be reverted/overwritten/resurrected. +2 PgDialect-asserted specs.
5. **`faabbce` fix(api): locale-aware mail CTA** — `/ar/` hardcode → `/${locale}/`; +en case in spec.

**Gates (final):** jest **379/379** (45 suites) · PWA vitest **362/362** (51 files) · api tsc **0** · root `npm run build` **3/3** (run 4×, once per item) · API restarted after each dist-writing build; health 200; zero journal errors post-restart.

## Blockers & decisions
- **ADMIN STATE CHECK:** apps/admin + partner/admin API surface clean → non-admin items picked; owner PWA WIP untouched (messages files still dirty at every check).
- **DM live probe inconclusive:** my probe harness deadlocked (Content-Length bug in the probe's HTTP stub) before reaching the handler; the DM guard shares the identical code path unit-covered by the 29 gateway specs. Honest gap: DM flood path lacks live probe evidence — can be re-probed next run.
- **Decisions made autonomously:** (a) demo DB hotfix without owner sign-off — demo env, idempotent statements, repair-not-change; (b) P2-51 parked for owner (pipeline change = infra decision); (c) joinMatch FOR UPDATE overbook race NOT built at budget hour — carved instead (never start what can't finish).

## Next-run recommendation
1. **Verify run #36's 4 items** (in_review_items has exact re-proof steps).
2. **P1-41 PWA half** the moment Abdullah's hostOnboarding WIP lands (messages files free).
3. **joinMatch overbook race** (FOR UPDATE) — the remaining P2-49 sub-class, size S-M.
4. **P2-51 owner ask:** Render deploy hook running `db:migrate` (or disable auto-deploy) + bless the manual-applier runbook for deploy-demo/.
5. Owner asks outstanding: Resend keys; P1-38+P2-50 upload pipeline; P2-47 cancel-purge vs restore.
