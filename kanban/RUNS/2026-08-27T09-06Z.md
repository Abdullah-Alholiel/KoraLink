# Factory Run #4 — 2026-08-27T09:06Z

**Mode:** full (review → kanban → build data-integrity + moderation correctness → board → report)
**Budget:** ~1.5h (well under 3.5h cap)

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260827T090642Z).
- Delegation self-heal: reset `delegation.model=deepseek-v4-pro` / `provider=deepseek` / `base_url=''`.
- `gh auth status` OK (Abdullah-Alholiel). `git pull --ff-only` → already up to date.
- Tree clean on `main` at `de0db30`. Services all active; `/api/v1/health` 200; 0 journal errors in 5h.
- Baseline (before any edit): build 3/3 · API jest 64/64 · PWA vitest 217/217 · API tsc 0.
- **Provider note:** session ran deepseek-v4-pro (live); reviewer subagent completed in 245s, no 402/429.

## Findings (reviewer `deleg_29111c51` + self-verification, evidence-cited)

**CRITICAL:** none. (uuid casts, eq(col,null), matches mutation-return contract all clean.)

**Built this run (3 slices, commit `3377567` + docs commit):**

1. **P2-9a — settlement `pay` TOCTOU** (settlements.service.ts:65-91): read `before.status`,
   UPDATE with `WHERE eq(id)` only → concurrent pays double-succeed. **Fixed**: conditional
   UPDATE `WHERE and(eq(id), eq(status,'pending'))` + `.returning()` + 0-row → BadRequest.
2. **P2-9b — settlement `generatePending` double-insert** (settlements.service.ts:99-150):
   `NOT EXISTS` guard in SELECT, per-venue inserts outside tx, no `unique(venue_id, period_start)`.
   **Fixed**: inserts in a tx with `onConflictDoNothing` against new unique index.
3. **NEW — `reports.create` dedup TOCTOU** (reports.service.ts:19-56): select-then-insert, no
   unique index → concurrent duplicate reports. **Fixed**: `onConflictDoNothing` against partial
   unique index `(reporter_id, subject_type, subject_id) WHERE status IN ('open','reviewing')`.
4. **NEW — unban wrong verb** (users.service.ts:188-194): `verb: updates.banned_at ? 'account_banned'
   : 'account_suspended'` → unbanned users told they are *suspended*. **Fixed**: emits
   `account_unbanned`. New verb wired through `ActivityVerb` enum (schema + activities type +
   DIRECTED_VERBS), PWA feed/bell/toast maps (ActivityCard, NotificationSheet,
   NotificationProvider, useFeed), and ar/en i18n (`feed.accountUnbanned`,
   `notifications.accountUnbanned`).

**Migration `0018_absent_shotgun.sql`:** `ALTER TYPE "ActivityVerb" ADD VALUE 'account_unbanned'` +
`CREATE UNIQUE INDEX settlements_venue_period_uidx (venue_id, period_start)` +
`CREATE UNIQUE INDEX reports_open_subject_uidx (reporter_id, subject_type, subject_id) WHERE status IN ('open','reviewing')`.

**Recorded for next run (NOT built):**
- **recordTransaction idempotency replay returns 409** not the original entry (wallet.service.ts:56-58) — webhook retry gets ConflictException. → P2-10.
- **verify-otp returns JWT in body when NODE_ENV≠production** (auth.controller.ts:86) — dev-only token leak. → P2-11.
- **Admin scope `sql\`true\`` inconsistent** — `scopedVenueIds`/`scopedPitchIds`/`getDashboard` use it but `getVenues`/`getPitches` inline the ternary (partner.service.ts:45,55,155,279). → P2-12.
- **Dead UI MapPin** in clubs header, no onClick/href (clubs/page.tsx:64). → P2-13.
- **P1 (money) — `joinMatch` never debits joiner's `price_per_player`** (matches.service.ts:603-716): joiners join paid matches free; only host pays pitch cost in koralink mode. **Deferred**: entangled with P0-2 (no real payment provider — dummy wallet). Correct once real payments land.
- 2 stray `console.error` in PWA (usePushNotifications.ts:74,92, ErrorBoundary.tsx:30) — acceptable error paths.

## Previous-run verification (claims ≠ facts)

- P1-6 Admin partner-portal scope — **CONFIRMED** (updatePitch → assertPitchAccess; getDashboard/getEarnings accept actorRole, Admin → `sql\`true\``).
- P1-7 markNoShow guard order — **CONFIRMED** (`if (!player)` precedes `player.no_show` deref).
→ Both promote to DONE ✅.

## Built this run — verification evidence (real terminal output)

- `npm run build` (root) → **3/3 tasks successful** (2m39s).
- `npx tsc --noEmit -p apps/api/tsconfig.json` → **0 errors**; PWA `npm run type-check` → **0 errors**.
- `npx jest` (apps/api) → **71/71 tests, 11 suites** (was 64/64; +7 new race-guard specs).
- `npx vitest run` (apps/player-pwa) → **217/217 tests, 29 files** (i18n parity test green — ar=en).
- **Live DB (postgres):** `settlements_venue_period_uidx` + `reports_open_subject_uidx` present in
  `pg_indexes`; `ActivityVerb` enum has `account_unbanned` (13 values). `db:migrate` applied.
- **Live E2E:** `POST /admin/settlements/generate` (Admin) → **201 `{generated:0}`** (tx +
  onConflictDoNothing path clean). `POST /reports` (venue) → **201**, duplicate → **400
  "You have already reported this subject."** (partial unique index enforced live). Test report
  cleaned up via SQL.
- API service restarted AFTER build wrote dist (dist grep confirms `account_unbanned` +
  `onConflictDoNothing` compiled); `/api/v1/health` 200.

## Board changes

- **P1-6** → DONE ✅ (verified run #4). **P1-7** → DONE ✅ (verified run #4).
- **P2-9** → DONE ✅ (built run #4, `3377567`). Scope expanded to include reports.create dedup.
- **NEW DONE** — unban verb `account_unbanned` (full-stack).
- **New backlog** — P2-10 (transaction idempotency replay), P2-11 (verify-otp token in body),
  P2-12 (admin scope inconsistency), P2-13 (MapPin dead UI). P1 money note (joinMatch debit) folded
  under P0-2 dependency.

## Blockers & decisions

- **P0-2** unchanged — real payment provider external (Abdullah's vendor decision); the new P1
  "joinMatch never debits joiners" is blocked behind it.
- Vitest `-C apps/player-pwa` flag is INVALID in vitest 2.1.9 (`Unknown option -C`) — correct
  invocation is `cd apps/player-pwa && npx vitest run`. **Recorded in factory-loop skill.**
- `graphify update .` re-dirtied `graphify-out/` (6 files) — committed at Phase 5.

## Next-run recommendation

1. **P1-2** discovery filters (`skill_level` filter + `radius_km` hard cutoff) — full vertical slice.
2. **P1-3** chat pagination/media.
3. **P2-5** bare-mutation contract sweep (createVenue/createDispute/reports/wallet) — now that
   reports.create is touched, the remaining bare-row mutations are a tidy batch.
4. Re-verify this run's built items (settlement/report races + unban verb) → promote to DONE.
