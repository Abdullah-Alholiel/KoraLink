# Factory Run #67 — 2026-09-21T15:17Z

**Mode:** full (1 build item + review lane + board)
**Budget:** ~50 min active of ~3h (reviewers 233s/167s sync; 1 vertical slice + gates)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on zai (probe: **200 in 2.9s** via ZAI_API_KEY; quota weekly 1%, 5h 1% — healthy) — 11th consecutive clean zai run. Delegation config verified clean (glm-5.3-flash/zai/coding base, reasoning_effort low).

## Preflight
- Lock acquired fresh (bridge pid 1054710, run67-151857). No dead-run recovery needed this slot — run #66 recovery had already cleaned up (pushed d9d1a8f..47ac2cc + report at 10:36Z).
- `git pull --ff-only origin staging` → up to date (staging == origin == fd0d503).
- gh auth OK (Abdullah-Alholiel, setup-git applied). Quota probe healthy.
- Strix monthly gate: **not due** (Sep 21; window is 1st–3rd; STATE note `strix_scan=not-due-2026-09` stands; next due Oct 1).
- Tree clean at start except untracked `.local/` (foreign, untouched).

## Sentry / error-log triage (Phase 1.6 — Sentry API primary, EU base, read token OK)
**Zero NEW events since Sep 14 in either project.** All 27 open issues are stale classes:
- KORALINK-API-B CORS evil.example ×110 (probe noise, by-design rejects; last Sep 11)
- API-1B Email provider ×9 (P1-41 transport disabled → expected until Brevo provisioning)
- API-19/1A/14/15/13/11/10/17/16 — demo-pod + drift classes (P2-51 owner decision pending), all ≤ Sep 8
- WEB-D hydration ×7 (last Sep 14, pre run-#50 sweep), WEB-6 ×53 viewport-diagnostic (last Sep 14)
- journalctl err-level: **0 lines** across api/pwa/admin in 5h. Services all active, /health 200.
→ No new P0/P1 from errors. **Observability note:** Sentry "unresolved" statuses are stale — issues auto-resolved in code (13, 14, 15, WEB-D) were never marked resolved in the UI; cosmetic only.

## Findings (reviewers + parent, merged, evidence-cited)

**Reviewer A (DB & Infra lane, 67%4=3) — 0 CRITICAL, 1 IMPORTANT, 5 MINOR, 10 REFUTED:**
- IMPORTANT → **P2-88 (new): drizzle meta snapshot chain gap** — `apps/api/drizzle/meta/` jumps 0026→0029 and stops there while the journal runs to 0043; a future `drizzle-kit generate` would diff against a stale snapshot and emit spurious DDL for 0027/0028 changes. Dedicated half-cycle; no runtime impact (VPS migrates via scripts/migrate-vps.mjs).
- Migrations audit: 45 journal tags = 45 .sql 1:1, no orphans, newest `when` < now (no re-fire). P2-78/P2-80 fixes intact + tripwire spec pins both.
- Minor: `count + 1 >= max_players` (:1601) flagged vs join guard — **parent REFUTED as a bug**: `count >= max` rejects an already-full match, `count+1 >= max` flips Full when *this* join takes the last seat (complementary, same FOR UPDATE tx). Left as a documented semantic note.
- Minor: deploy-staging.sh ordering correct; CSP P2-42 unchanged (expected); graphify-out gitignore is intentional-by-design (graph.json committed), no churn leak.
- Standing bug classes: clean (no ::uuid, no eq(null), FOR UPDATE present, spots count unfiltered, no console.*).

**Reviewer B (product gaps + verification) — all 4 run-#66 claims VERIFIED, 1 new P1, 4 P2:**
- **VERIFIED:** 25b3f70 (dto :16 protocols https + dual routes @Post :67/@Delete :80), afb53d7 (composite keyset :2277/:2297 + orderSignature spec :223-224 + cross-match cursor 404), 80d7f0d (hasMore flip :143/:160 + short-page spec :149), 47ac2cc (SlotPicker :125/:136/:181 + slotsLoading en/ar :453). Promoted DONE on the board.
- **P1 → P2-87 (new, BUILT this run): push unsubscribe fails silently** — hook swallowed errors (console only), profile fired it bare (:428): failed OFF tap left pushes ON while the UI said OFF (error-copy + 5-UX-states violation).
- P2 riders: hardcoded "Close chat"/"Close" aria-labels (ChatSheet:176, ReportSheet:65) → fixed this run as a rider; no isUnsubscribing pending state → fixed with P2-87; SlotPicker offline state → left as polish (booking CTA already errors through the sheet); chat end-of-history marker → polish, not boarded (acceptable affordance).
- i18n parity: full key-diff zero missing/empty (949/949 after the new key).

**Parent sweep:** two dead backlog lines verified already-fixed and pruned (gateway JWT_SECRET fallback → now `getOrThrow` at app.gateway.ts:115; seed header drift → header now says 26 users). Raw-interface backlog note folded into existing P2-82 row.

## Previous-run verification
Run #66 recovery in_review item: **all 4 claims verified** (greps + Reviewer B independent pass + jest 197/197 had already re-run on the final tree in #66). P2-76/P2-76b/P2-83/P2-79-residual/P1-3-pagination → **DONE ✅** with run-#67 evidence appended on the board rows.

## Board changes
- **VERIFIED → DONE:** P2-76 (1)+(1b), P2-79 residual, P1-3 pagination leg, P2-86, P2-83 (run-#66 claims + #65 follow-through).
- **NEW:** P2-87 (push unsubscribe silent failure — **built this run, DONE**), P2-88 (drizzle snapshot gap, TODO DB-lane), P2-89 (audit run-2 confirmed LOW finding, TODO under ADMIN HOLD).
- **Pruned dead backlog lines:** JWT_SECRET fallback (fixed), seed-header drift (fixed), users.service.ts:191 unban-verb (fixed long ago per P2-14), raw-subscribe-interface note (lives in P2-82).
- Mirror cards: created t_a6730a4e (P2-87), t_df7b26f0 (P2-88), t_8cd67ba6 (P2-89) — all gated under t_ce9a513a (verified todo + parent). Comments: built/verified on t_a6730a4e, t_b5482c76 (P2-79), t_720b2888 (P2-76), t_0b2b69ba (P1-3).

## Built this run
**P2-87 — push unsubscribe surfaces failure + pending state** — commit **`d4b5ef1`** (7 files, +107/−7):
- `usePushNotifications.unsubscribe()` → `Promise<boolean>` (true = server row gone + browser sub released; false = failure, Sentry already captured) + new `isUnsubscribing` state with try/finally.
- Profile toggle: double-tap guard (`if (isUnsubscribing) return`), spinner for `isSubscribing || isUnsubscribing`, and on failure a localized error toast `errors.pushUnsubscribeFailed` — EN: "We couldn't turn notifications off — you may still receive pushes. Check your connection and try again." / AR: "لم نتمكن من إيقاف الإشعارات — قد تستمر الإشعارات بالوصول. تحقّق من اتصالك وحاول مجددًا." (what/why/next standard).
- Rider (Reviewer B P2): ChatSheet + ReportSheet close buttons now use localized `common.close` aria-labels; ChatSheet auto-scroll dep extracted to `lastMessageId` (clears the pre-existing `--max-warnings 0` lint warning in a touched file).
- **Verification:** hook suite 6/6 (2 new cases: success flips isUnsubscribing + resolves true under a held fetch; failure resolves false, keeps the browser subscription, calls captureError) · full vitest **90f/635t** · `tsc --noEmit` 0 · eslint clean on all touched files · root `npm run build` **3/3 exit 0** (57.8s) · fallback-*.js post-build churn reverted.

## Blockers & decisions (CEO queue — unchanged this run)
P1-41 Brevo provisioning · P2-51 Render migrate step · P2-47 cancel-purge · P1-38+P2-50 uploads · P0-2 NODE_ENV=production before launch · P1-49 appeal channel · P1-51 rating shape · P2-82 expirationTime allowlist. **PR #26 still OPEN awaiting promote go; the projects-dir merge conflict (4 UU files + staged admin edits, no MERGE_HEAD) still blocks staging builds from going live → ADMIN HOLD continues (P2-12, P2-89 held).** New owner-checklist item (audit run-1, needs_validation): verify trust-proxy so the OTP per-IP cap keys on the real client IP behind Traefik/Tailscale (fp `api-auth:per-ip-cap-proxy-trust`).

## Next-run recommendation (run #68 — 68%4=0, API/PWA-screen lane per rotation map)
1. Verify P2-87 first (recipe in STATE.json — greps + hook suite + parity; all fast).
2. Rotation 0 = PWA screens lane: SlotPicker offline state (Reviewer B P2, untouched this run) or another 5-UX-states sweep item — NOT admin (hold).
3. If the projects-dir conflict clears: P2-12 (sql`true` consistency) + P2-89 unblock immediately.
4. Strix due Oct 1–3 window (run #69 or #70 slot): quota guard + one scoped scan per the monthly gate.
5. Kill-pattern watch: no new dead-session artifacts this slot; keep correlating kill times with systemd-oom/gateway restarts if one recurs.
