# Factory Run #44 — 2026-09-08T14:23Z (manual PWA-rotation dev cycle)

**Mode:** full (manual trigger per owner sequence — step-0 bookkeeping → verify #43 → slices)
**Budget:** ~1h of the 3h allocation (13:25–14:23Z); stopped after all 3 mandated slices landed
**Models:** session `glm-5.3-flash` · reviewer dispatch **skipped** (short manual run; owner sequence mandated gates + live E2E per slice instead — recorded as a runbook deviation)
**Push-auth:** FIXED (owner PAT) — all 6 commits pushed this run; the runs #41–#43 blocker is closed.

## Preflight
- No lock residue; on `main` = origin/main `1df03ea`; no NODE_ENV leak; only dirty path `kanban/STATE.json` (owner's blocker-resolution notes). Untracked `.local/` junk never staged.
- Step-0 commit **c5038e1** (docs(kanban): STATE/BOARD blocker-resolution) **pushed OK** — push path proven before feature work.

## Previous-run verification (run #43 → all 4 claims VERIFIED, promoted to DONE in BOARD.md)
1. **49a271b env-tunable DB pool** — `buildPoolOptions` at `database.module.ts:17`, env keys `DATABASE_POOL_MAX/IDLE_TIMEOUT/CONNECT_TIMEOUT`, defaults 10/30/10; spec `database.pool-options` **4/4 PASS**; dist compiled.
2. **a8a64ff SW 200-only caching** — both sw.js copies (`public/` + `.next/standalone/...`) carry 4× `statuses:[200]`, zero `[0,200]` (file is minified — loose grep).
3. **cc24d8a admin filter aria-labels + i18n parity** — 10 bare `<select>`s across 7 list pages labeled; parity was exactly 553=553.
4. **Restore drill** — runbook log figures 29=29 / 44=44 / 16=16; drill DB absent (`pg_database` → `[]`).

## Sentry / error-log triage
Not pulled this run (short manual cycle, owner sequence omitted Phase 1.6; scheduled run #45 resumes it). Live probes below surfaced the one real error (503 DI) and it was fixed in-run.

## Built this run

### SLICE 1 — P1-19 phone-change flow (lost-SIM recovery) — f43f46f + c45c9f3
- **API:** `POST /users/me/phone-change` + `POST /users/me/phone-change/verify` (users.controller.ts, before the `:id` catch-all). OTP to the NEW number via **scoped `otp:change:<phone>` namespace** — login-OTP reuse impossible **in both directions** (live-proven). Caps shared with login: 60s cooldown, 10/day/phone, 50/day/IP. `getPhoneChangeActor` blocks non-normalized-status users. Verify: body phone must equal dispatched number; unique-violation race → localized 409. Activity verb `phone_changed` (migration `0037_phone_changed_verb.sql` + schema enum + drizzle bookkeeping row id38, `when` 1788747885743 — run #39 invariant re-derived: DB watermark ≥ journal max `when`; tripwire green). Audit logs carry **last-4 only**. No token re-mint (JWT validates by `sub`, phone claim never re-checked).
- **PWA:** `ChangePhoneSheet` (314 lines, shared BottomSheet, two steps) on personal-info — Change buttons on view-mode phone row + edit-mode phone line; `useUser` mutations; **16 new i18n keys ×2 locales (905=905 parity)**.
- **Gates:** jest **12/12** (spec: 10 security-gate cases + pool 4/4 within full suite), API tsc clean, vitest **9/9** ChangePhoneSheet, PWA type-check **0**, `turbo run build` **3/3**.
- **LIVE E2E (5 probes, dev-login runbook, +966 577 123 001):**
  1. request → **200**, OTP **605770** recovered from journald (Unifonic logs without creds);
  2. re-request → **429** (cooldown);
  3. wrong code → **401**;
  4. correct code → **200**, profile now carries **+966577123001**;
  5. **old-number session still valid**; login OTP (420055) **cannot** verify phone-change (**401**).
  Seed users restored afterwards (user 3's phone back; scratch user cleaned).
- **DI fix c45c9f3 (caught by live probe, NOT by tests):** first live request 503'd — `AuthModule` didn't export `OtpStoreService`/`UnifonicService`, and `@Optional()` made DI skip silently. Fix: export both. **Lesson recorded: always live-probe optional-DI wiring.**

### SLICE 2 — P2-29 featured host CTA (decision locked 2026-09-02) — be15020
Before/after (play feed pinned header, next to search):

| Aspect | Before | After |
|---|---|---|
| Pill surface | white, 1.5px green **outline** | **solid brand-green**, no border |
| Plus disc | green on white | **white** on green |
| Copy | label only ("Host a Match" / "استضف مباراة") | label + **hint line**: "Create your game — players nearby join" / "أنشئ مباراتك — اللاعبون القريبون ينضمون" (`play.hostMatchHint`, +1 key ×2 → **906=906**) |
| Shadow | none | soft green (`shadow-[0_4px_14px_rgba(37,65,50,0.35)]`) |
| Placement | pinned header (unchanged per r4/r5 decisions) | unchanged |
| Guard | label+href only | colocated test asserts featured markers (solid green, `not.toContain('border-')`, shadow, hint text) |

Gates: vitest **4/4**, type-check **0**, player-pwa build green.

### SLICE 3 (stretch) — per-locale manifests — 9cd24da
`manifest.json` hardcoded `lang:ar/dir:rtl` with English copy → EN installs showed Arabic metadata. New `manifest.ar.json` (Arabic copy, rtl, `/ar` shortcuts) + `manifest.en.json` (English, ltr, `/en` shortcuts); `[locale]/layout.tsx` emits `/manifest.<locale>.json` in **both** channels (generateMetadata + the raw JSX link iOS needs). `manifest.json` kept byte-stable as fallback (installed clients only update on re-install). **Live-verified:** `/ar` HTML → `manifest.ar.json`, `/en` → `manifest.en.json` (both tags agree); standalone synced. Gates: type-check 0, build green.

## Board changes
- P1-19 (phone change): TODO → **DONE ✅** (run #44 evidence).
- P2-29 (featured host CTA): TODO → **DONE ✅** (run #44 be15020).
- Run #43's 4 in-review items: VERIFIED → recorded DONE in rows (P2-54 aria residual closed).
- STATE.json: last_run 44, commits, new in_review_items (below).

## Blockers & decisions
- **Sibling agent live on the shared tree:** HEAD was switched to `staging` mid-run (twice). Defense applied: commits parented on origin/main, pushed as `git push origin <sha>:main`. **`2c88dd5` (devops slice 1: deploy-staging.sh + migrate-vps.mjs + environment-segregation gate docs) rode to main between my pushes** — it is a complete conventional commit, left untouched; `scripts/deploy-staging.sh` was dirty again right after = sibling still working. **Do not build on `scripts/` this cycle; verify 2c88dd5's gates next run.**
- Journal `when` conflict (tripwire vs drizzle watermark) resolved by raising the **DB** row above the journal's 0036 entry and putting the journal entry 1ms below it — both invariants hold.
- Reviewer dispatch skipped (short manual run) — deviation recorded.

## in_review_items (for run #45)
1. f43f46f+c45c9f3 P1-19 — verify: seed phones intact (+966500000001-005), settings sheet renders EN+AR, endpoints still up on restarted API.
2. be15020 P2-29 — verify: pill renders solid green with hint on /en/play + /ar/play.
3. 9cd24da manifests — verify: locale link tags still correct post-any-rebuild.
4. 2c88dd5 (foreign devops) — run its gates (build + any script lint) before building on it.

## Next-run recommendation
Scheduled run resumes Phase 1.6 Sentry/journal triage; verify the 4 items above; PWA layer-rotation focus continues (manifest live-check on a rebuilt standalone, admin state check before any admin item — sibling was last seen on `scripts/`).
