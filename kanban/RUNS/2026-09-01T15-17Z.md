# Factory Run #24 — 2026-09-01T15:17Z (manual fire after the 10:15Z cron fire 402'd at boot)

**Mode:** full (review + 1 build item + Reviewer-A fix batch)
**Budget:** ~1h10m used of ~3h — deliberately compact after the provider-repair detour; large headroom left for the next slot
**Models:** session `glm-5.3-flash` (cron pin, opencode-go boot) · reviewer `glm-5.3-flash` on **opencode-go** (DEVIATION — details below; zai `glm-5.3` permanent rule stands but was unreachable from delegation children this run)

## Preflight

- **Missing prestate script (root cause of the 402 death):** cron job `03ed02bc8d04` pins
  `script: koralink-factory-prestate.py` (injects the PROVIDER DIRECTIVE before every fire).
  The file was MISSING from `/home/ubuntu/.hermes/profiles/koralink/scripts/` → the 10:15Z fire
  booted with no directive, inherited the stale cron pin, and died at the first LLM call with
  `HTTP 402: Insufficient Balance` on opencode-go (`failure_streak: 1`, stale lock left 10:17Z).
  **Repaired this run**: found the intact script at `/home/ubuntu/.hermes/scripts/`
  (sibling session misplaced it — default-profile scripts dir), copied + chmod +x into the
  profile scripts dir. Self-verified: file present (8,230 B), identical content.
- Lock takeover valid (stale >4.5h). gh auth OK (Abdullah-Alholiel). `git pull --ff-only`: level
  with origin (0/0).
- **z.ai weekly was 92% at start, reset 15:40Z mid-run** → all LLM work deliberately deferred
  past the reset; probes + verification ran on zero-LLM tool calls first. After reset: weekly 1–2%.
- **Delegation child 401 mystery (two dispatch batches failed instantly):** the zai credential
  pool (`auth.json`) carried a BROKEN manual entry `api-key-2` (junk 1-char token, wrong base_url
  `api.z.ai/api/paas/v4`, `failure_reason: auth`) sitting at priority 1 between the two valid
  env-backed entries; `fill_first` served it after skipping the rate-marked primary. Direct
  probes with both GLM_API_KEY and ZAI_API_KEY returned 200 — key itself is fine.
  **Fixed**: removed the broken entry, cleared stale pre-reset failure markers, backup at
  `auth.json.bak-run24`. Delegation children STILL 401'd after the fix → the gateway daemon's
  credential resolution for delegation (booted before today's config churn) is the remaining
  suspect; not fixable from inside the session without killing the run.
  **Working deviation recorded**: delegation.provider temporarily `opencode-go`
  (glm-5.3-flash) — the SAME model tier as the permanent rule, just the more expensive plan;
  reviewers were under the 900s cap (171s + 214s) and completed with full findings. Go rolling
  went 38%→45% (~7% of the 5h window) for both reviewers — bounded spend. **Next run must
  re-probe zai delegation first (post-pool-fix) and fall back to deepseek-v4-flash if still 401.**
- Tree dirty with foreign files only (`.gitignore` M, deleted `docs/architecture/*`, untracked
  `docs/agent harnessing architecture/`, `apps/api/.env.bak-expose-*`, graphify-out/*) — never
  staged, worked alongside.
- **ADMIN STATE CHECK: clean** all run (`apps/admin` + partner/admin API surface, no in-flight
  edits; admin service active). One sibling touched admin en/ar messages mid-run (net-zero
  outside my 5 reply keys).

## Sentry / error-log triage (Phase 1.6)

Sources: Sentry API (EU base, read token 200) + journalctl (zero err-level entries in 5h on all
three services). No NEW actionable signatures; two worth recording:

| Signature | Count | Verdict |
|---|---|---|
| KORALINK-API-Q `POST /admin/disputes//resolve` 404 | 3 (24h) | **Traced to curl 8.5.0 vs localhost** — a local diagnostic probe with an empty id, NOT the admin console. MINOR. |
| KORALINK-WEB-2 SW registration CSP violation `http://localhost:3000/sw.js` | 1 | dev-origin PWA open on a non-localhost origin — known dev-mode artifact. MINOR. |
| KORALINK-API-Y/X/W (stale), API-B CORS `evil.example` (21), API-4 401 probes (117), API-A `/health` (19), API-V/H/T/R/S/E/P/N validation noise | as before | Run #23 known-noise list stands. |

Nothing fixed, nothing new boarded from Sentry. (Push-send 5xx visibility gap was FIXED in code
this run — 34694a1 — before it could ever produce a Sentry signal.)

## Findings (Reviewers A + B on glm-5.3-flash/opencode-go + self, evidence-cited)

**Reviewer A — code quality (171s, 34 calls):**
- **CRITICAL 1 (BUILT, ec7cc56):** `admin/disputes.service.ts` resolve() ran the no-show
  reversal tx (:91-106) and the status UPDATE (:109-119) as TWO independent commits —
  mid-sequence failure ⇒ player un-marked while dispute stays `opened` ⇒ re-resolve double-
  decrements `no_show_count`; concurrent resolves both pass the advisory findOne check (TOCTOU).
  Parent verified in code before building.
- **CRITICAL 2 (BUILT, 87386cf):** same read-then-write pattern in reopen() (:158-173) —
  fixed with a status-predicated UPDATE (zero rows ⇒ 400).
- **IMPORTANT (BUILT, 5c57649):** worker deep-links — `match-cancelled` (carries matchId, dropped),
  `report-resolved`, plus the two semantic types the API never emitted honestly
  (reschedule/removal went out as generic `match-chat`); all routed now.
- **IMPORTANT (BUILT, 34694a1):** kickoffISO fallback fabricated "now"; renudge title could
  render empty quotes; web-push failures invisible to Sentry (debug-only). All three fixed.
- MINOR (backlog card t_b93d8de3): reopen evidence read-modify-write, `as never` double-casts,
  quiet-hours '24' ICU edge, worker dir/locale single-sourcing note.
- Standing classes clean: ::uuid, eq(col,null), console.*, z-index, i18n parity (PWA 685/685,
  admin 520/520).
- P2-8 commit 10f94ce verdict: sound (per-locale rendering total, allSettled fan-out correct).

**Reviewer B — product gaps + verification (214s, 60 calls):**
- **Run #23 verification: ALL 3 PASS** (independent, matches parent's own probe): P1-33
  (0029 journaled, live enum 20 values incl. all 3 verbs), P2-40 (worker bundle carries the
  fallback logic, both offline routes 200), P2-31(2) (envelope shape + validation live; caveat:
  probe user owns no reports so the hasMore:true branch wasn't observable live — jest covers it).
- **New product gaps (boarded, t_afc0593f):** P1 venue ratings structurally dead (column exists,
  no submission path), P1 no account deletion/data export (PDPL exposure), P1 waitlist (already
  P1-17), P2 chat media, P2 player reputation, P2 Socket.IO Redis adapter, P2 wallet statement.
- Checked-NOT-gaps list recorded (reschedule, calendar export, invite links, recurring slots,
  localized pushes, PostGIS radius, women-only, ban/suspend/broadcast, settlements, feed spots).

**Self-review during the reviewer window:** root-caused the prestate-script absence and the
zai-pool corruption; picked P2-2 as the build item (self-contained, ops-critical, admin tree
clean); verified every Reviewer-A claim in code before building.

## Previous-run verification (run #23 claims)

ALL VERIFIED-TRUE (parent probes + Reviewer B independent):
- P1-33 chain completion (4807bcd) — 0029 in journal (entry 30), 3 verbs present, live enum 20.
- P2-40 offline fallback (175df30) — bundle `worker-2a7f80a3ab5b4969.js` carries
  `koralink-offline-pages` + `/en/offline`, live sw.js references it ×2, both routes 200
  (survived later sibling rebuilds).
- P2-31(2) reports pagination (5d9684f) — envelope keys live, limit=999→400, jest 10/10.
- **Sibling-built P2-8 (10f94ce)** — verified 17/17 notif specs, boarded DONE + Reviewer-A
  follow-ups built this run.

## Board changes

- **DONE ✅**: P2-2 (dispute replies + atomicity — built this run), P2-8 (sibling 10f94ce,
  verified), P1-33 / P2-40 / P2-31(2) (run #23 items verified → DONE).
- **ADDED**: backlog minors card (mirror t_b93d8de3, gated); product-gaps card P1
  (mirror t_afc0593f, gated).
- Mirror comments: t_ac04f64f (P2-2 built+verified), t_fb9a5028 / t_d4ea7b66 / t_f657ca33
  (verified), t_a8904eed (P2-8 sibling-built + verified), t_c8493614 (atomicity note, money
  still P0-2-blocked), t_7f4f4bc4 (partial progress note). No completes, no blocks (t_ce9a513a
  gate respected).
- BOARD.md: header + P2-2 row rewritten (DONE ✅ with full evidence), P2-8 row rewritten
  (DONE ✅ sibling).

## Built this run (4 commits, gates per slice)

| Commit | Slice | Verification |
|---|---|---|
| ec7cc56 | **P2-2 dispute replies + resolve atomicity (CRITICAL fix)**: `POST /admin/disputes/:id/messages` + reply composer + single-tx guarded resolve | jest 208/208 (new spec 7/7), tsc 0, build 3/3; committed before further edits |
| 87386cf | **reopen status-predicated guard (CRITICAL #2)** | spec 8/8, tsc 0 |
| 34694a1 | **push-text hardening** (kickoff no-fabricate, title fallback, Sentry on send failures) | jest 209/209, tsc 0 |
| 5c57649 | **worker deep-links + honest push types** (4 types routed; API emits match-rescheduled/player-removed) | vitest 261/261, build 3/3, built worker bundle carries all 4 routes |
| — | API restart after build (`npm run restart`), health 200 | |

**LIVE E2E PASS (restarted API, dev-login admin ops surface):** reply 201 → populated response
with the new message; whitespace 400; unknown dispute 404; re-resolve 400 ("already decided");
reopen 200 → opened; re-reopen 400; audit rows `dispute.message`/`dispute.resolve`/`dispute.reopen`
present in audit_logs. Player-side reports envelope + `/en/reports` 200.

Full-gate finals: root build 3/3 (×3 invocations), jest 202→209, vitest 261/261, tsc 0 (api),
NODE_ENV verified unset before every gate.

## Blockers & decisions

- **zai delegation for children still broken at the gateway layer** (after pool repair) —
  flagged for Abdullah/factory-watchdog; next run re-probes and fails over to
  deepseek-v4-flash if needed. Reviewer model tier preserved (glm-5.3-flash) via opencode-go
  this run as the documented working path.
- **Strix DEFERRED** (weekly >75% at decision time; reset landed 15:40Z but scan + build
  couldn't both fit — recorded in STATE notes; next run picks it up FIRST).
- Quota: stopped at 1 build item + 3-fix batch after the repair detour; ~1h50m headroom left.
- Sibling messages/en|ar.json touched mid-run (net-zero outside my 5 keys) — only my keys staged.

## Next-run recommendation

1. **Strix monthly scan FIRST** (due this month, deferred today): rsync snapshot refresh from
   RUNBOOK, `run-scan.sh targets/koralink-src`, map findings to board (45-min budget guard).
2. Verify the 3 in_review items in STATE.json (spot-checks listed there).
3. **Re-probe zai delegation for children** (pool now clean); if still 401 → deepseek-v4-flash.
4. Buildable queue: dispute-reply push notification (reporter learns when admin replies — new
   push key in the catalog) · P1-16/P2-29/P2-32 need Abdullah · product-gaps card items are
   multi-run sized (venue ratings is the best next slice).
