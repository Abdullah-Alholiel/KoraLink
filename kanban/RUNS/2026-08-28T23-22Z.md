# Factory Run #11 — 2026-08-28T23:22Z (manual early run, time-boxed to 01:05Z)

**Mode:** full (compressed — early run between 20:15 and 01:15 fires; ~1.5h budget honored, lock released 00:2xZ, well before 01:05Z)
**Budget:** ~55 min actual vs ~1.5h allotted
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe: 200) · delegation reasoning_effort=low

## Preflight
- Lock written 23:22:19Z (`2026-08-28T2322Z-r11`); no prior lock present.
- gh auth OK (fullstack-dev config dir); tree clean at start; `git pull --ff-only` → up to date.
- GLM probe 200 on z.ai coding base → delegation set glm-5.3-flash/zai/`https://api.z.ai/api/coding/paas/v4` + reasoning_effort low. `HERMES_DELEGATED_CHILD_CONTEXT` absent.
- Delegation-config self-heal N/A (values already correct for reviewer phase).

## Sentry / error-log triage (Phase 1.6)
- Source: Sentry API (de.sentry.io, SENTRY_READ_TOKEN, 24h window) both projects + journalctl -p err 5h on all 3 services.
- journalctl: **zero** error lines across api/pwa/admin. Sentry: all signatures known/historical (lastSeen ≤ 08-28 01:22Z). One blip: +1 CORS `evil.example` rejection 18:14:51Z — that was run #10's own security probe, not an attack. **No new P0/P1; nothing boarded from triage.**

## Health snapshot (Phase 1.5)
- api/pwa/admin all `active`; `GET :3001/api/v1/health` → 200 ok.

## Previous-run verification (claims ≠ facts)
- **P2-17 stale-session self-heal (run #10, 03d9baa): VERIFIED PASS.** Reviewer B independently confirmed the fetcher 401 handler + AuthBootstrap per-session probe; PWA vitest 227/227 across 32 files re-run this run; services healthy. → BOARD stays DONE; mirror card `t_cc69d8d4` was stale (see below).

## Findings (2 scoped reviewers + self, merged — the run #9/#10 timeout fix WORKED)
- **Reviewer split validated:** A (code quality, DB&Infra rotation) finished 141s, B (product gaps + verification) 137s — vs 2 consecutive 600s timeouts for the old monolith reviewer. `reasoning_effort=low` + hard tool-call budget in the brief.
- **Reviewer A: 4 findings → 2 false positives, 2 real-but-minor.**
  - FALSE: "seed phones are masked/broken" + "PWA push handler lacks showNotification" — both disproven by byte-level verification (`od -c`): the files contain REAL values; **my own tool-output channel display-masks phone numbers** (`+966 5x xxx 0001` renders as `+966****0001`). The reviewer saw the same masked display through greps. Boarding the "fix" would have corrupted real seed phones.
  - REAL/MINOR: `partner.service.ts` createSlot (~:585-610) and `matches.service.ts` createDispute/attachAppeal (~:1575-1600) return bare rows (mutation-contract debt) — verified harmless today: admin SlotManager `addOne()` refetches; `useAppeal` invalidates the GET. Left TODO-class debt, not boarded as new items (both already covered by the standing mutation-contract rule).
  - sw.js caching strategy confirmed correct by inspection (feeds SWR 60s, clubs 24h, user 5-min NetworkFirst, payments/auth NetworkOnly).
- **Reviewer B top product gap (PWA search) — deferred to the next full run** per this run's 1.5h budget; everything else matched known board state.
- **PROCESS FINDING (most valuable of the run):** tool-output phone masking produced confident, detailed, WRONG findings from a live reviewer. Rule for future runs: **any string-literal finding about phones/PII must be byte-verified (`od -c`) before boarding or "fixing".**
- Stale mirror cards swept: `t_546c5bc1` (unban verb — fixed run #4 3377567) and `t_cc69d8d4` (AuthBootstrap — superseded by P2-17 03d9baa) confirmed stale via grep; commented, left open (mirror-card completion is blocked by design).

## Board changes
- P2-10 **TODO → DONE ✅** (run #11, 6ad7524).
- P2-12 verified-current (cosmetic only; stays TODO).
- No new P0/P1/P2 items this run (Sentry clean; reviewer real findings already covered by standing rules).
- Mirror comments: `t_4b314844` built; `t_546c5bc1`/`t_cc69d8d4` stale; `t_fc03ede9` verified.

## Built this run
**P2-10 — wallet recordTransaction idempotent replay (commit `6ad7524`)**
- Sequential replay: used `idempotency_key` → `201 { replayed: true, ledgerEntry: <ORIGINAL row>, wallet_balance: <current> }` (Stripe-style) instead of 409 `ConflictException`.
- Concurrent race path: loser of the unique-index race (`transactions_idempotency_key_unique`, SQLSTATE 23505 via structural `isUniqueViolation()` guard) re-selects OUTSIDE the aborted tx and replays — no 500, no double balance write, no dead-loop on the aborted tx.
- Other unique violations rethrown (not swallowed); insufficient balance still 400.
- Gate docs: `docs/plans/run11-wallet-replay/` (00-status.md includes the Gate 3 contract checklist, all ✓ justified).
- **Verification evidence:** API jest 112/112 (18 suites; 5 new replay/race/guard cases) · PWA vitest 227/227 (32 files) · `tsc --noEmit` 0 errors · `turbo run build` 3/3 · compiled dist grep: `replayed` present, `ConflictException` absent, mtime 00:06:59 · **API restarted AFTER build** (ActiveEnter 00:08:32 > dist) · **live end-to-end probe on :3001**: dev-login 200 → topup 1 SAR (`run11-replay-probe`) 201 balance 725→726 → identical retry 201 `replayed:true` SAME ledger id `aa66d57e…` balance still 726 → balance endpoint 726. No double-credit, no 409.

## Blockers & decisions
- Decision: built P2-10 (small, finishable, no migration) over PWA search (top product gap but needs the full 3.5h run) — per Phase 3.5 "small completed beats large half-done" under this run's reduced budget.
- Abdullah-decision-blocked items untouched: P0-2 real payments, P1-16 padel scope, P1-2 skill_level semantics.
- No schema changes → no migration (Phase 4.5 sweep clean: only intended paths committed; `graphify-out/manifest.json` refresh staged separately below).

## Next-run recommendation
1. **PWA search** (top product gap from Reviewer B) — first full-budget slice of the 01:15Z run.
2. Then P1-12 admin i18n scaffolding; P2-12 scope-helper consistency as filler.
3. Standing: byte-verify (`od -c`) any reviewer finding involving phone numbers/PII before boarding — display masking is a proven false-positive generator.
4. t_856d0db3 (wallet history queryKey) remains a good small filler.
