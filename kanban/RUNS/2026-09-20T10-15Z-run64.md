# Factory Run #64 — 2026-09-20T10:15Z (cron, gateway-hosted)

**Mode:** full (verification + 1 build item)
**Budget:** ~1.6h of the 3.5h slot (verification lane + P2-73 vertical slice + board/report)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe **200** via
ZAI_API_KEY in 1.8s; GLM_API_KEY .env line remains STALE) · provider `zai` · coding endpoint ·
delegation.reasoning_effort=low · **10th consecutive clean zai run**

## Preflight

- **Staging worktree** `/home/ubuntu/worktrees/koralink-staging` @ `49bd78c` (== origin/staging,
  ff-pull clean). Projects dir remains a foreign feature lane (`feat/search-neighborhood-suggestions`,
  mid-merge conflict from run #62 era) — untouched, still needs Abdullah to finish/abort.
- gh auth OK (Abdullah-Alholiel); node v22.23.2; no NODE_ENV leak (checked before gates).
- Delegation config verified clean (glm-5.3-flash / zai / coding endpoint / low).
- Lock acquired 10:17Z (bridge pid 2072754); z.ai weekly 1% per prestate directive.

## Strix (Phase 0 monthly gate)

Not due — Sep 20, window is 1st–3rd. No scan run.

## Sentry / error-log triage (Phase 1.6)

- **Sentry API (de.sentry.io, SENTRY_READ_TOKEN, 200):** koralink-api 10 issues / 24h window,
  koralink-web 10 — **newest event across both projects = 2026-09-14 (KORALINK-WEB-D hydration,
  WEB-6 viewport diagnostic). Zero new events in 6 days.** All listed signatures are the
  standing classes already on the board (CORS evil.example probes ×110, Brevo 503 = P1-41,
  Neon Sep-7/8 blips, SW-registration SecurityErrors = prod-infra class).
- journalctl -p err 5h window: clean on all three services.
- **No new work boarded; no fixes required this run.**

## Health snapshot (Phase 1.5)

api/pwa/admin all `active`; `GET /api/v1/health` (localhost:3001) → 200 ok. Post-build PWA
restart verified: `next-server` on :3000 ready; funnel :9450 binds 100.93.99.24 (plain-http
probes 400 by design — TLS-only funnel; `https://aa.tail2948f9.ts.net:9450/ar/offline` → **200**).

## Findings (GLM reviewers + self, merged, evidence-cited)

**Reviewer A — PWA lane + standing sweep, 13 tool calls, 115s:**
- SW/offline layer healthy: route matrix correct (NetworkOnly money/auth first, NetworkFirst
  match detail/feed, SWR clubs/venues, CacheFirst static, all `handlerDidError → fallback`);
  worker navigate fallback correctly scoped (same-origin GET, locale-keyed); push handler
  handles malformed JSON + deep links + RTL.
- `offline/page.tsx` clean (i18n, no hydration hazards); i18n parity **968/968** exact;
  z-index lineage conform; no dead UI in sample; no console.*/eq(col,null)/::uuid regressions.
- P3 carry-forwards (NOT boarded, noted for a future polish batch): (1) `::text` casts vs
  `::uuid` in follows/activities raw SQL — parameterized but index-defeating; (2) offline-page
  install cache (`addAll` swallow) has no re-population path — **partially hardened this run**
  (cache-miss now redirects instead of Response.error); (3) fetcher has no offline-aware error
  classification.

**Reviewer B — run-#63 verification + product gaps, 8 tool calls, 100s:**
- **Run #63 claims BOTH CONFIRMED repo-side** (sha256 1a2a0840… ✓; journal idx44
  when=1789846359444 ✓; migrate-vps gate exactly 1 hit at :116 ✓; dup-codes spec 5/5 ✓).
- P0-lead "no offline mutation queue" → **DEDUPED, not new**: existing P2-7 + P2-46
  (owner-parked 2026-09-03 — Level-4 Dexie + outbox only when offline ACTIONS become a
  product requirement).
- P1-lead "waitlist promotion never pushes" → **REFUTED**: `waitlist_promoted` category wired
  (notifications.service.ts:37) and fired post-commit (`waitlist.service.ts:330-341`
  notifyPromotion → activities.record + WS roster broadcast).
- P2-leads (post-match ratings = boarded P1-51 owner call; single-sport schema field = product
  decision) → surfaced to the decisions queue, not boarded.

## Previous-run verification (run #63 in_review_items — QA lane, parent-side)

| Claim | Verdict | Evidence |
|---|---|---|
| 0043 recovery commit (file + journal idx44) | **CONFIRMED** | sha256sum `1a2a08404e3a2198…` == live journal hash prefix; `_journal.json` tail idx44 when=1789846359444 tag=0043; **live DB** newest `drizzle.__drizzle_migrations.created_at` == 1789846359444; `enforce_match_capacity` fn def contains max_players AND min_players; **0 active matches with drifted min_players** |
| P2-80 migrate-vps `!e.code` gate | **CONFIRMED** | grep → 1 hit `scripts/migrate-vps.mjs:116`; `npx jest migrate-vps-dup-codes` → 5 passed |

Both items promoted to DONE (run #63's own report + this run's re-verification recorded).

## Board changes

- **P2-73 (PWA/Offline) TODO → DONE ✅** — built this run, `91ab5e6`.
- **P2-80 (PWA/Offline) TODO → DONE ✅** — same commit; premise updated (worker intercepts ALL
  /en|/ar document navigations — route-table extension moot; the real gap was the cache-miss
  `Response.error()`, now `Response.redirect`).
- Run #63's DONE annotations (P1-52/P1-53/P2-80-API/0043) now carry run-#64 independent
  verification (this report's table).
- No new rows. STATE.json: last_run 64, in_review_items = this run's 1 item (below).

## Built this run (slices, commits, verification evidence)

**Commit `91ab5e6` — feat(pwa): offline page restores the original deep link (P2-73 + P2-80
hardening).** Gates 0–3 compact docs: `docs/plans/pwa-offline-url-restore/` (retro + program
design with exact KV/i18n/state-machine contracts + status table).

- `apps/player-pwa/worker/index.js`: on offline document navigation, save `{u: req.url,
  t: Date.now()}` into Cache-API KV `koralink-offline-restore` (key `/__kl/restore-url`)
  BEFORE serving the locale offline page; SUCCESSFUL navigations clear the entry (fire-and-
  forget, never blocks respondWith); cache-miss branch now `Response.redirect(page, 302)`
  (was `Response.error()`). Cache-API chosen because sessionStorage/localStorage do not exist
  in SW context.
- `apps/player-pwa/src/lib/sw-offline-restore.ts` (new): page-side reader — 24h TTL,
  http(s)-only URL validation (javascript: etc. dropped), malformed/stale entries deleted on
  read (self-healing), `FRESH_SAVE_MS=15000` landing-toast window.
- `apps/player-pwa/src/app/[locale]/offline/page.tsx`: localized **"back to the page" CTA**
  (`offline.backToPage`, hidden when nothing saved) → `location.assign(savedUrl)` when online;
  still-offline CTA press → localized toast `offline.backUnavailable` + detail
  `backUnavailableHint` (error-message standard: what happened + why + what next); fresh save
  + offline → same toast once on landing; PostHog `offline_restore_attempted {stillOffline}`.
- i18n: `offline.*` namespace added to **both** en.json + ar.json (3 keys each); parity test
  green (968/968 before → 971/971 after).
- Tests (11 new): `test/lib/sw-offline-restore.test.ts` (6 KV-contract cases: valid, absent,
  malformed, non-http, stale-delete, no-caches) + `test/app/offline-restore.test.tsx`
  (5 page cases: CTA hidden when null, navigates online, offline toast + no navigation, ar
  copy, fresh-landing toast).

**Verification evidence (all re-run this session on the exact committed tree):**

| Gate | Result |
|---|---|
| `npm run type-check` (PWA, includes test/) | exit 0 |
| `npx vitest run` (full PWA suite) | **87 files / 619 tests passed** (was 87/608) |
| `npm run build` (root turbo) | **3/3 successful, exit 0** (55.9s; postbuild restarted koralink-pwa) |
| Live funnel | `https://aa.tail2948f9.ts.net:9450/ar/offline` → **200** |
| fallback-*.js hygiene | regen churn reverted before commit (run-#59 rule) |
| Foreign paths | zero staged (graphify-out/ + .local/ left untouched) |

## Kanban mirror (Phase 3.7)

- `board-P2-73` + `board-P2-80` (PWA) mirror updates: built-in-run comments with commit +
  verification evidence (see CLI transcript in this run's session log). Mirror failures are
  non-fatal by runbook; BOARD.md remains the authoritative record.

## Blockers & decisions (CEO lane — surface only, unchanged set)

P1-41 (email provider provisioning) · P2-51 (Render demo migrate drift) · P2-47 (cancel-purge
call) · P1-38+P2-50 (upload pipeline) · P0-2 (prod NODE_ENV + real wallet gate pre-launch) ·
P1-8/P1-14/P1-29 (money path, blocked on P0-2) · P1-12 (admin RTL visual check) · P1-49 (ban
appeal channel) · P1-51 (rating/MVP shape) · **new for Abdullah's eyes: the projects-dir merge
conflict is still unresolved (his feature lane, mid-merge since run #62) — finish or abort it.**
New from Reviewer B this run (decisions, not builds): multi-sport schema field (padel-only
today); post-match ratings shape (= P1-51, already queued).

## Next-run recommendation (run #65, 65%4=1 → API lane)

1. Verify THIS run's 1 in-review item first (recipe in STATE.json — grep pins + 2 vitest
   files + funnel 200).
2. API-lane buildables are thin: highest-value candidates are the run-#57-queued P2-76
   (unsubscribe DELETE-body + critical-event non-push channel) and P1-50 (ban force-disconnect,
   LOW) — re-check BOARD priorities at pick time. Admin items: ADMIN STATE CHECK first
   (projects-dir merge doesn't gate the staging lane, but verify apps/admin is clean).
3. PR #26 (search suggestions) still OPEN awaiting Abdullah — PWA discovery work stays parked.
4. Kill-pattern #12: no new kill this run (run #63 completed normally; this run completed
   normally). If a future run finds a young dead lock again, resume the systemd-oom/gateway/
   scheduler correlation hunt.
