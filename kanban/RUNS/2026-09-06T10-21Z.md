# Factory Run #37 — 2026-09-06T10:21Z
**Mode:** full (review + verify + 2 build items)
**Budget:** ~2.7h of 3.5h (stopped building at the 60-min line)
**Models:** session `glm-5.3-flash` (cron pin zai) · reviewer `glm-5.3-flash` (probe: 200; directive: zai weekly 22% ≤75% → primary plan; both reviewers zai, 114s + 109s, zero errors — 4th consecutive clean zai run)

## Preflight
- Lock absent → written. Removed at run end.
- gh authed (Abdullah-Alholiel), pull ff-only clean, HEAD 3222aab.
- Dirty tree: only foreign `graphify-out/` watch files (untouched, never staged).
- Strix monthly gate: **not due** (Sept 6 > 3rd; `strix_scan=2026-09` already recorded in STATE notes).
- Admin state check (mandatory): `apps/admin` + partner/admin API surface **clean** (no uncommitted changes; last admin commit 841680f docker). Admin service active. No admin items picked (both items were matches/notifications).
- Quota: zai weekly 22%, 5h 17%. Go weekly 100% (resets in ~13h) — irrelevant, zai healthy.

## Sentry / error-log triage (Phase 1.6)
Source: Sentry API (EU base, read token 200) + journalctl. Services all active, API /health 200, **0 journal error lines** across api/pwa/admin in 5h.
- `KORALINK-WEB-6 viewport-diagnostic:standalone` (23 evts, last 09:04Z today) → **INTENTIONAL telemetry**: ViewportHeightSync.tsx:53 one-shot per standalone session (Abdullah's own device ground-truth, documented in-code). Noise, no action.
- `KORALINK-WEB-8 Cannot read properties of undefined (reading 'waiting')` (5 evts) → **stale release**: all events on release `841680f…`, chunk 3391 absent from current build; only live `.waiting` access is optional-chained (ServiceWorkerUpdater.tsx:36). Resolved as noise.
- `KORALINK-API-11 nightly purge UPDATE failed` (2 evts, last 00:00Z Sept 6) → pre-dates run #36's demo-DB 0033 hotfix (P2-51); next confirmation point Sept 7 00:00Z. Tracked, no action this run.
- API-B CORS attacker.example (83) = old probe evidence; 4/A/H/R/S/T etc. = known auth/404 noise. No new actionable signatures.

## Findings (GLM reviewers + self, merged, evidence-cited)
**Reviewer A — code quality (API rotation focus):**
- **CRITICAL — joinMatch count-then-insert overbook race** (matches.service.ts:947-1043): no row lock; two concurrent joins both read `count < max` → both insert → overbook; loser's Open→Full flip leaves match Open with max+1 rows. = the boarded P2-49 carve-out. **FIXED this run (0e0a907).**
- **IMPORTANT — rate-limiter `hits` Map never evicted** (rate-limit.service.ts:24; handleDisconnect app.gateway.ts:186 never calls back) → unbounded memory growth. **FIXED this run (0e0a907): `release(socketId)` on disconnect.**
- MINOR — joinMatch stale-Full→Open flip fires before dedupe/count checks (:981) — brief wrong-advertising window on rejected joins; ordering note for next touch.
- MINOR — rate-limit reconnect bound is ~1 msg/s sustained with cheap reconnects (handshake is the deterrent); spec/docs should state the real bound. **Addressed in-code (honest bound note in release() doc).**
- Sweep clean: no `::uuid` casts, no `eq(col,null)`, no console.* in prod paths, no mutation return-contract violations; 894c3ac/d9023a9/faabbce spec-covered.

**Reviewer B — product gaps + verification:**
- Verification: **ALL 4 prior claims CONFIRMED** (P1-42 guards/specs; host-onboarding components + i18n; Host Guide never-writes-seen-flag 3 specs; email flow wired end-to-end EmailSection→useUser→mailer.controller→service).
- "P0 gaps" REFUTED by existing verified builds: push channel exists (P0-5 run #28, notifications.service.ts:5,68,191 web-push+VAPID — self-verified), player reporting exists (P1-31 ReportSheet, verified-OK list), wallet 5 UX states (verified-OK list). Not boarded.
- Not refuted, recorded not boarded: per-route error boundaries (only [locale]/error.tsx exists) — P3 polish candidate; admin locale-switcher parity — fold into next admin i18n touch.

**Self:** live probes /en/host-guide 200, unauth PATCH /email/me → 401 (auth guard live). push_subscriptions live count 0 (pre-launch — P2-43 sweep is preventive).

## Previous-run verification (Phase 1)
Build 3/3 (FULL TURBO), vitest **371/371** (52 files — one more than claimed 370, owner's personal-info redesign added a test), jest 379/379 (45 suites). Structural checks: WsRateLimitService + guards in both handlers ✅; render-error degradation + typed setEmail + mailer-users spec ✅; premise predicates at :976/:1037/:1125/:2302 ✅; HostOnboarding/Gate/Skeleton + HostOnboarding.test (5 seen-key refs) ✅; EmailSection ✅. All 3 in_review_items → promoted to DONE/verified.

## Board changes
- **P2-49 → DONE ✅ extended** (run #37: 0e0a907) — final sub-class (joinMatch FOR UPDATE) closed. Reviewer A CRITICAL resolved.
- **P2-43 → DONE ✅** (run #37: cea6544) — stale push-subscription sweep built.
- **P1-42 → verified run #37** + eviction follow-up noted on the row.
- No new rows (reviewer gaps refuted/already tracked). Owner-decision blockers unchanged (P2-47/P2-51/P1-38+P2-50/Resend keys).

## Built this run (all committed; gates per item)
1. **`0e0a907` fix(api): FOR UPDATE row locks + WS limiter eviction (P2-49 final)**
   - joinMatch `.for('update')` on the match select (first tx stmt); leaveMatch raw `SELECT id FROM matches WHERE id=${matchId}::text FOR UPDATE` first; removePlayer `.for('update')`. Lock order = matches-row-first everywhere (matches cancelMatch); join/leave/remove never touch pitch_slots → no deadlock cycle with rescheduleMatch.
   - `WsRateLimitService.release(socketId)` deletes `msg:`/`dm:` buckets; wired in `handleDisconnect`. Honest reconnect bound documented.
   - +12 specs (8 lock in matches.join-leave-lock.spec.ts incl. PgDialect-asserted raw lock SQL `FOR UPDATE` + `::text` never `::uuid`; 4 release/disconnect). remove-player spec stub extended (`.for` passthrough + `execute`).
   - Gates: jest 391/391 (46), tsc 0, build 3/3, API restarted + health 200.
2. **`cea6544` feat(api): nightly stale push-subscription sweep (P2-43)**
   - `sweepStaleSubscriptions()` DELETE `updated_at < now-90d` (constant `STALE_SUBSCRIPTION_DAYS=90`, `.returning()` count) + `NotificationsScheduler` `@Cron('7 3 * * *')` (03:07 UTC, off-peak, offset from :00 cluster; Sentry-captured, never rethrows; UsersScheduler pattern). Complements per-send 404/410 prune.
   - +4 specs (PgDialect WHERE + bound-cutoff window assertion, 0-row no-op, scheduler success/failure).
   - Gates: jest 395/395 (47), tsc 0, build 3/3, API restarted + health 200.

## Blockers & decisions (autonomous)
- **Item 3 NOT started**: castVote bare-return (P2-5 exemplar) scoped (~40 min cross-layer API+PWA) but pick time fell below the 60-min budget line → per Phase 4.5 clean-tree rule, deferred. No half-slice artifacts left.
- DM-path live re-probe (run #36 residual) not attempted — unit-covered, budget spent on P2-49/P2-43.
- Sentry WEB-6/WEB-8 resolved as intentional-telemetry/stale-release (evidence above) — no board rows.
- Kanban mirror comments below; failures would be non-fatal (repo board authoritative).

## Kanban mirror (Phase 3.7)
- t_b374e2da (P1-42): comment — verified run #37 + eviction follow-up 0e0a907.
- t_41f3f42c (P2-49): comment — final sub-class closed 0e0a907, DONE.
- P2-43 had no mirror card (created run #26 pre-mirror-convention; row DONE in BOARD.md is authoritative).

## Next-run recommendation
1. Verify run #37's 2 items (exact re-proof steps in STATE.json in_review_items).
2. **P2-51 owner ask** is the highest-value open decision (every migration re-breaks demo until Render deploy hook / auto-deploy change).
3. Build queue (non-admin, no owner dependency): P2-5 castVote mutation-contract fix (scoped this run, ~40 min) → P2-19 residual per-IP daily OTP cap (needs counter-store decision — Redis already in stack) → P2-4 settlements float casts.
4. Watch Sept 7 00:00Z: demo nightly purge should now pass (P2-51 hotfix confirmation).
5. Owner asks outstanding: Resend keys (P1-41 live), P1-38+P2-50 upload pipeline, P2-47 cancel-purge.
