# Factory Run #19 — 2026-08-30T15:16Z

**Mode:** full
**Budget:** ~1h50m actual (of ~3h) — stopped early per quota guardrail after 4 items + admin repair; review/mirror/report consumed the rest
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (permanent; probe: 200) · post-cycle review pair `glm-5.3` (69s + 83s, zero timeouts — 5th consecutive clean run)

## Preflight
- No lock on arrival (wrote `20260830T151650Z`, pid 832719). GLM probe 200 → delegation `glm-5.3`/zai/coding-endpoint set; `delegation.reasoning_effort` = low. gh auth OK (Abdullah-Alholiel). `git pull --ff-only` clean; HEAD `a0cbd0c` (run #18 docs).
- Dirty foreign paths respected, never staged: `.gitignore` (M), `graphify-out/*` (sibling watch daemon), `docs/architecture/hermes-harness*` (untracked sibling work).
- `HERMES_DELEGATED_CHILD_CONTEXT=1` inherited AGAIN — all kanban calls ran under `env -u` (runbook 3.7 rule 2).

## Sentry / error-log triage (Phase 1.6)
Sources: Sentry API (EU, read token — both projects) + journalctl (3 services, 5h).
- journalctl: **zero error lines** on api/pwa/admin.
- Sentry 24h: all issues pre-date 2026-08-29 (auth-401 probes 117×, old CORS-canary, 2026-08-28 ECONNRESET boot incident ×1, handled-4xx wallet noise). Newest PWA item: KORALINK-WEB-3 `FetchError: Insufficient wallet balance` ×1 on 08-30 07:53Z — handled client-side (PublishWarningSheet classifies + CTA), MINOR, no action.
- **Verdict: clean — nothing new, nothing to fix.**

## Previous-run verification (Phase 1, run #18 claims)
Reviewer B verified ALL FOUR live (independent re-run, not just build):
- **P1-31** chat message reports: full E2E re-run PASS (report 201 / dup 400 / own-msg 400 / mine label / admin `?subjectType=message` / resolve). → DONE.
- **P2-30** partner filters+pager: containment 0 wrong-venue rows, pitch filter, pager distinct+hasMore, fake venueId 0 rows. → DONE (residual pitchId-silent-ignore → new P2-32).
- **P2-28** SW NetworkFirst: `sw.js` grep PASS (NetworkFirst + networkTimeoutSeconds:3). → DONE.
- **A1** venue hours merged-pair: code read PASS (partner.service.ts:145-154 writes merged openHour/closeHour). → DONE.

## Findings (GLM A/B + parent, merged, evidence-cited)
- **Reviewer A (code quality, DB&Infra rotation):** run #18's six commits ALL PASS. Standing bug-class greps clean: `::uuid` 0, `eq(col,null)` 0, console.* in API 0, FK indexes 39/39, WS handlers guarded, i18n parity PWA 677/677 + Admin 239/239. 2 IMPORTANTs: (1) orphan migration 0014 (→ P1-33, FIXED this run); (2) systemd unit "unverifiable from sandbox" (refuted by parent: user unit verified, loads apps/api/.env — matches known P0-2 caveat). 1 re-flag: foreign pitchId silent-ignore (documented design → P2-32).
- **Reviewer B (gaps+verification):** all 4 claims verified (above). NEW: **P1-34** partner upcoming lists Cancelled matches (live evidence: 2 cancelled rows) + P2 nit (scope=today empty mid-day).
- **Parent corrections:** P1-32's board claim "no detail page" was a MISDIAGNOSIS — page exists (outside `(main)`, linked); real gap was missing hours/pitch list (fixed). PomConfirmModal z-[80]/[90] = documented modal tier (no action).
- **OS research (1 MCP call):** Leaflet (42KB, BSD-2, keyless OSM) / MapLibre GL JS (BSD-3) as candidate map libs for a future venue-map — ONE backlog card, no adoption.

## Board changes
- Verified DONE: P1-31, P2-28, P2-30 (+ A1) — re-verified entries appended.
- Added: **P1-33** (orphan migration), **P1-34** (cancelled pollution), **P2-32** (pitchId silent-ignore), **P2-33** (admin fresh-build fragility, found+fixed this run), OS-research backlog card.
- Built→DONE this run: P1-33, P1-34, P1-32 (re-scoped), P2-26, P2-33.
- Mirror: 3 new gated cards (`t_fb9a5028`, `t_8e1109c4`, `t_5ef932df` under `t_ce9a513a`) + 5 comment updates; P1-33 card was dispatcher-claimed mid-create → reclaimed + gated.

## Built this run (slices, commits, verification)
| Item | Commit(s) | What | Verification |
|---|---|---|---|
| P1-33 | `17d8e78` | migration `0026_rapid_vin_gonzales` (ADD VALUE IF NOT EXISTS ×4) + journal idx 26 + snapshot; applied live | 4 "already exists, skipping" NOTICEs; enum 17 values no dupes; `__drizzle_migrations` 28; post-cycle reviewer confirmed journal+snapshot |
| P1-34 | `06976ab` | `getPartnerMatches` upcoming default excludes Cancelled (additive AND; explicit `?status=` wins; today unchanged) + spec | jest 165/165 (new case via PgDialect.sqlToQuery); live probe: default 0 cancelled (was 2), `?status=Cancelled` → 2, today unrestricted; API restarted AFTER build |
| P1-32 | `6c34730` + `515908e` | club detail: open/closed badge + hours row (hidden when undefined) + pitch cards w/ SAR/hr; `clubs.hours`/`perHour` en/ar (679/679) | build 3/3, vitest 241/241, i18n 6/6, type-check 0, lint clean, route 200 live, `perHour` in built chunks |
| P2-26 | `357c543` | profile stats skeleton + error/retry; host Suspense form skeleton (replaces null flash) | build 3/3, vitest 241/241, type-check 0; /en/profile + /en/host 200, skeleton in initial HTML |
| P2-33 | `2d715a1` | admin: nullable `useParams`/`usePathname` guards (5 detail pages + Sidebar) + app-router `not-found.tsx` | fresh admin build green; service 200; full root build 3/3 |
| docs | this report + gate docs | `docs/plans/run19-review-fixes/` (00-retro, 01-program-design incl. Gate-3 checklist, 00-status), BOARD/STATE | — |

## Blockers & decisions (new this run)
- **DECISION (autonomous):** P1-34 scoped to upcoming-default only — today-recap all-status behavior kept (DTO documented it as the ops recap); explicit `?status=` override preserved. Recorded on board + mirror.
- **INCIDENT (self-inflicted, contained):** sourcing `apps/api/.env` for the enum probe leaked `NODE_ENV=development` into the persistent shell; turbo's env-hash cache then forced a fresh admin build which prerendered with the DEV runtime and crashed (`<Html> should not be imported outside pages/_document`). After `unset NODE_ENV`, admin builds clean. The detour surfaced the REAL latent bug (P2-33: React-19-types nullability — admin could never pass a fresh type-check since the 2026-08-29 lockfile sync; masked by turbo cache hits in runs #17/#18). Lesson recorded on the board: **unset NODE_ENV / never source app .env before gates.**
- P0-2 / P1-8 / P1-14 / P1-29 unchanged (real payment provider pending — Abdullah).
- Post-cycle reviewer minors queued: raw pitch size/surface enum strings on club detail (i18n mapping), transaction-wrapped-migration fragility note, IIFE style nit.

## Next-run recommendation
1. Verify run #19's five in-review items (STATE.in_review_items) — re-run gates + spot-check club detail page & partner upcoming list live.
2. Board P1 queue: **P1-13 (match reschedule)** is now the highest-value finishable item; P1-27 (withdrawal) needs a money decision; P1-28 (venue discovery) is a good vertical slice.
3. Cheap win: P2-31(1) admin venues NaN guard (2-line) if queue thins.
4. Keep probing GLM first; reviewer pipeline healthy (5 clean runs on glm-5.3).
