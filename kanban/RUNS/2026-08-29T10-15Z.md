# Factory Run #13 — 2026-08-29T10:15Z

**Mode:** full
**Budget:** ~1h25m actual (10:15Z preflight → ~11:40Z lock release) vs 3.5h — stopped at the
quota guardrail (3 items + a micro-fix batch; "NEVER exceed 3 items per run").
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (**5.3-as-reviewer trial,
run 1 of 3**; probe: **200 OK** 5-token ping on z.ai coding base, flash also 200)

## Preflight

- No prior lock; lock taken 10:15Z. gh auth OK. `git pull --ff-only` clean at `bf288a5`.
- Tree dirty with FOREIGN files only (`docs/architecture/hermes-harness*` — sibling session);
  untouched, never staged. `HERMES_DELEGATED_CHILD_CONTEXT` NOT inherited this run (run #3's
  trap absent) — kanban calls ran bare.
- GLM probes: `glm-5.3-flash` 200 (1.4s), `glm-5.3` 200 (2.3s) → delegation set to
  `glm-5.3`/zai/coding base (trial per koralink-review-workflow step 2; within-z.ai swap touches
  only delegation.model).

## 5.3-as-reviewer trial — data point 1 of 3

- Reviewer A (code quality, API-module rotation) + Reviewer B (verification + product gaps)
  dispatched in parallel on `glm-5.3`, `reasoning_effort low`, ≤30-call discipline.
- **Reviewer A: completed, 150s, 16 tool calls, 4.3k output.** No timeout.
- **Reviewer B: completed, 119s, 11 tool calls, 2.3k output.** No timeout.
- vs flash baseline (runs #11–#12: both <4 min): 5.3 is comparable on time here; findings
  quality assessment below. Trial continues runs #14–#15; wall-clock + false-positive rate
  tracked per run.

## Health snapshot (Phase 1.5)

api/pwa/admin all `active`; `/api/v1/health` 200; journalctl err-scan 5h: **zero errors**.

## Sentry / error-log triage (Phase 1.6)

Source: Sentry API (EU `de.sentry.io`, SENTRY_READ_TOKEN) — both projects, 24h window.
- All signatures known/historical: CORS `evil.example` probe ×21 (rejected by CORS — hostile
  probing, blocked), auth-cookie 401 noise ×117, legacy route probes, admin dispute-path 404s.
- **KORALINK-API-W** (1×, 2026-08-28T09:10Z): `ECONNRESET` inside the boot autoComplete UPDATE —
  the incident already covered by the run #9 post-mortem fix (`1bccdb0` boot try/catch). Single
  transient event, nothing new. MINOR, not boarded.
- Nothing new to fix; no new boards from triage.

## Previous-run verification (run #12 claims)

- **12f9f2d (time-of-day filter) — VERIFIED PASS** (Reviewer B, independently): TIME_WINDOWS
  presets exact, `?time=` validated, additive Riyadh-TZ clause, FilterBar chips, en/ar i18n,
  jest+vitest green. **BUT parent self-review found a DEAD WIRE**: play/page stored
  `filters.time` and never passed it to `useMatches` — the UI chips never filtered the feed.
  Fixed in this run (bundled with P1-19, commit `6b0ffab`).
- **035bad9 (eslint fixes) — VERIFIED PASS**: locale dep present, mock rewritten, import removed.
- **Promoted to DONE** (with the dead-wire follow-up noted).

## Findings (GLM-5.3 reviewers + self, merged, evidence-cited)

**Reviewer A (all verified in code by parent before boarding):**
1. **CRITICAL — scheduler auto-cancel refund non-atomic** (matches.service.ts:411-448): guarded
   `Cancelled` UPDATE commits first; wallet credit + ledger + slot release are separate
   auto-committed statements in a catch-that-logs. Mid-sequence failure ⇒ cancelled match,
   slot stuck booked, host never refunded, no retry. → **P0-4, built this run.**
2. IMPORTANT — clear-mark closes `under_review` disputes (matches.service.ts:1775) → **P1-21,
   built this run.**
3. IMPORTANT — scheduler push copy hardcoded English (Pass 1/2) → folded into existing **P2-8**
   evidence (not duplicated).
4. IMPORTANT — Pass 1 `LIMIT 50` no ORDER BY (arbitrary subset nudged) → **fixed this run**
   (both passes `ORDER BY scheduled_at ASC`, commit `b3c7445`).
5. IMPORTANT — markNoShow `wasFlagged` read inside tx without FOR UPDATE (theoretical duplicate
   notify) → noted in backlog; single-host likelihood low, not boarded as its own item.
6. MINOR — reports.service.ts create returns bare `.returning()` row (already P2-5 class).
7. MINOR — TIME_WINDOWS filter on start-hour only is a documented product choice.
   Standing bug-class sweep: all 6 classes **CLEAN** (0 `::uuid`, 0 `eq(col,null)`, 0 console.*,
   WS handlers all enforce membership, spots counting host-inclusive, mutation contract holds).

**Reviewer B:**
- Run #12 verification verdicts (above) — accurate.
- Product-gap pass: payment gateway / slot-hold / payout gaps → all already boarded (P0-2,
  P1-14) or **REFUTED** (slot double-booking: create path books the slot INSIDE the tx,
  matches.service.ts:1108-1189 with FOR UPDATE — Reviewer B's P0 there is a false positive).
  Admin EN-only/no-tests → already P1-12 (tests note added to its evidence base). Recurring
  bookings → already P2-18. Waitlist → already P1-17. Offline queue → already P2-7.
- Nothing new boarded from B beyond confirmations; 2 false positives refuted with evidence.

## Board changes

- NEW **P0-4** (auto-cancel refund atomicity) → **DONE ✅ this run** (`ecdf29c`).
- NEW **P1-21** (clear-mark vs under_review) → **DONE ✅ this run** (`ecdf29c`).
- **P1-19** (feed pagination) TODO → **DONE ✅** (`6b0ffab`).
- **P1-20** (push prefs/quiet hours) TODO → **DONE ✅** (`2b520e3`, migration 0022 applied).
- **P2-22** (calendar export leak) TODO → **DONE ✅** (`24e59af`).
- **P2-15, P2-16, P2-6** → **DONE ✅** (`b3c7445`).
- P2-8 evidence expanded (scheduler Pass 1/2 English push copy).
- Run #12 in-review items promoted DONE (verified), dead-wire follow-up recorded.

## Built this run (slices, commits, verification evidence)

All on `main`, pushed `5468009..24e59af` (final push includes sibling's interleaved docs commits).

1. **`ecdf29c` fix(matches): atomic auto-cancel refund + spare under-review disputes (P0-4 + P1-21)**
   - Pass 2 guard+refund+ledger+slot-release in ONE `db.transaction` (mirrors manual cancelMatch;
     `refund-<id>` idempotency key kept; lost-race skip; ops log gains refundedSAR/slotReleased).
   - Clear-mark dispute closure narrowed to `status = 'opened'`.
   - Tests: `matches.auto-cancel-atomicity.spec.ts` (5 specs: tx ordering, idempotency key,
     refund-failure ROLLBACK, lost-race no-notify, self-mode) + mark-noshow spec regression
     asserting the exact narrowed predicate via `PgDialect.sqlToQuery`.
   - Gates at commit: API tsc 0 · jest 130/130 · build 3/3 · vitest 231/231 · lint clean.
   - API restarted after build; live: health 200.
2. **`6b0ffab` feat(discovery): feed pagination + time-filter wire (P1-19 + run #12 follow-up)**
   - `GET /matches` → `{ matches, total, hasMore }` envelope (total = `COUNT(*) OVER()`, pre-LIMIT;
     hasMore = offset+rows<total); `?offset=` DTO param (Min 0, Max 100000).
   - `useMatches` → `useInfiniteQuery` (limit 50, offset cursor, legacy bare arrays never page),
     aggregated return; play feed Load More button (en/ar `play.loadMore`); club-detail consumer
     migrated; **`filters.time` now passed to the hook** (dead-wire fix).
   - Tests: 3 jest (offset bound, default envelope, window total + hasMore math) + 4 vitest
     (limit always sent, legacy no-page, page-2 offset carry, wrapped envelope).
   - Gates: API tsc 0 · jest 133/133 · PWA tsc 0 · vitest 233/233 · build 3/3 · lint clean.
   - API + PWA restarted; **live probe PASS**: `?limit=2&offset=0` → `{total:1,hasMore:false,…}`;
     `?offset=2` → 0 rows; `?offset=-5` → 400; bare GET → object (envelope); `?time=bogus` → 400.
3. **`b3c7445` fix(pwa,api): wallet paging key, Sentry on PWA errors, WS leave auth, underfill order
   (P2-15 + P2-16 + P2-6 + Reviewer-A #5)**
   - `useWalletHistory({page,perPage})` — key varies, params always sent; P2-16 captureError in
     push subscribe/unsubscribe + ErrorBoundary; `leave-conversation` auth guard (participant
     check deliberately omitted — rationale in code); Pass 1+2 `ORDER BY scheduled_at ASC`.
   - Gates: API tsc 0 · jest 138/138 · PWA tsc 0 · vitest 234/234 · build 3/3 · lint clean.
4. **`24e59af` fix(matches): members-only calendar export for private matches (P2-22)**
   - `getCalendarMatch(matchId, viewerId)` minimal projection; public unchanged; private ⇒
     host-or-roster only, others 403 (venue ADDRESS leaves user control ⇒ export, not page view).
   - Tests: 5 specs (public no-probe, host fast-path, member probe, non-member 403, missing 404).
   - Gates: API tsc 0 · jest 138/138 · build 3/3. API restarted; live: roster member → 200.
5. **`2b520e3` feat(notifications): per-user push prefs — mute + quiet hours (P1-20)**
   - 4 `users` columns (push_muted, quiet_hours_enabled, quiet_start_hour 23, quiet_end_hour 7);
     **migration 0022 committed BEFORE db:migrate** (no-half-slices order), applied, verified in
     information_schema (defaults false/false/23/7, NOT NULL).
   - `sendPushToUsers` inner-joins users: muted skip; quiet-window skip (Riyadh-local,
     midnight-wrap OR-form, end-exclusive; pure static `isInQuietHours`).
   - `PATCH /users/me/push-preferences` (partial update → full set back); `/users/me` exposes prefs.
   - PWA profile: mute MenuItem + quiet-hours switch + from/until selects (24h, `dir="ltr"` in
     RTL), `useUpdatePushPreferences` write-through to the profile query; en/ar i18n (6 keys).
   - Tests: 6 jest (non-wrapping/wrapping/always-quiet windows at Riyadh offsets; muted skip;
     in-window skip; unrestricted control delivers via mocked web-push).
   - Gates: API tsc 0 · jest 144/144 · PWA tsc 0 · vitest 234/234 · build 3/3 · lint clean.
   - API restarted; **live probe PASS** (dev user +966500000002): PATCH → full set echoed;
     `/users/me` reflects prefs; `quietStartHour:99` → 400; probe user's prefs reset afterwards.

Post-slice final sweep: root build **3/3**, jest **144/144**, vitest **234/234**, tsc 0 ×2, lint clean.

## Blockers & decisions

- **Decision:** stopped feature work after item 3 + micro-fix batch per the quota guardrail
  ("NEVER exceed 3 items per run") despite ~2h remaining headroom.
- **Decision:** participant check deliberately NOT added to `leave-conversation` (a removed
  participant must still be able to leave the room); auth-only, rationale recorded in code+board.
- **Decision:** P1-20 quiet hours enforced in JS after the join (not SQL WHERE) so hour math
  (Riyadh-local, wrap) stays pure/unit-testable; per-device rows of the same user share prefs.
- Sibling interleaving: origin advanced mid-run (`48f0e0c`, `5468009` docs reverts/re-adds) —
  my push fast-forwarded cleanly; foreign working-tree files left untouched.
- Trial note: `glm-5.3` reviewers completed with no timeout at ~2–2.5 min; findings mostly
  survived verification (2 CRITICAL/IMPORTANT true, 1 refuted product-gap P0, 1 folded into
  existing items). Data point 1/3 recorded.

## Next-run recommendation

1. Verify run #13's DONE claims (P0-4/P1-21/P1-19/P1-20/P2-22/P2-15/P2-16/P2-6) — esp. that
   Load More renders and quiet-hours actually suppress at night (observe a live quiet-window
   skip or add a controlled probe).
2. Board top items: **P1-12** (admin i18n — full slice) or **P2-13** (clubs decorative pills —
   wire or remove) then **P2-23** (reporter closure). P1-16 (padel scope) still needs Abdullah.
3. Trial: reviewer data point 2/3 (compare false-positive rate vs flash).
