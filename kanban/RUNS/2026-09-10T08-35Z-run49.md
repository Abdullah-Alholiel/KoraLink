# Factory Run #49 — 2026-09-10T08:35Z (manual, WebUI)

**Mode:** manual (Abdullah-triggered) · interleaved with a live Brevo API-key handoff
**Budget:** ~2.5h wall (gates + 2 slices + docs; no build items beyond the two review-driven slices)
**Models:** session `glm-5.3-flash` · reviewer `glm-5.3-flash` on zai (probe **200**; both children clean — 143s / 509s; the runs #45/#46 intermittent child-401 did NOT recur)

## Preflight
- Lock acquired 08:35Z (no stale lock). Git up-to-date on `staging` @ `3302708`.
- Foreign WIP noted at start: sibling `matches.service.ts` (+51 lines, underfill auto-cancel) — EXCLUDED from all reviewer scopes and my commits; sibling landed it cleanly mid-run as `983634d` (KSU incident fix) + `7c9d916` + `d71dc7d` + `431cc65` (email-otp Brevo port + handoff doc). No collisions.
- OmniRoute probe: **timeout this run** (fallback unverified — moot, zai was healthy).

## Sentry / error-log triage (Phase 1.6)
Sources: Sentry API (EU) both projects + journalctl ×3 (all clean). Day-one-of-prod-traffic triage:
| Issue | Count | Verdict |
|---|---|---|
| API-B CORS `evil.example` | 106, ongoing | scanner probing prod OPTIONS /auth/dev-login; **correctly rejected** — noise, no action |
| API-1B Email provider error (send-otp) | 5, today 08:15Z | **REAL**: prod OTP email 503s = the Resend no-domain limitation → hard evidence for the Brevo cutover (below) |
| API-13 `matches.waitlist` relation ×37 | last Sep 6 17:17 | stale-dist window artifact (run #39's documented window); live repro of the exact culprit match = 200 today. NOT recurring |
| API-4 stale-cookie `/users/me` ×117 | last Aug 28 | stale artifact, zero events since |
| API-19 ECONNREFUSED 5432 ×7 | Sep 8 14:36–14:42 | deploy-window DB restart blip, 6 min burst, gone |
| API-X enum `ReferenceType: "BOOKING"` ×2 | Aug 30 | was sibling WIP hitting a missing enum; superseded by sibling's landed code |
| WEB-8 `reading 'waiting'` ×6 | last Sep 8 | `reg?.waiting` already optional-chained (run #32 fix in tree); events = stale cached Vercel chunks, tapering |
| API-A `/health` 404 ×19 | last Aug 28 | alias exists in main.ts; events predate it |

## Findings (GLM A + GLM B, merged)
**Reviewer A (code quality, API rotation):** 0 CRITICAL · 4 IMPORTANT: (1) email-verify create-race → concurrent same-code verifies → loser 500s on `lower(email)` uidx; (2) `create-conversation.dto` userId missing `@IsUUID` → garbage id = 500 not 400; (3) OTP compare not constant-time (both phone + email flows); (4) hydration hazard: messages pages render `new Date()`/`toLocale*` in render path. 5 MINOR (deleted-account 403 leaks existence vs the always-202 contract; responseToken body-JWT channel wants an env kill-switch → backlog; match-type discussions hardcode `unread_count=0` → new row; waitlist backfill random idempotency suffix noted). Sweeps clean: no `::uuid`, no `eq(col,null)`, no console.*, no z-index regressions, i18n 945/945 parity programmatic, WS mark-read auth+idempotent, join/payout tx-guarded.
**Reviewer B (product gaps + verification):** ALL run #46/#48 claims VERIFIED on staging+prod (email login i18n EN+AR live, `/matches` + detail 200, unified Messages, host-responsibility strings EN+AR, **prod chunks 0 dev-login markers**). Gaps: wallet-money (≈P0-2, boarded), chat pagination (P1-3, boarded), token hygiene (P1-17b, boarded), **offline content-fallback (NEW → P2-57)**, POTM push locale (REFUTED — P2-8/P2-27 DONE on board).

## Previous-run verification
No `in_review_items` from run #46 (owner sessions #47/#48 superseded; Reviewer B independently verified the run #46/#48 claims above).

## Board changes
- NEW **P2-57** PWA/Offline: SW has offline *navigation* fallback but no content fallback for feed/match pages (cache statuses `[200]`-only, no SWR) — offline users get the banner, not usable data.
- NEW **P2-58** PWA/Messages: match-type discussions hardcode `unread_count = 0` — Messages badge asymmetric vs personal chats.
- **P1-41 updated**: transport upgrade staged — Brevo adapter built (sibling `b194397`), `BREVO_API_KEY` received + stored; cutover blocked ONLY on Brevo console steps (VPS IP allowlist `145.241.109.213` + verified sender).
- Backlog: responseToken env kill-switch candidate.

## Built this run (2 slices — review-driven, security first)
**Slice A — auth hardening (`fix(api)`, 7 files):**
- `common/security/otp-compare.ts` — `otpMatches()` constant-time compare (timingSafeEqual + same-length dummy burn on length mismatch); wired into BOTH `auth.service.ts` (phone) and `email-otp.service.ts` (email).
- email-verify create-race: 23505 catch → winner re-select → login completes (conversations.service pattern); non-23505 rethrown.
- `create-conversation.dto.ts`: `@IsUUID` → garbage id now 400.
- send-otp soft-deleted account: silent drop (restores the documented ALWAYS-202 anti-enumeration contract; PDPL guarantee intact — no code is ever sent or stored).
- Spec harness: `setup()` now honors `db` overrides; +5 otp-compare specs, +2 race specs, deleted-account test flipped to silent-drop. **jest 39/39 (3 suites), API tsc 0.**
**Slice B — hydration (`fix(pwa)`, 2 files):** both messages surfaces on the project `useNow()` hydration-safe clock (run-#40 idiom): date dividers, bucket grouping, per-bubble time labels identical on SSR + first client render. **vitest 465/465 (67 files), type-check 0.**
**Gates:** `turbo run build` **3/3** (1m54s) → API restarted AFTER dist write → health 200 (internal + funnel).
**Live staging E2E:** send-otp 202 → wrong-code **401 clean** → correct journal code → **200 `{isNewUser:true}`** → probe user cleaned up. Garbage conversation id **400** `userId must be a UUID`.

## Blockers & decisions
- **Brevo cutover (the run's live interleave):** key received, stored in `.deploy-tokens` (0600, name-only per §7). Brevo API probe from VPS = **401 unrecognised-IP** → hard-blocked on Abdullah's console steps: (1) authorise `145.241.109.213`, (2) add + verify sender (suggest `no-reply@koralink.sa`) + name the test inbox. Then: staging env trio (`EMAIL_PROVIDER=brevo`, `BREVO_FROM`, key) → real-inbox OTP test → Render T2 diff. Verification matrix + rollback pre-armed in `docs/plans/email-otp-login/05-no-domain-email-unblock.md`. **EMAIL_PROVIDER deliberately NOT flipped early** (a blocked key would 503 every send = the API-1B class).
- Render `DEV_LOGIN_ENABLED=false` still gated on real-inbox OTP (now one step closer).

## Next-run recommendation
1. The moment Abdullah confirms IP+sender: Brevo staging cutover + real-inbox OTP E2E (30 min, T1) then Render T2.
2. P2-57 offline content fallback (small SWR recipe on feed/match endpoints).
3. P2-58 unread-count parity (small: count unread per discussion type server-side).
4. API-4/13/19/A: resolve+suppress as archived in Sentry so the 24h list stays signal-only.
