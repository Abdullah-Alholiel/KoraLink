# Run #46 — Email + OTP Login (P0-9 scope) — 2026-09-08T16:11Z

**Branch:** staging · **Final SHA:** `109281a` · **Deploy:** H1–H6 all green ×2

## Shipped

| Slice | Content | Proof |
|---|---|---|
| 1 | Migration 0038 `users.phone` nullable + schema.ts + journal row (same commit) | applied on VPS (1 applied/39 skipped); tsc clean; 435 tests |
| 2 | `ResendService` (fetch-based, dev-log fallback w/ code) + `EmailOtpService` (email-keyed caps, anti-enum, PDPL+ban gates, account-on-first-verify, `email_verified_at`) + `/auth/email/*` controller (202 always-send; P2-11 `responseToken` opt-in) + 14 jest cases | 449/449 green |
| 3 | (pre-cycle) Gate docs 01–03 committed `deae9c2`; GLM reviewer dispatched (gateway degraded, self-review applied) | — |
| 4 | PWA: subtle email toggle (below legal row), email input, channel-aware verify (masked email, `setAuthToken` Bearer path), i18n EN+AR ×5 keys | vitest 422/422; eslint clean |
| 5 | `npm run build` 3/3 zero errors; staging deploy ×2 green; migration applied; services restarted in order | H1–H6 ✓ |

## Live E2E (staging API)

- send-otp 202 (non-committal) → code from user-journal (`code=` only when RESEND_API_KEY empty)
- verify → 200 `{isNewUser:true}` + 3-part JWT (responseToken path)
- Bearer `/users/me` → 200, email set, **phone null**, role Player
- DB: `phone_null=t`, `email_verified_at` stamped
- cooldown: immediate 2nd send → **429**
- phone regression: seeded staff phone → correct 403 surface separation (OTP verified)
- i18n parity + smoke: locale files load; login renders (dev-bar visible in staging)

## Key decisions / incidents

- API's cache-manager is Redis-**less** on VPS (coolify-redis publishes no ports) → OTP codes live in the in-process fallback store; journal-based E2E used instead. No functional loss (caps work per-process); revisit if staging multiplies API instances.
- ResendService dev-mode now logs the code (Unifonic parity) — **only** when the API key is absent.
- Resend 403 on /domains (scope-limited key) → sending-domain verification stays Abdullah's dashboard step; prod delivers to account-owner address only until then.

## Pending T2 (explicit go required — clarify timed out 2026-09-08 ~17:15Z)

1. Neon: journal reconciliation (36 rows vs VPS 40) + apply ONLY missing files incl. 0038, verify, journal. Neon IS reachable from VPS (DNS healed — runbook note stale).
2. Promote: PR staging→main, CI green, merge → Render+Vercel auto-deploy, then cutover matrix §6.

## RELEASE (21:30Z)
PR #15 squash-merged → main @ 16dc206. Render live + Vercel×2 READY on that SHA. Public cutover matrix ALL GREEN (health/PWA/admin/no-dev-login/CORS). Prod email endpoint live: non-owner send → 503 (Resend test-mode rejection = key ACTIVE, domain pending). Loop closed.
