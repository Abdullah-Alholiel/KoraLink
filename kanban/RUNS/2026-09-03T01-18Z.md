# Factory Run #28 — 2026-09-03T01:18Z
**Mode:** full (push categories + iOS install gate — P0-5 vertical slice)
**Budget:** ~80 min of 3.5h, headroom for next run
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (PERMANENT; **probe: 200 on z.ai, but delegation children STILL 401 at the gateway layer — 5th consecutive run, parent self-review**)

## Preflight

- 2026-09-03T01:18Z, repo at `463320e` (run #27 main, clean). No lock → wrote `kanban/LOCK.json`.
- gh auth ✓ (Tailscale profile dir).
- Strix: ALREADY DONE for September — `strix_scan=2026-09-02 (run #25)` recorded in STATE notes. Per runbook: skip + report.
- Quota: z.ai weekly 1%, z.ai 5h 14%, opencode-go rolling 0% (per directive). z.ai 200 on direct probe; deepseek 402 (Insufficient Balance, just probed). Per directive: USE z.ai glm-5.3-flash.
- Set delegation: `delegation.model=glm-5.3`, `delegation.provider=zai`, `delegation.base_url=https://api.z.ai/api/coding/paas/v4`.
- **zai children STILL 401** at the gateway daemon (PID 3537113, booted 2026-09-02T06:52:35Z). Pool clean, direct probe 200. Same as runs #24–#27. Fallback chain: opencode-go (no key) → deepseek-v4-flash (HTTP 402, just probed) → **parent self-review**.
- Cycle docs at `docs/plans/run28-push-categories-install/{00-retro.md,01-program-design.md}` (autonomous mode — single program-design doc).

## Provider / quota state

- z.ai 5h: 14% · z.ai weekly: 1% (per directive) — well under 75% cap.
- z.ai direct probe: **HTTP 200 OK** this run.
- **zai children STILL 401 — 5th consecutive run (#24–#28)** at the gateway daemon (PID 3537113, booted 2026-09-02T06:52:35Z). Pool clean, direct probe 200, gateway resolving through stale state.
- Fallback chain: opencode-go (no key on VPS) → deepseek-v4-flash (HTTP 402 Insufficient Balance, just probed) → **parent self-review**. Re-investigation of the gateway daemon needed on a follow-up; not blocking this run.

## Sentry / error-log triage (Phase 1.6)

- **API Sentry** (24h, 25 issues): all known noise — KORALINK-API-B (CORS probes), KORALINK-API-4 (auth noise, 117×), KORALINK-API-A (404 on `/health` — has `/api/v1/health` instead, refuted run #8), KORALINK-API-Y/X/W (probe artifacts, previously refuted). **No actionable new errors.**
- **PWA Sentry** (24h, 5 issues): KORALINK-WEB-2 (SW CSP, 2× from 2026-08-20, minor), KORALINK-WEB-4/5 (transient, minor).
- **journal** (5h): 0 errors across all 3 services. Clean.

## Findings (parent self-review, zai delegation 401'd)

### Run #27 re-verification (file:line evidence)
- **P1-17c** PASS — `app.gateway.ts:80-97` (no `isProd` gate, ALWAYS reject unlisted origins). Run #27 spot-confirmed.
- **P1-17d** PASS — `common/security/ics-text.ts` exports `escapeIcsText`; controller applies to title/location/description; real `•` char in DESCRIPTION.
- **P2-19** PASS — `otp-store.service.ts` has `OTP_DAILY_IP_CAP=50`, `keys.ip_day`, `getIpDailyCount`, `incrementIpDaily`; `auth.service.ts:58` `sendOtp(phone, ip?)`; `auth.controller.ts:57` `@Req() req: Request`, `:62` extracts IP, `:63` threads to service.
- **P2-32** PASS — `partner.service.ts:860-869` `if (q.pitchId && !pitchIds.includes(q.pitchId)) throw new NotFoundException('Pitch not in your scope.')` BEFORE the existing query.

### Bug-class sweeps
- `eq(.*, null)` in prod code: **CLEAN** (0 hits).
- `::uuid` raw casts: **CLEAN** (0 hits in prod).
- Bare `.returning()`: 10 hits, all pre-existing P2-5 backlog.

### P0-5 design sanity
- 4 trigger categories the API uses today map cleanly: `match` (kickoff/cancel/reschedule/POTM), `chat` (DMs), `system` (account_suspended/report_resolved/match_cancelled_admin), `promo` (reserved for future use).
- DM push uses the inline form, not the `key` form. Solution: extend inline-form union with optional `category`; conversations.service.ts threads `category: 'chat'`.
- `usePushNotifications` already requests permission on `subscribe()` but does NOT check `display-mode: standalone` (iOS) — exact gap the install-triggered enablement should fix.

ADMIN STATE CHECK: `git status --short apps/admin apps/api/src/modules/partner apps/api/src/modules/admin*` = clean. Run #28 doesn't touch admin area (PWA + API only).

## Board changes

| ID | Area | Status change | Commit |
|----|------|---------------|--------|
| P0-5 | API/PWA | WIP → **DONE ✅ (vertical slice)** | 27ee7f8 + ad0499f |

(P0-5 was a 3-sub-cycle effort per run #27's analysis. This run shipped the user-visible slice: schema + API + profile UI + iOS install gate. Future P0-5 sub-cycles: extend to 8 categories, per-category quiet hours, per-subscription push, observability for mute-rate metrics.)

## Built this run

### Slice 1: P0-5 schema + API
- **New enum + table** (`apps/api/src/database/schema.ts`):
  - `notificationCategoryEnum('NotificationCategory', ['match', 'chat', 'promo', 'system'])`
  - `user_notification_prefs(id, user_id, category, muted, created_at, updated_at)` — cascade-delete with users, unique index `(user_id, category)`, secondary index on `user_id`
  - Migration `0030_per_category_push_mutes.sql` (hand-written — drizzle-kit version-mismatch warning was fatal; journal updated to idx 30)
- **DTO** (`update-push-preferences.dto.ts`): nested `CategoryMutesDto` (each key optional, partial PATCH). `class-validator` + `class-transformer` wired.
- **Service** (`users.service.ts`):
  - `updatePushPreferences` upserts each present `categoryMutes` key (on conflict on `(user_id, category)`)
  - `getPushPreferences` returns the full state with `category_mutes` always fully populated (missing rows default false)
- **Controller** (`users.controller.ts`): new `GET /users/me/push-preferences` for the cold-load case
- **Notifications gate** (`notifications.service.ts`):
  - `CATEGORY_BY_KEY` map: 8 semantic `PushKey` → category
  - Inline-form payload union extended with optional `category` (conversations.service.ts:308 threads `category: 'chat'`)
  - Per-send mute lookup: ONE query for the whole fan-out, O(1) per sub. The gate runs BEFORE the SSRF + render work (mutes are free).

### Slice 2: PWA UI + iOS install gate
- **Profile page** (`apps/player-pwa/src/app/[locale]/(main)/profile/page.tsx`):
  - 4 toggle rows under the existing "Mute all" / quiet-hours block (match / chat / promo / system), each with a localized hint and stable icon (Calendar / MessageCircle / Tag / Shield)
  - One-line `common.installRequired` hint surfaces when subscribe returns false (iOS not installed)
  - Tap on a single row writes only that category — partial PATCH respected
- **usePushNotifications** (`apps/player-pwa/src/hooks/usePushNotifications.ts`):
  - Install-gate check FIRST: `display-mode: standalone` OR `navigator.standalone === true`. iOS Safari only delivers web push to installed PWAs.
- **Types** (`useUser.ts`): `PushPreferences` + `PushPreferencesInput` extended with `category_mutes` / `categoryMutes`
- **i18n**: en.json + ar.json new keys under `profile.push` (categories, categoriesHint, matchHint, chatHint, promoHint, systemHint) + top-level `common.installRequired`. Parity 717/717 leaves, 0 diff.

## Verification

- `tsc --noEmit -p apps/api/tsconfig.json` → **0 errors**
- `npx tsc --noEmit` in `apps/player-pwa` → **0 errors**
- `turbo run build` (root) → **3/3 tasks successful** (1m51s)
- `npx vitest run` in `apps/player-pwa` → **272/272 tests, 38 files** (unchanged — no test edits this run)
- `npx jest` in `apps/api` → **36 suites, 302 tests, 0 failures** (+6 from run #27's 296: 6 new `notifications.push-categories.spec.ts` cases)
- API restart + health 200; dist grep confirms new code shipped (`user_notification_prefs`, `CATEGORY_BY_KEY`, `categoryMutes` all in dist)
- **Live E2E** (dev-login, restarted API, DEV_LOGIN_ENABLED=true then back to false):
  - PATCH `/api/v1/users/me/push-preferences` with `{categoryMutes:{chat:true, promo:true}}` → **200** with full state
  - GET `/api/v1/users/me/push-preferences` → **200** persisted
  - DB rows: `chat=true, promo=true` (partial PATCH respected; match/system absent → default false on read)
- DEV_LOGIN_ENABLED flipped back to false (P0-7 in force)

## Blockers & decisions

- **zai delegation children 401 — 5th consecutive run (#24–#28).** Pool is clean, direct key probes return 200, the gateway daemon (booted 06:52:35Z, PID 3537113) is resolving children through stale state. Parent self-review used this run. **Next run (#29) MUST investigate a gateway restart** — at this cadence the staleness is degrading review quality (no second pair of eyes). Working path: re-probe zai → if still 401, attempt `hermes gateway restart` (or contact operator); fallback chain remains: opencode-go (no key) → deepseek-v4-flash (402) → self-review.
- **P0-5 (broader scope)**: 3 sub-cycles identified in run #27 — this run shipped the user-visible slice. Future P0-5 sub-cycles: extend to 8 categories, per-category quiet hours, per-subscription push, observability for mute-rate metrics. **NOT blocking** — current 4-category model covers every trigger in production today.
- **P0-9 email infrastructure** → still needs owner call. No nodemailer / no SMTP / no managed provider in repo. Carve separate cycle.
- **P0-6 PDPL delete + export** → owner decision 2026-09-02: soft delete + 30-day grace + JSON export of 7 data groups. Buildable; queued for #29.
- **drizzle 0.45.2** → carry to a focused half-day cycle (not a cron run).
- **P1-17b** remaining (secrets rotation, Sentry token, Redis password, admin→HttpOnly cookies) → multi-step; defer.
- **Money items (P0-2 real + P1-8/P1-14/P1-27/P1-29/P2-1/P2-3)** → PARKED until KSA company.

## Next-run recommendation

#29 — P0-6 PDPL delete + export (full cycle, ~2.5h):
1. **Soft delete + 30-day grace**: `users.deleted_at` column (already on schema), `POST /users/me/delete` that sets `deleted_at` + reverses sensitive effects (drop push subs, anonymize activity verbs referencing the user), 30-day grace window for `POST /users/me/restore`.
2. **JSON export of 7 data groups**: `GET /users/me/export` returns `{profile, matches, wallet, transactions, disputes, reports, activities, push_subscriptions}` as a downloadable JSON file. Owner-locked: profile + matches + wallet + transactions are P0; the others are P1.
3. **Admin hardening**: `users.service` admin queries must filter `WHERE deleted_at IS NULL`; back-office "deleted users" view is admin-only.
4. **drizzle 0.45.2** if budget allows.

Defer P0-9 (email infra) to its own cycle after a Gate 1 product spec from Abdullah. Money items remain parked.

If zai delegation is STILL 401 on #29, fall back to self-review + a smaller build (PDPL is the one cycle I want a second pair of eyes on).
