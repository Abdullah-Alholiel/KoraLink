# Factory Run #2 — 2026-08-26T22:29Z

**Mode:** full (review → verify → build P0-3 → board → report)
**Budget:** ~15 min preflight+context, ~75 min review, ~45 min build/verify (well under 3.5h)

## Preflight

- Lock: none present → wrote `kanban/LOCK.json` (run_id 2026-08-26T22:14:43Z).
- Delegation self-heal: reset to `deepseek-v4-pro`/`deepseek`/`''` (prevents stale glm-5.2 402).
- `gh auth status` OK (Abdullah-Alholiel, classic token). `git pull --ff-only` → already up to date.
- Tree clean on `main` at `0a87f0f`. Since run #1, 3 commits landed (5af877e STATE handoff,
  d521449 lockfile, 0a87f0f landing drafts) — all docs/deps, no code drift to reconcile.

## Findings (deepseek-v4-pro reviewer deleg_a6eb6d32 + self-verification, evidence-cited)

**CRITICAL → built this run:**
- **P0-3 placeholder auth secrets.** `apps/api/.env` had `JWT_SECRET=change-me` +
  `COOKIE_SECRET=change-me` loaded into the RUNNING systemd process (PID 1058923, verified via
  `/proc/<pid>/environ`). HS256 signing key publicly known → any tailnet peer can forge an
  Admin/user JWT or signed cookie. Compounded by `NODE_ENV=development` keeping dev-login live.

**IMPORTANT → new board items (not built this run):**
- `partner.service.ts:317` `updatePitch` omits `actorRole` → Admin cannot edit a pitch via the
  partner portal (inconsistent with `deletePitch:352`). → P1-6.
- `partner.service.ts:149,374` `getDashboard`/`getEarnings` always owner-scoped → Admin opening
  `/partner` sees empty dashboard/earnings. → P1-6.
- `apps/api/.env` `NODE_ENV=development` on the live box → P0-2's wallet-topup prod-gate is NOT
  exercised; dev-login + dummy top-up are live for any tailnet peer. Noted on P0-2 (dev box,
  must set `NODE_ENV=production` + real secrets before public launch). Not changed this run
  (flipping it would break dev-login on the dev box).

**MINOR → board (P2-8, P2-9) + backlog:**
- `notifications.service.ts:192-196` `sendPomDecidedNotification` omits locale (POTM pushes
  always `en`). → P2-8.
- Money/state races: `settlements.service.ts:99-150` `generatePending` not in tx;
  `transactions.service.ts:78-99` double-refund → unhandled 500; `disputes.service.ts:87-118`
  no-show reversal + status update not atomic. → P2-9.
- Backlog: `use-live-data.ts:61-86` per-instance sockets; `settings.service.ts:27-40` unaudited
  settings; `users.service.ts:191` unban verb bug; `seed.ts:8,10` doc drift.

## Previous-run verification (claims ≠ facts → all re-verified live)

- **P1-4 hot-FK indexes — CONFIRMED**: 7/7 indexes present in `pg_indexes` (matches.host_id/
  pitch_id, activities.actor_id, disputes.reporter_id, reports.reporter_id, venues.owner_id,
  transactions.reference_id).
- **P1-5 SW push locale — CONFIRMED**: `push_subscriptions.locale` column live (varchar).
  Caveat found: POTM notification omits locale (→ P2-8).
- **P1-1 scheduler — CONFIRMED**: 3 @Cron jobs in `matches.scheduler.ts`; `reminders_sent_at`
  column live (timestamptz); `pom_winner_id` set on 4 matches; live log line
  "POTM scheduler: finalized 2 match(es)" at 18:40Z. (0 reminders sent = expected, no upcoming
  matches in seed.)
- P0-1, P1-3, P0-2 code claims re-confirmed by reviewer (see Section C of its summary).

## Board changes

- **Added P0-3** (env-secret hardening) → DONE this run.
- **Added P1-6** (Admin partner-portal scope inconsistency).
- **Added P2-8** (POTM push locale), **P2-9** (settlement/refund tx races) + **Backlog** section
  (admin socket/audit/verb/seed-drift minors).
- **Updated P0-2** with the NODE_ENV=development caveat (prod-gate not exercised on live box).
- **P1-1, P1-4, P1-5 re-verified DONE** (cleared from in_review_items).

## Built this run — P0-3 env-secret hardening

**Slice 1 (only slice):** bootstrap secret guard + real secrets + `.env.example` fix.
- `apps/api/src/common/security/bootstrap-secrets.ts` — `assertBootstrapSecrets()` +
  `isPlaceholderSecret()`: hard-fails on placeholder/missing secrets (any env) and <32-char
  secrets (prod); placeholder markers include `change-me`, `fallback-dev-secret`, `secret`, etc.
- `apps/api/src/main.ts:23-30` — calls the guard immediately after `ConfigService` is available,
  BEFORE `app.listen`.
- `apps/api/.env.example:14-17` — empty `JWT_SECRET`/`COOKIE_SECRET` + `openssl rand -hex 32`
  generation guidance.
- `apps/api/src/common/security/bootstrap-secrets.spec.ts` — 10 test cases (placeholder/missing/
  short-prod/short-dev/valid).
- **Live remediation**: regenerated `apps/api/.env` secrets (64-char hex each, gitignored,
  NOT committed), restarted `koralink-api.service`.

**Verification evidence (real terminal output):**
- `npm run build` → **3/3 tasks successful** (7.9s).
- `npx jest` (apps/api) → **52/52 tests pass** (7 suites, incl. new spec).
- `npx vitest run` (apps/player-pwa) → **217/217 tests pass** (29 files).
- `npm run type-check` (apps/player-pwa, = `tsc --noEmit`) → **0 errors**.
- Compiled-dist proof: `grep "Refusing to boot" apps/api/dist/src/` → guard present in
  `main.js` + `bootstrap-secrets.js`; runtime probe threw on `change-me`, accepted valid.
- Live restart: `koralink-api.service` active, `/api/v1/health` → **200**, process env now shows
  real secrets (not `change-me`), log "Nest application successfully started" (22:27:33Z), no
  guard error.

**Commit:** `42d2d86` `feat(api): refuse to boot on placeholder auth secrets (P0-3)` (7 files,
+300/-2).

## Blockers & decisions

- **P0-2** remains WIP (real payment provider — Moyasar/HyperPay/Tap — pending Abdullah's
  vendor decision). Interim prod-gate is code-correct but not exercised on the dev box.
- **NODE_ENV=development** on the live box: intentionally left as-is (dev box; dev-login is the
  working auth path). Documented as a pre-launch MUST.
- **Admin partner-portal scope bugs (P1-6)** deliberately NOT built this run — P0-3 security
  outranks it; it's a full vertical slice (service + controller + test) better suited to its own
  run.

## Next-run recommendation

1. **P1-6** Admin partner-portal scope — small, self-contained (fix `updatePitch` actorRole +
   `getDashboard`/`getEarnings` Admin bypass), matches layer-rotation emphasis continuing on Admin.
2. Then **P1-2** discovery filters (`skill_level` filter + `radius_km` hard cutoff) — full
   vertical slice with UI, needs a dedicated run.
3. Re-verify P0-3 on next run: confirm the guard still holds (secrets non-placeholder) and
   consider adding a `COOKIE_SECRET` length check parity test if any drift appears.
