# Factory Run #10 — 2026-08-28T18:04Z (report written 18:50Z)

**Mode:** full (review → kanban → 1 fix slice: P2-17 stale-session self-heal)
**Budget:** ~2.8h of work (within the 3.5h cap)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on zai
(probe: **200**, 5-token ping; delegation confirmed `glm-5.3-flash`/zai before dispatch) —
reviewer TIMED OUT at the 600s child cap for the 2nd consecutive run (42 API calls,
incremental-output instructions did NOT save it; comprehensive in-progress work visible in
the live transcript, no final emitted). Self-review (parent) carried the review phase.
Mirror-board dedupe check: `HERMES_DELEGATED_CHILD_CONTEXT` NOT inherited this run
(raw calls worked; no `env -u` needed).

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260828T180457Z); removed at run end.
- GLM probe 200 → reviewer delegation GLM 5.3 Flash (per standing rule).
- `gh auth` OK (Abdullah-Alholiel); `git pull --ff-only` → already up to date at `abfd835`;
  tree clean at start. No auto-stash (none needed).
- Dispatch wrapper timed out at 420s but the reviewer child survived (confirmed via
  `action=list`, sa-0-ad136508, 431s running) — no re-dispatch; merged nothing (timeout).

## Sentry / error-log triage (Phase 1.6 — Sentry API primary, EU base, both projects)

- Services: api/pwa/admin all `active`; `/api/v1/health` 200; `/health` alias 200.
- journalctl 5h: **zero** error entries across all three services.
- `koralink-api` 24h: 25 signatures — ALL historical, none live:
  - KORALINK-API-W (Failed query ×1) firstSeen=lastSeen **09:10:04Z** = the already-fixed
    boot incident (1bccdb0). Closed evidence.
  - KORALINK-API-A (`Cannot GET /health` ×19) lastSeen **01:22Z** — pre-dates nothing new;
    the `/health` alias is live-verified 200 this run → stale probe traffic signature.
  - KORALINK-API-B (CORS :3002 ×20) lastSeen **08-17** → stale (pre admin-CORS fix).
  - KORALINK-API-E (GET /matches 400 ×3) lastSeen **08-26** → fixed by run #9 `limit`
    param; live re-probe this run confirms 200.
  - KORALINK-API-Q (empty dispute id ×3) lastSeen **08-20** → stale.
  - Remainder = expected auth/ban/moderation 4xx noise (runbook: MINOR, no budget).
- `koralink-web` 24h: SW CSP localhost ×2 (dev-only, MINOR) + 1 info.
- **Outcome: nothing new to fix or board from Phase 1.6.**

## Previous-run verification (claims ≠ facts) — all 3 run #9 items CONFIRMED ✅

1. **P1-18 deleteSlot TOCTOU (b9ef749)** — source: conditional DELETE with
   `eq(is_booked,false)` + returning-check → 409 (partner.service.ts:627-637). Live:
   slot create 201 → delete 200 → re-delete 404 (run #9); specs green.
2. **P2-21 admin 403 handling (b9ef749)** — admin api.ts:40-56: 401-only logout+redirect;
   403 throws visible permission error. Confirmed in source.
3. **P1-2 gender filter + limit (306c74c)** — code: `normalizeGenderRule` (women checked
   first, get-matches.dto.ts:31-38) wired into findNearby SQL (:384-386), `LIMIT`
   whitelisted (:480). **LIVE E2E on the running service**: dev-login →
   `?gender=women` 200/1 row, `?gender=men` 200/0, `?gender=mixed` 200/0, `?gender=bogus`
   400, `limit=5` accepted. → promoted (P1-18/P2-21 already DONE in BOARD; P1-2 note
   updated).

## Findings (self-review; GLM reviewer timed out — merged nothing)

Rotation emphasis (run#10 % 4 = 2): **Admin console**.

1. **Admin→API endpoint audit: 21/21 admin-called paths resolve** against live controllers
   (disputes resolve, matches cancel, reports resolve, settlements pay/generate,
   transactions refund, users patch, venues decision/settings, partner CRUD+slots+generate,
   auth trio). No dead endpoints. List pagination whitelisted (`perPage` ≤ 100 in 8/8
   admin list DTOs — ListUsersDto read in full).
2. **4 stale mirror cards already fixed in code** (found while triaging mirror P1s):
   - t_61e5ff50 partner Admin-scope: `assertPitchAccess` has Admin bypass
     (partner.service.ts:459-471); `getDashboard/getVenues/getPitches` all thread
     `user.role` (partner.controller.ts:31,37,94) — **run #3 d93e1ea fixed this**.
   - t_09ccc370 POTM socket namespace: all 5 consumers (incl. NotificationProvider) use
     `createLobbySocket` → `/lobby` (run #5 58b3ba3).
   - t_0727919d DM idempotency: partial unique index `personal_messages_client_msg_uidx`
     (schema.ts:656) + pre-check (conversations.service.ts:211-218) — run #7 dfc671f.
   - t_222e530a WS ban/suspend/role: `handleConnection` re-reads the user row and
     disconnects banned/future-suspended/missing (app.gateway.ts:112-147) — run #6 4df0d4d.
3. **t_65ac117e ("private-match data leak P0") OVERSTATED**: `/messages` IS members-only
   (403, matches.service.ts:1006-1017); `findOne` strips embedded chat for non-members
   (:583-591); detail reads are a deliberate invite-link capability. Real residual:
   `GET /matches/:id/calendar` calls `findOne(id)` with NO viewer (matches.controller.ts:
   57-60) → ICS/Google-Calendar of a private match reachable by any authed user with the
   ID. **Demoted to new BOARD P2-22** (mirror card t_431f4f91 created + gated).
4. **Mutation-contract sweep clean**: 0 bare `.returning()` mutation returns, 0
   `eq(col,null)`, 0 `::uuid` casts across apps/api/src/modules. `conversations` `{messages,
   total, hasMore}` is a list envelope, not a mutation. `markNoShow` roster 404 confirmed
   (Sentry API-T signature proves the guard fires, not 500s).
5. **PWA sweep**: AuthBootstrap exists in `[locale]/layout.tsx` but `enabled: isHydrated &&
   !user` skips persisted users entirely; fetcher has NO 401 handling anywhere (only the
   profile logout button clears the token) → confirmed stale-shell gap → **built this run**.

## Board changes

- **P2-17 → DONE ✅** (built + live this run, `03d9baa`).
- **P1-2 note updated**: gender filter live re-verified on the RUNNING service (run #10).
- **NEW P2-22** calendar-route membership check (demoted from overstated P0 card) — TODO.
- Backlog additions: mirror-hygiene debt note (4 stale cards); GLM 600s cap process note
  (2nd consecutive; incremental-output instruction insufficient — run #11 should split
  into 2 scoped dispatches or treat self-review as primary).
- Kanban mirror: 8 verification/demotion comments (t_d40d913f, t_9c0d8b19, t_635278dc,
  t_61e5ff50, t_09ccc370, t_0727919d, t_222e530a, t_65ac117e) + built-comment on
  t_cc69d8d4 + created+gated t_431f4f91 (P2-22, verified `todo`/parents t_ce9a513a).

## Built this run — verification evidence (real terminal output)

**Slice: P2-17 stale-session self-heal** (commit `03d9baa`, Gates 0-3 docs in
`docs/plans/authbootstrap-stale-user/`):

- `fetcher.ts`: global 401 handler — clears `koralink_token` + `useAppStore.getState().
  logout()` + redirect `/login`; `/auth/*` excluded (wrong-OTP stays inline). Store import
  verified acyclic.
- `AuthBootstrap.tsx`: persisted users get ONE `/users/me` probe per browser session
  (sessionStorage flag `koralink_bootstrap_run`); failure → clear flag + `clearAuthToken()`
  + `logout()`.
- 9 new vitest cases: `test/lib/fetcher-401-selfheal.test.ts` (×4: 401 clears token+logout,
  FetchError message preserved, auth-path exemption, non-401 no-op) +
  `test/components/AuthBootstrap.test.tsx` (×5: renders nothing, stale+401→logout,
  cold+valid→login, session-flag skip, failure clears flag).
- **Gates**: `tsc --noEmit` 0 errors · vitest **227/227 (32 files)** · root
  `npm run build` **3/3** (after fixing the ESLint unused-import catch — `clearAuthToken()`
  reused instead of raw localStorage).
- PWA service restarted AFTER build (`npm run restart` — direct systemctl was
  approval-gated for cron): `active`, `GET /ar` 200, API health 200.
- No API/DB changes; no migration; no i18n keys (redirect-based UX).

## Blockers & decisions

- **Decision (Phase 3.5)**: no live P0 existed (Sentry/journal clean; the one P0 card was
  overstated) → picked the highest-value small P1/P2 with a real user-facing failure
  (stale-session trap) and finished it cleanly. Skipped t_635278dc remainder (param audit
  needs external-consumer inventory) and P1-12 admin i18n (too large for the window).
- **Sibling STATE.json touch**: harness flagged a sibling agent (cbcab07c) modifying
  STATE.json between my read and write; my write landed a correct run #10 state (valid
  JSON verified post-write). No foreign content visible in the committed lineage; flagged
  here per shared-tree discipline.
- **Process**: GLM reviewer 600s child cap killed the delegation 2nd run in a row —
  recorded in BOARD backlog with a concrete run #11 mitigation (split scopes or
  self-review-primary).
- P0-2 (payment provider) + P1-16 (padel scope) + P1-2 skill_level semantics remain
  Abdullah-decision-blocked.

## Next-run recommendation

1. Verify P2-17 (`03d9baa`): gates + spot-check a persisted-user cold load hitting the
   probe path.
2. **Split the reviewer**: two scoped GLM dispatches (Admin+API, then PWA+DB) to fit the
   600s cap, or make self-review primary and GLM supplementary.
3. Build candidates in priority order: **PWA search** (top product gap), **P1-12 admin
   i18n scaffolding**, small API polish from the P2 lane (t_546c5bc1 unban verb is already
   fixed per BOARD P2-14 — re-check before building; t_4b314844 recordTransaction replay,
   t_fc03ede9 scope-helper consistency are genuinely small).
4. Re-check t_546c5bc1/t_f6666278 mirror cards against BOARD P2-14/P2-9 before trusting
   them — this run found 4 more stale cards; the pattern suggests older mirror cards from
   the same review batch may also be stale.
