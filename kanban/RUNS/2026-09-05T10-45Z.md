# Repair Session — 2026-09-05T10:45Z (interactive, Abdullah-triggered)

**Mode:** manual-repair (NOT a scheduled factory run — Abdullah asked why the loop didn't fall
back to z.ai when OpenCode Go died). Investigated runs #33/#34 dead fires, fixed provider
wiring, rebuilt run #32's lost code slices.

## What happened (root cause of the dead fires)

1. **Run #32 (Sep 4 15:15Z) crashed at the final commit step** (after all gates passed). Its 5
   code fixes were still only in the working tree; only its report/docs/spec survived on disk.
   Its report and handoff notes were never committed either.
2. **Fire #33 (Sep 5 01:15Z) died at BOOT**: the cron job was still pinned `provider=opencode-go`
   (left over from the run #30 zai-child-401 failover week) and the profile `fallback_providers`
   ladder was **all four rungs on opencode-go** (glm-5.3-flash → minimax-m3 → qwen3.7-plus →
   deepseek-v4-flash, same provider). Go's weekly bucket was exhausted → boot 429 → ladder
   walked all-Go rungs → all 429 → session dead before skill_view/runbook/prestate could act.
   z.ai (the cheap primary) was never tried — it wasn't in the ladder at all.
3. **Fire #34 (Sep 5 10:15Z)**: scheduler retried the dead session (deepseek-v4-flash @ Go),
   429 again, died 10:35Z. `failure_streak` hit 3. **The 15:15Z fire today should now succeed**
   (wiring fixed — see below); if it does, it is factory run #34 proper.

**The irony:** z.ai was at **1% weekly / 15% 5h** the whole time, direct probes 200. The cheap
plan had full headroom; the wiring just pointed everything at the expensive plan.

## Fixes applied (wiring — all verified live)

- `delegation.provider/model/base_url` → `zai / glm-5.3-flash / https://api.z.ai/api/coding/paas/v4`
  (was opencode-go since run #30 week). **Live delegation child probe: `ZAI-DELEGATION-OK`,
  completed 6.5s** — the exact re-probe run #32's handoff asked for. DONE.
- Cron `03ed02bc8d04` pin → `--model glm-5.3-flash --provider zai` (verified in jobs.json).
- `config.yaml fallback_providers` → diversified: deepseek/deepseek-v4-flash → zai/glm-5.3 →
  opencode-go/glm-5.3-flash → opencode-go/minimax-m3 (backup: config.yaml.bak-20260905).
- `auth.json` deepseek entry: stale `exhausted` marker (Aug 26 billing 402) cleared so the pool
  re-tries it (backup: auth.json.bak-run33-20260905). Go's `exhausted` mark is accurate and kept.
- Watchdog (`factory-watchdog.sh`): +2 checks — cron pin must be `zai` (else alert), and
  fallback ladder must not point at a single non-zai provider (else alert).

## Fixes applied (code — run #32's lost slices, rebuilt from its surviving spec + report)

- `f40acd9` — admin users PDPL ghost guards: `status=active` adds `deleted_at IS NULL`;
  `update()` 409 ConflictException on any soft-deleted ghost BEFORE writes.
  `users.pdpl-guard.spec.ts` (run #32's surviving file) now passes **4/4** (was 2/4 failing
  before the rebuild — it was pinning the LOST service code).
- `45cdb91` — PWA: `ServiceWorkerUpdater` `reg?.waiting` (fixes live Sentry error
  KORALINK-WEB-8), PromoBillboard goTo() no longer re-arms auto-advance for reduced-motion
  users, InstallLanding useCallback dep fix (run #32's surviving uncommitted edit).
- **Deliberately NOT rebuilt:** admin Dockerfile `mkdir public` + `socket.io-client` dep — the
  Coolify path reportedly builds fine today and root-causing needs a Coolify-verified cycle
  (run #32's own A3 caveat). Board them as P2 if the next Coolify deploy breaks.

## Gates (all real, this session)

- jest API **338/338** (41 suites) · vitest PWA **362/362** (51 files — includes Abdullah's new
  host-onboarding tests) · tsc --noEmit api 0 + pwa 0 · eslint pwa src --max-warnings 0 clean
- **`npm run build` (root turbo): 3/3 successful, exit 0** (1m50s; 1 cached — the api cache from
  a pre-crash attempt; api source unchanged by cache semantics, jest+tsc re-proved it).
- PWA postbuild restarted koralink-pwa.service with the new build.

## Left in the tree (untouched, Abdullah's in-flight work)

host page + HostOnboarding components + DeleteAccountSheet/useScrollMemory test edits +
en/ar messages (+174 lines) + docs/plans/run32-postdeploy-hardening/. NOT staged, NOT committed.
Note: his test files pass (362/362) and lint is clean, so his WIP is healthy.

## State

- STATE.json: last_run=33 (this repair), zai re-probe blocker CLOSED, Docker pair carved to P2.
- kanban/LOCK.json removed at end of session.
- Go weekly resets ~Sep 7 — until then Go stays as ladder rungs only.
- Next scheduled fire 15:15Z today → should be factory run #34 (full cycle) on zai.
