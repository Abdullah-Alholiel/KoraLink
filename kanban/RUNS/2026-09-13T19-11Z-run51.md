# Factory Run #51 — 2026-09-13T19:11Z (manual extra cycle, gateway-hosted)

**Mode:** full (manual one-shot fire by Abdullah ~19:05Z after run #51-manual was infra-killed at 31m)
**Budget:** ~1h20m of ~3h (3 slices + full review + board + mirror)
**Models:** session `glm-5.3-flash` · reviewer `glm-5.3-flash` on `zai` (probe: **200**; prestate directive: weekly 1%, 5h 27% — healthy). **Both reviewers clean on zai (76s / 190s) — no child-401.**

## Preflight
- Lock acquired 19:11:30Z (none present — the killed run's stale lock had been removed).
- gh auth OK (fullstack-dev PAT); `git pull --ff-only` → already up to date; HEAD `499065f` on `staging`.
- Tree dirty only with known foreign files (`graphify-out/manifest.json`, `.local/`, untracked `verify-pr16-overflow-flow.mjs`) — recorded, never staged. Plus the killed run's orphaned half-slice: `apps/player-pwa/test/lib/sw-config.test.mjs` (untracked, INERT — `.mjs` never matched vitest's `.{ts,tsx}` include glob, and its factory-call design was wrong: the config's default export is the ALREADY-COMPOSED object, there is no factory to invoke).
- Delegation set to `glm-5.3-flash`/`zai`/coding base/reasoning-low after a 200 probe (per prestate directive; cron pin irrelevant).

## Health snapshot + Sentry triage (Phase 1.5/1.6)
- Services api/pwa/admin **3/3 active**; `/health` 200 (internal :3001); **zero journal errors** in 5h.
- Sentry (EU base, READ token): API top = KORALINK-API-B (CORS evil.example probe noise ×110, last Sep 11) + API-1B (email provider ×9 = boarded P1-41, expected until Brevo/Resend cutover). Web: WEB-2 ×6 + WEB-8 ×8 — both are what run #50's 4726498 fixes; stop-watch continues once promoted. WEB-6 (×52 viewport diagnostics) = standing noise. **Nothing new actionable; no new board items from Sentry.**

## Findings (GLM reviewers + parent, merged, evidence-cited)
- **Reviewer A (code quality) — verdict SHIP** on both killed-run commits:
  - 8e859b0 (P1-46): error branch correctly ordered isLoading→error→empty (clubs/[id]/page.tsx:355-374); `classifyError`/`errorKey` copy keys present in BOTH locales (verified programmatically); retry wired to `refetchMatches()`; hydration-safe (useNow-fed nowMs).
  - 499065f (P2-62): reset effect (useMessages.ts:225-227) ref-independent; the only ref consumer (batch mark effect :321-332) declared after the reset → sees a clean set on match switch; hook ordering legal; MW-6 spec covers m1→m2 rerender. Minor (noted, not blocking): reset could be folded into the open-mark effect to guarantee ordering by construction.
  - Standing bug-class sweep on touched files: no `::uuid`, no `eq(col,null)`, no `console.*`, no render-time `new Date()`, no z-index violations.
- **Reviewer B (product gaps + verification): all 5 prior-run claims CONFIRMED** (P2-58 watermark end-to-end incl. users.service.ts:289-331 real unread + matches.service.ts:2218-2238 guarded UPDATE→403; P2-59 hydration trio; P2-60 register:false + guarded registration; P1-46; P2-62).
- **Reviewer B product-gap leads → P2-63** (classified error copy ChatSheet:181-187 + reports:73; OfflineBanner coverage). Parent RE-SCOPED the banner half on inspection: 7 of the 9 claimed surfaces ALREADY render OfflineBanner (home, play, clubs, messages, my-games, reports, wallet); real gaps were only profile + personal-info.
- **Parent finding (the big one): the ENTIRE SW API runtime-caching table was inert in every environment.** Real API URLs are cross-origin `${host}/api/v1/...` (global prefix `api/v1`, apps/api/src/main.ts:118) and workbox RegExpRoute matches the FULL href including the query string (`regex.exec(url.href)`, verified in the bundled workbox-routing source; cross-origin patterns must match from index 0). The shipped patterns were `^https?://.*/api/<name>(/.*)?$` — no `v1` (fail #1), query-intolerant `(...)?$` tail vs `?lat=&lng=` (fail #2), and `/api/user` while the real path is `/users/me` (fail #3). Run #18's NetworkFirst flip, run #43's opaque-response hardening and the clubs/venues SWR never engaged offline. Bonus shadowing bug: workbox is first-match-wins and static-assets `CacheFirst` (`\.js$`) was registered BEFORE the payments NetworkOnly — the Moyasar SDK script (`cdn.moyasar.com/v1/moyasar.js`) matched CacheFirst (payment code cacheable).
- **Ops finding: the multica bridge is in a 5-min reclaim loop on mirror card t_3fd910ae** since 16:33Z (41 `[multica] moved to in_progress` comments; daemon PID 656772, bridge PID 3562313 with `--push-watch`). Needs ops attention outside this run; factory work on the same card had zero collision (bridge works in scratch workspaces).

## Previous-run verification
- Run #50 in-review items P2-58/P2-59/P2-60: **promoted to trusted** (Reviewer B confirmed all three; same-HEAD full gates this run: turbo 3/3, vitest 530→532, tsc 0).
- Run #51-manual commits 8e859b0 (P1-46) + 499065f (P2-62): **claimed and DONE** (Reviewer A SHIP + Reviewer B CONFIRMED, above).

## Board changes
- **DONE ✅:** P1-46 (8e859b0, verified) · P2-62 (499065f, verified, new row) · P2-57 (e305eb9, scope grew: inert-table fix) · P2-63 (1e3778f, closed; banner claim re-scoped).
- **NEW rows:** P2-62, P2-63 (both closed same run), P2-map-pin (e6e7a9f, closed — the long-standing MapPin dead-UI).
- Mirror: P2-57 → `t_a34d0303`, P2-62 → `t_ac2814a8`, P2-63 → `t_bde44ea9` (created + reclaimed + gated todo/parent t_ce9a513a); P1-46 `t_ad437de5` commented with verification evidence; MapPin `t_3fd910ae` commented (built, e6e7a9f).
- Owner queue unchanged (P2-51 demo-migrate, P2-47 cancel-purge, P1-38+P2-50 uploads, P0-2 payments, P1-41 Resend/Brevo console steps).

## Built this run (3 slices — each: full gates + separate commit)
1. **P2-57 (`feat(pwa)`, e305eb9):** runtimeCaching rewritten — NetworkOnly money/auth FIRST (un-shadowing Moyasar), match-detail-cache NetworkFirst 1h (the P2-57 goal: last-good match detail survives an offline revisit), feed NetworkFirst 60s collection-only, static CacheFirst, clubs/venues SWR 24h, users/me NetworkFirst 5min; ALL patterns anchored `^https?://[^/]+/api/v1/...` with `(?:\?.*)?$` query-tolerant tails and single-segment `[^/?#]+` scoping. Test: `test/lib/sw-config.test.ts` (rebuilt from the killed run's inert .mjs) — 12 cases incl. the **URL-REALITY suite** asserting patterns against real production-shaped URLs (the test class that would have caught the inert table at run #18). Sub-resources (messages/pom-result/calendar) match NOTHING — chat never served stale.
2. **P2-63 (`feat(pwa)`, 1e3778f):** ChatSheet + reports error states now render `t(errorKey(classifyError(error)))` what/why/next copy; OfflineBanner added to profile + personal-info (the two genuinely missing surfaces); specs CS-6/CS-6b/RPT-1/RPT-2 + banner pairs. Hygiene en-route: PersonalInfoPage.test phone fixture carried a **redaction-masked literal** (`+966****0001` — an artifact of an earlier session copying from masked tool output); replaced with a numeric seed-shaped value + `/^\+966\d{9}$/` shape-predicate assertion (no literal in source).
3. **P2-map-pin (`feat(pwa)`, e6e7a9f):** club address row is now a real Google Maps deep-link (`api=1&query=` + encodeURIComponent(name+address+city), target=_blank rel=noopener, aria-label, RTL-mirrored chevron); hero MapPin stays decorative. `clubs.openInMaps` EN+AR (parity 947=947). Specs MAP-1/MAP-2. The venues table has no coordinates (schema checked) — free-form query is the honest fix; no coords faked.

**Gates (every slice):** `npx turbo run build` **3/3 exit 0** (×3) · PWA vitest **78 files / 532 tests** · `npm run type-check` **0 errors** · no migrations (no DB surface touched — nothing to journal; Phase 4.5 sweep clean).

## Blockers & decisions
- **Multica bridge reclaim loop on t_3fd910ae** — surfaced, not fixed (ops item; possibly the push-watch bridge fighting the factory mirror). Record for the ops lane.
- Admin untouched (no admin items picked; P2-61 stays deferred to the next admin touch per run #50's deliberate deferral).
- Reviewer B's OfflineBanner claim partially refuted by parent inspection (7/9 already covered) — board row records the re-scope.
- Redaction trap re-confirmed: the masked phone literal was baked into a test by an earlier session; fixed + asserted by shape predicate. (Memory already warns about this; the fixture proves it keeps happening via read-patch loops.)

## Next-run recommendation
1. Verify run #51's 3 in-review slices (P2-57/P2-63/P2-map-pin) — spot-check: SW `match-detail-cache` populates after browsing a match detail (DevTools → Cache Storage) once the staging deploy bakes the new sw.js; ChatSheet failure shows classified copy; club address opens Maps.
2. Ops: investigate the multica bridge reclaim loop (t_3fd910ae) — stop the churn or scope it away from factory mirror cards.
3. P2-61 admin batch (ADMIN STATE CHECK first) — the highest-value remaining open item.
4. Watch Sentry WEB-2/WEB-8 for stop-accumulating once 4726498 promotes to prod.
5. Owner queue unchanged — P1-41 (Resend/Brevo) remains the single unblock for the PDPL email-receipt sub-cycle.
