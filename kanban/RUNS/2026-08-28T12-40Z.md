# Factory Run #9 — 2026-08-28T12:40Z (report written 12:58Z)

**Mode:** full (review → kanban → 2 fix commits: discovery gender filter + deleteSlot/admin-403)
**Budget:** ~3h of work (within the 3.5h cap)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on zai (probe: **200**,
5-token ping; delegation set before any model work) — reviewer #1 TIMED OUT at the 600s child cap
(27 tool calls done, no findings emitted); reviewer #2 completed in 564s with a full report.
Self-review (parent) ran in parallel throughout.

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260828T120609Z).
- GLM probe 200 → delegation `glm-5.3-flash`/zai/`https://api.z.ai/api/coding/paas/v4` set BEFORE review.
- `gh auth` OK (Abdullah-Alholiel); `git pull --ff-only` → already up to date at `1bccdb0`; tree clean.
- `HERMES_DELEGATED_CHILD_CONTEXT=1` was INHERITED in this cron session (run #3 trap recurred) →
  every kanban call wrapped with `env -u HERMES_DELEGATED_CHILD_CONTEXT`.

## Sentry / error-log triage (Phase 1.6 — Sentry API primary, EU base, both projects)

- Services: api/pwa/admin all `active`; `/api/v1/health` **200**; `/health` alias **200** (1bccdb0 live).
- journalctl 5h: **zero** error entries across all three services.
- `koralink-api` 24h: ~20 signatures. Auth/ban/moderation 4xxs = expected noise (runbook: MINOR).
  REAL items: **KORALINK-API-W `Failed query` ×1 @09:10:04Z — the 2026-08-28 INCIDENT**
  (restart 09:09:38 → boot-time `autoCompletePastMatches` ECONNRESET → unhandled rejection killed
  boot → **API down ~52 min**; fixed by parent session `1bccdb0` at 10:04Z; run #9 verified the fix
  compiled in dist + `/health` 200 + scheduler ticks individually try/caught). **KORALINK-API-E
  `GET /matches?limit=5` 400 ×3** → REAL gap (no `limit` in GetMatchesDto) → **FIXED this run**.
  KORALINK-API-B CORS `:3002` (last seen 08-17, pre-dates admin CORS fix — stale, left unresolved).
  KORALINK-API-Q `POST /admin/disputes//resolve` (08-20; admin list page href looks fine; can't
  reproduce path — MINOR/watch).
- `koralink-web` 24h: SW registration CSP violation localhost:3000 ×2 (MINOR; only affects
  localhost dev, prod origin unaffected) + 1 connectivity-check info.

## Findings (reviewer #2 GLM + parent self-review, evidence-cited)

**CRITICAL:** none.

**IMPORTANT (all 3 FIXED this run):**
1. **Discovery gender filter broken E2E** (self-review): PWA FilterBar sends
   `gender=men|women|mixed` (FilterBar.tsx:21-25) but `GetMatchesDto` validated
   `@IsIn(['Men Only','Women Only','Mixed'])` → EVERY gender chip tap 400'd the Play feed.
   → commit `306c74c`.
2. **deleteSlot booking TOCTOU** (reviewer #2, partner.service.ts:607-624): DELETE without
   `is_booked = false` predicate — a match booking the slot between SELECT and DELETE loses its
   slot (dangling `booked_match_id`). → commit `b9ef749`.
3. **Admin 403-as-401** (reviewer #2, admin/src/lib/api.ts:36-44): any 403 cleared the token +
   redirected to /login → one forbidden response logs the whole console out. → commit `b9ef749`.

**MINOR (boarded/backlog):** send-otp per-route throttle absent (P2-19 new; per-phone caps DO
exist) · `?limit` param class (P2-20 new, partially fixed) · GetMatchesDto rejects unknown params
generally · TopAppBar double-dead (already backlog; reviewer re-confirmed buttons lack onClick) ·
createVenue bare partial return · rbac.ts ConsoleAction flags never consumed.

**PRODUCT-GAPS (new, backlog):** PWA has NO search at all (static decoration only) · Admin has no
broadcast/announcement tooling · API has no venue ratings/reviews module.

**Clean-area verification (reviewer #2):** PWA z-index rules hold; adapter/queryKey/endpoint
consistency verified across useWallet/useMatches/usePitchSlots/useFollow; partner ownership checks
route through assertPitchAccess; generateSlots + push subscribe upsert on unique indexes; all
admin-called API paths exist. Prior-run spot-check: `1bccdb0` /health alias + onModuleInit
try/catch CONFIRMED in source.

## Previous-run verification (claims ≠ facts)

**P1-15 createDispute idempotency (run #8, 8fa95f9) — CONFIRMED ✅:** onConflictDoNothing insert
(matches.service.ts:1608) + winner re-read/attachAppeal (:1611-1640); `disputes_open_uidx` in
schema.ts:774-776 AND live in pg_indexes; full gates green. → **promoted to DONE**.
**Parent-session commit 1bccdb0 (boot hardening) — CONFIRMED live ✅:** dist grep hit + `/health`
200 + journal shows clean boot 10:03:32Z.

## Board changes

- P1-15 → **DONE ✅** (verified run #9).
- P1-2 → **WIP**: gender filter + limit DONE ✅ (live-verified); skill_level TODO (needs
  `matches.skill_level` column + semantics decision); radius cutoff stays descoped (product).
- **NEW P1-18** deleteSlot TOCTOU → **DONE ✅** (built + verified this run).
- **NEW P2-19** send-otp throttle → TODO. **NEW P2-20** query-param allowlist audit → PARTIAL.
  **NEW P2-21** admin 403 handling → **DONE ✅**.
- Backlog: 09:09Z incident post-mortem · reviewer-timeout process note · 3 product gaps ·
  createVenue bare return · rbac dead flags.
- Kanban mirror: commented t_387aa677 (verified) + t_43ad38ec (built); created + gated
  t_d92dfd69 (P2-19), t_635278dc (P2-20), t_d40d913f (P1-18), t_9c0d8b19 (P2-21) — all
  `todo` under t_ce9a513a. create→link dispatcher race hit on the first pair → `reclaim`
  applied per runbook; cards settled to `todo` within 60s.

## Built this run — verification evidence (real terminal output)

- `306c74c` fix(api): discovery gender filter + limit param
  - `normalizeGenderRule()` in get-matches.dto.ts; DTO accepts tokens + enum strings, bogus 400s;
    `limit` 1-50 (@Type/@Min/@Max); findNearby normalizes + `LIMIT ${limit ?? 50}`.
  - 10 new specs (matches.discovery-filters.spec.ts): normalization incl. women-before-men guard,
    DTO acceptance/rejection, normalized value in SQL params, LIMIT wiring.
  - **LIVE E2E**: dev-login → seeded real Women-Only match (201) → `?gender=women` 200 (1 row,
    hit=true) → `?gender=men|mixed` 200 (0 rows) → legacy `Women Only` 200 (1 row) → `?limit=1`
    200 → `?gender=bogus` 400 → cleanup DELETE 200.
- `b9ef749` fix(api,admin): deleteSlot booking TOCTOU + admin 403 no longer logs out
  - conditional DELETE + returning-check → 409 on lost race; 3 specs (partner.delete-slot.spec.ts);
    admin apiFetch 401-only logout, 403 → visible error.
  - **LIVE**: slot create 201 → delete 200 {deleted:true} → re-delete 404.
- Gates: `tsc -p apps/api` **0 errors** · API jest **105/105 (16 suites)** · PWA vitest
  **218/218 (30 files)** · root `npm run build` **3/3**.
- Services restarted AFTER build (npm run restart ×3): api/pwa/admin active, API health 200,
  PWA 307 (locale redirect), admin 200.

## Blockers & decisions

- P0-2 unchanged (real payment provider = Abdullah's vendor decision); P1-8/P1-14 still blocked.
- Decision: built gender-filter fix (broken user flow, silent failure) + both reviewer IMPORTANTs
  (data integrity + UX) instead of starting P1-16 padel (needs Abdullah's scope decision).
- Process: GLM reviewer children keep hitting the 600s cap when findings are emitted only at the
  end — run #10 should demand incremental per-area output or split scopes (recorded in BOARD
  backlog).

## Next-run recommendation

1. Verify P1-18/P2-21/P1-2(gender) claims (routine).
2. **P1-2 remainder**: `matches.skill_level` discovery filter — needs product decision on
   semantics (host skill? match-declared level?) before building; ask Abdullah or propose
   match-declared-level default.
3. **P2-17** AuthBootstrap stale-user (small, self-contained) or **P1-12** admin i18n scaffolding.
4. PWA venue/match search (new product gap) — likely the highest-value next feature slice.
