# Factory Run #8 — 2026-08-28T06:59Z

**Mode:** full (review → kanban → build createDispute idempotency + migration-tracking reconcile → board → report)
**Budget:** ~2.5h of work (well under 3.5h cap)

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260828T064030Z).
- Delegation self-heal: reset `delegation.model=deepseek-v4-pro` / `provider=deepseek` / `base_url=''`.
- `gh auth status` OK (Abdullah-Alholiel). `git pull --ff-only` → already up to date at `dffc703`.
- Tree clean on `main`. All 3 services `active`; API `/api/v1/health` **200** — note: API listens on
  `:3001` internally (NOT `:8443`, which is the HTTPS proxy); the runbook's `:8443` probe returns 000 by
  design. 0 journal errors in 5h.
- Baseline build 3/3 · API jest 83/83 · PWA vitest 218/218.

## Findings (reviewer `deleg_d83be24a`, deepseek-v4-pro, 362s + parent self-review)

**CRITICAL:** none new — 0 `::uuid` casts, 0 `eq(col,null)`, 0 API `console.*`, all 24 controllers
guarded.

**IMPORTANT (NEW, unboarded):**
- `createDispute` TOCTOU duplicate race — `matches.service.ts:1559-1607` select-then-insert with no
  unique index on `(match_id, reporter_id, type)` → concurrent double-tap opens two identical disputes.
  Same class as P2-9 (reports) but `disputes` was missed. **→ BUILT this run (P1-15).**

**Parent self-review finding (not in reviewer output):**
- **Migration tracking stale** — `drizzle.__drizzle_migrations` had 19 content-hash rows vs 20 journal
  entries. SHA256 audit: `0002_mushy_fenris.sql` (`88b01448…`) and `0008_light_karnak.sql`
  (`eb3dc001…`) were edited in-place after apply and their new hashes never recorded → `npm run
  db:migrate` would re-apply 0002 (`CREATE TYPE "BookingMode"`) and fail. Verified live DB already has
  ALL objects from current 0002/0008 content, so reconciling was safe. **→ FIXED this run (live-DB).**

**MINOR (added to backlog):** `TopAppBar.tsx` wholly orphaned dead code · `reports.service.ts:64` bare
return · `settlements.service.ts:155` bare array return · seed.ts still seeds dead `rating`.

**PRODUCT-GAPS (NEW):** P1-16 no padel support (football-only enums/schema) · P1-17 no waitlist/overflow ·
P2-18 no recurring/standing matches. Re-affirmed P1-2/P1-12/P1-13/P2-7/P2-8.

## Previous-run verification (claims ≠ facts)

**P1-11 (DM idempotency, run #7, dfc671f) — CONFIRMED ✅:**
- `conversations.service.ts:242` `.onConflictDoNothing()` insert; `:250-270` winner re-read; side-effects
  (`markRead` `:275`, activity `:287`, push `:298`) skipped on duplicate. Partial unique index
  `personal_messages_client_msg_uidx` in schema.ts:656-658 + migration 0019.
- Live `personal_messages_client_msg_uidx` present in `pg_indexes`. Reviewer independently CONFIRMED.
- Baseline green (build 3/3, jest 87/87, vitest 218/218).

→ **P1-11 promoted to DONE ✅.**

## Board changes

- **P1-11** → DONE ✅ (verified run #8).
- **NEW P1-15** createDispute TOCTOU → IN-REVIEW (built run #8, `8fa95f9`).
- **NEW P1-16** no padel support (football-only) → TODO (needs Abdullah scope decision).
- **NEW P1-17** no waitlist/overflow → TODO.
- **NEW P2-18** no recurring/standing matches → TODO.
- Backlog: migration in-place-edit note · TopAppBar dead code · reports/settlements bare returns.

## Built this run — verification evidence (real terminal output)

- `npx tsc --noEmit -p apps/api/tsconfig.json` → **0 errors**.
- `npx jest` (API) → **87/87 tests, 14 suites** (+4 new `matches.dispute-idempotency.spec.ts`).
- `npx vitest run` (PWA) → **218/218 tests, 30 files** (unaffected, green).
- `npm run build` (root) → **3/3 tasks successful** (API rebuilt).
- `npm run db:generate` → `0020_medical_norman_osborn.sql` (`CREATE UNIQUE INDEX disputes_open_uidx …`).
- Migration tracking reconciled: inserted 0002/0008 hashes into `drizzle.__drizzle_migrations` (now 22
  rows); removed the pre-inserted 0020 hash before migrating.
- `npm run db:migrate` → **applied 0020 only** (0000-0019 correctly skipped — proves the reconcile).
- Live `disputes_open_uidx` confirmed in `pg_indexes`.
- `systemctl --user restart koralink-api.service` → `active`, health **200**.

## Blockers & decisions

- **P0-2** unchanged — real payment provider is Abdullah's vendor decision. P1-8/P1-14 still blocked.
- Decision: built createDispute idempotency (data-integrity, mirrors P2-9 reports) over P1-2 discovery
  filters — and reconciled migration tracking first as a prerequisite (any future migration would have
  failed otherwise).

## Next-run recommendation

1. **Verify P1-15** (createDispute idempotency) → promote to DONE.
2. **P1-2** discovery filters (`skill_level` filter + `radius_km` hard cutoff) — now unblocked (db:migrate works).
3. **P1-16** padel scope — needs Abdullah's decision; if in scope it's a whole-sport slice.
4. **P2-17** AuthBootstrap stale-persisted-user (small).
