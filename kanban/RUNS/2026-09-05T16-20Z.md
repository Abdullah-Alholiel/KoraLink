# Factory Run #35 — 2026-09-05T16:20Z

**Mode:** full (owner-directed priority)
**Budget:** ~1h55m used of ~3.5h (single priority item + review + board + report; stopped per two-item cap in the run directive)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on zai (OWNER DIRECTIVE 2026-09-05 15:45Z — overrides run-#15 glm-5.3 adoption; probe: **200** healthy, weekly 1%, 5h 16%) · deepseek probe: 402 (no credits) — irrelevant, GLM healthy

## Preflight
- Lock: none present → created `kanban/LOCK.json` (run 35, 15:20Z). Released at end of run.
- gh auth OK (fullstack-dev profile config dir); tree dirty with **Abdullah's host-onboarding WIP**
  (player-pwa host page + HostOnboarding components/tests + messages en/ar) — preserved untouched,
  never staged. Drove the API-only scope decision (run #34 staging-collision lesson).
- `git pull --ff-only`: already up to date at 2c721df.
- Delegation set per run directive: `delegation.model glm-5.3-flash`, provider `zai`, base_url z.ai coding endpoint.
- Strix monthly scan: **not due** (due 1st–3rd; today 5th; no strix_scan= entry needed).
- Services at start: api/pwa/admin all active.

## Sentry / error-log triage (Phase 1.6)
- Source: Sentry API (de.sentry.io, SENTRY_READ_TOKEN, 24h window, both projects).
- Signatures: KORALINK-API-Z / -Y / -W ("Failed query") — all match run #34's already-triaged
  items (historical / probe-artifact / transient); WEB-8 events all predate the 10:50Z fix deploy.
- **No new P0/P1 from Sentry.** No fixes needed from this phase.

## Findings (reviewers + self, merged, evidence-cited)
- **Reviewer B confirmed ALL run #34 claims** (6ef1d77 ghost guard, 148acb2 no-show visibility,
  1dea7b0 decision guards) — promoted IN-REVIEW → verified DONE on the board.
- **NEW P1-42** (Reviewer B): WS chat has NO rate limiting — app.gateway.ts message handlers
  unbounded (HTTP has ThrottlerGuard + @Throttle; sockets have nothing). Flood risk to rooms,
  push fan-out, and now email fan-out. Size S-M.
- **NEW P2-50** (Reviewer B): `users.avatar_url` is dead schema — no endpoint ever mutates it;
  players can't set a photo. Fold into the P1-38 upload-pipeline owner decision.
- Reviewer A open IMPORTANT: `matches.service.ts:2262` removePlayer reverts status → 'Open' with
  NO status predicate (race with concurrent join flipping Open→Full can be silently reverted).
  Carved as P2-49 follow-up (guarded-UPDATE pattern like auto-cancel at :423).
- OS research (Phase 1.7): Resend contract verified via zread on resend/resend-node —
  `POST /emails` {from,to,subject,html,text}; no SDK needed (never-guess-signatures rule).

## Previous-run verification
Run #34 items: build/tests green today, spot-checks + Reviewer B independent pass → all CONFIRMED.
(Commit discipline: verified before promoting; no claims trusted on faith.)

## Board changes
- **P1-41** → **BUILT ✅ (API layer, run #35: 16d4cc1)**; residual PWA email UI marked IN-REVIEW
  (blocked on Abdullah's messages WIP).
- **P0-9** → **DONE ✅ — absorbed by P1-41** (decision made: Resend HTTP API; OTP-email-backup
  remains future scope).
- **P1-39/P1-40** → resolved per owner directive (P1-40 REJECTED — instant-join is the product).
- **ADDED P1-42** (WS chat rate limiting, TODO) · **ADDED P2-50** (avatar dead schema, TODO).
- STATE.json: last_run 35, commits [16d4cc1], in_review_items = run-#35 verification recipe
  (5 concrete checks for run #36), open_blockers refreshed (24; new: Resend provisioning,
  PWA email-field residual, P1-42, removePlayer guard follow-up).

## Built this run (slices, commits, verification)
**`16d4cc1` — P1-41 transactional email layer (API layer). 20 files, ~+1,450 lines.**
1. **Migration 0033** (`drizzle/0033_user_email_columns.sql`, hand-written + journal per convention):
   `users.email` (optional), `email_verified_at`, `email_muted` kill-switch; partial unique index
   (verified only) + suppression index; NULL email = never emailed; PDPL purge anonymizes.
   **Applied to live DB** (columns + indexes verified via information_schema/pg_indexes) +
   bookkeeping row in `drizzle.__drizzle_migrations` (id=34, hash12 fde425a51095, sha256-verified).
2. **MailerModule** (`apps/api/src/modules/mailer/`): Resend HTTP transport, **self-disabling**
   (empty RESEND_API_KEY/MAIL_FROM → logs instead of sending — safe dev/test default, zero new
   deps); `fillVars` HTML-escapes interpolated vars (single-escape policy — double-escape bug
   caught by jest and fixed); **20 bilingual en/ar templates (ar-first RTL)**, server-side copy
   (API has no next-intl); suppression = verified + unmuted + alive.
3. **Endpoints**: PATCH /email/me (set email; guarded + throttled), POST /email/me/resend,
   GET /email/verify (signed token, shared JWT secret, bilingual RTL result page).
4. **Event hooks**: `activities.record()` choke point (16 tx verbs via EMAIL_ACTIVITY_VERBS,
   queueMicrotask fire-and-forget, chat deliberately excluded) — every transactional event now
   fans out to email alongside push, zero per-callsite churn; match reminders (matches.service),
   PDPL soft-delete receipt + purge warning (users.service); purge nulls email columns.

**Verification (all real):** tsc --noEmit exit 0 (first pass) · jest **358/358** (43 suites,
+15 mailer specs: template parity, RTL, XSS escaping, suppression matrix, transport gating,
verb filter) · PWA vitest **362/362** (51 files) · eslint --max-warnings 0 clean · root
`npm run build` **3/3 tasks, exit 0** · migration live-verified · API restarted AFTER build
(`npm run restart`, health 200) · **live E2E: GET /email/verify renders the bilingual RTL
page (200), zero journal errors post-restart.**

**Deliberately deferred:** PWA profile email field + emailMuted toggle + i18n — the messages
files carry Abdullah's uncommitted hostOnboarding keys (staging collision, run #34 lesson);
documented in `docs/plans/run35-email/FRONTEND_INTEGRATION.md` + plan amendments.

## Blockers & decisions
- ADMIN STATE CHECK: apps/admin + partner/admin API surface clean — not touched this run.
- API-only scope: owner WIP owns the PWA messages files; PWA half of P1-41 waits for it. Not a
  blocker for the API slice — feature is usable end-to-end at the API level once keys land.
- Resend provisioning is an OWNER step: set RESEND_API_KEY + MAIL_FROM (+ optional API_PUBLIC_URL)
  in apps/api/.env, restart API. Until then the mailer is log-only by design.
- Governance cleanups honored: P1-40 REJECTED row, P0-9 absorbed, zai health line refreshed.

## Next-run recommendation
1. **P1-41 PWA half** as soon as Abdullah's host WIP lands (email field + emailMuted toggle +
   en/ar keys; integration doc pre-written in docs/plans/run35-email/FRONTEND_INTEGRATION.md).
2. **P1-42 WS chat rate limiting** — size S-M, cleanest next autonomous build.
3. removePlayer status-predicate guard (P2-49 follow-up) — size S.
4. Owner asks outstanding: Resend keys; P1-38+P2-50 upload pipeline (now one decision);
   P2-47 cancel-purge vs restore.
