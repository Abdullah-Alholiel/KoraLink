# Factory Run #53 — 2026-09-15T01:17Z

**Mode:** full (recovery #2: the 2026-09-14T15:17Z fire DIED mid-run — lock pid 277842
DEAD, zero STATE updates — this run adopted its committed-but-unpushed OTP hardening
AND its uncommitted in-flight slice, then continued the normal cycle)
**Budget:** ~75 min active (01:17–02:35Z) vs 3.5h slot. All gates green throughout.
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` · provider
`zai` · probe **200** (5h 27%, weekly 0% per prestate directive) · delegation config
reset in Phase 0 (model/provider/base_url + reasoning_effort=low). Both reviewers ran
clean on zai (A 163s / B 137s) — **third consecutive clean zai run**; policy stays
try-zai-first, failover on first child error. OmniRoute probe: timeout (:20128 down)
— irrelevant, not needed.

## Preflight
- Stale `kanban/LOCK.json` (started 15:17:53Z Sep 14, pid 277842 **DEAD**) — cleared,
  re-acquired. Dead-run pattern now hit 3× (run #51-manual, #52 original, this one);
  watchdog suggestion repeated in Next-run.
- Tree dirty with the dead session's work: **committed-but-unpushed `1bdf0fc`**
  (OTP single-use hardening — its jest 65/525 + turbo claims re-verified locally
  before pushing) plus an UNCOMMITTED coherent slice (OfflineBanner on match detail
  + structure test) and foreign files (`scripts/verify-pr16-overflow-flow.mjs` mtime
  Sep 9, `.local/` scratch, graphify-out churn — all left unstaged per policy).
- `git pull --ff-only` clean; gh auth OK (Abdullah-Alholiel); NODE_ENV unset.
- Delegation self-heal applied; Strix **not due** (Sep 15; window 1st–3rd, STATE
  carries `strix_scan=not-due-2026-09`).

## Sentry / error-log triage (Phase 1.6)
Sources: Sentry API (de.sentry.io, read token) — koralink-api 24h quiet (top
signatures unchanged: API-B CORS probe noise n=110 stale, API-1B email 503 = boarded
P1-41 provider gap); koralink-web 15 issues; journalctl 5h = **zero err lines** on
all three services; API /health 200; gateway heartbeat fresh.

- **WEB-D (hydration) n=7, lastSeen 23:10Z Sep 14 — still the ZOMBIE-TAB signature:**
  latest event URL `https://aa.tail2948f9.ts.net:10000/en/play` (port CLOSED since
  Sep-13 cleanup), release `6bb22d3c` (prod cutover SHA, pre-fix). Continuation of
  the known class — NOT a 5th survivor. Root fixes (4726498 + ecfab67) reach prod at
  the next promote; watch WEB-D stop accumulating then.
- WEB-6 (n=53) intentional ViewportHeightSync diagnostic — MINOR/noise.
- WEB-2/8: no new events since Sep 13/12 — P2-60 fixes awaiting promote. Watch holds.
- Nothing new to triage on the API side; no new board items from Sentry this run.

## Findings (GLM reviewers + parent, merged, evidence-cited)
- **Reviewer A (code quality + security, API focus) — 1bdf0fc APPROVE on all six
  axes:** `runExclusive` releases on throw (`tail.finally(release)`,
  otp-store.service.ts:142-150); no same-key nesting anywhere; fail-counter
  semantics correct; `otpMatches` genuinely constant-time (timingSafeEqual + dummy
  burn, otp-compare.ts:15-31); token mint outside the lock; sendOtp untouched.
  **Its one IMPORTANT-grade catch: `verifyPhoneChange` charged the shared fail
  counter ONLY when the claim returned a code** — a missed claim (expired/consumed
  code) 401'd free → replay races could brute-force the 6-digit space with the
  lockout never advancing. FIXED this run (95200e7). Minors recorded:
  incrementFail-outside-change-lock under-charge (rate-limit weakening only —
  cosmetic given TTL), resetFails-after-lock, banner `className` duplicating the
  `inline` variant defaults, structure test could assert wiring (APPLIED).
- **Reviewer B (verification + product gaps): ALL 3 run-#52 in-review claims
  CONFIRMED** with file:line evidence (see Previous-run verification). PWA i18n
  parity 947/947 (programmatic, symmetric diff empty). New leads triaged:
  - "DiscussionCard has no message-report UI" — **REAL but re-scoped**: DiscussionCard
    is an inbox row, not a message; the genuine gap is the lobby ChatSheet (DMs
    already ship per-message reporting, messages/[id]:317-322) → **built as P2-66**.
  - "No send-rate limiting on conversations" — **REFUTED as stated**: global
    ThrottlerGuard IS wired (app.module.ts:30, 60/min/IP); a chat-specific send-rate
    remains a product call, not boarded.
  - "No message delete/retract" — real gap, product decision needed; noted on P2-66
    row, not boarded.
  - "No CSV export in admin/partner" — MINOR backlog polish, not boarded.
- **Parent refutations:** Reviewer A's `withTimestamp` findings (reports.service.ts
  :106/:179/:217, disputes.service.ts :98/:227/:267) — **all sites already wrap
  `withTimestamp(...)`**; the bare `match_players` set is CORRECT (table has no
  `updated_at` — the known 500 trap). Bug-class sweep CLEAN: `::uuid` 0 (comment
  only), `eq(col,null)` 0, console.* 0, FOR UPDATE on all 4 match transitions +
  wallet atomic-delta path.

## Previous-run verification
Run #52's 3 in-review items → **promoted DONE** on Reviewer-B confirmation:
- **ecfab67 (P2-59 survivor + P2-64):** DiscussionCard.tsx:7,73 `useNow()` posture,
  no render-path `new Date()` for display; lib/format.ts:27/38/53/66 all four
  helpers; MatchDetailsForm 4 sites (:188,190,211,229); time-ns i18n both locales.
- **86723ea (P2-61):** venues/[id]/page.tsx:24 `useTranslations('venueDetail')`;
  venueDetail 18 keys ar == 18 en; EmptyState imported + JSX-rendered in all 5
  surfaces (pitches:8,190; partner/venues:8,93; partner/pitches:8,236;
  partner/matches:10,236; partner/matches/[id]:8,151).
- **1066a29 (club banner):** clubs/[id]/page.tsx:29-30 imports, :135
  useOnlineStatus(), :141 `<OfflineBanner isOffline={!isOnline}` rendered.
- **P2-20 (PARTIAL → DONE, zero code):** param audit — PWA discovery sends exactly
  the 11 whitelisted params (useMatches.ts:95-112); `/users/me/matches` is
  param-less (users.controller.ts:68-73); admin/partner use their own endpoints +
  DTOs; no external consumer found in docs/ or scripts/. All 11 params carry
  ApiPropertyOptional → OpenAPI allowlist already documented.

## Board changes
- **DONE ✅ promoted (verified):** P2-59-survivor+P2-64 (ecfab67) · P2-61 (86723ea) ·
  P2-offline-banner-club (1066a29) · P2-20 (audit closed, zero code).
- **NEW rows, built + closed same run:** **P2-65** (match-detail offline banner +
  structure guard, a4adca6) · **P2-66** (lobby-chat message reporting, ed5b7f5) —
  also carries the refuted-leads record (throttle/delete) so they don't re-surface.
- **Adopted + pushed:** 1bdf0fc (dead session's OTP single-use hardening — no board
  row; adjacent auth hardening on the verify path, covered by t_12d2fac6's family
  with an explicit correction comment that the JWT-in-body finding stays OPEN).
- **Backlog:** OS-research card added — `obscenity` (MIT, maintained) for the
  recorded UGC-profanity gap; caveat: English dataset only, Arabic dataset is the
  real work of any adoption cycle.
- **Mirror (Hermes kanban, board koralink-factory-loop):** verified-comments on
  t_7dded2f0 (P2-59), t_04d66a1e (P2-61), t_e881f155 (P2-64), t_bde44ea9 (banner
  lineage); built+audit comments on t_12d2fac6 (with the OPEN correction) and
  t_635278dc (P2-20); P2-65 → **t_94553c59**, P2-66 → **t_f95b84de**, both created
  + gated under t_ce9a513a (verified: status todo, parents correct). Parent
  untouched; no mirror card completed/blocked. One CLI quoting slip posted a `-`
  comment on t_12d2fac6 — immediately followed by the proper correction comment.

## Built this run (slices, commits, verification evidence)
1. **`1bdf0fc` (fix(api), ADOPTED from the killed session, verified + pushed):**
   OTP verifies single-use + constant-time across login/email/phone-change —
   per-key promise mutex (`withVerifyLock`), atomic claim/consume for the phone
   flip, 7 new OtpStoreService cases + replay spec. Verification THIS run: jest
   65/525 re-run locally before push; Reviewer A six-axis APPROVE.
2. **`a4adca6` (feat(pwa), P2-65, ADOPTED in-flight slice):** OfflineBanner +
   useOnlineStatus on match detail (inside MobileFrame, above scroller — same
   posture as club detail) + `test/structure/offline-banner-coverage.test.ts`
   pinning BOTH surfaces (presence AND `isOffline={!isOnline}` wiring after
   Reviewer A's hardening). Gates on commit: turbo 3/3 (1m35s), vitest 80f/548t,
   pwa tsc 0, structure test 2/2.
3. **`95200e7` (fix(api), security):** phone-change verify charges the fail counter
   on EVERY miss — the `if (storedCode)` guard removed; lockout still throws 429
   without charging further; external contract unchanged (401/429/409 byte-identical).
   +2 specs (missed claim charges; lockout does not over-charge). Gates: jest
   65 suites/527 tests (+2), `turbo run build --force --filter=api` exit 0 (0
   cached, dist grep-verified), API service restarted AFTER dist, /health 200,
   journal clean.
4. **`ed5b7f5` (feat(pwa), P2-66):** lobby-chat message reporting — ghost
   MoreVertical button on RECEIVED messages only (DM pattern/placement) → shared
   ReportSheet (subjectType='message'); new `report.chatMessage` key
   ('Lobby message' / 'رسالة في غرفة المباراة') — the DM's 'Direct message' label
   would read wrong in a lobby; specs CS-9 (exactly one report button, received
   only) + CS-10 (opens sheet with lobby label + title). Gates: vitest 80f/550t,
   pwa tsc 0, parity 948/948, turbo 3/3 (1m53s). PWA service restarted by postbuild.

NO DB surface touched → no migrations, nothing to journal (Phase 4.5 sweep clean:
`git status` carries only foreign graphify-out churn + the foreign script +
`.local/` scratch, all deliberately unstaged).

## Blockers & decisions
- **Dead-run pattern #3:** a factory session dying at ~39 min mid-run now has a
  3× track record. The recovery recipe works, but detection is manual each run.
  Recommendation (repeat): a cron prestate check — LOCK present + pid dead → alert
  + note in prestate output so the next run adopts without re-diagnosing.
- **Delegation-model inheritance:** this session inherited `delegation.model
  glm-5.3-flash` correctly from run #52's Phase 0; Phase 0 probe + reset ran
  anyway per the runbook (cheap, idempotent). No stale-config damage found.
- **Decision: run number 53** (the dead 15:17Z session self-labelled its commit
  "Run #53" and never updated STATE; STATE said last_run 52 → incrementing to 53
  keeps continuity AND matches the dead session's own label).
- **Decision: Reviewer-B chat-throttle lead refuted, not boarded** — the global
  ThrottlerGuard is wired and covers the route; a chat-specific send-rate limit is
  a product decision (recorded on the P2-66 row).
- Strix: not due (Sep 15; window 1st–3rd).

## Next-run recommendation
1. **Verify run #53's in-review items (claims ≠ facts):** 95200e7 (spot-check the
   2 new spec cases + confirm no OTP regression in Sentry/journal), ed5b7f5
   (CS-9/10 still green; parity 948/948), a4adca6 (structure test pins both
   banner surfaces). All were gated this run but the next run re-verifies.
2. **Watch Sentry WEB-D after the next promote** — it should STOP accumulating
   once 4726498+ecfab67 reach prod; if hydration errors reappear from `:9450`
   staging URLs, hunt a 5th survivor then. WEB-2/8 should also go quiet (P2-60).
3. **Dead-run watchdog:** implement the prestate check (LOCK pid dead → alert)
   before the pattern bites during an unattended window.
4. **Owner queue unchanged:** P1-41 (Resend/Brevo) still the single unblock for
   the PDPL email-receipt sub-cycle; P2-51/47/38+50 need Abdullah's calls. NOTE
   for any promote: Neon migrations FIRST (promote precondition), and P2-51's
   Render demo drift re-arms with every new migration until the owner call lands.
5. **Buildable-without-owner candidates after verification:** P2-56 is deferred by
   its own terms; the open TODO lanes carry P1-27 (wallet withdraw — PARKED on
   KSA company), P2-18 (recurring matches, multi-hour), P2-7 (offline mutation
   queue, Serwist decision). Realistic next small items: the mirror card
   t_a8904eed (POTM push locale — small API fix) or t_f6666278 (settlement tx
   race — bounded).
