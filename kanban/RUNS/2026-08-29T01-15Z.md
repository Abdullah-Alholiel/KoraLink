# Factory Run #12 — 2026-08-29T01:15Z

**Mode:** full
**Budget:** ~50 min actual (01:15Z preflight → 02:07Z lock release) vs 3.5h — well under.
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe: **200 OK**, 5-token ping on z.ai coding base)

## Preflight

- Lock `kanban/LOCK.json` taken at 01:15Z (no young lock present).
- GLM probe 200 → delegation set to `glm-5.3-flash` / zai / coding base (+`reasoning_effort low`).
  No mid-run re-switch needed; delegation left clean for next run.
- `HERMES_DELEGATED_CHILD_CONTEXT=1` was INHERITED in this session (run #3 trap) → all kanban
  calls run with `env -u HERMES_DELEGATED_CHILD_CONTEXT`. Recorded for next run's preflight.
- gh auth OK; `git pull --ff-only` clean at `8eb8774`; tree was clean at start (no foreign edits
  all run — none staged, none trampled).

## Health snapshot (Phase 1.5)

api/pwa/admin all `active`; `/api/v1/health` 200; journalctl err-scan over 5h: **zero errors**.

## Sentry / error-log triage (Phase 1.6)

Source: Sentry API (EU `de.sentry.io`, SENTRY_READ_TOKEN) — both projects, 24h window.
All signatures known/historical (OTP-throttle 429s, legacy `/matches/nearby` 400 probes,
run #10's CORS probe blip). **Nothing new, nothing boarded, nothing to fix.**

## Previous-run verification (run #11 claims)

- **P2-10 wallet replay — RE-VERIFIED PASS (live, restarted service):** fresh idempotency key
  `run12-verify-a` → 201, credited once; same key replay → 201 `{replayed:true, <SAME ledger
  id>}`; balance moved exactly once (726→727). Reviewer B independently confirmed code+7 jest
  cases. Mirror card t_4b314844 commented.
- Run #11's "PWA search missing" note: DISMISSED as garbled — client-side search exists in both
  play feed and clubs page (Reviewer A + direct reads).

## Findings (GLM reviewers + self, merged, evidence-cited)

Reviewer A (code quality, PWA focus) + Reviewer B (product gaps + verification) — both returned
in <4 min (reasoning_effort low; the run #9/#10 timeout fix holds for a 3rd consecutive run).

**Refuted (not boarded):** Reviewer B's P0 "no notifications inbox" is a FALSE POSITIVE —
`activities.controller.ts:16` `@Controller('users/me')` serves `/notifications`, `/unread-count`,
`/read`; consumed by NotificationSheet/NotificationBell/BadgeHydrator via `useNotificationsFeed`.
Verified in code before boarding;Reviewer B's wallet-verification section was accurate.

**Boarded:**
- **[P1-19] Play feed silently capped at 50 matches** — plain `useQuery`, no page/offset
  (get-matches.dto.ts:129 limit≤50 no cursor), API `hasMore` discarded; search filters only the
  loaded ≤50. (Reviewer A)
- **[P1-20] No push prefs/quiet hours** — notifications.service.ts:106-121 broadcasts to all
  subscribers; profile has only a global toggle; Saudi users can get night pushes. (Reviewer B,
  scope corrected by self-review)
- **[P2-23] Reporters never learn outcome** — no reporter-facing status/closure. (Reviewer B)
- **[P2-13 expanded] Clubs decorative UI** — dead MapPin (already known) PLUS "Nearby" and
  "Available Now" pills `return true` (clubs/page.tsx:44-49), "Top Rated" shows all → 3 of 4
  pills no-ops. (Reviewer A)
- Minor backlog notes: FilterBar "≤ {p} SAR" hardcoded English; camera-icon on profile actually
  navigates to personal-info; WhatsApp href LTR-only construct; `Date.now()`-in-render latent
  hydration risk (4 sites, currently safe); bare `.returning()` at matches.service.ts:1101/:1624.
- Standing bug-class sweep (Reviewer A): `::uuid` casts 0, `eq(col,null)` 0, console.* in API 0,
  z-index compliant, mutation contract on join/leave/start/complete/cancel FIXED.
- OS research (Phase 1.7): RN-native map SDKs only (react-native-maps/rnmapbox) — KoraLink is a
  PWA → **none adopted, no card** (1 web_search_prime call, under budget).

## Board changes

- P1-2 updated: **time-of-day filter FIXED + LIVE-VERIFIED** (added to gender/limit WIP; skill_level remains).
- P2-10 updated: run #12 RE-VERIFIED annotation.
- P2-13 expanded (clubs decorative pills).
- NEW: P1-19, P1-20, P2-23.
- Mirror: 3 cards created + gated under t_ce9a513a (t_f3c34bf8, t_bf317cf9, t_3f4aab1c — all
  verified `status: todo`, `parents: t_ce9a513a`); comments on t_43ad38ec (built),
  t_4b314844 (verified), t_c6621368 (scope expanded). No completes/blocks on gated cards.

## Built this run

**Discovery time-of-day filter** (P1-2 slice — "padel tomorrow *evening*" now answerable):

- **API:** `GetMatchesDto` `TIME_WINDOWS` presets (`morning` 04–12, `afternoon` 12–17,
  `evening` 17–23, `night` 23–04 Riyadh local; night wraps midnight as OR-form) + optional
  `time` param → one additive Riyadh-TZ SQL clause in `findNearby` (mirrors `dateClause`; no
  migration, pure query slice).
- **PWA:** FilterBar "Time of day" chip section (toggle/re-tap clears, counts in activeCount,
  included in reset), `useMatches` `time` passthrough, play/page initial state,
  en/ar i18n (`filters.time.*` = وقت اليوم/صباحاً/عصراً/مساءً/ليلاً).
- **Tests:** 4 new jest specs (window bounds incl. midnight wrap + no-param negative) and
  4 new vitest FilterBar cases (render/set/clear/switch, portal-aware).
- **Follow-up fix commit:** 3 PRE-EXISTING eslint violations in COMMITTED files (CI
  `--max-warnings 0` gate was already red): missing `locale` dep (usePushNotifications.ts:79),
  unused params in PostMatchSection socket mock (rewritten to the socket.test.ts wrapper
  pattern — keeps `mock.calls[0][0]` working), unused React import (usePwaInstall.test).

**Verification evidence:** API jest **116/116** · PWA vitest **231/231** · API tsc **0 errors** ·
PWA tsc **0 errors** · PWA eslint **clean** · root `npm run build` **3/3** (1m56s) · API service
restarted AFTER build · **live probe on restarted service:** `?time=evening` 200 → exactly the
18:36-Riyadh match; `?time=morning` 200 → 0 rows; `?time=bogus` → 400; unfiltered → 200; both
filtered results strict subsets of unfiltered (Riyadh-hour math checked in Python).

**Commits (all pushed to main, `8eb8774..156775d`):**
- `12f9f2d` feat(discovery): time-of-day filter — API time windows + FilterBar chips
- `035bad9` fix(pwa): clear pre-existing eslint violations (CI --max-warnings 0 gate)
- `96440d3` docs(kanban): run #12 review — time-filter built+verified, P1-19/20 + P2-23 + clubs no-op pills boarded
- `156775d` chore(graphify): refresh graph artifacts (run #12)

## Blockers & decisions

- **Decision:** build item = time-of-day filter (finishes run #11's real discovery gap;
  small, query-only, no migration, no external dependency). Feed pagination (P1-19) is the
  bigger lift and needs the full budget of a dedicated run.
- **Decision:** fixed the 3 pre-existing lint violations in a separate commit — CI gate is red
  without them; all are mechanical, zero behavior change (verified by full suite re-run).
- **Decision:** `allFormats` ar.json value restored to `جميع الصيغ` (an earlier edit this run had
  drifted it to `كل الصيغ`; unintended).
- Route-path note: discovery is `GET /matches` (not `/matches/nearby`) — first live probes 404'd,
  re-aimed from matches.controller.ts:46.
- API used **jest** (not vitest) — first spec run used the wrong runner; corrected.

## Next-run recommendation

1. **P1-19 feed pagination** (useInfiniteQuery + cursor end-to-end) — top product gap, give it
   the full ~2h slice budget.
2. Then P1-20 (push prefs/quiet hours) or P1-12 (admin i18n scaffolding).
3. Keep doing the `env -u HERMES_DELEGATED_CHILD_CONTEXT` prefix on kanban calls (marker was
   inherited this run).
4. Re-verify run #12's time-filter claim next run (gates + one live `?time=` probe).
