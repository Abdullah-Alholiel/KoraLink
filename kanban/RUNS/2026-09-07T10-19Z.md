# Factory Run #40 — 2026-09-07T10:19Z

**Mode:** full (review + board + 3 build items)
**Budget:** ~1h05m used of ~3h (fast run — reviewers landed in 209s/86s combined, no timeouts)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` via zai (probe 200; z.ai weekly 16% ≤ 75% → PRIMARY per provider directive; delegation config re-asserted pre-run)

## Preflight

- Lock absent → acquired (`2026-09-07T10-19Z-40`). gh auth OK (Abdullah-Alholiel). Pull clean (already up to date).
- Dirty foreign files respected, never staged: `docs/landing-drafts/factory-dashboard.html`,
  `docs/plans/e2e-waitlist-join/00-status.md`, `graphify-out/graph.html`.
- Delegation re-asserted to zai glm-5.3-flash + `reasoning_effort low` (probe 200, 5-token ping).
- **Strix: not due** (Sep 7; window 1st–3rd; notes carry strix_scan=not-due-2026-09; next Oct 1).
- Health snapshot: api/pwa/admin all active; `/health` 200; ZERO journal errors (5h) on all three services.
- Graphify god-nodes sane (CurrentUser 85, fetcher 72, useAppStore 59, MatchesService 58).

## Sentry / error-log triage (Phase 1.6 — source: Sentry API, EU base, read token OK)

| Signature | Count 24h | Last seen | Verdict |
| --- | --- | --- | --- |
| KORALINK-API-14/15 (skill_level query fails) | 18+2 | Sep 6 21:20Z | Known P0-10 drift incident window; no recurrence post-restart. MINOR/noise. |
| KORALINK-API-13 (`relation "matches.waitlist"`) | 37 | Sep 6 17:17Z | Fixed by 5f61ec0 (17:40Z) before the last event; both relation sides in source. Resolved. |
| KORALINK-WEB-6 (`viewport-diagnostic:standalone`) | 32 | Sep 6 21:01Z | INTENTIONAL one-shot-per-session diagnostic (sessionStorage-gated, ViewportHeightSync.tsx:53). MINOR — by design. |
| KORALINK-API-11 (demo purge UPDATE) | 0 new | **Sep 6 00:00Z** | **CORRECTION: did NOT fire Sep 7** — run #38's "recurrence confirmed" was a timestamp misread. Run #36's demo hotfix HELD. See Board changes. |
| API-12 abort / API-B CORS probe / API-4 401s | few | old | Expected noise / security headers working as designed. |

**Outcome: no new P0/P1 from Sentry this run.**

## Findings (GLM reviewers + self, merged, evidence-cited)

- **Reviewer A (PWA focus, 209s):** 2 CRITICAL (render-path `Date.now()` POTM window —
  MatchCard.tsx:35, my-games/page.tsx:18), 5 IMPORTANT (NotificationSheet optimistic badge
  without rollback :111-114; unreadCount from first page only :109; DatePicker mount-closure
  footgun; RescheduleSheet 30-day window vs 7-day spec :4-5; slotDate hydration claim),
  5 MINOR. Clean sweeps: no `::uuid`, no `eq(col,null)`, no console.* in API prod paths,
  FOR UPDATE present, z-index conform, i18n parity exact.
- **Reviewer B (86s):** all 3 run #39 claims independently VERIFIED (dispute contract both
  paths matches.service.ts:2612/:2677 via toMyDispute :2555; createVenue full row
  partner.service.ts:103-118; journal 37 entries ending 0036). Product gaps: Moyasar
  placeholder (env.mjs:8) and OTP brute-force flagged P0 — **parent refuted the OTP one**
  (OTP_FAIL_LIMIT=5 lockout, otp-store.service.ts:10,89) and folded Moyasar into P0-2
  (env key currently unused by any wallet code path).
- **Parent self-review:** refuted RescheduleSheet slotDate hydration (sheet renders null
  until open — initializer never renders during hydration); refuted NotificationSheet
  null-actor CRITICAL claim (kept as MINOR guard); confirmed banner drift (mx-4 vs mx-5,
  py-3 vs py-2.5) and the stale-`[]`-memo canVotePotm finding (not in reviewer's list —
  fixed in passing).
- **OS research (Phase 1.7):** React Aria (Adobe, react-aria.adobe.com) flagged for a future
  dedicated a11y-library cycle — NOT adopted (P2-52 was surgical; adoption only via Gate cycle).

## Previous-run verification (claims ≠ facts)

| Run #39 item | Re-proof | Verdict |
| --- | --- | --- |
| cb5b890 journal parity | journal 37 entries, last 0036_venue_geo_gist_index; tripwire spec 4/4 jest live-run | **DONE ✅** |
| e10b342 dispute/venue contracts | 6+2 spec cases live-run; toMyDispute on both paths in source | **DONE ✅** |
| 54dcb12 0036 venue GiST | `venues_location_gist_idx` in pg_indexes; bookkeeping newest id 37 (0036 sha), zero dup rows; EXPLAIN seq-scan = planner-rational at 5 venues (index ready at scale) | **DONE ✅** |

## Board changes

- **P2-52 → DONE ✅** (257514e, 3da181e, 10bb2ae). Residual noted: generic AsyncStatus wrapper.
- **Run #39's 3 in-review items → DONE ✅** (evidence above).
- **P2-51 urgency DOWNGRADED**: API-11 lastSeen Sep 6 00:00Z — the Sep 7 purge did NOT fail;
  run #38's recurrence note was a fast-clock misread. Owner decision still pending (Render
  migrate hook vs manual runbook); next probe point Sept 8 00:00Z. 0036 is perf-only, so the
  demo cannot break on it even un-migrated.
- Board-tail notes: run #40 reviewer-notes entry added (incl. refutations + correction);
  run #21 notes preserved.
- No new board rows created (both Reviewer-B P0 candidates resolved without a row).

## Built this run (slices, commits, verification evidence)

1. **`257514e` — P2-52 main batch** (18 files): `useNow` hook + hydration-safe POTM contract
   (`isPotmVotingOpen` now-param) fixing both Reviewer-A CRITICALs; shared `OfflineBanner`
   replacing 7 drifted page copies (role=status, inline/plain variants); NotificationSheet
   server-derived unread count (hasMore no longer hides Mark-all-read) + optimistic badge
   rollback onError + localized `notifications.markAllFailed` (en/ar) role=status line;
   FilterBar format chips `aria-pressed` + `min-h-hit` 44px token (WCAG 2.5.8).
2. **`3da181e` — RescheduleSheet 7-day window** (P1-13 spec restored; was silently 30 via the
   DatePicker default) + IDB `onversionchange` close hygiene; spec re-pinned 30→7.
3. **`10bb2ae` — reviewer minors**: null-actor guard in NotificationSheet rows;
   DatePicker re-anchors at Riyadh midnight (todayKey in memo deps).

**Gates (all final):** PWA tsc 0 · API tsc 0 · eslint 0 · vitest **58 files / 412 tests** (+13 new)
· jest **408/408** (51 suites) · turbo build **3/3** ×3 (2m02s / 1m52s / 1m51s, no OOM)
· i18n parity 886/886 en/ar · PWA service restarted by postbuild with fresh dist.

## Blockers & decisions

- **P2-51** — owner call stands (Render migrate hook vs manual runbook), but urgency
  downgraded (see correction). Next purge probe: Sept 8 00:00Z.
- P1-41 Resend, P2-47 cancel-purge, P1-38+P2-50 uploads, P0-2 money — unchanged owner queue.
- Decision recorded: Reviewer-B OTP P0 refuted with code evidence; Moyasar folded into P0-2.

## Next-run recommendation

1. Verify run #40's 3 commits (expected quick: specs already assert the contracts; live
   spot-check `/en/play` offline banner + notification sheet unread-count envelope).
2. **P2-4 float casts** (matches.service.ts:198, admin/users.service.ts:103,
   admin/settlements.service.ts:36,107,115) — next in priority order; money-adjacent, do it
   in a fresh-run budget, not end-of-run.
3. Rotation 41 % 4 = 1 → **API modules** focus. Reviewer A should sweep auth/matches/wallet
   DTO+contract surfaces; keep the standing bug-class list.
4. P2-52 residual (generic AsyncStatus wrapper) is polish-backlog, not a next-run pick.
5. Watch Sept 8 00:00Z demo purge — if API-11 fires again, re-escalate P2-51 to urgent.
