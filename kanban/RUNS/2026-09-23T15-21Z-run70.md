# Factory Run #70 — 2026-09-23T15:21Z (resume of the OOM-killed 01:15Z fire)

**Mode:** full (dead-run adoption + build)
**Budget:** ~45 min used of the 3h slot (15:21–16:0xZ); the 01:15Z session burned ~20 min before its kill
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` zai (probe **200** via ZAI_API_KEY — the GLM_API_KEY .env line is stale/401; weekly quota 1%, 5h 5% · directive honored: zai primary)

## Preflight

- **DEAD-RUN ADOPTION (kill-pattern #14):** `kanban/LOCK.json` held `run70-resume-1018`, pid 3582788 — **dead** (~5h old, started 10:21Z as a recovery of the OOM-killed 01:15Z fire; that recovery session itself died before finishing). Per the watchdog directive: pid dead → rm lock → adopt coherent half-done work → re-run every gate.
- The projects dir (`/home/ubuntu/projects/koralink`) is still the `feat/search-neighborhood-suggestions` lane with Abdullah's in-flight edits (conversations/users services + controllers, admin users page, seed, Dockerfiles, i18n) — **ADMIN HOLD continues**, nothing there was staged or touched. All work happened in the staging worktree `/home/ubuntu/worktrees/koralink-staging`.
- Delegation config found clean: glm-5.3-flash / zai / coding base (no self-heal needed). z.ai probe 200; weekly 1% → no failover, no Go spend.
- `git pull --ff-only` up to date; gh auth OK (Abdullah-Alholiel). NODE_ENV clean.

## Sentry / error-log triage (Phase 1.6)

- Sentry API (EU base, SENTRY_READ_TOKEN): **zero new signatures** — all 20 tracked issues (10 API + 10 web) last seen Sep 6–14. API-B (CORS evil.example ×110) remains the P1-17c probe artifact by design; KORALINK-API-1B (email provider) = boarded P1-41; web-D hydration class = standing.
- journalctl `-p err` 5h window on all three services: **no entries**. Health: all 3 services active, API `/health` 200 (localhost:3001).
- **Boarded: nothing. Fixed this run: nothing from Sentry** (quiet window).

## Findings (reviewer + parent, merged, evidence-cited)

- **Reviewer A** (zai glm-5.3-flash, 212s, 12 tool calls) on the uncommitted P2-95/P2-96 slice: **zero CRITICAL, zero blocking IMPORTANT — verdict SHIP.** Verified clean: rollback scoped to the fresh-subscribe branch only (locale re-upsert path keeps its non-destructive catch — traced), `isSubscribed = !!subscription` flips truthfully, worker tag = `${data.type}:${matchId}` so the renames genuinely split the chat tag, no else-if shadowing, locale 'ar' flip affects only locale-omitting subscribers (PWA always sends it), `notifyPromotion` has exactly 2 callers (both post-commit, single promotion per tx — no double-fire). MINOR: rollback doesn't clear `kl.push.syncedLocale` (harmless — recorded on the P2-96 row); `.local/` dirs at repo root + apps/player-pwa are untracked but NOT gitignored (left untracked; candidate one-liner for a DB/Infra lane).
- **Parent finding — lint gate silently broken:** `npm run lint` (PWA) failed with 6 errors + 1 warning, all in files predating run #70 (login-revamp merge `90fa5c7`, run #66 pagination `80d7f0d`, older chat suites). **Run #69's "lint 0" claim was inaccurate** — the dashboard was dark Sep 19–23 (all three fires OOM-killed or skipped), so the gate never actually ran on staging. Fixed this run (→ P2-97, `c02fae7`).
- **Ledger correction:** run #64's board note "waitlist-promotion push REFUTED — wired at waitlist.service.ts:330-341" was **wrong** — those lines are the activity/bell leg; the `waitlist_promoted` push-text catalog key had no caller until this run. The dead session's Reviewer B was right. Root cause of the bad refutation: a dedupe grep matched the `notifyPromotion` caller, not a `sendPushToUsers` key. Comment left on the board; P2-95 row records the overturn.
- OS research (Phase 1.7): skipped — adoption + gates consumed the priority budget (recorded as "OS research: none").
- Security lane (Phase 1.8): FIX queue empty (run-1/run-2 findings.json: 4 findings, 0 confirmed/high). Strix: **not due** (Sep 23; next window Oct 1–3). HUNT skipped (budget + rotation).

## Previous-run verification (run #69 claims — claims ≠ facts)

| Claim | Re-verified independently | Verdict |
|---|---|---|
| P2-82 SubscribeDto (7ea1cdb + e721823 512-cap) | `grep -c "class SubscribeDto"` → 1; notifications+matches jest **53/53** incl. DTO matrix; api tsc 0 | **TRUE** |
| P2-91 PushSubscribeOutcome + permissionDenied parity (6159f83) | grep `PushSubscribeOutcome` hook → 2; `permissionDenied` in en.json + ar.json → 1/1; hook suite green in the 671 | **TRUE** |
| P2-92 pushsubscriptionchange (2b4f080) | grep worker → 3; structure suite green in the 671 | **TRUE** |

→ all three promoted to DONE on the board (last-updated line + ledger).

## Built this run

| Slice | Commit | Gates (all re-run by THIS session) |
|---|---|---|
| P2-95 waitlist promotion push + tag-collision renames + P2-96 subscribe rollback + 'ar' default | `80ce0a3` (adopted slice; written by the dead 01:15Z session) | turbo **3/3** (`--concurrency=1`, exit 0) · notifications+matches jest **53/53** (4 new waitlist-push cases) · api tsc **0** · vitest **94f/671t** · pwa tsc **0** · Reviewer A **SHIP** |
| P2-97 lint-gate unblock (6 errors + 1 hook warning, zero behavior change) | `c02fae7` | `npm run lint` **exit 0** · touched suites **24/24** · tsc 0 |
| P2-93 notificationclick soft-nav (adopted commit) | `c767696` | tripwires+units green inside vitest 671; turbo 3/3 |
| P2-94 pushsubscriptionchange absolute API origin (adopted commit) | `72fc2f0` | structure suite green inside vitest 671; turbo 3/3 |

Adoption discipline: every gate re-run on the final tree BEFORE the adoption commit (run-#54 lesson — never trust dead-session docs; plan docs matched the diff exactly this time, and the Reviewer A pass + 671-test suite confirmed).

## Board changes

- **New rows:** P2-93 (notificationclick soft-nav, DONE-staging) · P2-94 (pushsubscriptionchange origin, DONE-staging) · P2-95 (waitlist promotion push, DONE-staging) · P2-96 (subscribe rollback, DONE-staging) · P2-97 (lint gate, DONE).
- **Overturned:** run #64's waitlist-push "REFUTED" ledger note (inline comment under the new rows).
- **Verified DONE:** run #69's P2-82 / P2-91 / P2-92 claims (all TRUE).
- **ADMIN HOLD unchanged** — projects-dir lane still carries Abdullah's uncommitted merge-conflict state; no admin items touched.
- STATE.json: last_run=70, commits [e721823*, 72fc2f0*, c767696*, 80ce0a3, c02fae7], 3 fresh in_review_items for run #71 to verify.

## Blockers & decisions

- None new. Standing owner queue unchanged (P1-41 email provisioning, P2-51 demo migrate hook, P2-47 cancel-purge, P1-38+P2-50 uploads, P1-49 ban-appeal channel, P1-51 rating/MVP shape, P0-2 payments).
- Deploy fork unchanged: staging→live still needs Abdullah's directed merge (services serve the projects dir).
- PR #27 (promote staging→main) remains OPEN — Abdullah's gate.

## Next-run recommendation (run #71)

1. Verify this run's 3 in_review_items (recipes in STATE.json) — cheapest first signal: the greps, then jest/vitest suites.
2. Rotation 71%4 = 3 → **DB & Infra lane**. Strong candidate already queued: **P2-88** (drizzle meta snapshot chain gap — regenerate 0027/0028 snapshots; no runtime impact, pure hygiene) or the `.local/` gitignore one-liner folded into whichever item goes first.
3. Re-check `git status` in BOTH worktrees before staging; the projects-dir conflict and any sibling watch daemon may have moved things.
4. If the cron-worker OOM pattern recurs despite `--concurrency=1`, escalate to Abdullah: raise the 4 GiB `MemoryMax` on the worker scope (this run stayed at ~1.5–2 GiB peak, sequential — the discipline works).
