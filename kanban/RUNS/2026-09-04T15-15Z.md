# Factory Run #32 — 2026-09-04T15:15Z

**Mode:** full (review + board + build)
**Budget:** ~50min used of 3.5h (cycle was tight, well under budget)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on **opencode-go FAILOVER** (zai delegation children 401'd "token expired or incorrect" while direct key probes returned 200 — the run #24/#27 gateway stale-credential signature; pool clean, gateway up 7d, booted before today's 13:21Z auth resync; NOT restarted mid-run per runbook). Quota directive honored: zai weekly 17% healthy → zai preferred, but the delegation PATH was broken → documented Go failover (~2 reviewer children only, bounded).

## Preflight
- Lock taken over: previous LOCK was run #31's stale resume lock (02:15Z, 13h old — owner pid dead). New lock `2026-09-04T15-15-54Z`.
- gh auth OK (Abdullah-Alholiel), `git pull --ff-only` already up to date, tree CLEAN at start.
- GLM probe (direct API, 5-token): **200** on glm-5.3 + both env keys — but delegation children 401'd → failover. zai config left on glm-5.3 first; delegation provider switched to opencode-go + base_url cleared for the reviewers.
- Strix: not due (UTC 4th; `strix_scan=2026-09-02` covers September).

## Sentry / error-log triage (Phase 1.6)
Sources: Sentry API (EU, read token) for koralink-api + koralink-web; journalctl err for 3 services (zero entries in 13h — clean).

| Signature | Count | Verdict |
|---|---|---|
| KORALINK-API-Z `column mp.joined_at does not exist` | 3 (≤09-03 10:49) | **Noise — stale-dist window**: fix a17755a (run #29, stripped joined_at from the export SELECT) predates the last event; events fired while the process ran pre-restart dist; 0 events in 28h since run #31's restart; current dist has no joined_at in service code. Closed. |
| KORALINK-WEB-8 `TypeError: Cannot read properties of undefined (reading 'waiting')` | 4 (TODAY, rel 841680f, localhost:3000/en) | **FIXED this run** — `ServiceWorkerUpdater.tsx:36` `reg.waiting` → `reg?.waiting` (WebKit private-mode `serviceWorker.ready` quirk; Reviewer A independently confirmed). |
| KORALINK-WEB-6 `viewport-diagnostic:standalone` | 13 today | Diagnostic probe channel (PWA's own instrumentation, url :10000/ar) — not a product error, MINOR, no action. |
| KORALINK-WEB-7/-4/-5 fetch failures on :8443 | few | Device-level network errors (known noise). |
| KORALINK-API-B CORS attacker.example | 83 (≤09-02) | Control working as designed (P1-17c rejection proof). |

## Findings (GLM reviewers + self, merged, evidence-cited)

**Reviewer A — code quality (25 checks):**
- A1 MEDIUM — `admin/users.service.ts:63-66` `status=active` filter does not exclude soft-deleted users → deleted ghosts display as "active" (default view excludes; the active filter didn't). **FIXED this run.**
- A2 MEDIUM — `admin/users.service.ts:124` `update()` has no deleted_at guard — ban/unban/role-change on a ghost succeeds via direct API call. **FIXED this run (409).**
- A3 LOW-MED — root Dockerfile:7-9 `npm ci` at root against the 3-workspace lockfile while only apps/api manifests are copied — latent strictness risk (reported passing on Coolify; NOT rebuilt this run — Coolify path works today, noted for the next docker cycle).
- A4 MINOR — PDPL grace "30" hardcoded client-side ×2. **Fixed (named constants + sync pointers).**
- A5 MINOR — PromoBillboard `goTo()` re-arms auto-advance for reduced-motion users. **Fixed.**
- A6 MINOR — match/[id] lacks distinct not-found + offline branches (profile is exemplary). Board-adjacent, not built (polish).
- A7 CONFIRMED KORALINK-WEB-8. **Fixed.**
- Clean sweeps: `::uuid` 0 · `eq(col,null)` 0 · console.* 0 · i18n parity PWA 746/746 + admin 0 diff · z-index ladder compliant · 1b0150c single-scroller correct · WS rejects deleted users ✓.

**Reviewer B — product gaps + verification (13 verification checks + gaps):**
- Run #31 claims **13/13 VERIFIED ✓** (P1-35/P1-36/replay guard/purge atomicity/GRACE_DAYS single-source; targeted jest re-run live 28/28). P2-45 still intact ✓. P1-37 (1043293) matches the board ✓.
- **NEW P1 (fixed this run): admin Docker build broken** — (a) `apps/admin/Dockerfile:35` COPYs `public/` which doesn't exist; (b) `use-live-data.ts:4` imports `socket.io-client` NEVER declared in apps/admin/package.json (phantom dep — resolvable on host via hoisting, unresolvable in-image; standalone lockfile had 0 entries). **Both fixed; live `docker build` now exit 0.**
- P2 footgun corrected: PWA Dockerfile comment claimed NEXT_PUBLIC_* is runtime-supplied — it's compile-time only. Comment fixed.
- P2 stale-closure nit in PromoBillboard onTouchMove (index from render closure) — cosmetic, not fixed (5s window).
- **Product gap boarded (P2-47)**: P1-37 delivered VISIBILITY only — support cannot cancel a scheduled purge or restore-on-behalf (needs owner decision).
- **REFUTED run #31's "match-detail lobby not realtime" P1**: `useMatch` already subscribes + invalidates on roster-update/status-update/pom-decided (useMatches.ts:168-191); only `user-joined` (gateway:220) has no PWA listener and it's redundant with roster-update. Claim dropped, card never created → nothing to undo.
- No new P0s: booking race-safe, ledger idempotency, throttler, AdminAuthGuard, .dockerignore excludes .env ✓.

## Previous-run verification (Phase 1)
- in_review_items resolved: P1-35+P1-36+hardening — Reviewer B verified all 6 commits + re-ran the 3 PDPL suites (28/28 PASS) → DONE. "Lobby gap" claim → REFUTED (above). P2-46 parked (unchanged).

## Board changes
- **ADMIN HOLD LIFTED** (owner's work landed as 1043293; tree clean).
- P1-37 TODO→**DONE ✅** (owner 1043293, verified run #32).
- NEW **P2-47** (admin cancel-purge/restore-on-behalf — needs owner decision).
- Run #31 lobby-gap P1 → **dropped (refuted)**.
- Sentry API-Z → closed as stale-dist noise (documented).

## Built this run (slices, commits, verification)
1. **Docker fixes** (admin image build-breakers): `apps/admin/Dockerfile` `RUN mkdir -p public` + PWA NEXT_PUBLIC comment fix + `"socket.io-client": "^4.8.3"` declared in apps/admin + standalone lockfile regenerated (isolated dir, 3 refs verified).
   **Live proof: `docker build -f apps/admin/Dockerfile -t koralink-admin:run32-verify apps/admin` → exit 0** (image sha e5f4cd16d4d7). First attempt reproduced BOTH failures (COPY not found → then webpack `Can't resolve 'socket.io-client'`) before the fixes.
2. **Admin users API PDPL ghost guards**: active filter + deleted_at IS NULL; update() 409 on ghosts BEFORE any write; +`users.pdpl-guard.spec.ts` (4 cases, SQL pinned via PgDialect.sqlToQuery).
3. **PWA minors**: `reg?.waiting` (KORALINK-WEB-8), PromoBillboard reduced-motion re-arm (15/15 suite green), PDPL_GRACE_DAYS_CLIENT + _ADMIN named constants with sync pointers.
4. **Lint debt**: 3 pre-existing CI-lint errors fixed (unused imports ×2, no-explicit-any disable-line, useCallback extra dep) + InstallLanding.
5. **Gates**: API jest **338/338** (41 suites) · PWA vitest **346/346** (49 files) · `npx tsc --noEmit` api 0 + pwa type-check 0 · PWA eslint --max-warnings 0 **clean** · `npm run build` (root) **3/3, uncached** (baseline 53ms cached + final uncached pass after test edits) · docker admin image exit 0.

## Blockers & decisions
- **zai delegation-path 401** (children only; direct probes 200): gateway-level stale credential, NOT repaired mid-run (runbook). Next run MUST re-probe with a real reviewer dispatch; if still 401 → opencode-go continues; if 200 → restore zai delegation config.
- P2-47 needs Abdullah's decision (cancel-purge vs restore-on-behalf).
- A3 (root Dockerfile workspace-manifests strictness) NOT touched — Coolify path reportedly works; changes there need a Coolify-verified cycle.
- Root package-lock.json got auto-modified by npm 12 during the first lockfile regen attempt (walks up to workspace root) — REVERTED via git checkout; regen redone in an isolated /tmp dir against the admin standalone lockfile only. Runbook note: **never regen app lockfiles in-tree on this box (npm 12 escapes to the root workspace lockfile)**.

## Next-run recommendation
1. Re-probe zai delegation with a live child dispatch (gateway state may have healed after config churn); restore `delegation.provider zai` + `base_url https://api.z.ai/api/coding/paas/v4` if children return 200; else stay on Go.
2. Next buildable items (non-blocked): P2-47 (needs owner decision first); match/[id] not-found/offline branches (Reviewer A A6, PWA polish); P1-17 waitlist (product-sized); A3 root-Dockerfile hardening (Coolify-verified cycle).
3. Watch for Coolify's next deploy — the admin image now actually builds; if Coolify builds BOTH apps, the PWA image should be spot-verified once too.
