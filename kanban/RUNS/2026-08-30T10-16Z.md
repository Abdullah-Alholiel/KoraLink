# Factory Run #18 — 2026-08-30T10:16Z (scheduled slot, 10:15 UTC)

**Mode:** full (review + board + 3 build items + 1 review fix)
**Budget:** ~55m of the ~3.5h budget (3 board items + 1 fix finished with full gates + live
verification; stopped per stop-rule with remaining items needing longer slices)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (permanent; probe: **200**
in 2s) — 2 scoped reviewers dispatched together, ran synchronously (background delegation
unavailable in this runner, same as #16/#17): Reviewer A 131s / Reviewer B 184s, no timeouts.

## Preflight

- Lock taken 10:16Z (no prior lock). gh auth OK (Abdullah-Alholiel). `git pull --ff-only`: up
  to date. Delegation config was already `glm-5.3`/zai/coding-base (probe 200 → no fallback);
  `reasoning_effort low`; `model.context_length 1000000`.
- `HERMES_DELEGATED_CHILD_CONTEXT=1` INHERITED again → every kanban CLI call under
  `env -u HERMES_DELEGATED_CHILD_CONTEXT` (list/create/link/comment all worked).
- Foreign dirty paths recorded, never staged: `.gitignore` (M), `docs/architecture/*`
  (untracked harness files), `graphify-out/manifest.json` (foreign watch daemon re-dirties).
- Sibling agents quiet this run — only the standing foreign paths in the tree.

## Sentry / error-log triage (Phase 1.6)

- Source: Sentry API (SENTRY_READ_TOKEN, EU base) — 200 both projects; journalctl clean
  (zero err entries 5h, all 3 services active, health 200).
- koralink-api 24h: 25 issues, ALL last-seen ≤ 2026-08-28T18:14Z (pre-run-#15), all known
  signatures (401 probe noise ×117, CORS evil.example guard ×21, /health probes ×19…). MINOR.
- koralink-web 24h: **one NEW issue** — `KORALINK-WEB-3` `FetchError: Insufficient wallet
  balance. Required: SAR 350.00, Available: SAR 195.00` (2026-08-30T07:53Z, count 1).
  Triaged MINOR, no action: the publish path already classifies this error into the localized
  PublishWarningSheet with a top-up CTA (`lib/publish-error.ts` insufficient_balance →
  `host.errorInsufficientBalance`; PaymentSheet has its own top-up CTA). Sentry noise from a
  handled 4xx. Remaining 2 web issues stale (08-20/08-16).
- Board note added: do NOT re-flag wallet FetchError; `surface:"ops"` needed for admin
  dev-login in E2E recipes.

## Findings (GLM reviewers + self, merged, evidence-cited)

**Reviewer A** (code quality; 18 calls): zero CRITICAL. IMPORTANT ×2, both verified by
direct read:
1. `partner.service.ts:127-128` hours PATCH wrote raw `dto.open_hour`/`dto.close_hour`
   (undefined on partial updates) instead of the merged pair. Behavior was accidentally
   correct — drizzle 0.44 `mapUpdateSet` filters undefined keys (verified in
   node_modules/drizzle-orm/utils.js) — but the code depended on a library detail. **FIXED
   this run** (89acff8) to write the resolved merged values.
2. `useReports.ts` observability regression: the P2-23 rewrite dropped
   `trackEvent('report_submitted')` + `onError captureError` (verified via
   `git show 2060c65:...`) — report failures reached neither Sentry nor PostHog. **FIXED**
   inside the P1-31 slice (695c896).
3. A3 "generateSlots skipped double-count" — **REFUTED**: categories are disjoint
   (`rows.length - created` conflicts vs `totalCapacity - rows.length` clamped/closed); sum =
   `totalCapacity - created` = exact. Recorded on the board so nobody re-flags it.
MINOR ×6 (boarded as P2-31): admin hours NaN guard, listMine 50-cap + raw-id label fallback,
overnight venue windows unrepresentable, offline banner on only 3 screens, POTM worker
winnerId-consumer check. Admin rotation focus (run#%4=2): types current, RBAC single-source,
no dead buttons — clean. Bug-class sweep ALL PASS: `::uuid` 0, `eq(col,null)` 0, console.* 0,
i18n 673/673 (pre-run), z-index clean.

**Reviewer B** (product gaps + verification; 24 calls): all 4 run-#17 claims **VERIFIED**
(P1-25 hours incl. schema/migration/DTO/generateSlots clamp/createSlot 400s/admin form + 4/4
vitest; P2-23 mine-list/resolve-activity-push/page states/673-parity; P2-27
sendPomDecidedNotification → sendPushToUsers + jest 9/9 re-run; P2-13 pill + MapPin gone).
Product gaps: **P1-31 chat messages not reportable** (REPORT_SUBJECT_TYPES 3-value, no
affordance in messages page — moderation hole for a chat product) → **BUILT this run**;
**P1-32 no club detail page** (`(main)/clubs/` list-only, API detail exists) → boarded;
P2 ×4 (offline banner coverage, no post-match ratings/reviews, no waitlist — P1-17 already
tracks waitlist — host share deep-link parity). No new P0.

**Self-review:** confirmed A1/A2 by read, refuted A3 by arithmetic; verified
`subject_type` is varchar(50) → P1-31 needs NO migration; spotted `classifyPublishError`
already handling the wallet FetchError before boarding it.

## Previous-run verification

- Run #17's four in_review items: **all verified as claimed** (Reviewer B + my gates re-run:
  vitest 238/238 pre-slice, jest 9/9 notifications, 4/4 venue-hours vitest). STATE.json
  in_review_items from #17 → promoted: P1-25, P2-23, P2-27, P2-13-partial all confirmed
  (board rows already carried DONE from #17's own records; no change needed).

## Board changes

- ADDED: **P1-31** (chat message reports — built same run, DONE), **P1-32** (club detail page,
  TODO), **P2-31** (reviewer minors queue, TODO).
- P2-28 → **DONE ✅** (86b927c, this run). P2-30 → **DONE ✅** (589cd2d, this run).
- Backlog note: run-#18 leftovers (A3 refutation, wallet-noise triage, surface:ops recipe,
  /tmp/run18-e2e.sh recipe).
- Mirror cards: t_e182f0bf (P1-31, created+gated), t_cb0c7399 (P1-32, created+gated),
  t_d7bfbf7c (P2-31, created+gated) — all `status: todo` + `parents: t_ce9a513a` verified;
  t_02ba5be1 (P2-28) + t_88cc929e (P2-30) commented with build+verify records. No
  kanban_complete/kanban_block used (t_ce9a513a gate respected).

## Built this run (slices, commits, verification evidence)

**Fix 1 — A1 venue hours PATCH (`89acff8`, includes Gate docs).**
`updates.open_hour/close_hour` now write the merged pair. Gates: tsc 0, partner jest 18/18.

**Item 1 — P1-31 chat message reports (e50c727 API + 0b7b8cb admin DTO fix + 695c896 PWA).**
API: `REPORT_SUBJECT_TYPES` += `message`; self-report guard via sender lookup (400 "You
cannot report your own message."); `assertSubjectExists` message branch (404); `listMine`
joins `personal_messages` + aliased sender (`message_sender` via drizzle `alias`) → label
`Message from <sender>` (content admin-only); admin `resolveSubject('message')` →
`Message from <sender> · "<60-char snippet>"` status `sent`; admin list SQL +pm/su joins with
COALESCE; `ListReportsDto.subjectType` imports the shared enum (was hardcoded 3-value —
**caught live in E2E**: admin filter 400'd before this fix). PWA: per-bubble overflow ⋯ on
received DMs → ReportSheet (new optional `title` prop); `useReports` type widened +
observability restored; i18n en/ar 677/677 (report.reportMessage/subjectMessage/
messageSheetTitle, reports.subjectType.message). Tests: 4 new jest (self-guard, 404, create,
label) + 3 new vitest (submit disabled→enabled, subjectType=message body, inline error).
Gates: tsc 0, build 3/3, jest 164/164, vitest 241/241, type-check 0. API restarted on fresh
dist (dist grep verified new DTO + service), health 200.
**Live E2E PASS**: 002 sends probe msg → 001 reports 201 → duplicate 400 ("already
reported") → 002 reports OWN msg 400 ("You cannot report your own message.") → 001 mine-list
shows `message | Message from Khalid Al-Otaibi | open` → admin queue
`?subjectType=message` shows it → detail subject `{type:'message', label:'Message from
Khalid Al-Otaibi · "E2E probe msg run18"', status:'sent'}` → resolve 201 → 001 mine-list
`resolved` + resolution text. Probe data intentionally left (2 dismissed/resolved message
reports — real moderation data).

**Item 2 — P2-30 partner matches venue/pitch filters + pager (589cd2d).**
API: `GetPartnerMatchesDto` += `venueId`/`pitchId`; `getPartnerMatches` additive
`scopeFilter` AND clause (pitchId wins, foreign pitch ids fall through to venueId, never
short-circuits scope/status, scopedPitchIds ownership unchanged). Admin `/partner/matches`:
venue select + dependent pitch select (cleared on venue change) + server-side pager (page 20,
Prev/Next, Page X of Y, filter change resets page); `OpsEntity` += `pitches`; i18n en/ar
239/239. Gates: tsc 0, jest 18/18, build 3/3, restart + health 200.
**Live verify PASS** (owner +966500000003, surface ops): upcoming 3 rows/2 venues →
venueId=KSU → 1 row 0 wrong-venue; venueId=Olaya → 2 rows 0 wrong-venue; pitchId → 1 row 0
wrong-pitch; pager limit=1 offset=0/1 → distinct rows + hasMore; fake venueId → 0 rows (no
scope widening).

**Item 3 — P2-28 SW match-feed staleness (86b927c).**
Root cause bigger than boarded: route was `StaleWhileRevalidate` = stale-first on EVERY load,
compounding React Query's 60s staleTime. Fixed at source (`next.config.mjs`): `NetworkFirst`
+ `networkTimeoutSeconds: 3` — fresh preferred, cache only the offline/slow fallback.
Generated `public/sw.js` is gitignored (discovered at commit — repo tracks the config, not
the artifact); regenerated + grep-verified `NetworkFirst` in sw.js post-build. Gates: build
3/3, vitest 241/241.

## Blockers & decisions

- No auto-stash used (runbook); foreign paths untouched, zero foreign hunks entangled.
- Admin dev-login requires `{"surface":"ops"}` — player-surface logins 403 admin accounts
  ("Use the ops console"); recipe recorded on the board backlog for future E2Es.
- Bash pipe-to-python inline commands hit approval guards → E2E scripts written to /tmp files
  and run via `bash` (also the repeatable-recipe benefit). Two blocked-command artifacts
  (parser-limit) were reviewed and safe.
- Budget stop: after P2-28, remaining board work = P2-26 (profile/host UX states, ~60-90m),
  P1-32 (club detail page, >90m), P1-13 (reschedule, large) — per the stop-rule (<60m remain
  → verification/board-keeping), spent the rest on Phase 5. Small completed beats half-done.

## Next-run recommendation (#19, 15:15Z Aug 30)

1. Verify run #18's items: re-run gates (tsc/build/jest/vitest/type-check) + spot-checks —
   P1-31: live `GET /reports` as player 001 shows the resolved message report; admin queue
   `?subjectType=message` non-empty. P2-30: venue/pitch filter containment on the live box.
   P2-28: grep `NetworkFirst` in apps/player-pwa/public/sw.js. A1: code read merged pair.
2. Then pick by priority: **P1-32** club detail page (real product gap, uses existing public
   endpoint; ~90m) → **P2-26** profile/host UX states (~60-90m) → **P1-27** wallet withdrawal
   (check the P0-2 boundary first).
3. Keep glm-5.3 probe + 2-reviewer pattern (working well: 3rd consecutive run under 3 min
   each, zero timeouts). Watch for sibling branch switches before staging.
