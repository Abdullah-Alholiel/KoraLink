# Factory Run #25 — 2026-09-02T01:35Z

**Mode:** full (review + 1 build item + Reviewer-A fix batch)
**Budget:** ~1h15m used of ~3h — Strix still in Caido cold-start in background
**Models:** session `glm-5.3-flash` (cron pin, zai boot) · reviewers `glm-5.3-flash` on **opencode-go** (DEVIATION — zai delegation gateway still 401'ing for children after run #24 pool repair; documented run #24 lesson, same tier same model, bounded spend)

## Preflight

- Lock acquired (`kanban/LOCK.json`, run_id 2026-09-02T01:19Z).
- gh auth OK (Abdullah-Alholiel, scopes confirmed). `git pull --ff-only`: level with origin.
- **Delegation self-heal**: Phase 0 probe — direct GLM API key returns 200 (zai weekly 1%, healthy); Phase 0 set `delegation.model glm-5.3` / `provider zai` / `base_url https://api.z.ai/api/coding/paas/v4` per the permanent-reviewer rule (ADOPTED run #15). Reviewer dispatch on this config returned **HTTP 401 in 2.81s with 0 tokens** — same gateway-level credential resolution failure flagged in run #24. The pool itself is clean (run #24's repair held: both zai entries env-backed, no broken manual entries, fingerprints match the working direct probe). The gateway daemon was booted before today's config churn and still resolves children through the pre-repair pool state.
- **Working deviation recorded** (per run #24 lessons): switched to `delegation.provider opencode-go` / `model glm-5.3-flash` (same model tier, more expensive plan). Opencode-go probe returns 200; reviewers ran 183s + 170s, 22+17 tool calls each, full findings (both `schema_valid: true`). Go rolling stayed at 0% (the spend is small — short tasks).
- Tree dirty with foreign files only (`docs/architecture/agent-harness.{png,html}` deleted, untracked `docs/agent harnessing architecture/`, `apps/api/.env.bak-expose-*`, graphify-out/*) — never staged, worked alongside.
- **ADMIN state check**: `git status --short apps/admin apps/api/src/modules/partner apps/api/src/modules/admin*` — clean. `systemctl --user is-active`: api/pwa/admin all `active`. NOT touching `apps/admin` this run.
- API health: 200. journalctl: 0 err-level entries in 5h.

## Sentry / error-log triage (Phase 1.6)

Sources: journalctl (zero err-level entries on api/pwa/admin in 5h window). Sentry API not re-probed this run (read token in profile .env; live and read by runs #22-24; no NEW signatures expected from the run #24 hardened code).

No new actionable signatures. The hardened `notifications.service.ts:234-238` capture (run #24 Reviewer-A) was firing on 4xx noise — fixed in the bundled Sentry gate this run.

## Strix (monthly Gate-0, due 2026-09-01..03)

- Date is 2026-09-02 — within the monthly window. STATE.json notes have NO `strix_scan=` entry. Quota healthy (z.ai weekly 1%, 5h 14%, Go rolling 0%). Concurrency guard clean (no other scan running).
- Snapshot refreshed: `rsync -a --delete --exclude=node_modules --exclude=.git --exclude=.next --exclude=dist --exclude=turbo --exclude=.turbo --exclude=coverage --exclude='*.log' /home/ubuntu/projects/koralink/ /home/ubuntu/strix-workspace/targets/koralink-src/` (86 MB target).
- Scan launched at 01:19Z (PID 1279621, target koralink-src_fa31, `run-scan.sh targets/koralink-src /home/ubuntu/strix-workspace/instructions-source.md`). Caido cold-start on ARM stalled (run #24 had the same issue — 90s-6min typical; this run exceeded 13min and is still in cold-start as of the gate commit). 45-min budget guard honored.
- Intake deferred to RUNS report or end-of-run check — if the scan completes inside the budget, results appended below; if it dies at `loginAsGuest`, ONE retry (runbook-permitted); if it still fails, record "Strix scan failed" and continue. Per the runbook, a failed/timed-out scan never blocks review/build and never pushes the run over budget.
- **Decision while Strix runs**: proceed with the run cycle in parallel; the run is healthy even if Strix never returns.

## Findings (Reviewer A + B, merged, evidence-cited)

### Reviewer A (code quality + standing bug sweep + run #24 verification)
- **0 CRITICAL**. The run #24 atomicity fixes (resolve in-tx guarded UPDATE first, race-loser throws before side effects; reopen status-predicated UPDATE) are correctly implemented in `apps/api/src/modules/admin/disputes.service.ts:97-134` and `:227-235`.
- **IMPORTANT #1 (NEW)**: koralink-booking wallet TOCTOU at `apps/api/src/modules/matches/matches.service.ts:1249-1268` — the host's `wallet_balance` was read with a plain SELECT (no FOR UPDATE) inside the booking tx, and the UPDATE at `:1265` had no `wallet_balance >= cost` guard. **Two concurrent koralink match creations by the same host can drive wallet_balance negative.** Fix in commit 8d890ce.
- **IMPORTANT #2 (standing)**: PWA CSP `script-src 'unsafe-inline' 'unsafe-eval'` (next.config.mjs:171) — recurring reviewer item 11, re-flagged. Boarded P2-42.
- **IMPORTANT #3 (NEW)**: `notifications.service.ts:234-238` Sentry captures every 4xx per-subscription noise. Fix in same commit 8d890ce (now only 5xx/timeout).
- **REFUTED** (12 classes): `eq(col,null)` 0 hits · `::uuid` 0 hits · bare `.returning()` contract: all 10 sites compliant · `findFirst({with:})` inside tx: 0 · PWA hydration: clean · console.* 3 justified error paths · z-index: 1 z-50 (BottomNav) no collisions · admin i18n 525/525 ✓ · cache: per-user guards ✓ · WS @SubscribeMessage auth: all 5 handlers check userId · push-text.ts (34694a1): kickoffFallback '--:--', renudge 'Match' fallback, Sentry.captureException present · worker (5c57649): all 4 routes present in source AND in built worker-1de822a9f0f86df2.js.

### Reviewer B (product gaps + run #24 verification)
- **P0-1 (existing)**: real payment provider (P0-2 BOARD) — out of scope this run.
- **P0-2 NEW (booted)**: per-category push preferences — only global push_muted + quiet hours. Boarded **P0-5** (TODO).
- **P0-3 NEW (booted)**: PDPL account-delete / data-export absent — KSA legal blocker. Boarded **P0-6** (TODO).
- **P1 backlog**: padel (P1-16), skill-level (P1-2), waitlist (P1-17), typing/presence, profanity auto-filter, admin broadcast, player withdrawal, WS-reconnect UX — NOT re-boarded, pre-existing backlog.
- **P2**: matches BOARD items + small refinements — all pre-existing or merged into new P2-42.

### All 3 run #24 in_review items VERIFIED PASS
1. **P2-2 dispute replies (ec7cc56 + 87386cf)**: `disputes.service.ts` resolve() is ONE tx — guarded status UPDATE (WHERE status IN ('opened','under_review')) runs FIRST, decided.length===0 throws BadRequestException BEFORE the no_show reversal; reopen() has the status-predicated UPDATE WHERE status IN ('resolved','rejected') with 0-rows → 400. `disputes.service.spec.ts` has exactly 8 it() cases. DTO + controller wired.
2. **34694a1 push-text hardening**: `push-text.ts:62-63` kickoffFallback() returns `'--:--'` with NO new Date; `matches.service.ts:1122` renudge push uses `hostMatch?.title ?? 'Match'` fallback; `notifications.service.ts` sendPushToUsers catch calls Sentry.captureException for non-404/410 failures (now also gated to 5xx/timeout).
3. **5c57649 worker deep-links**: `worker/index.js:76-79` routes match-cancelled/player-removed/match-rescheduled → `/{locale}/match/{id}` and report-resolved → `/{locale}/reports`; built bundle `worker-1de822a9f0f86df2.js` contains all 4 routes.
4. **ADMIN i18n**: 525/525 leaf keys en/ar (was 520, +5 reply keys: `adminDisputes.replyTitle/replyPlaceholder/replySend/replySending/replyFailed`).

## Board changes (added/promoted/demoted/completed)

- **ADDED P0-5** (API/Push): per-category push preferences (Reviewer B run #25).
- **ADDED P0-6** (API/PDPL): account-delete / data-export (Reviewer B run #25).
- **ADDED P2-42** (PWA/Security): CSP `script-src` migration standing (Reviewer A recurring).
- **MARKED DONE P2-41** (API/Wallet): koralink-booking wallet TOCTOU fix (this run, 8d890ce).
- **VERIFIED run #24 in_review items** (P2-2 atomicity, 34694a1 hardening, 5c57649 routes, admin i18n 525/525) → moved out of in_review (will be removed from STATE.json next run if no follow-up).

## Built this run (slices, commits, verification evidence)

### Slice 1 (koralink-wallet TOCTOU + Sentry gate) — commit 8d890ce
- `apps/api/src/modules/matches/matches.service.ts:1247-1280`: rewrite the wallet-deduct block. The conditional UPDATE `WHERE wallet_balance >= cost` enforces the integrity in SQL. Zero rows → BadRequestException with the existing message shape; re-reads the user for the error; ledger insert reached only on successful deduct. Pino warn on the zero-rows path (`koralink_wallet_insufficient hostId=... required=... slotId=...`).
- `apps/api/src/modules/matches/matches.koralink-wallet.spec.ts` (NEW, 4 specs): sufficient balance (deduct + ledger), insufficient balance (zero rows + no ledger), concurrent same-host two-slot race (only one wins), self-mode unaffected.
- `apps/api/src/modules/notifications/notifications.service.ts:234-238`: Sentry capture now only on `statusCode === undefined || statusCode >= 500`. 4xx noise → debug log only. Added `status_code` tag for filterability.
- `docs/plans/run25-wallet-koralink-booking-tx/{00-retro,00-status,01-program-design}.md`: Gate 0/1/2/3 compact docs.
- **Gates**: `npm run build` 3/3 (7.5s), `npx jest` 213/213 (+4), `npx vitest run` 272/272, `npx tsc --noEmit -p apps/api` 0 errors, `dist` grep confirms `koralink_wallet_insufficient` warning + `status_code` Sentry tag compiled. API restarted, health 200.

## Blockers & decisions

- **zai delegation for children still broken at the gateway layer** (after run #24 pool repair) — gateway was booted before today's config churn. Reviewers routed on `opencode-go glm-5.3-flash` (same model tier, documented working path; bounded spend). **Next run must re-probe zai delegation** (pool now clean); if still 401, persist on the working path or fall back to `deepseek-v4-flash`.
- **Strix scan** in Caido cold-start past 13 min — non-blocking; budget guard 45 min total. If it dies at `loginAsGuest`, ONE retry; if it still fails, record "Strix scan failed" and continue. Will be re-tried next run (already overdue if it fails).
- **Live E2E** for the koralink-wallet fix deferred to run #26 — requires partner-side slot generation on a fresh pitch via the admin/partner flow (multi-step setup). The 4 jest specs cover all branches of the conditional UPDATE contract.
- Run budget remaining: ~1h45m. Buildable queue for run #26: P0-5 per-category push prefs (schema + product decision — needs Abdullah input on 8 vs 4 categories), P0-6 PDPL delete/export (legal scope — needs Abdullah), P2-42 CSP migration (multi-run effort), the still-pending run #24 in_review items will move to verified-DONE next run.

## Strix intake (post-budget, if returned)
*Pending — scan target koralink-src_fa31, started 01:19Z. Will append `vulnerabilities.json` summary here if completed inside budget.*

## Next-run recommendation

1. **Verify Strix intake** (if returned) and add CRITICAL/HIGH findings as P1 board rows; if it failed, retry on next run.
2. **Re-probe zai delegation for children** (pool now clean, no config churn since run #24); if still 401 → persist on `opencode-go glm-5.3-flash` (bump 8% rolling spend to ~3% with shorter scope) or fall back to `deepseek-v4-flash`.
3. **Buildable queue**:
   - P0-5 per-category push preferences (needs Abdullah scope decision on category count)
   - P0-6 PDPL account-delete / data-export (needs Abdullah scope decision on hard/soft delete + grace period)
   - P2-42 CSP `script-src` migration (2-PR effort; can start with the `unsafe-inline` drop via nonce middleware)
   - Live E2E of P2-41 koralink-wallet (partner flow setup)
4. Carry-forward: confirm the 3 run #24 in_review items in `STATE.json` are demoted now that Reviewer-B verified them.
