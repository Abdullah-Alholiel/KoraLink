# Factory Run #22 — 2026-08-31T15:15Z

**Mode:** full
**Budget:** ~1h15m of 3h (9 buildable items landed; stopped while ahead — remaining board items need Abdullah decisions or are sub-60-min polish deferred per quota guardrail)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (permanent; probe: **200**)

## Preflight

- Lock acquired 15:15:47Z (no prior lock). gh auth OK (Abdullah-Alholiel). `git pull --ff-only` up to date.
- GLM 5.3 probe 200 (18 tokens); deepseek fallback probe 200 (unused). Delegation set to glm-5.3/zai/coding-endpoint.
- Tree dirty with sibling/foreign files only (`.gitignore` M, deleted docs/architecture/*, untracked `docs/agent harnessing architecture/`, `kanban/multica-bridge/`, graphify-out/*) — never staged, worked alongside.
- **ADMIN HOLD LIFTED**: `apps/admin` + partner API clean at run start (Abdullah committed through d0862a7) — admin-layer rotation focus (run#22 % 4 = 2) was therefore viable, with a fresh state-check before every admin item.

## Sentry / error-log triage (Phase 1.6)

Source: Sentry API (EU base, SENTRY_READ_TOKEN, 200) + journalctl (all three services: **zero** err entries in 5h).

| Signature | Count | Verdict |
|---|---|---|
| KORALINK-API-Y `PostgresError: schema "v" does not exist` on a pitches SQL | 1 @ 10:22:49Z | **Probe artifact (resolved-as-noise).** Query text matches NO shipped code: the only `FROM pitch_slots ps` subqueries live in admin pitches.service.ts:55-57 where `v` is properly declared (INNER JOIN venues v); partner.service.ts has no raw pitches SQL. Event sits inside run #21's live-probe window (journal: probe 404s + RBAC denials same minutes; three API restarts 10:19→10:25). Never reproduced. (Reviewer A independently reached the same verdict.) |
| KORALINK-WEB-4 `TypeError: Failed to fetch` :8443 | 1 @ 10:25:24Z | Same restart-churn window. MINOR, no action. |
| KORALINK-API-X (run #20's own E2E artifact), API-B CORS `evil.example`, API-4 401s, and all others | stale | Known noise / expected auth probes — MINOR. |

## Previous-run verification (run #21 → all VERIFIED-TRUE, promoted DONE)

- **P1-28 venue search** — VERIFIED: `venues.service.ts:68-71` additive ILIKE, composed alongside approved/city/partner/geo at :104-108; 4-case SQL-render spec; PWA 300ms debounce (:35-38) feeding the queryKey (server-side, not client-filtered); `clubs.searchPlaceholder` en/ar :442.
- **P2-37 reschedule max_players<2 guard** — VERIFIED: guard at `matches.service.ts:1849-1851`, BEFORE money section (:1853+); spec `matches.reschedule.spec.ts:264-274` asserts BadRequest AND zero inserted transactions AND zero slot updates.
- **P2-35 locale date labels + h23** — VERIFIED: `clubs/[id]/page.tsx:46-53` (`ar-SA`/`en-US` switch; file lives at `app/[locale]/clubs/[id]/`, not `(main)` — task path was stale, code correct), `venue-hours.ts:43` hourCycle h23.

## Findings (GLM A + GLM B + self, merged, evidence-cited)

**Reviewer A — code quality (216s, 16 calls, focus: admin overhaul commits + standing bug classes):**
- IMPORTANT: admin HQ dashboard hardcoded English (dashboard/page.tsx:66-98) — 8 MetricCard labels + dispute-rate sub + chart series names. → **BUILT this run (5052fe1)**.
- IMPORTANT: admin refund re-checks nothing in-tx (transactions.service.ts:75-92) — concurrent refunds both pass the outside-tx pre-check; only the `refund-<id>` unique key prevented a double credit, and the loser died as a raw 500. → **BUILT this run (85e8326, P2-39)**.
- Clean sweeps: `::uuid` casts 0, `eq(col,null)` 0, console.* in API 0, findOne-inside-tx 0, z-index consistent, no dead buttons in the new admin pages, i18n parity held in both apps.
- MINOR: dashboard `.toLocaleString()` without locale arg won't follow ar number formatting (backlog note).

**Reviewer B — product gaps + verification (100s, 9 calls):**
- Run #21 claims: all VERIFIED (above). No overstatement.
- Top gap re-confirmed: notification reachability (P2-34) — sole bell mount at `(main)/page.tsx:97`. → **BUILT this run (0803e10)**.
- Residuals recorded to backlog notes: offline mutation queue (P2-7 stands), UGC/profanity auto-filtering (product decision, not boarded), admin broadcast tooling + venue ratings (pre-existing), float-money `::float` in settlement sums (polish).
- Also verified P2-38 STALE: disputes/reports pages already have loading/empty/error states (admin-ux-overhaul slice 5, a4cf5da) → marked DONE with evidence; run #22 additionally localized their error line.

**Parent self-review:**
- Sibling regression found: 9645e95 (partner dashboard enrichment) added `.groupBy()` + raw `execute` to getDashboard without updating the access-control spec stub → 2 pre-existing test failures on main. Fixed test-only (f69c965).
- Backlog security item executed: WS gateway + JwtModule + jwt-cookie strategy still carried the silent `'fallback-dev-secret'` default (b0f13cb).
- P2-31(5) winnerId: grep across PWA/API/SW → zero consumers → closed as verified-nothing-to-do.

## Board changes

- **DONE ✅**: P1-28, P2-37, P2-35 (verified run #22) · P2-34 (built 0803e10) · P2-38 (stale/done) · P2-11 (ed7a63f) · P2-19 (8552504) · P2-31 items (4) + (5).
- **ADDED**: P2-39 admin refund TOCTOU (fixed same run, 85e8326).
- **HEADER**: ADMIN HOLD banner replaced with HOLD-LIFTED notice (re-check git status before admin items).
- Backlog: run #22 reviewer notes + P2-19 per-IP daily-cap residual.

## Built this run (9 commits, gates per slice)

| Commit | Slice | Verification |
|---|---|---|
| 85e8326 | **P2-39** admin refund: FOR UPDATE lock + in-tx status re-assert + idempotent loser-400 | new spec 7/7; jest 190/190; build 3/3; API restarted + healthy |
| 0803e10 | **P2-34** bell on play/clubs/my-games/wallet/messages | vitest 261/261; tsc 0; lint clean; build 3/3 |
| 5052fe1 | Admin HQ dashboard i18n (labels, chart series, dispute-type map) | admin tsc 0; hq parity 123/123; build 3/3 |
| f69c965 | partner access-control spec stub +groupBy +execute (pre-existing breakage) | jest 28/28 suites, 190/190 |
| 2c46403 | `hq:loadFailed` across 11 HQ pages (+2 detail pages gain first useTranslations) | admin tsc 0; build 3/3 |
| 3016721 | offline banner on clubs/my-games/messages/reports (P2-31(4)) | vitest 261/261; tsc 0; lint; build 3/3 |
| b0f13cb | drop `fallback-dev-secret` JWT default → getOrThrow ×3 (P0-3 follow-through) | gateway spec +getOrThrow stub; jest 190/190; build; restart; live dev-login 200 + /users/me 200 |
| 8552504 | send-otp @Throttle 3/min/IP (P2-19) | **live probe: 400×3 then 429×2** on restarted service |
| ed7a63f | verify-otp never returns token in body (P2-11) | jest 190/190; build; restart; live login 200 + me 200 |

Full-gate finals: `turbo run build` 3/3 zero errors (6 invocations across the run), jest 190/190, vitest 261/261, tsc 0 (api/pwa/admin), PWA lint clean, `NODE_ENV` verified unset before every gate.

## Blockers & decisions

- Admin i18n `.toLocaleString()` locale arg → backlog (next admin i18n touch).
- Per-IP daily cap on send-otp → deferred (needs Redis-vs-DB counter decision).
- No `/notifications` route: deliberately not built — the sheet on every tab covers reachability; a route would duplicate the surface.
- Reviewer outputs arrived complete via async completion messages (no truncation trap this run).

## Next-run recommendation

Verify the 3 in_review items (STATE.json), then the buildable queue is: P2-31(2) reports listMine pagination (small, self-contained) · P2-8 push-text localization (medium) · P2-13 Nearby/TopRated pills (needs radius-scoring decision) · P2-32/P2-29/P1-16 (need Abdullah).
