# Factory Run #16 — 2026-08-29T20:15Z (first full-3h-budget run)

**Mode:** full (review + board + 2 build items)
**Budget:** ~55 min of the ~3h budget (both remaining buildable items were either DONE,
blocked on sibling edits, or needed Abdullah's decision — see Blockers)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (permanent; probe: **200**)

## Preflight

- No lock; lock taken 20:16Z. gh auth OK (Abdullah-Alholiel). `git pull --ff-only`: up to date.
- Delegation config already `glm-5.3`/zai/coding-base, `reasoning_effort low`,
  `model.context_length 1000000` — no self-heal needed. GLM probe 200 → reviewer stays glm-5.3.
- Foreign dirty paths recorded, never staged: `.gitignore` (M), `docs/architecture/*`
  (untracked harness files), later `graphify-out/*` (foreign watch daemon refresh).
- `HERMES_DELEGATED_CHILD_CONTEXT=1` INHERITED this session → all kanban CLI calls under
  `env -u HERMES_DELEGATED_CHILD_CONTEXT`.
- Reviewers ran SYNCHRONOUSLY this session (background delegation unavailable in cron
  one-shot runner; noted — result identical, wall-clock fine: 117s + 177s, zero timeouts).

## Sentry / error-log triage (Phase 1.6)

- Source: Sentry API (SENTRY_READ_TOKEN, EU base de.sentry.io) — 200 on both projects.
- koralink-api 24h: 10 issues, ALL last-seen ≤ 2026-08-28T18:14Z (pre-run-#15), all known:
  KORALINK-API-4 UnauthorizedException ×117 (expected 401 probe noise, MINOR),
  -B CORS `evil.example` ×21 (guard working as intended),
  -W Failed-query ×1 (the documented 2026-08-28 boot incident),
  -A NotFoundException /health ×19 (pre-alias probes), remainder single-digit 4xx noise.
- koralink-web 24h: 2 stale issues (CSP SW registration from 08-20, connectivity check).
- journalctl: zero err entries in 5h across api/pwa/admin. **No new P0/P1 error clusters.**

## Findings (GLM reviewers + self, merged, evidence-cited)

Reviewer A (code quality, ~117s): zero CRITICAL. IMPORTANT: (1) `(main)/profile/page.tsx`
has zero loading/error/empty branches; (2) `host/page.tsx:9` Suspense fallback={null}
blank-flash → boarded P2-26. MINOR: createVenue/createSlot bare returns (already P2-5
class — NOT extended by today's new endpoints, which return explicit selections). Standing
bug-class sweep ALL CLEAN (::uuid 0, eq(null) 0, console.* 0, z-index clean, i18n parity
exact, hydration guards present, spots-count correct incl. host).

Reviewer B (product gaps + verification, ~177s): run #15 claims **100% BUILT-AS-CLAIMED**
(apiFetch AbortSignal.any + caller-abort rethrow confirmed at api.ts:40-57; common.close in
both catalogs; build 3/3; vitest 234/234). New gaps: **P1-27** no wallet withdrawal
(wallet/page.tsx:178 comingSoon toast; no withdraw endpoints), **P1-28** venue-level
discovery missing (venues.controller.ts: GET / and GET /:id only). Payout read-only = known
P2-3; payment provider = known P0-2.

Self-review: confirmed P1-26 design composes existing scopedPitchIds/UPCOMING_STATUSES
patterns; no new contract surface on the PWA (admin-only slice).

OS research (Phase 1.7): `@serwist/background-sync` (`BackgroundSyncPlugin`, MIT) is the
natural path for P2-7 offline mutation queue; caveat: hand-written worker → migration or
hand-rolled IDB queue. Recorded in BOARD.md backlog; NOT adopted (Gate-cycle rule).

## Previous-run verification

- P1-12 admin i18n: stands IN-REVIEW (visual RTL check still needs Abdullah's eyes —
  browserless box, unchanged).
- P1-24 / P2-24 / schedule policy: run #15 re-verified them already; Reviewer B re-ran the
  full gate suite this run (build 3/3, vitest 234/234) — all green.

## Board changes

- ADDED: P1-27 (wallet withdrawal), P1-28 (venue discovery) — P1; P2-26 (profile/host UX
  states) — P2. Mirror cards t_2ba0aa83, t_e9bad506, t_465fd520 created + gated under
  t_ce9a513a (verified `status: todo` + parent).
- P1-26: TODO → IN-REVIEW (built this run, see below).
- P2-25: UI-warning half built this run (cda3833); category exceptions remain an Abdullah
  decision — row annotated.
- Backlog: serwist background-sync research note added.

## Built this run (slices, commits, verification evidence)

**Item 1 — P1-26 partner match/roster visibility (anchor).**
- Gate docs: `docs/plans/run16-partner-match-visibility/` (retro + program design +
  Gate 3 contract checklist, all items ✓).
- `4e7ad44` API: `GET /partner/matches` (scope today|upcoming, Riyadh-local calendar day via
  `AT TIME ZONE 'Asia/Riyadh'`, `?status=` enum-validated, limit/offset paging, `COUNT(*) OVER()`
  total, roster counts incl. host, no_show counts) + `GET /partner/matches/:id` (populated
  roster: name/phone/team/is_host/no_show, host first; ownership via assertPitchAccess,
  Admin bypass). GetPartnerMatchesDto (forbidNonWhitelisted-safe). 6 jest specs.
- `6171bd4` Admin: `/partner/matches` list (scope/status filters, live socket refresh) +
  `/partner/matches/[id]` roster detail; `partner.matches` nav section (Trophy) for both
  roles; PartnerMatchRow/List/Detail types; en/ar `partner.matches` namespace (24 keys),
  **223/223 leaf parity re-verified programmatically**.
- Gates: `tsc --noEmit` 0 (api + admin), root build **3/3**, partner jest **18/18**,
  vitest **234/234**. Services restarted AFTER dist written (grep confirmed new code in
  dist before restart); health 200.
- **Live E2E PASS**: owner dev-login → `?scope=upcoming` 200 (2 real rows, envelope
  matches contract, spots 2/8, host name) → `&status=Cancelled` strict subset → detail 200
  (roster 2 players, host-first, phones, no_show flags; `pitch_id` stripped);
  negative: player-role 403, `?scope=bogus` 400, anon 401.

**Item 2 — P2-25 quiet-hours equal start/end warning.**
- `cda3833` PWA profile: when `quiet_start_hour === quiet_end_hour`, inline amber warning
  (role=status) under the hour selects — "same start and end mutes all day" (en + ar).
- Gates: vitest 234/234, `type-check` 0 errors.
- NOTE: deliverability-category exceptions (the run #15 Reviewer B half) remain a design
  decision for Abdullah — intentionally not built.

## Blockers & decisions

- Sibling agents ACTIVELY editing PWA files this run (three en.json/ar.json/profile-page
  mid-edit collision warnings from the write guard). My diffs verified clean of foreign
  content before both commits. P2-23 (reporter closure) NOT started: it edits exactly the
  PWA files siblings are touching right now — high entanglement risk, so the build loop
  stopped at 2 items with ~2h nominal budget left. Decision: a clean tree beats a rushed
  third slice; next run picks P2-23 first (siblings should be done).
- P2-13 skipped (needs Abdullah's wire-vs-remove decision); P1-25 next-up but is the bigger
  schema slice (migration + PWA slot gating) and sibling churn made starting it late unwise.
- Background delegation unavailable in cron sessions (runner limitation) — reviewers ran
  synchronously; no impact on results.

## Next-run recommendation

1. **P2-23** first (siblings' PWA churn should have landed — re-check `git status` first).
2. Then **P1-25** venue operating hours as the anchor (schema slice: venues hours columns +
   migration + slot generation clamp + PWA display), fillers P2-13 (if decided), P2-26,
   P2-24-style smalls.
3. Keep glm-5.3 probe + 2-reviewer pattern; consider re-checking whether background
   delegation returns in cron (cosmetic — sync worked fine).
