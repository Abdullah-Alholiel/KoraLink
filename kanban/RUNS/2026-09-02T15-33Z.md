# Factory Run #27 — 2026-09-02T15:33Z
**Mode:** full (4 small slices built — P1-17c, P1-17d, P2-19, P2-32)
**Budget:** ~70 min of 3.5h, headroom for next run
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (PERMANENT; **probe: 200 on z.ai, but delegation children STILL 401 at the gateway layer — 4th consecutive run, parent self-review**)

## Preflight

- 2026-09-02T15:19Z, repo at `1c91c45` (run #26 main, clean). No lock present → wrote `kanban/LOCK.json`.
- gh auth ✓ (Tailscale profile dir).
- Strix: ALREADY DONE for September — `strix_scan=2026-09-02 (run #25)` recorded in STATE.json notes. Per runbook: skip + report.
- Quota: z.ai weekly 0% (per directive), z.ai 5h 14%, opencode-go rolling 39%. z.ai 200 on direct probe; deepseek 402 (budget exhausted). Per directive: USE z.ai glm-5.3-flash.
- Set delegation: `delegation.model=glm-5.3`, `delegation.provider=zai`, `delegation.base_url=https://api.z.ai/api/coding/paas/v4`.

## Delegation result (zai children 401 — 4th consecutive run)

Two parallel reviewer dispatches both 401'd at the gateway daemon (PID 3537113, booted 06:52:35Z). Direct zai key probe = 200 (same key, fresh payload). The gateway is resolving children through stale state. Per run #26 plan: fall back to opencode-go → deepseek-v4-flash → **parent self-review**. Neither alternative is available on this machine. **Recorded for run #28+ as a re-probe + opencode-go investigation.**

## Sentry / error-log triage (Phase 1.6)

- **API Sentry** (24h, 25 issues): top hits are noise — KORALINK-API-B (80×, HeadlessChrome probing `127.0.0.1:3000`, external scanner); KORALINK-API-4 (117×, `Invalid or missing session cookie`, expected auth noise); KORALINK-API-Y (1× 2026-08-31, the previously-refuted probe artifact with the `schema "v" does not exist` query — query text matches NO shipped code, refutation stands); KORALINK-API-X (2×, transactions unique-violation race — fixed in P2-9/P0-4). **No actionable new errors.**
- **PWA Sentry** (24h, 5 issues): KORALINK-WEB-2 (ServiceWorker `scriptURL violates CSP`, 2× from 2026-08-20 — minor, out of scope).
- **journal** (5h): 0 errors across all 3 services. Clean.

## Findings (self-review, zai delegation 401'd)

Run #26 spot-verification (file:line evidence):
- **bfd453b P0-7** PASS — `auth.service.ts:234` always passes `expiresIn`; `auth.controller.ts:120-124` reads `DEV_LOGIN_ENABLED` flag; `DevLoginBar.tsx` honors `NEXT_PUBLIC_DISABLE_DEV_LOGIN_BAR` build flag. Live-confirmed dev-login is 403.
- **9678790 P0-8** PASS — `push-endpoint.validator.ts` exists; called in BOTH `notifications.controller.ts` (subscribe) AND `notifications.service.ts:233` (send). Reject ranges verified.
- **835e27d P1-17a** PASS — root overrides + apps/*/package.json all carry the expected pins; drizzle-orm stays at 0.44.7 (deferred). The 6 tsc latent-error fixes are in the running dist.

P0-5 area scan (broader, buildable sub-items):
- **Per-category push preferences**: NOT buildable in one run (new table + per-user prefs + API endpoints + UI + i18n — full Gate 3 contract needed). → defer to #28.
- **Install-triggered push enablement**: Buildable (~60-90 min), but pairs naturally with per-category prefs (single wiring). → defer to #28.
- **Email-worthiness matrix + email infra**: NOT buildable — zero email infra in repo (no nodemailer, no SendGrid, no SMTP). Building from scratch in one cron slot is reckless. → carve out a separate P0+ cycle; record as a new open P0+ (call it P0-9).
- **Per-IP daily OTP cap (P2-19 residual)**: buildable in ~20 min. → BUILT slice 3.
- **Per-phone daily cap (existing)**: already 10/day in `otp-store.service.ts` (cache-backed, sliding TTL).

Other board items triaged for THIS run:
- **P1-17c WS origin** (Strix): buildable, single file, drop `isProd` gate. → BUILT slice 1.
- **P1-17d ICS escape** (Strix LOW): buildable, single helper + apply + spec. → BUILT slice 2.
- **P2-32 foreign pitchId 404** (decided 2026-09-02): buildable, single check in partner service. → BUILT slice 4.
- **drizzle 0.45.2** (deferred from #26): NOT this run — 6 type tightenings need a half-day audit; better as its own focused cycle.
- **P0-5/P0-6**: per the run plan, #27 was supposed to land the P0-5 broadened scope. P0-5 turned out to be 3 sub-cycles in itself; the small items above were the realistic budget for one cron run.

ADMIN STATE CHECK: `git status --short apps/admin apps/api/src/modules/partner apps/api/src/modules/admin*` = clean; admin service active. Run #27 touches partner.service.ts (P2-32) — within scope.

## Board changes

No new items added. 4 items closed (will mark in BOARD.md commit):

| ID | Area | Status change | Commit |
|----|------|---------------|--------|
| P1-17c | API/WS | DONE ✅ | 27-WS-ORIGIN |
| P1-17d | API/PWA | DONE ✅ | 27-ICS-ESCAPE |
| P2-19 | API/Auth | DONE ✅ | 27-OTP-IP-CAP |
| P2-32 | API/Partner | DONE ✅ | 27-PITCHID-404 |

## Built this run

### Slice 1: P1-17c WS origin allowlist
- **File**: `apps/api/src/modules/gateway/app.gateway.ts:80-97` — dropped the `isProd` gate; unlisted origins are now ALWAYS disconnected (was: warn-and-allow in dev).
- **Tests**: 4 new jest cases in `app.gateway.spec.ts` (rejects unlisted origin in dev; rejects unlisted origin in prod; accepts PLAYER_URL; accepts ADMIN_URL). 11/11 pass.
- **Live probe** (real socket.io upgrade with `Origin: https://attacker.example`): journal shows `WS connection from unlisted origin "https://attacker.example" rejected` — disconnect fired correctly. (The socket.io client's auto-fallback to a different transport that drops Origin is the reason a naive client script may report "connected" — the first attempt is rejected as designed.)

### Slice 2: P1-17d ICS escape
- **New file**: `apps/api/src/common/security/ics-text.ts` — `escapeIcsText()` per RFC 5545 §3.3.11 (backslash → `\\`, semicolon → `\;`, comma → `\,`, CRLF/CR/LF → `\n`).
- **File**: `apps/api/src/modules/matches/matches.controller.ts:33` + `:91-105` — applies the helper to `match.title`, `location`, and the description. Bonus fix: the original DESCRIPTION had `\\u2022` (literal `\u2022`) instead of a real bullet — now `•`.
- **Tests**: new `apps/api/src/common/security/ics-text.spec.ts` (7 cases: backslash first-order, semicolons, commas, CRLF collapsing, Strix-shaped injection payload, empty input, plain text pass-through).
- **Live probe** (`/api/v1/matches/<id>/calendar`): the response has real `\,` escapes in the LOCATION line, real `0d 0a` line terminators, and the real bullet `•` in DESCRIPTION. (Verified via xxd.)

### Slice 3: P2-19 per-IP daily OTP cap
- **File**: `apps/api/src/modules/auth/otp-store.service.ts:6,15,29,73-90` — added `OTP_DAILY_IP_CAP = 50`, `keys.ip_day(ip)`, `getIpDailyCount(ip)`, `incrementIpDaily(ip)`. Same cache + sliding-TTL pattern as the per-phone counter.
- **File**: `apps/api/src/modules/auth/auth.service.ts:58,87-107,121-123` — `sendOtp(phone, ip?)` now gates on the per-IP counter BEFORE incrementing; 429 with a distinct message ("Daily SMS limit reached for this IP") on the cap.
- **File**: `apps/api/src/modules/auth/auth.controller.ts:4,11,52-61` — `@Req() req: Request` extracts `req.ip ?? req.socket.remoteAddress ?? undefined`; threads through to the service.
- **Tests**: 3 new jest cases in `auth.service.spec.ts` (429 on cap, increment on success, no-op when IP absent). 9/9 pass.

### Slice 4: P2-32 foreign pitchId → 404
- **File**: `apps/api/src/modules/partner/partner.service.ts:860-869` — after `scopedPitchIds` is loaded, an explicit `?pitchId` not in scope now throws `NotFoundException('Pitch not in your scope.')` instead of silently falling through to the venue/no filter.
- **Tests**: 2 new jest cases in `partner.match-visibility.spec.ts` (throws 404 on foreign-but-valid, accepts own).

## Verification

- `tsc --noEmit -p apps/api/tsconfig.json` → **0 errors**
- `turbo run build` (root) → **3/3 tasks successful** (api 4m11s, admin 1m22s, pwa 1m11s)
- `npx vitest run` in `apps/player-pwa` → **272/272 tests, 38 files** (unchanged from #26; no PWA changes this run)
- `npx jest` in `apps/api` → **35 suites, 296 tests, 0 failures** (+16 from #26: 4 gateway + 7 ics-text + 3 auth + 2 partner)
- API restart + health 200; dev-login 403 (P0-7 still in force); dist grep confirms new code shipped
- Live WS probe (Origin: attacker.example) → gateway journal shows "rejected" message; first attempt disconnected
- Live ICS download → commas escaped, real CRLF, real bullet character

## Blockers & decisions

- **zai delegation children 401 — 4th consecutive run (#24/#25/#26/#27).** Pool is clean, direct key probes return 200, the gateway daemon (booted 06:52:35Z, PID 3537113) is resolving children through stale state. Parent self-review used this run. **Next run (#28) MUST re-probe** AND investigate a gateway restart — at this cadence the staleness is degrading review quality (no second pair of eyes). Fallback chain after re-probe: opencode-go (no key on this VPS) → deepseek-v4-flash (402) → self-review.
- **P0-5 per-category push preferences + install-triggered push enablement** → run #28. Clean cycle: Gate 3 contract defines the 4 categories (match / chat / promo / system), schema migration for `notification_categories` + `user_notification_prefs`, API endpoints, profile UI, en/ar i18n. Owner directive 2026-09-02 stands.
- **Email infrastructure** → must be a separate cycle. No nodemailer, no SMTP, no Resend/SendGrid in the repo. Carve as a new P0+ (let's call it **P0-9: email infra** — needs an architecture decision: managed provider vs self-hosted SMTP). Build an email-worthiness matrix FIRST, then ship the transport.
- **drizzle-orm 0.45.2** → carry to a focused half-day cycle (not a cron run). The 6 type tightenings are tractable but cumulative.
- **P1-17b remaining** (secrets rotation, Sentry token, Redis password, admin→HttpOnly cookies) → multi-step; defer.

## Next-run recommendation

#28 — push reliability + P0-5 broadened scope:
1. **P0-5 per-category push preferences** (the user-visible piece the cycle is for) — schema migration + API + UI + i18n (the full sub-cycle, 2-3h)
2. **Install-triggered push enablement** (iOS PWA install-first gate) — single wiring through the existing `InstallPrompt` + push permission flow (~60-90 min)
3. **drizzle 0.45.2** if budget allows

Defer P0-9 (email infra) to its own cycle after a Gate 1 product spec from Abdullah. The cycle is gated on the email provider decision.

If zai delegation is STILL 401 on #28, fall back to deepseek-v4-flash (which means a smaller build this run); if no path works, do board+review-only and document the persistent gateway staleness for the user to investigate.
