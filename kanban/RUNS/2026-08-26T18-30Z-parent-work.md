# Factory Supplement #2 — Parent-session work before run #2 (2026-08-26T18:30Z)

**Mode:** parent-session continuation (not a factory tick) — verified claims, built 2 board items.

## What was done this session

1. **Verified P0-1 (chat access control) LIVE** → promoted DONE:
   - member (u1, Ahmed) `GET /matches/:id/messages` → 200 with 2 messages
   - outsider (u9, Bandar) same endpoint → **403** "You are not a member of this match."
   - outsider match detail → 200, roster intact, `messages` stripped to 0
2. **Verified P1-3 idempotency LIVE** → idempotency portion DONE:
   - send#1 with cmid `e2e-idem-test-…` → 201, id `45c62ab9`
   - send#2 same cmid → 201, **SAME id returned**, DB count = 1 row
3. **P1-4 hot-FK indexes — BUILT** (`0953c4b`):
   - 7 index declarations (matches.host_id/pitch_id, activities.actor_id,
     disputes.reporter_id, reports.reporter_id, venues.owner_id,
     transactions.reference_id) → migration `0015_bored_wraith`, applied, verified 7/7 in pg_indexes.
   - tsc 0 · jest 30/30 · build 3/3
4. **P1-5 SW push locale — BUILT** (`2752c27`):
   - `push_subscriptions.locale` column (migration `0016_faithful_warpath`, applied, live-verified)
   - PWA subscribe sends `useLocale()`; `sendPushToUsers` injects per-subscription locale;
     SW reads `data.locale` (fallback en)
   - Live E2E: subscribe `locale:"ar"` → stored `ar` in DB (test row cleaned after)
   - tsc 0 · jest 30/30 · vitest 217/217 · build 3/3 · API restarted

## Handoff state

- **Board:** P0-1 ✅, P0-2 WIP (dummy gated, provider pending), P1-3 idempotency ✅ (pagination/media TODO),
  P1-4 ✅, P1-5 ✅. Remaining P1: P1-1 (scheduler), P1-2 (discovery filters), P1-3 remainder.
- **Tree:** clean at `2752c27` (schema/migrations committed with their code in the same slice —
  Phase 4.5 rule respected).
- **Provider:** deepseek-v4-pro primary → deepseek-v4-flash fallback (live-tested). Cron pinned.
- **Next-run recommendation:** P1-1 (scheduler — @nestjs/schedule: reminders + POTM finalize +
  periodic auto-complete) deserves a dedicated run; P1-2 (discovery: skill_level filter +
  radius_km hard cutoff) is a full vertical slice with UI work. Both fit a single tick each.
