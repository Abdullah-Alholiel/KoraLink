# Factory Run #21 — 2026-08-31T10:16Z

**Mode:** full
**Budget:** ~2h10m actual (of ~3.5h) — 4 build commits + full gates + live probes; stopped early on board cleanliness (nothing left finishable in remaining budget)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3` (permanent; probe: 200) · 2 reviewers 78s + 189s, zero timeouts — 7th consecutive clean review run

## Preflight
- No lock on arrival (wrote `2026-08-31T10-16-04Z`, pid 2733623). GLM probe 200 → delegation `glm-5.3`/zai/coding-endpoint set; `reasoning_effort low` confirmed; `context_length 1000000` confirmed.
- gh auth OK (Abdullah-Alholiel). `git pull --ff-only` clean; HEAD `26a0179`.
- Dirty foreign paths respected, never staged: sibling's admin-ux-overhaul WIP (pitches + matches-edit slices: `apps/admin` pages/components, `apps/api/src/modules/admin/{pitches,matches,venues}.*`, schema.ts, drizzle journal, activities.service.ts, `.gitignore`, harness docs).
- **Env trap discovered mid-run:** `HERMES_DELEGATED_CHILD_CONTEXT=1` IS set in this cron session (initial preflight grep missed it — runtime injects it late). Every kanban call prefixed `env -u HERMES_DELEGATED_CHILD_CONTEXT` (runbook 3.7 rule 2 remedy). No state lost.

## Sentry / error-log triage (Phase 1.6)
- journalctl (5h, 3 services): **zero error lines**.
- Sentry 24h: ONE new signature — **KORALINK-API-X** (transactions insert failed ×2, lastSeen 2026-08-30T20:56Z) = run #20's OWN live E2E drill exercising the idempotency-key unique-constraint path (the A→B→A bug it found and fixed in `a5b9251`). Self-inflicted test artifact, pre-fix; no action. Everything else pre-dates 08-29 (auth-401 probes 117×, CORS canary, boot-incident residue) — MINOR/no action.
- **Verdict: clean — nothing new to fix.**

## Previous-run verification (Phase 1 — all 5 in_review items, by Reviewer B with real test evidence)
- **P2-36** DatePicker strip → PASS (vitest 2 files/17 tests; deployed chunk grep 1× aria-current + TODAY; i18n 684/684).
- **P1-13 cross-day** (48170d8 + ff49a35) → PASS (jest 13/13 incl. 2 past-slot specs; guard at matches.service.ts:1833 BEFORE money; pickDay/noSlots en+ar).
- **P1-13 core** reschedule → PASS (jest 74/74 matches suite; FOR UPDATE row+slots ORDER BY id; findOne outside tx; cancelMatch FOR UPDATE).
- **P2-31(1)** admin hours guard → PASS (venues/page.tsx:76-85 validates before patch; `hoursInvalid` en.json:139 + ar.json:139).
- **Quiet-hours spec fix** (aa7a2d2) → PASS (jest notifications.push-preferences 9/9, `quiet_end: 0` end-exclusive fixtures).
→ All five promoted DONE on the board.

## Findings (GLM A/B + parent, merged, evidence-cited)
- **Reviewer A (code quality, API rotation):** 0 CRITICAL. 2 IMPORTANT, both adjudicated by parent:
  - **matches.service.ts:1920 divide-by-(max_players−1)** in rescheduleMatch → CONFIRMED real class; parent verified DTO floors max_players ≥2 (create-match.dto.ts @Min(2)) so it's defense-in-depth for drifted rows — **FIXED this run** (P2-37, guard BEFORE money moves, spec proves zero ledger writes).
  - **WS join-conversation missing participant check** → **REFUTED by parent**: app.gateway.ts:353 DOES throw `WsException('You are not a participant in this conversation.')`; the "deliberately omitted" comment at :388-390 is the documented P2-6 LEAVE-conversation decision. False positive — recorded in backlog.
  - Minors: per-attempt reschedule idempotency keys make `onConflictDoNothing` dead code (intent/mechanism comment gap); wallet balance read in reschedule not FOR UPDATE (low risk, single-user wallet, tx-isolated); `Date.now()`-in-render hydration class (already backlog line 131). Standing sweeps clean: `::uuid` 0, `eq(col,null)` 0, console.* 0, i18n 684/684, z-index convention holds, contract §2 ✅.
- **Reviewer B (gaps + verification):** 5/5 claims PASS. NEW gaps: P0 wallet-ledger-audit → folded into P0-2 (real provider needed); P0 "no chat moderation" → partially refuted (P1-31 ships per-message reporting; residual automated profanity filtering = product decision, NOT boarded); P1 no-show/late-cancel penalty → product rule on P0-2 money path, not boarded; P1 SMS-bomb throttle → re-confirms P2-19 (dup); P1 reminder-fanout failure surface → observability backlog; P1 unread-badge offline sync → P2-7-adjacent; P2 admin disputes/reports empty states → **boarded P2-38**; P2 offline deep-link fallback + per-sheet error isolation → polish backlog.
- **Parent self-review:** verified both IMPORTANTs in code (A1 real, A2 refuted); confirmed `GET /venues` already had city/geo filters — the real P1-28 gap was name search + PWA wiring (client-side filter of ≤50 rows only).
- **OS research (Phase 1.7):** 1 web_search_prime call — pg_trgm + GIN index confirmed as the canonical Postgres fuzzy-name-search approach (P1-28 uses plain ILIKE now; pg_trgm similarity ranking documented as a later option in the program design). Recorded "OS research: pg_trgm note" — no library adopted.

## Board changes
- Verified → DONE: P2-36, P1-13 (cross-day + core, re-verified), P2-31(1) sub-item, quiet-hours spec fix (on P1-20 row).
- Built → DONE: **P1-28** (venue search), **P2-37** (max_players guard), **P2-35** (locale labels + h23).
- Added: **P2-38** (admin disputes/reports empty states — TODO).
- Mirror: 4 cards created + linked under `t_ce9a513a` (P1-28 t_5837cbc6, P2-36 t_4289530f, P2-37 t_92441bf4, P2-38 t_00785a0c); 6 comments applied (built/verified); never completed (parent gate rule).

## Built this run (slices, commits, verification evidence)
| Item | Commit(s) | What | Verification |
|---|---|---|---|
| P1-28 slice 1 | `2caffce` | `GET /venues ?search=` — additive name/city ILIKE clause (never short-circuits geo/city/partner/approved), DTO MaxLength(80); first-ever venues jest spec (4 cases, PgDialect.sqlToQuery) | jest 4/4 new; full jest 183/183 |
| P1-28 slice 2 | `0a2b05d` | useVenues sends `search`; clubs page 300ms-debounced search → server-side (search reaches whole table, not first ≤50 rows); 3 hook specs | vitest 261/261 (38 files); type-check 0 |
| P2-37 | `d833f03` | rescheduleMatch rejects max_players<2 BEFORE money moves (divide-by-zero defense); spec asserts zero ledger writes | jest reschedule 14/14 |
| P2-35 | `5e98f07` | clubs/[id] formatDateLabel ar-SA/en-US; venue-hours riyadhHour hourCycle h23 | vitest full green; build 3/3 |
| Gates | — | `npm run build` **3/3 tasks, 0 errors** (admin first attempt OOM-killed 137 — transient: 3 resident tsservers + logflare + webui; solo retry passed 1m28s, service restarted) | API+PWA services restarted post-build; /health 200 |
| Live probe | — | `?search=KSU`→KSU Stadium · `?search=olaya`→Olaya Sports Park (city, case-insensitive) · `?search=ksu&lat=24.7&lng=46.7`→distance_m 5425 (additive) · 81-char→400 MaxLength · nonexistent→0 · no-search→4 | PASS on restarted API (recipe `/tmp/run21-search-probe.sh`) |

## Blockers & decisions (new this run)
- **DECISION (autonomous):** A2 (WS join-conversation) refuted after parent code verification — no build. Evidence rule applied both ways: A1 fixed, A2 dismissed.
- **Shared-tree:** sibling admin-ux-overhaul landed `c6e329a` + `9490354` mid-run (partner endpoints VenueOwner-only fix + admin edit-matches slice 3); their WIP broke the API build once (activities verb union), they fixed it themselves before my next build pass. My 4 commits pushed cleanly on top (`9490354..5e98f07`); their remaining WIP (schema/venues/matches edits) untouched.
- **Admin OOM:** first admin build killed (137) under memory pressure; solo retry passed. Watch VPS memory (24GB, 0 swap) when running admin builds alongside LSP servers.
- P0-2 / P1-8 / P1-14 / P1-29 unchanged (real payment provider pending — Abdullah).

## Next-run recommendation
1. Verify run #21's three in_review items: P1-28 (re-run gates + live `?search=` probe via `/tmp/run21-search-probe.sh`), P2-37 (reschedule spec 14/14), P2-35 (locale labels + h23 grep). Visual check of the clubs search UX on-device (no browser on box) for Abdullah.
2. Board queue: **P2-34** (notification bell in (main) layout or `/notifications` route — cheap, high player value, finishable single-run slice) or **P2-38** (admin disputes/reports empty+error states). P2-31(2) (reports listMine pagination) is a solid API slice.
3. Keep probing GLM first; reviewer pipeline healthy (7 clean runs on glm-5.3). Note the `HERMES_DELEGATED_CHILD_CONTEXT` late-injection — preflight grep is unreliable; just always prefix kanban calls with `env -u`.
