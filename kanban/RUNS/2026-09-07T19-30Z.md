# Factory Run #41 — 2026-09-07T19:30Z (manual, owner-triggered)

**Mode:** full (review + board + 2 build items)
**Budget:** ~55 min used of ~3h (reviewers 186s combined; second item completed well under budget)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` via zai (probe 200; delegation re-asserted pre-run)

## Preflight

- Lock absent → acquired (`manual-20260907T184127Z`). gh auth OK (Abdullah-Alholiel). Pull clean (already up to date).
- Delegation → zai glm-5.3-flash after a 200 5-token probe.
- Health: api/pwa/admin all active, `/health` 200, ZERO journal errors (5h window).
- Admin state check PASS (clean tree across apps/admin + partner/admin API modules → no ADMIN HOLD).

## Sentry / error-log triage (Phase 1.6 — source: Sentry API, EU base)

- koralink-api 24h: API-14/15 (skill_level query fails, 18+2, lastSeen Sep 6 21:20Z) = P0-10
  drift-incident window, no recurrence post-restart. API-13 (matches.waitlist relation, 37,
  lastSeen Sep 6 17:17Z) = fixed by 5f61ec0 before the last event; stale issue. API-11 (demo
  purge) STILL QUIET since Sep 6 00:00Z — Sept 8 00:00Z probe stands.
- koralink-web 24h: WEB-6 (viewport-diagnostic, 33) intentional per-session diagnostic; WEB-9/7
  transient fetch failures (1/3 events, tailnet network blips); nothing new.
- **Outcome: no new P0/P1 from Sentry this run.**

## Findings (reviewers + parent, merged, evidence-cited)

- **Reviewer A (API modules rotation, 0 CRITICAL / 3 IMPORTANT / 4 MINOR):**
  - IMPORTANT #1 **FIXED this run** — startMatch/completeMatch status transitions read status
    then `UPDATE … WHERE id` with no lock and no in-WHERE predicate (double-write race;
    cancelMatch already locked). → 3ea4a30.
  - IMPORTANT #2 — the `::float` money-cast class extends past the four boarded sites; parent
    had already enumerated all 15 live sites pre-dispatch; Tier A fixed this run via a49033f
    (aggregates), display-only casts deliberately Tier-ed out with rationale.
  - IMPORTANT #3 — wallet DEBIT "burns an idempotency_key slot on failed debit" — **REFUTED by
    parent**: recordTransaction's ledger insert is INSIDE the tx; a rolled-back negative-balance
    debit discards the key (wallet.service.ts:114-141 + catch path :143+). Recorded, not boarded.
  - MINORs queued to backlog notes (DTO bound drift: cast-vote candidateId, pitchCostSar @Max,
    mark-no-show @IsUUID convention, benign bare-eq timestamp updates).
- **Reviewer B (run #40 verification + product gaps, 7/7 VERIFIED):** useNow + POTM
  hydration-safety, shared OfflineBanner (6 pages, no remnants), NotificationSheet unread-count
  endpoint + rollback, FilterBar aria-pressed + 44px token, RescheduleSheet days={7},
  null-actor guard, DatePicker todayKey memo dep — all confirmed with file:line evidence →
  run #40 in-review items promoted DONE.
  - Product gaps (deduped against the board): wallet withdraw = **existing P1-27**; recurring
    matches = **existing P2-18**. NEW leads recorded: VAT/maqsoos-compliant invoices (P1),
    venue review SUBMISSION route (rating column exists, no POST route — unwritable), per-player
    bill-split (P2). None boardable without an owner call (all are features, not defects).
- **Parent self-review:** pinned the real P2-4 blast radius (15 sites, aggregates vs display),
  empirical 2M-case float sweep (0 fils loss at numeric(12,2) scale — the boarded risk was
  theoretical; the hazard CLASS was removed from the payout write path instead), refuted
  Reviewer A's idempotency-key corollary, identified cancelMatch as already-locked (narrowed
  the fix to start/complete).

## Previous-run verification (claims ≠ facts)

| Run #40 item | Re-proof (Reviewer B, file:line) | Verdict |
| --- | --- | --- |
| 257514e useNow + POTM hydration | useNow.ts:26; MatchCard.tsx:9,37; my-games:11,41; zero bare Date.now() in POTM render | **DONE ✅** |
| 257514e OfflineBanner shared | layout/OfflineBanner.tsx consumed by 6 pages; no duplicate-banner JSX remnants | **DONE ✅** |
| 257514e NotificationSheet unread + rollback | unread-count queryKey feed:25-26; rollback :118-121 | **DONE ✅** |
| 257514e FilterBar a11y | aria-pressed :62; tailwind hit token :28-30 | **DONE ✅** |
| 3da181e RescheduleSheet 7-day | days={7} :125; spec pinned | **DONE ✅** |
| 10bb2ae null-actor + todayKey | :240-243 guard; :64,88 memo dep | **DONE ✅** |

## Board changes

- P2-4 → **DONE ✅** (a49033f).
- P2-49 class note: final sub-class (start/complete transitions) closed by 3ea4a30.
- Run #40 in-review items → DONE ✅ (verification table above).
- Backlog additions: run #41 reviewer notes line (DTO minors, refutation record, VAT/review/
  bill-split leads). No new P-rows (both reviewer "P0 candidates" deduped/refuted per rule).

## Built this run (slices, commits, verification evidence)

1. **`a49033f` — P2-4 DONE: money aggregates as exact strings** (6 code files + 1 new spec).
   Admin settlements/transactions lists, users totalSpent, partner revenueToday/weekRevenue →
   `::text`; generatePending write path string-in/string-out (`quantizeMoney2dp`, float removed
   from the payout decision path). +7 specs incl. 2 PgDialect SQL tripwires (`::text AS amount`
   pinned, `::float` forbidden).
2. **`3ea4a30` — startMatch/completeMatch FOR UPDATE locks** (Reviewer A IMPORTANT #1).
   +4 lock specs (12/12 in matches.join-leave-lock.spec.ts). Refutation of the idempotency-key
   corollary recorded in the commit message.

**Gates (final, after both slices):** API tsc 0 · jest **419/419** (52 suites, +11) · vitest
**58 files / 412 tests** · eslint untouched by these paths · turbo build **3/3 exit 0** ×2.
**NOT executed:** `systemctl --user restart koralink-api.service` — approval wall (see Blockers).

## Blockers & decisions

- **API restart deferred (owner approval wall on systemctl restart).** dist/ is built and
  committed; until a restart, admin/partner endpoints serve the pre-#41 code (float values).
  Owner command: `systemctl --user restart koralink-api.service`. Next cron run will also
  attempt it. Code, DB, and tests are unaffected.
- Owner decisions queue unchanged: P0-2 payment provider (gates P1-8/P1-14/P1-29), P1-38+P2-50
  upload pipeline, P2-47 cancel-purge, P2-51 demo-migrate runbook, P1-41 Resend provisioning.
- Reviewer-B feature leads (VAT invoices, venue review submission, bill-split) need an owner
  call before boarding — recorded in backlog notes, not P-rows (features, not defects).

## Next-run recommendation

1. **Restart koralink-api.service, then verify run #41's 2 items** (string `amount` on admin
   settlements list; locks are code-only — spec-verified).
2. Rotation 42 % 4 = 2 → **Admin console** focus (Reviewer A sweeps HQ + partner console; check
   whether Abdullah's in-flight admin work creates a HOLD first).
3. Sept 8 00:00Z demo-purge probe (API-11) — if it fires, re-escalate P2-51.
4. Candidate build item: Reviewer-A DTO-bound minors batch (small, low-risk) or deeper admin
   rotation work per findings.
