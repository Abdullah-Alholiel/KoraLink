# Factory Run #69 — 2026-09-22T15:15Z (slot 15:15 UTC; adopts the OOM-killed 10:15Z fire)

**Mode:** full (dead-run adoption + 3 build items)
**Budget:** ~2h used of 3.5h (15:15Z–17:10Z window; 3-item quota cap reached)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` on zai (probe: **200 in 2.4s** via ZAI_API_KEY; GLM_API_KEY line stays stale-401; 13th consecutive clean zai run) · delegation set glm-5.3-flash/zai/coding base at Phase 0, `reasoning_effort=low` confirmed. Reviewers A+B sync: 160s + 192s, both completed.

## Preflight

- **Worktree layout respected:** ALL phases ran in `/home/ubuntu/worktrees/koralink-staging` (staging @ `774872e` at boot); the projects dir (Abdullah's feature lane) untouched.
- **Dead-run adoption (the headline):** the 10:15Z fire of run #69 was killed at ~10:35Z and left TWO pushed commits (`7ea1cdb` wip subscribe-DTO slice "gates pending" + `774872e` pr-review-automation spec) with NO lock (self-cleaned), NO RUNS report, NO board/state updates. Per the watchdog/run-#51 recovery recipe this session: verified the WIP slice's gates MYSELF, adopted it as real work, and finished the run's remaining phases.
- **KILL-PATTERN #13 root cause CONFIRMED** (first time in 13 dead sessions): kernel log `Sep 22 10:35:06` — `Memory cgroup out of memory: Killed process 1769265 (node)` inside `hermes-worker-cron-03ed02bc8d04-exec-...scope`, total-vm 12.2GB, anon-rss ~977MB at kill. The cron worker cgroup's memory limit OOM-killed the session (likely during the turbo build). Recommendation for Abdullah: raise the worker cgroup limit, or make the loop's build step run with reduced parallelism (`TURBO_REMOTE_ONLY` n/a — better: `NODE_OPTIONS=--max-old-space-size` + `turbo run build --concurrency=1` for cron fires).
- Lock acquired (bridge pid 2961857, watchdog-compatible); `git status` clean except foreign `?? .local/` (untouched); `git pull --ff-only` already up to date; gh auth OK (Abdullah-Alholiel).
- Strix: not due (2026-09-22 is day 22; window 1st–3rd; `strix_scan=not-due-2026-09` noted). No scan.
- Notepad candidates STALE (again): P2-37 (reschedule divide-by-zero) DONE run #21; P2-20 (GET /matches param audit) DONE run #53 zero-code. STATE.json/board wins over notepad — verified against live board rows before picking.

## Service health (Phase 1.5)

api/pwa/admin all `active`; `GET :3001/api/v1/health` → 200; journald error-scan **clean on all three services** (5h window).

## Sentry / error-log triage (Phase 1.6)

Sentry API read (SENTRY_READ_TOKEN, EU base), 24h window: **nothing new.** koralink-api newest event 2026-09-11 (KORALINK-API-B CORS `evil.example` ×110 = intentional origin-rejection probe noise; API-1B email provider = known P1-41 provisioning blocker). koralink-web newest 2026-09-14 (WEB-D hydration ×7, WEB-6 viewport diagnostic ×53 — both previously triaged). No board changes.

## Security audit lane (Phase 1.8)

FIX-before-HUNT: `/home/ubuntu/audits/koralink/run-1` + `run-2` findings parsed — **0 confirmed/high records outstanding**. Nothing to fix; HUNT skipped (board had buildable items + budget went to the adoption).

## Previous-run verification (claims ≠ facts)

**Run #68 fe546c9 (P2-13 clubs residual) — VERIFIED DONE ✅ (Reviewer B, all 5 sub-claims PASS):**
(a) `FILTER_KEYS = ['Nearby', 'Indoor', 'Available Now']` at clubs/page.tsx:18, "Top Rated" gone from the page; (b) Nearby comparator = stable ascending distance_m, null-last, ties stable (Array.sort stability, page.tsx:69-77); (c) empty-state split correct (noClubs vs clubs.noClubsEmpty, page.tsx:166-181); (d) `clubs.noClubsEmpty` present exactly once per locale (en.json:718 / ar.json:718); (e) hard evidence re-run: clubs page test **7/7**, i18n test **6/6**. Promoted to DONE on the board.

## Findings (Reviewers A+B, glm-5.3-flash, merged with parent self-review)

**Reviewer A (code quality, adopted slice + notifications module):** verdict **CLEAN** — 0 CRITICAL, 0 IMPORTANT. Verified: `tsc --noEmit` 0 errors; controller spec 20/20 incl. the `design:paramtypes` reflection pin (ts-jest inherits emitDecoratorMetadata — faithful); `@IsOptional` passes Chrome's `expirationTime:null`; whitelist trims/rejects unknown `keys` fields; no orphaned refs to the deleted interfaces; PWA payload conforms. Bug-class sweep on the module: all clean. MINOR nits recorded (not blocking): `'en'` default duplicated in controller + service layers; keys accept any string within caps (not base64url — `@Matches` would tighten; garbage-but-short strings still stored and fail in the send loop — scope-widening only).

**Reviewer B (product gaps + verification):** run-#68 claims 5/5 PASS (above). Product-gap pass of the push/notifications surface:
1. **P1 — permission-denied mislabeled "install required"** (usePushNotifications.ts:84-92,113-118 + profile/page.tsx:461-466) → **BUILT this run (P2-91, 6159f83)**.
2. **P2 — no `pushsubscriptionchange` handler** → **BUILT this run (P2-92, 2b4f080)**.
3. P2 — `notificationclick` focuses+navigates an existing window unconditionally (worker/index.js:150-154) — can clobber mid-action user state; better: focus-only when a window for the target route exists, or postMessage → client router. NOT built (behavior change with product tradeoffs — needs its own slice).
4. P2 — unlabeled push payloads render ar by fallback (documented P2-72 convention); Reviewer B suggests a spec assertion that every sendNotificationToUsers payload carries locale. NOT built (assertion-only polish).
5. P2 — push toast verb map (NotificationProvider.tsx:72-92) and API push-text.ts are two sources of truth for the same verb taxonomy. NOT built (drift risk only; fold into a future push slice).
Items 3-5 recorded here as leads; no new board rows (kept the P2 table lean — they live in this report and can be boarded when a push-focused cycle next runs).

## Board changes

- **P2-82 → DONE ✅** (run #69 `7ea1cdb` — adopted, all gates re-run; allowlist decision documented on the row).
- **P2-91 ADDED + DONE ✅** (same run `6159f83`).
- **P2-92 ADDED + DONE ✅** (same run `2b4f080`).
- Run-#68 in-review item cleared (verified). Reviewer-B residual leads (notificationclick navigation, verb-map drift, locale-presence assertion) recorded in this report only.

## Built this run (slices, commits, verification evidence)

1. **ADOPTED `7ea1cdb` — P2-82 subscribe DTO hardening (API).** The 10:15Z session's committed WIP: `PushKeysDto` (p256dh `@Length(1,256)`, auth `@Length(1,64)`) + `SubscribeDto` (endpoint https+require_tld, keys `@ValidateNested`, `expirationTime` `@IsOptional @IsNumber` — the Chrome-null allowlist decision, locale `@IsIn(['ar','en'])`), controller rebound, old plain interfaces deleted, 8 contract tests. Gates re-run by THIS session: notifications jest **5 suites 47/47**, api tsc clean; Reviewer A CLEAN.
2. **`6159f83` — P2-91 subscribe failure OUTCOME contract (PWA).** `PushSubscribeOutcome` = `'ok'|'not-installed'|'permission-denied'|'error'`; profile maps permission-denied → NEW `common.permissionDenied` (browser-settings recovery copy, EN+AR), error → NEW `errors.pushSubscribeFailed` toast (what/why/next); state `installHintShown` → `subscribeHint`; +5 hook tests; mount-effect `serviceWorker.ready` rejection guard (real hygiene gap found by the tests). i18n parity +2 leaves ×2.
3. **`2b4f080` — P2-92 `pushsubscriptionchange` handler (PWA worker).** Rotated subscriptions re-subscribe with the same public VAPID key and re-upsert `/notifications/subscribe` (POST, credentials:include) with the hook-synced locale from a new worker-readable Cache-API KV (`koralink-push-meta`//`/__kl/push-locale`; hook writes on subscribe + primes per-locale at mount; ar fallback per P2-72); every failure path swallowed. +5 source-tripwire tests + 1 KV test. Built worker file verified carrying the handler (`worker-59715e3e237bab3f.js` — gitignored artifact, never committed).

**Full gates (final tree):** `npx turbo run build` → **3/3 successful, exit 0** (twice: after slice 2 and slice 3) · PWA `npx vitest run` → **92 files / 653 tests passed** (was 91f/642t at run #68: +11 new, 0 broken) · `npm run type-check` → 0 errors · eslint `--max-warnings 0` on all touched files → clean. fallback-*.js post-build churn restored per the run-#59 trap.

## Blockers & decisions

- **ADMIN HOLD continues** — projects-dir merge conflict (UU users.service.ts, profile/page.tsx, ServiceWorkerUpdater, PromoBillboard + staged admin Dockerfile/package/users-page edits) unresolved; no admin/partner items touched.
- **Deploy-target fork unchanged:** systemd units serve `/home/ubuntu/projects/koralink`; staging commits reach live only via Abdullah's directed merge. Nothing this run is live yet (all three slices are PWA/API source on staging).
- **Owner input requested (kill-pattern):** 13 dead sessions, root cause now CONFIRMED as cron-worker memcg OOM. Suggested: raise the cgroup limit for `hermes-worker-cron-*` scopes, or pin `--concurrency=1` for the loop's turbo builds. No action taken without Abdullah.
- Decisions queue unchanged (P1-41 provisioning, P2-51 Render hook, P2-47, P1-38+P2-50 upload pipeline, P0-2 real provider, P1-49, P1-51, P2-90 venue ratings).

## Next-run recommendation

- Rotation 70%4=2 → **Admin lane — likely HELD again** (ADMIN HOLD); fall to PWA/API board order.
- Verify THIS run's three items per the standard recipe: grep `PushSubscribeOutcome` in usePushNotifications.ts + `common.permissionDenied` in BOTH locales; grep `pushsubscriptionchange` in worker/index.js; notifications jest 47/47 + the two new test files; full vitest + turbo.
- If a PWA cycle opens: the three Reviewer-B leads in this report are the natural slice material (notificationclick navigation guard first — real UX risk).
- P2-88 (drizzle meta snapshot regeneration, DB-lane half-cycle) remains the oldest open TODO.
