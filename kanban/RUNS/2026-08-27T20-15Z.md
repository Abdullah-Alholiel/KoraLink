# Factory Run #6 — 2026-08-27T20:15Z

**Mode:** full (review → kanban → build WS ban/suspend enforcement → board → report)
**Budget:** ~1.5h (well under 3.5h cap)

## Preflight

- Lock: none → wrote `kanban/LOCK.json` (run_id 20260827T200233Z).
- Delegation self-heal: already `delegation.model=deepseek-v4-pro` / `provider=deepseek` /
  `base_url=''` — no reset needed.
- `gh auth status` OK (Abdullah-Alholiel). `git pull --ff-only` → already up to date at `1982c4b`.
- Tree clean on `main`. All 3 services `active`; API `/api/v1/health` **200**; 0 journal errors in 5h.
- **Provider probes (both live):** deepseek 200 (`deepseek-v4-flash` alias), zai/glm-5.3 200.
- `HERMES_DELEGATED_CHILD_CONTEXT=1` was set in env → all kanban CLI calls prefixed with
  `env -u HERMES_DELEGATED_CHILD_CONTEXT`.
- Baseline (before edit): build 3/3 · API jest 71/71 · PWA vitest 218/218.

## Findings (reviewer `deleg_fffbf028`, deepseek-v4-pro, 310s + self-verification)

**CRITICAL:** none.

**IMPORTANT (1 built, 1 recorded):**
1. **WS handshake skips ban/suspend + role staleness** — `app.gateway.ts:68-127`
   `handleConnection` verified only the JWT signature; a banned/suspended user keeps full
   chat/DM/lobby access over the socket until token expiry (≤7d), while REST 401s immediately
   (`jwt-cookie.strategy.ts:71-76`). The `ops` room join used the stale token `role` (demoted
   admin retains live console pings). **BUILT this run (4df0d4d).**
2. **DM send idempotency TOCTOU** — `conversations.service.ts:210-241` does findFirst→insert with
   no unique index on `(sender_id, conversation_id, client_message_id)`; `schema.ts:632-652` has
   only `conv_idx`/`conv_created_idx`. Match chat has the partial unique index; DMs don't.
   **Recorded as P1-11.**

**MINOR (→ board/backlog):** decorative Play search (`TopAppBar.tsx:31`) · wallet history queryKey
omits page/perPage (`useWallet.ts:29-38`) · `console.error` vs Sentry captureError
(`usePushNotifications.ts:74,92`, `ErrorBoundary.tsx:30`) · CSP `unsafe-eval`/`unsafe-inline`
(`next.config.mjs:171`) · no per-category notification prefs.

**Verified clean:** i18n parity (634/634 keys) · mutation-return contract all `findOne` outside tx ·
`spots_filled` counts host · no `::uuid` casts · no `eq(col,null)` · wallet idempotency in-tx ·
reports/settlements dedup real · WS send-message/join-lobby/send-dm enforce membership.

## Previous-run verification (claims ≠ facts)

**P1-9 (POTM realtime namespace, run #5) — CONFIRMED ✅:**
- Reviewer CONFIRMED: `PostMatchSection.tsx:43` calls `createLobbySocket()`; the only raw `io(` in
  PWA src is the helper's own definition (`socket.ts:50`). Regression test
  `test/components/PostMatchSection.test.tsx` exists and asserts the dialed URL is
  `…/lobby` (no `/api/v1`).
- Self-check: grep confirms all realtime call sites (`NotificationProvider:53`, `useMessages:155`,
  `useMatches:108`, `useConversations:181`, `PostMatchSection:43`) use the helper; zero raw `io()`.
- Baseline green (build 3/3, vitest 218/218 incl. PostMatchSection.test).
- **Note:** commit msg said "6 call sites" — actual is 5 (overcount, substance correct).

→ **P1-9 promoted to DONE ✅.**

## Board changes

- **P1-9** → DONE ✅ (verified run #6).
- **NEW P1-10** WS ban/suspend enforcement → BUILT this run (`4df0d4d`), IN-REVIEW.
- **NEW P1-11** DM idempotency TOCTOU → TODO.
- **NEW P1-12** Admin/partner console English-only → TODO.
- **NEW P1-13** No match reschedule/edit-after-create → TODO.
- **NEW P1-14** Settlement payout never executes → BLOCKED (P0-2).
- **NEW P2-15** wallet history pagination · **NEW P2-16** console.error vs Sentry.
- Backlog: decorative Play search, notification prefs, CSP unsafe-inline, P1-11 cross-ref.

## Built this run — verification evidence (real terminal output)

- `npm run build` (root) → **3/3 tasks successful** (1 rebuilt API, 2 cached FULL TURBO).
- `npx jest` (API) → **78/78 tests, 12 suites** (+7 new `app.gateway.spec.ts`).
- `npx vitest run` (PWA) → **218/218 tests, 30 files** (unaffected, green).
- `npx tsc --noEmit -p apps/api/tsconfig.json` → **0 errors**.
- `systemctl --user restart koralink-api.service` → `active`, health **200**.
- New spec covers 7 cases: clean Player (no ops) · Admin joins ops · demoted-admin no ops ·
  banned reject · future-suspended reject · expired-suspension allow · missing-user reject.
  Rejection paths emit Pino warnings (seen in jest output).

## Blockers & decisions

- **P0-2** unchanged — real payment provider is an external vendor decision (Abdullah).
  P1-8 and P1-14 remain blocked on it.
- Decision: built the WS moderation enforcement (security) over the DM idempotency (data
  integrity) and discovery filters (feature) — security > data integrity > feature > polish.
  One slice per run; the DM fix is a clean follow-up (migration + onConflictDoNothing).

## Next-run recommendation

1. **Verify P1-10** (WS ban/suspend) → promote to DONE.
2. **P1-11** DM idempotency — partial unique index + `onConflictDoNothing` (clean vertical slice,
   migration + service + spec).
3. **P1-2** discovery filters (`skill_level` filter + `radius_km` hard cutoff).
4. **P2-4** settlement money-as-numeric (drop `::float` for payout sums).
