# Factory Run #38 — 2026-09-07T21:50Z (owner-triggered from WebUI)
**Mode:** full (manual fire — review + visualise + build, per Abdullah's ask)
**Budget:** ~2.5h active (assessment → tooling → build → board)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (permanent; probe: 200) · recommendation/dashboard generators = pure Python, zero LLM

## Preflight
Lock acquired 21:0xZ (kanban/LOCK.json), zai probe 200, delegation pinned glm-5.3-flash/zai/coding base, gh auth OK, tree current with origin (owner commits 449e927/88d69dd/562a240 landed). Integrity review of yesterday's lanes implementation: script copies md5-identical (koralink + global + social), all 8 cron jobs ok + zai pin held, services 3/3 active, ticker heartbeat fresh.

## Sentry / error-log triage (Phase 1.6) — **P0 FOUND & FIXED**
- **KORALINK-API-14 (×6) + API-15: `column "skill_level" does not exist` on users queries, from 21:12Z — LIVE.** Root cause: owner profile redesign (a36e8b2, 19:42Z) shipped migration 0035 (DROP users.skill_level) applied live, but koralink-api.service still ran its 17:35Z dist → every GET/PATCH /users/me 500'd; the app's auth bootstrap calls /users/me on every open → full-screen blocker for every user.
- **Fix (run #38):** turbo rebuild (NODE_ENV unset) → dist clean of skill_level → `npm run restart` → health 200; live probe: dev-login → /users/me 200 with karma_score, no skill_level; /matches (cookie) 200. Bookkeeping repair: 0035 had no __drizzle_migrations row (ids stopped at 34) → inserted id 35 (mirrors row-34 shape).
- **KORALINK-API-13** (`cannot infer relation "matches.waitlist"` ×37): already auto-resolved by 5f61ec0 + 17:35Z restart (last event 17:17Z, before fix landed); P1-17 lesson in code. Sentry issue remains unmarked — needs resolve in UI (no write scope on read token).
- **KORALINK-API-11 ×2 again at 00:00Z Sep 7**: demo PDPL purge UPDATE failing AGAIN → P2-51 recurrence confirmed; run #36's manual hotfix did NOT hold. Boarded as ⚠️ on P2-51; owner call (Render migrate hook) is now the most urgent queue item. Next purge window Sept 8 00:00Z.

## Findings (self + tools, pre-reviewer)
1. **Implementation review (lanes work)** — PASS: copies synced, cron pins held, prestate/digest filters correct (8 genuinely-open decisions), dashboard engine deterministic.
2. **Recommendations engine found 3 of its OWN bugs on first runs** (scope leak, escaped-pipe cell mismatch → DONE items leaking as picks, substring blocker false-positive) — fixed with escape-aware parsing + row-level status sniffing + word-boundary matching. Engine output drove this run: P2-5 pick, P1-17 drift flag.
3. **Board drift:** P1-17 (waitlist) was TODO while fully built (dbd1d8e, 4a64415, 5f61ec0, 4a00f72, 562a240 + 34/34 E2E) → promoted DONE with evidence.

## Previous-run verification (#37 items) — VERIFIED
- FOR UPDATE locks (0e0a907): matches.join-leave-lock 8/8 (lock-first assertions, ::text raw-lock SQL, guard semantics).
- Rate-limit disconnect release (0e0a907): rate-limit.service 15/15 incl. release-on-disconnect.
- Stale push-subscription sweep (cea6544): 4/4 specs + `7 3 * * *` cron present in built dist.

## Board changes
- P0-10 NEW (drift incident) → DONE ✅ with lesson.
- P1-17 TODO → **DONE ✅** (promotion).
- P2-5 TODO → PARTIAL (castVote fixed this run; disputes/venues/wallet remain).
- P2-51 ⚠️ recurrence flagged (owner decision urgency raised).
- STATE.json: run 38, in_review_items = this run's 3 items, 2 new blockers (P0-10 lesson, P2-51 recurrence).

## Built this run
1. **deab57c — P2-5 castVote populated-match contract** (run #37's recommended pick): castVote returns findOne-shaped populated match + additive votedFor/message (PWA VoteResult type stays truthful; optimistic path never read the body — consumers verified by grep). +3 contract specs (happy/404/candidate-not-in-roster). jest 398/398.
2. **7538c2d — DatePicker Riyadh-first day strip** (found by the review gate itself): chips now UTC-anchored via riyadhDateKey (Riyadh calendar day), isSameDay key-based, labels UTC-pinned — previously device-local, which disagreed with the sheet for 3h daily (21:00–24:00 UTC) and for any Saudi user abroad. Fixtures (DatePicker.test + RescheduleSheet.test) now wall-clock-proof. vitest 399/399.
3. **8e3628b — lint-gate repair**: dead stubMutation in ProfilePage.test (pre-existing on main from a36e8b2). eslint 0.
4. **67b7647 — owner control-tower dashboard**: docs/landing-drafts/factory-dashboard.html, live on :9520, auto-refresh 10 min; dark Stadium-Night style; board stats, lane shift plan, engine picks, CEO decisions queue. Serves over the existing landing-drafts server (pid 1458) — zero new infra.
5. **Tooling (profile scripts, not repo):** factory-recommendations.py (deterministic scoring: priority × rotation-fit × unblock-power, escape-aware board parser) + factory-dashboard.py (regenerates the HTML from live board+quota each run).

**Gates at end:** vitest 56 files / 399 tests ✅ · jest 398/398 ✅ · PWA+API tsc 0 ✅ · eslint 0 ✅ · turbo build 3/3 (1m58s, no OOM) ✅ · API restarted on fresh dist, health 200 ✅.

## Blockers & decisions
- P2-51 (demo migrate strategy) — urgency RAISED (recurrence). Recommend option (a): Render deploy hook running db:migrate.
- P1-41 (Resend provisioning) — unchanged, email layer log-only until keys.
- P0-10 lesson needs adoption as convention: migrations ⇒ rebuild+restart koralink-api in the SAME session (postbuild hook does not fire from root turbo or owner-side builds).

## Next-run recommendation
1. Verify #38's 3 in-review items (expected quick — specs already assert the contracts).
2. P2-5 residuals next slice: createDispute/createVenue/createSlot populated returns (same pattern as castVote, low risk).
3. Reviewer findings (deleg_bfe8a7f2) merge → fix CRITICAL/IMPORTANT same run if present.
4. Board hygiene: blockers list is 24 entries — prune absorbed/resolved history into notes next run.

## Reviewer outcomes (deleg_bfe8a7f2, post-build)
- **Reviewer B — VERIFIED all run #37 claims** (locks :1022/:1172/:1862, disconnect release app.gateway.ts:186, 8 lock specs assert lock-first ordering) + **live E2E PASS** (dev-login → matches 200 populated → detail 200: host object, 12 players, waitlist fields). Product gaps ranked; P0s are the known money pair (P0-2/P1-8/P1-14). NEW gap boarded as **P1-43 (in-app notification center)**. EN+AR parity of profile redesign verified exact (39/39 keys).
- **Reviewer A — 0 CRITICAL / 1 IMPORTANT / 6 MINOR.** IMPORTANT FIXED same run: profile/page.tsx:683 raw `softDelete.error?.message` → `t(errorKey(classifyError(error)))` (errors.* standard, EN+AR). MINOR adopted: DatePicker `date` contract doc comment. Remaining minors (castVote window race-tolerance — upsert is unique-safe; fragile any-typed spec mocks; profile purgeDate device-local display) noted for later cycles, no action this run.
- Reviewer A child status=failed on output-schema technicality (truncated final message) — partial output was complete and usable; schema retry budget exhausted on glm-5.3-flash verbosity. Consider widening per_field budget next dispatch.

**Post-review gates:** tsc clean · eslint clean · vitest matches+date+profile 24/24 · turbo build 3/3.
