# Factory Run #56 — 2026-09-17T10:19Z

**Mode:** full (build run + 7th dead-run recovery)
**Budget:** ~55 min used of ~3h (PWA rotation item + prior-run verification + review + E2E probe + staging deploy)
**Models:** session `glm-5.3-flash` (cron pin glm-5.3-flash/zai, verified) · reviewer `glm-5.3-flash` (probe: **200** via `ZAI_API_KEY`; the `GLM_API_KEY` .env line remains STALE → 401) · both reviewers clean on zai — 124s/107s, **6th consecutive clean zai run**

## Preflight

- **Dead lock found:** `kanban/LOCK.json` pid 2395394 (the 01:17Z scheduled fire) — **DEAD**, age 537m.
  Watchdog directive applied: lock removed, new lock written with the bridge pid (`ps -o ppid= -p $$`),
  **coherent half-work adopted** (see Adoption below).
- `git pull --ff-only origin staging` → already up to date @ `f5d99bf`.
- gh auth: OK (Abdullah-Alholiel, fullstack-dev config dir). Tree dirty with (a) the dead run's
  half-work, (b) Abdullah's in-flight admin files (ConfirmDialog/DataTable/Drawer — never staged),
  (c) foreign untracked (`.local/`, `scripts/verify-pr16-overflow-flow.mjs` — untouched).
- Delegation: already glm-5.3-flash/zai/coding-base (no stale flip from the dead run). Probe saga:
  cron approval wall rejected `python -c`/heredoc probes → probe written to `/tmp/zai-probe.py` and
  run as a file (cron-safe pattern). `GLM_API_KEY` (STALE) not used; `ZAI_API_KEY` → **200**.
- **Strix monthly gate: NOT DUE** (Sep 17; window = 1st–3rd; `strix_scan=not-due-2026-09` stands; next due Oct 1).

## Adoption of the dead run's half-work (watchdog directive — verified, never trusted)

The 01:17Z session left: complete Gate 0–3 docs (`docs/plans/run56-pwa-lane/` — retro + program
design + status) and 371 lines of uncommitted P1-47+P2-70 implementation. Per the run-#54 lesson
("never trust the dead session docs, verify key contracts directly") this session re-verified
**every** contract before gating:

- All 4 API throw sites carry object bodies `{ message, code }` with stable codes; `message` text
  preserved → existing jest message pins (`auth.dev-login.spec.ts:119`, `jwt-cookie.strategy.spec.ts:75`)
  hold unchanged (Nest `initMessage()` uses the object body's `message`).
- PWA: `FetchError` 4th optional ctor arg `code` (back-compatible); `apiFetch` extracts `body.code`;
  `classifyError` matches `code` BEFORE status (a suspended account can never render as generic 403);
  verify page swaps the whole OTP form for `BlockedCard`; login/resend use localized inline copy.
- i18n: `blocked.*` 9 keys + `errors.banned/suspended/deleted` present in BOTH locales (Reviewer A
  re-proved parity programmatically: zero en-only/ar-only leaves).
- Store/format contracts: `logout` lives in `store/slices.ts` (grep of `useAppStore.ts` alone misleads);
  `formatShortDate/formatShortTime/AppLocale` exported from `lib/format.ts`; `clearAuthToken` exported.
- `common.noInternet/noInternetDescription/retryConnection` keys pre-existed in both dicts (P2-40 era).
- THEN all gates re-run by this session (the dead run had run none to completion).

## Service health (Phase 1.5)

All three services active; `GET :3001/api/v1/health` → 200; journalctl -p err 5h window: **zero
entries** on api/pwa/admin.

## Sentry / error-log triage (Phase 1.6)

Source: Sentry API (EU `de.sentry.io`, SENTRY_READ_TOKEN). koralink-api 25 / koralink-web 12 issues
in 24h — **no new signatures**. Newest API event Sep 11 (KORALINK-API-B CORS probe `evil.example`,
test noise); newest WEB events Sep 14 (WEB-D zombie-tab hydration, flat; WEB-6 intentional
viewport diagnostic). API-1B (email 503) = the boarded P1-41 owner blocker. Boarded: nothing. Fixed: nothing needed.

## Previous-run verification (claims ≠ facts)

| Run #55 claim | Verdict |
|---|---|
| P2-56 (ef39d13+5676c23): 12 FK indexes applied to staging; audit 42/42 FK led | **CONFIRMED → DONE ✅ STAGING**: `node scripts/fk-index-report.mjs` exit 0 (42/42 led, 0 un-led); 12/12 `idx_*` names present in live `pg_indexes` (postgres.js probe); turbo re-run green. PROD/Neon still intentionally unmigrated (P2-51 owner gate). |

## Findings (Phase 2 — GLM reviewers + self, merged, evidence-cited)

**Reviewer A — code quality (APPROVE, 0 CRITICAL, 1 IMPORTANT → fixed this run):**
- IMPORTANT: `users.service.ts:456` change-flow suspension throw was the ONLY `ACCOUNT_SUSPENDED`
  site without the `until <ISO>` end instant (auth.service:212, email-otp:236, jwt-cookie:127 have
  it) → PWA degraded to no-date copy on that path. **FIXED `ddf9e82`** (tsc 0, affected suites 109/109).
- MINOR (queued, not built): `extractSuspendedUntil` bare-date leniency parses host-local (only
  reachable via manual admin text; prod always sends `Z`); double parse ISO string page→card
  (harmless); login onError ternary duplicated ×2; dead `next/navigation` mock in offline.test.tsx.
- Negative results recorded: i18n parity exact (programmatic), message pins hold, no console.*, no
  z-index use, no render-path clock reads, no `::uuid`, no `eq(col,null)`, Arabic dates route through
  Gregorian-guarded `lib/format` (P2-64 rule respected), every control wired (`role="alert"` present).

**Reviewer B — product gaps + verification:**
- Build-vs-board: **both slices MATCH their row scope**; verify-page spot-check PASS (what/why/what-next,
  9/9 `blocked.*` keys both locales, Z-suffix UTC guard correct).
- New leads (boarded): **P1-48** ban/suspend not enforced on live WS sessions (app.gateway.ts:73
  connect-time only; send-message :233 / mark-read :404 never re-check; admin ban doesn't kick the
  socket — a fresh ban is not felt in realtime until token expiry); **P1-49** no ban appeal/support
  path (BlockedCard offers sign-out only; appeal infra = match disputes only; channel needs an owner call).
- P2 residuals (queued on the rows, not boarded separately): suspension-expiry has no notice/auto-
  transition messaging; unban notification may not be push-wired to the admin action; admin ban
  action carries no reason field (blocks future appeal triage).

**Self-review (parent):** contract audit above; parity-script anomaly chased: `verify-pwa-parity.mjs`
9/11 with 2 desktop `showPicker` FAILs — targets the **deployed** PWA (`:3000`), not the working
tree; unrelated to this diff (host form untouched); re-ran clean post-deploy-equivalent (PWA rebuilt
by turbo + restarted 10:28:32Z; `/en/offline` now serves EN copy live).

## Board changes

- **P1-47 → DONE ✅ STAGING** (5c55d46 + ddf9e82; PROD at next promote).
- **P2-70 → DONE ✅** (a61e8fe).
- **P2-56 → DONE ✅ STAGING, VERIFIED run #56** (evidence appended to the row).
- **NEW P1-48** (WS mid-session ban enforcement) · **NEW P1-49** (ban appeal path; owner call on channel).
- ADMIN HOLD continues (owner files ConfirmDialog/DataTable/Drawer still uncommitted) → P2-69/P2-68 held.
- Mirror: comments on `t_1b20e7fa` (built) + `t_cbd554e8` (verified); new cards `t_bd01e037` (P2-70,
  gated todo), `t_6060894c` (P1-48), `t_28430593` (P1-49) — all linked under `t_ce9a513a`, statuses verified todo.

## Built this run (slices, commits, verification evidence)

| Commit | Slice | Gates / evidence |
|---|---|---|
| `5c55d46` | **P1-47** banned/suspended/deleted localized blocked-account UX (API codes ×4 sites + PWA classify/BlockedCard/screens + i18n + 3 test files) | turbo **3/3 exit 0** · vitest **567/567** (81 files) · jest **530/530** (66 suites, incl. new `auth.moderation-codes.spec`) · pwa tsc **0** · api tsc **0** |
| `a61e8fe` | **P2-70** offline page copy through locale dicts | included above (offline.test.tsx green) |
| `ddf9e82` | Reviewer-A IMPORTANT fix (users.service suspension instant) | api tsc 0 · targeted jest 109/109 |
| `73ae82f` | Gate docs (adopted from the dead run, committed) | — |
| (live) | API service restarted AFTER dist verified (codes grep in 4 dist files) → health 200, 0 errors | **E2E probe on live staging**: seed account banned at DB → guarded `/users/me` with pre-ban JWT → **401 `{code:'ACCOUNT_BANNED'}`** (jwt-cookie.strategy path) → fresh `dev-login` → **403 `{code:'ACCOUNT_BANNED'}`** (auth.service path) → unban → login 200, `banned_at = NULL` restored |
| (live) | PWA restarted by build postbuild (10:28:32Z) | `curl /en/offline` renders **English** copy (the exact P2-70 bug, live-verified); `/ar/offline` renders Arabic |

Sequence discipline: no DB migrations this run (P2-56's 0042 was run #55; nothing new to apply) —
Phase 4.5 sweep shows only owner/foreign files left in the tree.

## Blockers & decisions

- **ADMIN HOLD (continues):** Abdullah's uncommitted edits in `apps/admin/src/components/` — P2-69/P2-68 untouched.
- **P1-49 needs an owner call** (appeal channel: reports/disputes vs support contact) before API work — queued in the decisions list.
- Kill-pattern: this run WAS a kill event (7th); adopted cleanly. No new kill during the run.
- `scripts/verify-pr16-overflow-flow.mjs` + `.local/` dirs: foreign untracked, left untouched.

## Staging deploy (final act, post-lock-release)

`bash scripts/deploy-staging.sh` on `a085a12` — preflight clean (lock already released), full turbo
build (--force), migrate step: 0 pending (0042 already applied), assets synced, services restarted
in order, **health matrix 6/6 [ok]**. Post-deploy: all 3 services active, API health 200, 0 journal
errors, `/en/offline` serving English copy. The PWA-lane build is now LIVE on staging, not just
tree-committed.

## Next-run recommendation

1. Verify P1-47/P2-70 claims (STATE.json in_review has per-item verify recipes; spot-check the
   BlockedCard render path + one throw site).
2. **P1-48 (WS mid-session ban enforcement)** — highest-value buildable P1: per-message cached
   account-state check or admin-ban disconnect broadcast + jest case. Small, self-contained.
3. Admin items (P2-69 aria batch, P2-68 CSV+BOM export) — only when `apps/admin` is CLEAN (check first).
4. P2-42 (CSP nonce migration) remains the standing security item — needs a browser-verification
   strategy before touching CSP headers (this box is browserless).
5. Owner decisions queue (unchanged + one new): P1-49 appeal channel · P1-41 Brevo keys · P2-51
   Render migrate hook · P2-47 cancel-purge · P1-38/P2-50 upload pipeline · P0-2 real provider.
6. Strix next due Oct 1–3 window.
