# Factory Run #74 — 2026-09-25T10:17Z

**Mode:** full (review + build; second build candidate in progress at report time — see tail)
**Budget:** on track vs ~3.5h (started 10:16Z)
**Models:** session `glm-5.3-flash` (cron pin) · reviewer `glm-5.3-flash` (probe 200 via BOTH GLM_API_KEY and ZAI_API_KEY, ~2s; zai weekly 0%, 5h 5% per prestate directive — PRIMARY plan, Go untouched at 0%)
**Worktree:** /home/ubuntu/worktrees/koralink-staging (staging @ 2071901 → item-1 commit `3d4cb7f`). Rotation 74%4 = 2 → Admin lane.

## Preflight
- No pre-existing lock; staging worktree clean @ 2071901 = origin/staging (run #73 pushed clean). Projects dir = foreign feature lane @ 294f8eb — untouched.
- gh auth OK (Abdullah-Alholiel). Delegation already glm-5.3-flash/zai/coding base; reasoning_effort=low confirmed.
- Strix: **not due** (Sep 25; window Oct 1–3) — skipped, one line per runbook.
- **Prestate note:** the pre-run script read the STALE projects-dir STATE (last_run 70); the staging STATE (last_run 73) is authoritative — no action needed, but future runs should note the prestate collector targets the wrong checkout.
- Cron notepad run-directive matched reality (run #74, verify run #73's 2 items).

## Sentry / error-log triage (Phase 1.6 — source: Sentry API, EU base, read token OK)
- **P1-52 STILL BURNING (owner/infra):** KORALINK-API-**1C** (Neon quota) n=4→**6**, lastSeen **2026-09-25T10:00Z** — fires at every scheduled window, TODAY. NEW **KORALINK-API-1F** (n=1, 09-25T03:07Z): push_subscriptions purge DELETE in the notifications sweep — same quota root cause. 1D (signup) n=1 and 1E (JWT session SELECT) n=4 unchanged since their first windows. **Prod auth + signup remain hard-down since 09-24T10:00Z.** Board row re-escalated with 1F; notepad directive says "if silent since 09-25 → billing fixed" — NOT silent, still escalating.
- KORALINK-API-B (CORS evil.example, n=113) — known release-verify probe noise, lastSeen 09-23. MINOR, skipped.
- koralink-web: zero new issues; newest signatures end 09-14. Clean.
- journalctl 5h: api/pwa/admin 0 error lines. Services all active; API /health 200 (localhost:3001).

## Findings (Reviewer A + B via deleg_b523e4a4, glm-5.3-flash, 121s + 145s — 14th consecutive clean zai run)
**Reviewer A (code quality, ADMIN-lane rotation):** standing bug-class sweep **ALL CLEAN** (12 classes: no ::uuid, no eq(null/undefined), .set timestamps compliant, no console.*, all @Cron try/catch-wrapped, FOR UPDATE + guarded status predicates verified on settlements/refund/venue-decide/dispute-decide/deleteSlot, z-index conform, i18n parity 0-diff, no hardcoded admin English, money on numeric strings). IMPORTANT ×2:
1. users pages `Date.now()` hydration (users/page.tsx:22,45 + [id]:22) — **REFUTED by parent**: `useLiveAdminData` fetches in useEffect; SSR renders data=null/loading, so `userStatus`/`purgeInfo` never execute server-side. Same evidence shape as run #73's banner refutation. No fix needed.
2. `deletePitch` count-then-DELETE TOCTOU (partner.service.ts:462-483) — **CONFIRMED by parent**: the count is checked outside any tx and `matches.pitch_id` (schema.ts:420) is a bare varchar(36) with NO DB FK — a match booked between check and delete orphans its pitch reference. **ADMIN HOLD → boarded P2-105, not built.**
Minor notes: settlements pay() has no FOR UPDATE but its conditional guarded UPDATE closes the double-pay race (sound); `as never` casts on withTimestamp(updates) bags (type-drift nit).

**Reviewer B (admin product gaps + run-#73 verification):** **NO P0s.** All 17 dashboard/partner routes exist, nav 1:1 to real pages, dispute/report resolve flows fully wired, server RBAC solid. P1 ×3: (1) HQ settings hardcoded-English labels (settings/page.tsx:17-22 — **parent-CONFIRMED by direct read**) → **P2-104**; (2) rbac.ts `can()` flags unused on dispute/settings → **P2-106** (grep-only, re-verify when hold lifts); (3) no bulk users moderation → **P2-107** (needs owner scope call). P2 ×4 (loading skeletons, audit CSV export, raw-enum status fallback, earnings drill-down) → backlog lines, admin-held.
**Part 2 verifications — BOTH run-#73 claims CONFIRMED independently:** csp-config **11/11** + prod scriptSrc mapbox/moyasar-free (dev-branch only), connect-src wss: + isDev-only ws: spread, style-src/img-src clean; dto-caps **9/9** + VenueDecisionDto @MaxLength(1000), CastVoteDto @Matches UUID-shape + @MaxLength(36), no @IsUUID.

## Previous-run verification (claims ≠ facts)
| Item | Verdict |
|---|---|
| run73-csp-other-directives (c63c843) | **PROMOTED DONE** — parent + Reviewer B both green (suites + greps exact; the :229 mapbox strings live in the DEV scriptSrc branch only, exactly as claimed) |
| run73-dto-caps (0638b06) | **PROMOTED DONE** — 9/9 + decorator greps exact |

## Board changes
- **P2-100 → DONE ✅** (built this run, `3d4cb7f`; verified by full gates).
- **P1-52 → re-escalated**: 1C firing today (10:00Z) + new 1F evidence; still BLOCKED (owner/infra).
- **New rows: P2-104** (settings labels, ADMIN HOLD), **P2-105** (deletePitch TOCTOU — CONFIRMED, ADMIN HOLD), **P2-106** (rbac can() unused, ADMIN HOLD), **P2-107** (bulk moderation, ADMIN HOLD + owner call).
- P2-88 snapshot-gap pin consciously extended to include 0044 (the guard spec worked as designed — flipped red on the new hand-written migration, fixed in the same commit).
- Mirror cards: t_79cf28e7 (P2-100 built-comment), t_a450b5a9 (P1-52 escalation-comment); created+gated t_f5eb047c / t_48e9f157 / t_75aa0df4 / t_d589340c (P2-104..107).

## Built this run (item 1)
| Slice | Commit | Verification |
|---|---|---|
| P2-100 — match-start reminder LADDER: T-24h leg ([23h45m,24h15m], own `reminders_24h_sent_at` stamp, `match_starting_24h` push w/ Riyadh-local DATE+time, `match_reminder_24h` mail template, worker deep-link route) + T-45m leg preserved; stamp written only AFTER successful fan-out (failure stays armed); **migration 0044 applied + live-verified** (column exists, journal sha256-keyed, cannot re-fire); PLUS push-text formatters hardened to TOTAL render — **pre-existing latent bug found**: the `--:--` kickoff fallback was unreachable (`new Date('--:--')` → RangeError escaped renderPushText, violating its documented never-throws contract; the run-#24 "visibly broken marker" never actually rendered — fixed for BOTH legs) | `3d4cb7f` | **turbo 3/3 (`--concurrency=1`, 1m06s)** · **vitest 721/721** (`Test Files 99 passed` grep-verified) · **PWA tsc 0** · **lint 0** · **API jest 643/643** (78 suites; new matches.reminder-ladder 8/8: SQL-predicate pins via PgDialect, per-leg stamps, armed-on-failure, ar-SA Arabic-Indic digit assertion, total-render contract) · worker cross-layer type-contract test updated |

**ADMIN STATE CHECK:** projects-dir lane dirty (Abdullah's in-flight: apps/admin Dockerfile/package.json/package-lock + users/page.tsx + api seed.ts + pwa api-adapter/match-timing + untracked tests) → **ADMIN HOLD continues**; NO admin-surface code touched. Built item is API-matches (outside the hold).

## Blockers & decisions
- **P1-52 (Abdullah, urgent):** prod auth+signup hard-down since 09-24T10:00Z; 6th quota event window running. Neon free-plan CU reset lands ~Oct 4 unless billing/plan changes sooner. Factory cannot touch billing.
- **ADMIN HOLD continues** (projects-dir dirty). The 2 CONFIRMED admin defects (P2-104, P2-105) + 2 others boarded BLOCKED — they are first in line when the hold lifts.
- Refutations recorded: reviewer hydration claim #2 (client-only fetch evidence); both run-#73 items promoted on independent re-verification.
- Standing owner queue unchanged (P1-41, P2-51, P2-47, P1-38+P2-50, P1-49, P1-51, P0-2, + new P2-107 scope call). PR #27 promote still open (T2 gate). Deploy-target fork unchanged.

## Next-run recommendation (#75; rotation 75%4 = 3 → DB & Infra lane)
1. Verify this run's in_review_items in STATE.json (recipes there; both suites + greps are cheap).
2. P1-52: pull Sentry 1C/1D/1E/1F — if silent, Abdullah fixed billing → live-verify a real login path → mark DONE.
3. If ADMIN HOLD lifted: P2-105 (deletePitch tx+lock fix) is the top CONFIRMED defect, then P2-104.
4. Quota: healthy (~4-5% weekly post-run).
