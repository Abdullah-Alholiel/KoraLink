# Ops Log — environment-segregation (devops-cycle §9 ledger)

| Date | Tier | Action | Surfaces | Verification | Undo path |
|---|---|---|---|---|---|
| 2026-09-08 | T1 | `staging` branch created at docs baseline, pushed | git | branch exists on origin | delete branch (nothing depends yet) |
| 2026-09-08 | T1 | Journal reconciliation: backfilled 4 missing journal rows (0014/0030/0031/0034 — applied-but-unjournaled objects verified present in DB first) | VPS PG `drizzle.__drizzle_migrations` | migrate-vps reports 0 pending, idempotent ×2 | rows deletable (objects predate this cycle) |
| 2026-09-08 | T1 | `scripts/deploy-staging.sh` + `scripts/migrate-vps.mjs` shipped; first full staging deploy | repo, VPS services | build 3/3, matrix H1/H2/H3/H5 ok, H4 failed (pre-existing funnel 000) | git revert + redeploy |
| 2026-09-08 | T1 | tailscale serve: admin moved `:443`(dead, shadowed by Coolify Traefik self-signed) → `:9451` tailnet-only; dead 443 handler removed | tailscale serve | `:9451` → 307; API `:8443` + PWA `:9450` unaffected (re-probed) | `tailscale serve --https=9451 off` |
| 2026-09-08 | T1 | **Slice 2 executed**: staging API `NODE_ENV=staging` + DB host pinned `127.0.0.1` + CORS cutover (Vercel origins dropped from PLAYER_URL/ADMIN_URL); postgres rebind verified `127.0.0.1:5432`; dev-login regression 200+token; CORS matrix C1 (staging origin allowed) + C2 (Vercel rejected) green; authenticated `/users/me` read OK | API env, DB | undo: previous env values (in git history of ops-log) + compose image revert |
| 2026-09-08 | T1 | Postgres incident + resolution: amd64 pin + broken host binfmt emulation caused crash-loop; fixed by switching to **native arm64 `imresamu/postgis:16-3.5`** (multi-arch postgis build; Debian-12 glibc 2.36 = collation match, warning GONE; qemu-x86_64 binfmt re-registered via tonistiigi/binfmt); data verified intact (45 users, 40 journal rows) | docker-compose.yml, host binfmt | revert = restore postgis/postgis amd64 pin in compose (git history) |
| 2026-09-08 | T1 | **Slice 3 executed**: `NEXT_PUBLIC_SENTRY_ENV` wired in pwa+admin client configs; VPS bundles verified baking `environment:"staging"` (chunk-grep on `_app-*.js` ×2 apps); full deploy re-run exit 0 with all 6 probes green | sentry.client.config.ts ×2, .env.local ×2 | revert: 1-line configs + rebuild |
| 2026-09-08 | T1 | Factory retargeted to `staging`: koralink-factory-loop skill branch-model section added + 3 runbook `main` refs fixed; STATE.json `default_branch=staging` | skills, kanban/STATE.json | n/a |
| 2026-09-08 | T2 | Neon migration reconciliation (user go): journal-sequence collision diagnosed (drizzle-kit seeded rows w/o setval) → setval fixed → 5 files applied 0034–0038 (incl. phone-nullable) via `MIGRATE_DATABASE_URL` override; **zero data moved**; verify: phone_nullable=YES, verb37=1, waitlist=✓, trigger=✓, rerun idempotent 0 applied | Neon `falling-frost-44866281` | none needed (schema-only, forward-safe) |
| 2026-09-08 | T1 | CI/CD pipeline shipped: pr-gate.yml (lint→tsc×2→jest+vitest→turbo build→journal check→fresh-apply scratch PG+idempotency) + release-verify.yml (post-merge prod cutover matrix) + check-migration-journal.mjs + release-verify.sh; **run 1 caught real drift** (0014 meta gap, CI-no-.env crash) → fixed → run 2 GREEN; legacy ci.yml retired; deploy-staging.sh now `unset MIGRATE_DATABASE_URL` (ambient-override foot-gun, NODE_ENV class) | `.github/workflows`, scripts, docs/CICD.md | git revert workflows |
| 2026-09-08 | T1 | Staging redeploy ×2 (f07e0d1 → failed on leaked env → unset → **green on 78564f3**); VPS runs full CI/CD state; H1–H6 green | VPS quartet | redeploy previous sha |
| 2026-09-09 | T1 | Prod console-error sweep (user report → headless-CDP repro): CSP script-src gap (posthog-js `us-assets` script injection), locale-manifest 307→404 (matcher gap), chat 403 ×2 amplification (ChatSheet always-mounted query + `retry:1` on 4xx); fixes `5f4ea6c` + `23ff42c` on staging; 463/463 tests, turbo build green; staging probes: manifests 200, CSP `*.posthog.com`, headless console zero errors; env-shape guard `safePostHogHost()` shipped | pwa next.config.mjs, middleware.ts, ChatSheet.tsx, QueryProvider.tsx, ObservabilityProvider.tsx + 3 regression tests | git revert ×2 commits |
| 2026-09-09 | T2-PENDING | Vercel `NEXT_PUBLIC_POSTHOG_HOST` = markdown-wrapped paste (baked literal `[https://us.i.posthog.com](…)` in prod chunks) — env fix + redeploy AWAITING USER GO (consent gate blocked env-value read this session) | Vercel env, kora-link-player-pwa | revert env value + redeploy |
| 2026-09-10 | T0 | Coolify setup audit (user-requested): Coolify v4.0.0-beta.462 healthy but IDLE (2 projects, 2 dead apps, 0 managed DBs); staging quartet = systemd, NOT in Coolify (verdict: never move it in); external scan 13 ports ALL closed/filtered → OCI Security List is the only gate (host iptables EMPTY, Docker binds 0.0.0.0 on :8000/:8001/:8444/:5433/:6544); funnel :10000 PUBLIC → staging PWA dev-login build (PID-confirmed); backup local chain green, offsite PENDING | VPS infra (read-only) | full evidence: docs/plans/coolify-consolidation/00-assessment.md | n/a |
| 2026-09-10 | T1 | Cleanup (user-approved scope: idle containers + dead Coolify apps ONLY; funnel :10000 close, 127.0.0.1 rebinds, swapfile offered and DECLINED): `docker rm -f` koralink-{pwa,admin,api} demo containers (images kept); both dead Coolify apps deleted via API (HTTP 200 ×2, `applications` → 0 rows). ⚠️ VPS Dashboard app was Coolify-managed AND live (stale `exited:unhealthy` display): its Coolify record got deleted, container SURVIVED as standalone on `coolify` net — snapshot + recreation runbook in assessment §6 | docker containers, Coolify DB records | post-check: systemd quartet active, API health green, only koralink-postgres container remains, vps-dashboard Up/healthy | containers recreate from kept images; vps-dashboard per assessment §6; Coolify apps recreatable from source repos |
| 2026-09-10 | T0 | Prod read-only probes: KSU 7v7 (57377940) = Completed 1/12 min_players; 2 more Sep-6 rows (5/12, 7/12); zero ledger entries, fee_paid NULL, self-mode, venue-owned → status-only repair | Neon falling-frost | n/a (read-only) |
| 2026-09-10 | T1 | Root cause: Render FREE spin-down ate check-min-players band; wake-up auto-completed underfilled rows. Fix 983634d: overdue underfill net in autoCompletePastMatches + resolveEffectiveStatus underfill rule + keep-warm timer (koralink-keepwarm.timer every 10m, first ping OK). Staging E2E: synthetic 1/12 past-due match → Cancelled at boot with activity+log. Staging deploy green (b194397). | staging quartet | rollback = git revert + redeploy |
| 2026-09-10 | T1 | Run #49: auth hardening (otpMatches constant-time, verify-race 23505 recovery, @IsUUID DTO, deleted-account silent-drop) + hydration-safe messages clock; jest 39/39, vitest 465/465, build 3/3, API restart post-build, live E2E PASS | api auth + conversations DTO, pwa messages ×2 | health 200 ×2 probes; E2E send 202 / wrong-code 401 / verify 200 isNewUser / garbage-id 400 | git revert slice commits |
| 2026-09-10 | T0 | Brevo key received from Abdullah, stored .deploy-tokens (name-only); API probe from VPS 401 unrecognised-IP → cutover BLOCKED on owner console: authorise 145.241.109.213 + verify sender. EMAIL_PROVIDER left unset (log-mode intact) | Brevo account (external), koralink .deploy-tokens | probe evidence in run #49 report | n/a (no change made) |
| 2026-09-10 | T1 | Brevo cutover (staging): EMAIL_PROVIDER=brevo + BREVO_FROM="KoraLink <trialsabdullah@gmail.com>" + key → .env, API restart; first real send 503 → diagnosed BrevoService endpoint typo (/api/v3 vs /v3, spec had pinned it) → fixed, jest 25/25, rebuild, retry → **202 accepted, real OTP dispatched to owner Gmail** | apps/api/.env, brevo.service.ts, staging API | journal: 404 pre-fix → clean 202 post-fix; health 200 | env revert to log-mode (unset trio) + git revert endpoint fix |
| 2026-09-10 | T2 (user go) | Prod email cutover: real-inbox E2E on staging confirmed (owner's code 467020 → 200 isNewUser, /users/me session verified) → Render env PUT ×3 (EMAIL_PROVIDER/BREVO_FROM/BREVO_API_KEY, 200 ×3) + deploy dep-dahcn1id0e5s738tbkeg triggered; post-live probe + DEV_LOGIN flip pending | Render srv-dadabif10e5c73e207bg | deploy watcher polling to `live` (this row updated next) | revert = DELETE 3 env vars + redeploy (≤60s) |
| 2026-09-10 | T2 (user go) | Promote PR #22 MERGED (c7d712b): KSU underfill net + keep-warm + player-host/profile-DM cycles + run#49 auth/hydration + Brevo endpoint fix; gate-fix eab01bf (findOne `players?.length` — root cause verified PR-introduced via clean-main worktree run, evidence comment on PR). Precondition met BEFORE merge: Neon journal 43/43, rows 42/43 sha256-match 0039/0040, 5 new columns + conv_pair_unique_idx + HostPayoutState enum live. Deploys: Render c7d712b live; Vercel pwa+admin Ready. Post-verify: health 200 {status:ok}, feed 401 (auth guard correct), pwa/admin 307→locale, 12 prod chunks = 0 dev-login markers | Render srv-dadabif10e5c73e207bg, Vercel ×2, Neon falling-frost | matrix green | Vercel rollback button / Render redeploy 06bf4b8 |
| 2026-09-10 | T1 fix | **Phone-OTP prod signup 401 bounce — ROOT-CAUSED and FIXED.** Field report: email signup → verify OK → complete-profile → hard refresh to /login. Root cause: prod PWA (vercel) to API (onrender) is cross-origin → browser refuses access_token cookie; phone channel set cookie ONLY (email channel had the P2-11 responseToken exception, phone did not) → zero stored credentials → first authed call 401 → fetcher self-heal bounce. Fix `369e325`: responseToken:true contract on the phone channel (DTO+controller+spec), PWA hook opts in, admin console opts in (was already coded for it). Gates: API 49/49, PWA 472/472, turbo 3/3, staging Bearer /users/me 200. Reaches prod on next promote. |
