# Gate 3: Program Design — POTM Card Button Redesign & Voted State

> Program specifications and verification plan.

---

## 1. i18n Keys Added

### `en.json` & `ar.json`:
- `"votedShort"`: `"Voted POTM"` / `"تم التصويت"`

---

## 2. Verification Plan
1. Run `npx vitest run` (91/91 unit tests passing).
2. Run `npm run build` (`turbo run build` green).
