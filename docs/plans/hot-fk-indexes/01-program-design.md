# Program Design — P1-4 Hot-FK Indexes (migration 0015)

## Contract

Pure schema change — no API/behavior delta. Contract = the 7 indexes exist in the live DB:

| Index | Table/Column | Serves |
|---|---|---|
| `matches_host_id_idx` | matches.host_id | My Games (host view), admin filters |
| `matches_pitch_id_idx` | matches.pitch_id | Pitch availability lookups, slot booking |
| `activities_actor_id_idx` | activities.actor_id | Activity feed per actor |
| `disputes_reporter_id_idx` | disputes.reporter_id | Admin disputes queue |
| `reports_reporter_id_idx` | reports.reporter_id | Reports moderation queue |
| `venues_owner_id_idx` | venues.owner_id | Partner portal venue listing |
| `transactions_reference_id_idx` | transactions.reference_id | Settlement/refund lookups |

## Contract verification checklist

- [x] Migration 0015 generated by drizzle-kit from schema declarations (no hand-written SQL)
- [x] Applied to live DB via `npm run db:migrate` (journal idx 15)
- [x] All 7 indexes present in `pg_indexes` (verified via node postgres driver)
- [x] EXPLAIN on `matches WHERE host_id=…` — seq scan expected on 5-row table (correct;
      index engages as data grows)
- [x] `tsc --noEmit` 0 errors · jest 30/30 · `turbo run build` 3/3
- [x] No API/DTO/adapter/i18n changes (zero code risk by design)

## Files changed

- `apps/api/src/database/schema.ts` — 7 index declarations
- `apps/api/drizzle/0015_bored_wraith.sql` + `meta/0015_snapshot.json` + `meta/_journal.json`
- Gate docs: this file + `00-retro.md`
